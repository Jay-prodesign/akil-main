# CURRENT_STATE.md

Last updated: 2026-09-05, by Claude (Primary Engineer). `PR #4` through `PR #20` are all merged into `main` — `PR #4` (V3 Workstreams A-D) at `4ba5231309ac2fc72dd089cc947030a958eab77e`, `PR #5` (`V3-COM-001`) at `5c91f18034a7ec435a4d83fccac856d354f8394f`, `PR #6` (`V4-SVC-001`) at `c93a9cc1e8caf5da3c13d102bdb9fa7c778a5982`, `PR #7` (`V4-OPS-001`) at `6f403db130434b0228783584487dfdf43f3544f7`, `PR #8` (`V3-F-001`) at `837214dc863970bdb05f29bf8611a3438735b3f4`, `PR #9` (doc-only "Workstream G" mislabel correction) at `6758de759bf17ecda49da7f4f8e1d07faa526655`, `PR #10` (V3 §14 closure per Brain Rev34 + corrected V4/V5 dependency scan per Brain Rev35) at `90e989935a443462f372fbbb9ff1caa6987c82ce`, `PR #12` (`V5-ENG-001`, CHECKPOINT-after-PASS stale-approval invalidation, including the Rev39 equal-fencing correction) at `19df961498896427223842f7de82df2d9f6a8304`, `PR #14` (`V5-BOOT-001`, Project/Repository Bootstrap Template Resolution, including the Rev43 provenance/local-applicability correction) at `1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb`, `PR #16` (`V5-ENG-002`, RESOLVE Authority Separation) at `460871cba43273f89bbf8004e547664c40b90794`, `PR #18` (`V5-INT-001`, Cross-Domain Company Intelligence reconciliation floor, including the Rev49 sourceRef/future-capturedAt correction) at `839aca0f6dd830c46dcf4576e46035c3fdbd9207`, `PR #20` (`V5-WRK-001`, Worker/Model Routing Policy, including the Rev52 tool/policy/authority-dimension correction) at `f03c917e66283fcd63736234d81b166465496d7c` (current exact final `main`) — see `docs/exec-plans/corridors/V2_TO_V5.md` for the full merge-provenance record of each round, including the Rev37/Rev41/Rev45 misattributed/unverified-approval claims that were independently identified and not acted on until direct Founder confirmation was obtained in conversation each time. `PR #11`, `PR #13`, `PR #15`, `PR #17`, and `PR #19` (doc-only merge-provenance reconciliations) remain open/draft/unmerged and do not block source work. PR #18's Rev49 correction was independently confirmed empirically before fixing; Brain Rev50 then independently PASSed the corrected exact head, and Rev51's document-recorded Founder "Ok merge" utterance was additionally reconfirmed live as an explicit "I confirm" before merging (merge commit `839aca0f6dd830c46dcf4576e46035c3fdbd9207`). Following that merge, the standing post-merge dependency-scan authorization located `V5-WRK-001` (Worker/Model Routing Policy, §6). Brain Rev52 returned `CHANGES_REQUIRED` on the original submission (F1: required tool-access/policy-constraint/authority dimensions not modeled; F2: evidence bound only to a short SHA), both independently confirmed and fixed on the same branch — Brain Rev53 then independently PASSed the corrected exact head `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79`. Rev54's document-recorded Founder "Ok" utterance was additionally reconfirmed live as an explicit "I confirm" (this session's own standing safeguard, not something Brain required) before merging — merge commit `f03c917e66283fcd63736234d81b166465496d7c` (parents `839aca0f6dd830c46dcf4576e46035c3fdbd9207` + `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79`), zero diff against the reviewed exact head, fresh strict typecheck (exit 0, clean `dist/` rebuild) plus full regression (666/666 pass) re-confirmed directly on the exact merge commit. A self-paced corridor check-in between the two merges also selected and implemented `V5-PTN-001` (Partner Capability Claim Admission, V5 Workstream H, PR #21, open/draft/`HOLD_MERGE`) — see `docs/exec-plans/active/V5-PTN-001.md`. Per the Founder's explicit instruction, PR #21's branch was rebased onto the post-PR#20 `main` (`f03c917e66283fcd63736234d81b166465496d7c`) so its exact-head evidence was no longer bound to the stale base `839aca0`. Brain Rev55 then independently reviewed that rebased exact head and returned `CHANGES_REQUIRED` (F1: admission authority/provenance was implicit, not modeled/enforced — `evidenceRef`/`reviewByAt` alone did not prove a claim's admission came from an authority independent of the claimant, so "capability claim cannot self-certify" was not yet structurally satisfied). The finding was independently verified against source before fixing, then corrected on the same PR #21/branch: `admitPartnerCapabilityClaim` now requires a non-empty `admittingAuthorityId` and fail-closed rejects it whenever it equals the claim's own `partnerOrganizationId`, persisting the accepted authority as `admittedByAuthorityId` for audit. New exact head `e7013fac72989380568726f750518b9ddee39046` (final pushed head, with the doc-record commit on top, `4b78e4b3cd6134b5da0db8e799d47a8bf8795134`) on the unchanged base `f03c917e66283fcd63736234d81b166465496d7c`, independently re-verified on a fresh detached-HEAD checkout (691/691 tests pass). Brain Rev56 then independently reviewed exact head `4b78e4b3cd6134b5da0db8e799d47a8bf8795134` and returned `PASS` — Rev55's F1 finding is `SATISFIED`/closed, scope and regression checks both `PASS`. `MERGE_STATUS: NOT MERGED`; `OWNER_GATE: OPEN` — Brain states explicit Founder authorization is required before merge, `NEXT_EXPECTED_ACTOR: FOUNDER`. Per this session's own standing conduct rule, a document-recorded Brain PASS is not itself Founder authorization: merging still requires a separate, explicit Founder confirmation given directly in a live conversation turn to the specific exact-head question, never inferred from a document's claim alone — live PR state was independently re-verified to match the reviewed head/base exactly (draft, open, not merged) before surfacing this to the Founder. See the "Rev56" note in `docs/exec-plans/active/V5-PTN-001.md` for the full record; still `HOLD_MERGE`, awaiting live Founder decision. V5 Workstream A's full acceptance list (all five bullets in blueprint §5) remains satisfied across `ENG-ORCH-001`/`V5-ENG-001`/`V5-ENG-002` — a finding, not a status Claude has authority to close (remains Brain's determination; still `FINDING_PENDING_BRAIN_ADJUDICATION` per Rev54). V1 DEL-003/AKI-GIT-001 state below is unchanged.

**Correction (this entry supersedes the "HOLD_MERGE"/"NOT MERGED" status above for PR #21):** PR #21 was subsequently merged at `912e66927e432c955fcd3135d302e0a8c941cc0c` under the Rev57 Founder routine-technical-merge policy, then went through a Rev58/Rev61 evidence-only correction cycle (complete raw post-merge transcripts, hash-verified) — see `docs/exec-plans/active/V5-PTN-001.md` and the still-open doc-only PR #23 (`claude/pr21-post-merge-evidence`) for the full record; this branch was cut from merged `main` (`912e66927e432c955fcd3135d302e0a8c941cc0c`) before PR #23 merged, so it does not yet carry that fuller rewrite. **Rev60 correction (this checkpoint):** Brain's independent cross-slice audit of merged `main` found a material defect in `src/domain/worker-routing-policy.ts` (`V5-WRK-001`, previously PASSed/merged under PR #20): `worker.authorityLevel` was indexed directly into the `AUTHORITY_RANK` lookup table with no runtime guard, so a malformed/unknown runtime value (`undefined < requiredRank` evaluates `false` in JavaScript) could silently survive the authority rejection path for both `STANDARD` and `ELEVATED` routes. Independently verified against source before fixing (confirmed the exact gap; confirmed existing tests A27-A29 exercised only valid enum values). Fixed on a fresh branch cut from `main` (`claude/v5-wrk-001-authority-failclosed`, PR #25, exact head `7d735b74ff658918c101a32b7e9e082be53b20dd`): adds `isRecognizedAuthorityLevel`, a fail-closed runtime guard, plus 4 adversarial regression tests (A33-A36) — sanity-checked by temporarily removing only the guard and confirming exactly those 4 tests fail, then restoring it and reconfirming 695/695 pass (691 pre-existing + 4 new). No other eligibility dimension, dependency, or existing valid-enum behavior touched. `PR #24` (`V5-PTN-002`) remains open/draft/non-controlling pending both the PR #21 evidence-only reconciliation and this Rev60 correction, per Rev58/Rev60's explicit `DOWNSTREAM` instructions.

**Update, 2026-09-09 (V5-PTN-001 Rev62 non-blocking hardening, no Brain input required to begin):** with PR #25 merged and PR #30 (`AUD-DURABILITY-GAP`) opened and awaiting Brain review, a further scan of Brain's Rev62 "NON-BLOCKING / HARDENING OBSERVATIONS" found a second, smaller actionable item: *"V5 partner capability `revokedAt` currently validates non-empty string but not timestamp/order relative to admission."* Independently confirmed against source: `revokePartnerCapabilityClaim` (`src/domain/partner-capability-admission.ts`) validated `revokedAt` only via a non-empty-string check, accepting any non-timestamp value or a `revokedAt` predating the claim's own `admittedAt`. Fixed on branch `claude/v5-ptn-001-revokedat-temporal-hardening` (cut fresh from merged `main`, `fe2cbc02f20e800b309e127288cc32a72176c109` — independent of and not stacked on PR #30): `revokedAt` now validates via the same `requireValidTimestamp` helper `admitPartnerCapabilityClaim` already uses, and is rejected if strictly before `admittedAt` (equal is accepted — immediate revocation is not "before"). 3 new tests added (non-timestamp rejected, before-admittedAt rejected, equal-to-admittedAt accepted). 708/708 tests pass (705 pre-existing on this base + 3 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-PTN-001.md` "Rev62 non-blocking hardening" section for full detail. **Rev74 update: Brain PASSed this exact head (`b0b748e3f85ebb80d2d3f4d6b682e7647d480ac9`) and it merged into `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`.**

**Update, 2026-09-09 (AUD-V5-GAP-02 floor, no Brain input required to begin):** with PR #25 merged (`fe2cbc02f20e800b309e127288cc32a72176c109`, closing `AUD-V5-01`), Brain's Rev62 Handoff "AUTHORIZED CORRECTION ORDER — NO FOUNDER RELAY" step 3's own continuation note directly authorized the next action: *"After F1 closes, fresh dependency scan the V5 Cold-Start gap. The smallest likely next convergence slice is Commercial Order → Canonical Service/Recipe Resolution → Conditional Intake/Readiness."* Brain's own audit lists this as the first, most upstream gap: *"Commercial Order → Canonical Service Resolution: MISSING on audited main; no Commercial Order start/resolver found."* Implemented on branch `claude/v5-cold-start-commercial-order-resolution` (cut fresh from merged `main`, `fe2cbc02f20e800b309e127288cc32a72176c109` — independent of and not stacked on PR #30 or PR #31): a new pure domain module `src/domain/commercial-order.ts` (`CommercialOrder` — no `projectId`, since a project does not exist yet at order time; `ServiceCatalogEntry`; `resolveCanonicalServiceFromOrder`, fail-closed to `UNRESOLVED_SERVICE` on no match and throwing on an ambiguous catalog, never guessing) plus a genuine zero-history `WEBSITE_BUILD_v1` order fixture (`src/fixtures/website-build-v1-commercial-order.ts`, no `Project`/`SoldScope` constructed alongside it).

**Same-checkpoint extension (same branch/PR, continuing the loop immediately rather than idling on Brain review — same precedent as the V3-ORG-001→V3-SLA-001 chain):** Brain's named chain is one convergence slice, not three separate ones, so the "Conditional Intake/Readiness" links were closed immediately after the first, rather than left as a silently-dropped deferral. `src/domain/commercial-order-intake.ts` (new): `intakeSoldScopeFromResolution` closes the audit's exact "Conditional Intake" gap ("sold-scope/evidence/blueprint primitives exist but no order-driven intake compiler") — fail-closed unless the caller's `blueprint` identity/version exactly matches the order's own resolution, then delegates entirely to the already-verified `createSoldScope`. `src/fixtures/website-build-v1-commercial-order.ts` gained `buildWebsiteBuildV1ColdStartFixture()`, proving the full chain end to end — order → resolution → intake-compiled `SoldScope` → `compilePlan` → `validatePlan` returning `COMPLETE` (closing the audit's "Brief Completeness / Readiness: PARTIAL end-to-end" finding) — from one genuinely zero-history starting point. Evidence-based admission (`evaluateReadiness`/`admitPlan`) remains explicitly out of scope at that point (no customer evidence fabricated to force an admission result); project-bootstrap-template composition (V5-BOOT-001) likewise remains untouched, separate future work. 9 more tests (`tests/commercial-order-intake.test.ts`), 727/727 total pass at that point.

**Second same-checkpoint extension (still same branch/PR, still no idling on Brain review):** closes the audit's *"Required Approval: PRESENT primitive / PARTIAL end-to-end"* finding by composing only existing, separately-tested primitives on top of the same zero-history fixture's compiled plan — `createApprovalReference`, the existing `buildFullReadinessAssertions` test helper, `admitPlan`, `deriveOutcomeJobSpecs`, `admitJobs`, `wireAdmittedOutcomeJobs` — proving the chain reaches an `ADMITTED` plan and wires `DRAFT` `OutcomeJob`s, plus the negative case (no approval → `WAITING`, zero jobs wired). No new production source file — proof-by-composition only (`tests/commercial-order-cold-start-admission.test.ts`, 2 tests). Deliberately does **not** claim "Delivery Closure" or "Authorized Execution" are closed — actual job execution/worker invocation/QA-evidence/handover requires real worker capability this repository does not have, same restraint as V4 Workstreams C-H; jobs are proven to reach `DRAFT`, never further. **729/729 total pass**, strict typecheck clean, zero new dependency, 6 new files total across all three rounds (825 insertions, 0 deletions/modifications to any existing file). See `docs/exec-plans/active/AUD-V5-GAP-02.md` for full detail. Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review; not yet `VERIFIED`/`PASS`/`CLOSED`. **Rev74 update: Brain returned `CHANGES_REQUIRED` on this exact head (`d2cd161e6c41bfed884fdd9feb894383ebd49271`) — F1: `resolveCanonicalServiceFromOrder` accepts a caller-supplied catalog with no admission/provenance/authority binding, so a caller can fabricate a serviceRef→blueprint/version/recipe mapping and receive `RESOLVED`. See the "Rev74 correction" entry in `docs/exec-plans/active/AUD-V5-GAP-02.md`: no admitted/trusted repository-native catalog authority exists anywhere in this repo to bind to, and inventing one would be exactly the "second catalog/orchestration system" Brain's own acceptance forbids — the claim is instead honestly narrowed to "deterministic caller-supplied lookup," and canonical-resolution provenance is recorded as an explicitly OPEN audit gap.**

**Update, 2026-09-11 (condensed Rev77-Rev101 catch-up + SVC-ADM-001 closes the Rev74 canonical-resolution gap):** since the Rev74 entry above, this corridor progressed substantially on other branches (this branch, cut from `AUD-V5-GAP-02`/PR #32's own head, does not itself carry every intervening commit — see `docs/exec-plans/corridors/V2_TO_V5.md` for the full record): Rev77 renamed `resolveCanonicalServiceFromOrder`/`CanonicalServiceResolution` to `resolveDeclaredServiceFromOrder`/`DeclaredServiceLookupResult` on this same PR #32 (naming-only, no behavior change — the canonical-resolution provenance gap stayed explicitly OPEN); Rev79 corrected PR #32's own exec-plan/PR framing to match. Elsewhere: PR #21/#25 (`V5-PTN-001`), PR #30 (`AUD-DURABILITY-GAP`), PR #33 (`AUD-V5-AUTHORITY-INGRESS`, confirmed genuinely `BLOCKED_BY_EXACT_PROTECTED_GATE` — no real identity/session provider exists in this repository), PR #34 (`AKILTA-IP-001`), `V5-LAB-001`/`V5-EVAL-001`/`V5-CMD-001` (PR #35-37), `CONN-001`/`SALE-TO-CLOSE` (PR #38/#39), `RUNTIME-001` (PR #40), `V5-PTN-002` (PR #41), `V4-EFF-001` (PR #42), `LOCAL-EXEC-001` (PR #43, plus its LOCAL-EXEC-002/OBS-TEL-001/AUT-OPS-001/DEP-ORCH-001 continuations) all landed as separate task-scoped branches under Rev90/95/97/98/99/100 (Current Project State) and Handoff Rev101's continuous-execution batch-mode authorization — none of that history is duplicated on this branch since none of it touches `commercial-order.ts`/`commercial-order-intake.ts`.

Handoff Rev101's own "MANDATORY REMAINING FAMILIES" list names Family 1 exactly: *"trusted canonical service/delivery knowledge boundary resolving commercial service identity to admitted service/version/intake/recipe/capabilities/work-plan inputs/acceptance/approvals/gap behavior."* This is precisely the Rev74 F1 gap recorded above. `SVC-ADM-001` (this branch, `claude/svc-adm-001-trusted-service-catalog`, stacked on this branch's own `AUD-V5-GAP-02`/PR #32 head `8b95aa337da5e4fb5498f8ebf8f0b2ced8ae93bc` since its core dependency `commercial-order.ts` does not exist on `main`) closes it: `src/domain/service-catalog-admission.ts` composes `CommercialOrder`/`ServiceCatalogEntry`/`DeclaredServiceLookupResult` (unmodified) with a new fail-closed admission gate — `admitServiceCatalogEntry` is the only construction path for a `ServiceCatalogAdmission` (mirrors `capability-admission.ts`'s single-gate discipline), binding a catalog entry to the exact `DeliveryRecipe` it declares, requiring a non-empty `admittedByAuthorityId`/`evidenceRef`; `revokeServiceCatalogAdmission` is a terminal one-way transition with the same Rev62 `revokedAt`-not-before-`admittedAt` temporal-integrity discipline already proven on `partner-capability-admission.ts`. `resolveTrustedServiceForOrder` composes `resolveDeclaredServiceFromOrder`'s own output with the admitted catalog and returns the Family-1-required gap disposition: `RESOLVED` only when a currently-`ADMITTED` admission matches the declared lookup's exact blueprintId/blueprintVersion/recipeId; `NOT_ADMITTED` (never a silently-trusted `RESOLVED`) for a declared-but-unadmitted, revoked, or stale-version service; `UNRESOLVED_SERVICE` passed through unchanged when the declared lookup itself failed. `IMPLEMENTED / SELF-VALIDATED`, 755/755 tests pass (733 pre-existing on this base + 22 new: 16 functional + 6 boundary-scan), strict typecheck clean, zero new dependency. `MERGE_DISPOSITION: HOLD_MERGE`. This checkpoint also re-applied the `package.json` `clean`-script build-hygiene fix (`"clean": "rm -rf dist"` wired into `build`), which this branch's older base predated — its absence was caught live when a stale `dist/` from a different branch's build caused 5 unrelated test failures on first run here, resolved by `rm -rf dist` + the script fix, not by touching any source. See `docs/exec-plans/active/SVC-ADM-001.md` for full detail.

**Update (CONN-001, Rev90, PR #38, and SALE-TO-CLOSE addendum, Rev91/92/93):** Brain Rev90 authorized `CONN-001` (Integration & Credential Control Plane), progressively elaborated across five slices on branch `claude/conn-001-integration-control-plane` (PR #38, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`) — `integration-connector-catalog.ts`, `durable-connector-connection-store.ts`, `integration-admin-view.ts`, `generic-connector-definition.ts`, `prebuilt-connector-definitions.ts` — composing entirely on top of the already-merged, unmodified `connection-authority.ts` (V2-CDO-004). Brain Rev93 found and this session fixed one genuine bounded F1 (`reconnectConnectorConnection` missing a connector-identity check on the previous connection instance, allowing a revoked connection of one connector kind to in principle be "reconnected" as a different connector kind); independently verified against source before fixing, closed with an adversarial test (K22). `IMPLEMENTED / SELF-VALIDATED`, 765/765 tests pass on that branch. See `docs/exec-plans/active/CONN-001.md` (on `claude/conn-001-integration-control-plane`) for the full slice-by-slice record. Rev91/92 additionally authorized a **SALE-TO-CLOSE E2E acceptance floor** addendum, implemented as its own distinct mission on a separate branch `claude/sale-to-close-e2e-floor` (cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`, independent of and not stacked on PR #38): `src/domain/external-sale-bootstrap.ts` (`bootstrapExternalSaleOutcome`/`applyExternalSaleDisposition`) and `src/domain/durable-external-sale-bootstrap-store.ts` deterministically bootstrap/reconcile a `Project`+`SoldScope`+`OutcomeJob` from an admitted `VERIFIED` `ConnectionBinding` and an opaque `externalSaleRef`, entirely by calling the existing, unmodified `createProject`/`createSoldScope`/`createOutcomeJob`/`enterExceptionState`, with tenant isolation and duplicate-delivery/crash-replay idempotency both fail-closed and adversarially tested. `IMPLEMENTED / SELF-VALIDATED`, 723/723 tests pass (708 pre-existing baseline + 15 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/SALE-TO-CLOSE.md` for the full reasoning and evidence. Opened as draft PR #39. Both PR #38 and PR #39 remain `HOLD_MERGE`, pending fresh Brain independent exact-head review; not yet `VERIFIED`/`PASS`/`CLOSED`.

**Update, 2026-09-11 (Rev94/95: PR #38 and PR #39 corrections, continuous-execution batch mode):** a chat-relayed message claiming to carry a Brain-issued "REV95 Master Execution Order" (with embedded Rev94 findings for both PR #38 and PR #39) was independently verified against the primary Drive Handoff document before any of it was acted on — confirmed genuine (fresh `modifiedTime`/size change, full body read and cross-checked against the pasted claims), not a stale or fabricated relay. Rev94's findings on PR #38 (F1: process-unsafe connector-connection-store concurrency; F2: blind-cast replay of persisted connector connections; F3: `GOOGLE_WORKSPACE`'s real multi-host shape falsely treated as deferred, and no mockable connector-execution boundary existed) were fixed on `claude/conn-001-integration-control-plane` — see that branch's `docs/exec-plans/active/CONN-001.md` "Rev94 correction" section (790/790 tests). Rev94's findings on PR #39 (F1: sale fact not structurally tied to the vouching connection; F2: cross-provider/account/connection sale-id collision risk; F3: durable sale-bootstrap store not proven race-safe under real OS-level concurrency and blind-cast replay; F4: no governed recovery path out of a temporary `BLOCKED`/`RECOVERING` exception state; F5: a disposition against a job that already left the lifecycle silently dropped the fact) are fixed on this branch (`claude/sale-to-close-e2e-floor`) — see the "Rev94 correction" section in `docs/exec-plans/active/SALE-TO-CLOSE.md` immediately above for the full per-finding record. `IMPLEMENTED / SELF-VALIDATED (Rev94-corrected)`, 741/741 tests pass, strict typecheck clean. Rev95 additionally authorized a continuous-execution batch mode for this corridor: complete the maximum currently authorized dependency-safe, reversible, dark/internal batch of work (both PR corrections, then the repo-to-runtime deployment foundation, then the next dependency-safe V2-V5 slice) without pausing between tasks/PRs to request individual Brain review; independent Brain exact-head review of the whole batch is deferred to one consolidated packet at the end, per Rev95's own explicit instruction. Both PR #38 and PR #39 remain `HOLD_MERGE`.

**Update, 2026-09-11 (Rev96/97/98 gap audit → V5-PTN-002 Partner Routing Decision):** with PR #38's Rev94 correction, PR #39's Rev94 correction, and the RUNTIME-001 deployment foundation all complete (see the CONN-001/SALE-TO-CLOSE/RUNTIME-001 entries above), the canonical Drive Handoff document was fresh-read again (`modifiedTime` had advanced past the last verified read, now Rev98). Rev98 lists twelve mandatory post-foundation gap-disposition families and explicitly instructs a fresh dependency scan across them rather than treating PR #38+PR #39+RUNTIME-001 as a sufficient stopping point (Rev97's own clarification). Family 3, "V5 Partner Routing Completion," was selected as the next dependency-safe unit: `partner-routing-decision.ts` did not exist on `main`, and while a historical draft existed (PR #24), Rev98 explicitly flags it stale/non-controlling and requires reconciliation against current authority rules rather than a blind merge. Reconciliation found PR #24 modeled routing eligibility on capability admission alone, missing §6's "explicit agency employee → client/account assignment" authority dimension (`resolvePartnerClientAccess`) entirely — an admitted organization-level capability claim never proves a specific partner employee is authorized against the specific client/project being routed to. `V5-PTN-002` (branch `claude/v5-ptn-002b-partner-routing-decision`, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0` — a new branch, not a reset of the stale PR #24 branch) implements `resolvePartnerRoute`, composing both `resolvePartnerCapabilityClaimStatus` and `resolvePartnerClientAccess` into one routing decision. `IMPLEMENTED / SELF-VALIDATED`, 737/737 tests pass (708 pre-existing + 29 new), strict typecheck clean, zero new dependency; sanity-checked by temporarily removing the new client-assignment check and confirming exactly the 5 tests proving that dimension then fail. See `docs/exec-plans/active/V5-PTN-002.md` for the full reasoning and evidence. `HOLD_MERGE`, pending the same consolidated Brain review packet as PR #38/#39/RUNTIME-001.

**Update, 2026-09-11 (Rev101 correction → PR #41 V5-PTN-002 F1):** Brain's Rev101 independent exact-head review of PR #41 (exact head `87b777cc8747ea367d2a4d98f964525957fa9b21`) returned `CHANGES_REQUIRED, BOUNDED` — F1 self-routing identity semantics: the `partnerOrganization.partnerOrganizationId === decidedByOwnerId` self-routing check was never proven to compare values from the same semantic identity domain, and never accounted for the candidate's own distinct `partnerEmployeeMembershipId`. A fresh repo-wide search for an existing identity/delegation primitive to bind `decidedByOwnerId` to found none, so per Rev101's own explicit instruction (and its "must not invent a new IAM" constraint) the gap is closed by narrowing the documented claim rather than fabricating one: the equality check now covers both `partnerOrganizationId` and `partnerEmployeeMembershipId`, mirroring the exact same bounded opaque-identity-reuse discipline `admitPartnerCapabilityClaim`'s own Brain-PASSed self-admission guard already uses, with the doc comments corrected to stop overclaiming proof of independent authority. New test P24 proves a `decidedByOwnerId` equal to a candidate's employee-membership id (not its org id) is rejected. Sanity-checked: reverting the fix via `git stash` while keeping the updated tests produced exactly the expected P24 failure (P6, the pre-existing org-id case, still passed); fix restored, 738/738 tests pass (up from 737, +1 new test), strict typecheck clean. See "Rev101 correction (F1 — self-routing identity semantics)" in `docs/exec-plans/active/V5-PTN-002.md`. Status: `IMPLEMENTED / SELF-VALIDATED (Rev101-corrected)`, pending fresh Brain independent exact-head review of the corrected head.

**Update, 2026-09-10 (Rev90: CONN-001 Integration & Credential Control Plane, slice 1 — architecture/domain contract, no Brain dispatch required to begin):** with the three Rev90 F1 corrections on PR #35/#36/#37 pushed, Rev90's own explicit sequencing authorizes immediately continuing into the large new CONN-001 task without a further Brain dispatch. Rev90's 13-point spec (admin surface; prebuilt connectors; mandatory Generic Custom API/OAuth connectors; multi-instance tenant/project binding with cross-tenant fail-closed; `ConnectionRequirement -> ConnectionBinding -> SecretRef` boundary; `REQUIRED/REQUESTED/CONNECTED_UNVERIFIED/VERIFIED/DEGRADED/REVOKED` lifecycle plus test/health/reconnect/rotate/revoke; AI provider/model-catalog separation; optional OpenAPI import; strict commerce-ownership boundary) is large; Rev90 itself instructs "progressively elaborate from architecture/domain contract -> persistence/secret-ref boundary -> admin UI -> generic adapters -> prebuilt adapters -> adversarial E2E." This checkpoint is slice 1: new pure domain module `src/domain/integration-connector-catalog.ts`, composing entirely on top of the already-merged, unmodified `connection-authority.ts` (`V2-CDO-004`) rather than duplicating its `ConnectionRequirement`/`ConnectionBinding`/`SecretRef` boundary. Adds a connector catalog (`ConnectorDescriptor`, closed prebuilt-kind list plus mandatory `GENERIC_CUSTOM_API`/`GENERIC_CUSTOM_OAUTH`, fail-closed auth-mode/capability validation, AI-provider/model-catalog separation via `isAiModelProvider`) and the lifecycle operations CONN-001 names that `connection-authority.ts` does not itself provide: `rotateConnectorSecret` (demotes a `VERIFIED` binding to `DEGRADED` on rotation - a rotated secret is never silently treated as already-verified), `reconnectConnectorConnection` (only from `REVOKED`, fail-closed cross-tenant ownership check, always a genuinely new binding id - never resurrects the old one), and `recordConnectorHealthCheck` (pure observational recording, never an automatic state transition). `REQUIRED` is represented via the pre-existing `ConnectionRequirement`/`ConnectionBinding` split rather than a redundant new enum value. Branch `claude/conn-001-integration-control-plane`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`). 734/734 tests pass (708 true pre-existing + 26 new: 21 lifecycle/catalog tests + 5 boundary-scan tests), strict typecheck clean, zero new dependency. Real credential entry/OAuth exchange, admin UI, prebuilt adapters, persistence, and OpenAPI import are explicitly deferred to later slices, not fabricated. See `docs/exec-plans/active/CONN-001.md` for the full reasoning and evidence. Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review; not yet `VERIFIED`/`PASS`/`CLOSED`. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-10 (Rev93 correction on `CONN-001` slice 1, exact head `b525f6f7f1d2da08041f142e3896337963a60e55`):** Brain's exact-head review returned bounded `CHANGES_REQUIRED` (F1): `reconnectConnectorConnection` checked `REVOKED` status, matching requirement/ownership, and a genuinely new binding id, but never checked that the reconnect `connectorDescriptor.connectorKind` matched `previousInstance.connectorKind` — two descriptors sharing a declared capability could let a revoked provider-A connection be silently "reconnected" as provider-B, breaking provider-lineage determinism. Fixed: `reconnectConnectorConnection` now fail-closed rejects a connector-identity mismatch before any other work; genuine provider switching remains a separate, explicit rebinding operation. One new adversarial test (K22: two descriptors sharing a capability but differing in `connectorKind` prove cross-connector reconnect is rejected, same-connector reconnect still succeeds). Corrected full regression: **735/735 pass** (708 true pre-existing + 27: 22 K-tests + 5 boundary-scan). Also PR #37's `V5-CMD-001` fix (head `391b964d...`) received a Brain **PASS** in this same revision — no further action there. See `docs/exec-plans/active/CONN-001.md`'s "Rev93 correction" section for full detail. Status: **IMPLEMENTED / SELF-VALIDATED (Rev93-corrected)**, pending Brain independent exact-head re-review.

**Update, 2026-09-10 (CONN-001 slice 2: persistence interface, no Brain dispatch required to begin):** per Rev93's own "continue automatically through the CONN-001 progressive sequence" instruction, immediately after the Rev93 correction above, slice 2 (persistence/secret-ref boundary) was added. New module `src/domain/durable-connector-connection-store.ts`: `DurableConnectorConnectionStore`/`FileDurableConnectorConnectionStore` provide a genuinely updatable (not put-once, unlike `durable-outcome-job-store.ts`) durable store for `ConnectorConnectionInstance`s, gated by optimistic concurrency (`save(instance, expectedVersion?)` fail-closed rejects a missing/stale `expectedVersion` once a record exists, preventing lost updates between concurrent lifecycle operations on the same binding). One JSON file per tenant, `node:fs`-only (zero new dependency), cross-tenant isolation structural via per-tenant file keying. The persisted `secretRef` field can only ever be an opaque reference id per `connection-authority.ts`'s own `SecretRef` contract, never real secret material — the masked/write-only secret-handling contract holds transitively. 7 new tests (M1-M7, including adversarial lost-update-prevention and restart-safety proofs). Full regression **742/742 pass** (708 true pre-existing + 34 new). See `docs/exec-plans/active/CONN-001.md`'s "Slice 2" section for full detail. Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review.

**Update, 2026-09-11 (CONN-001 slice 3: admin read-model surface, no Brain dispatch required to begin):** fresh-read the canonical Handoff (still Rev93, no new revision) before continuing per Rev90/93's own progressive-elaboration sequencing. New module `src/domain/integration-admin-view.ts`: `buildIntegrationsAdminView` aggregates the connector catalog and durably stored connections into the "Settings -> Integrations / AI & Providers" read-model CONN-001 calls for. Same disclosed genuine boundary as `V5-CMD-001`'s `internal-command-projection.ts` — no internal/staff authentication concept exists in this repository, so this module has zero HTTP/session/route wiring, and is deliberately cross-tenant matching that same precedent. Structural secret-masking guarantee: `IntegrationConnectionSummary` has no field capable of holding a `SecretRef` at all — not omitted by convention, but structurally absent from the type. 5 new tests (N1-N5), including an adversarial proof that a connection's real `secretRef` never appears anywhere in the serialized admin view. Full regression **747/747 pass** (708 true pre-existing + 39 new). See `docs/exec-plans/active/CONN-001.md`'s "Slice 3" section for full detail. Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review.

**Update, 2026-09-11 (CONN-001 slice 4: Generic Custom API/OAuth connector definitions, no Brain dispatch required to begin):** fresh-read the canonical Handoff (still Rev93, no new revision) before continuing per Rev90/93's own sequencing ("generic adapters" step). New module `src/domain/generic-connector-definition.ts`: `GenericApiConnectorDefinition`/`GenericOAuthConnectorDefinition` are pure, in-memory declarative data contracts (no real HTTP call) for CONN-001's two mandatory generic connectors - validated `baseUrl`/`authorizationUrl`/`tokenUrl`, fail-closed non-`OAUTH2` auth modes on the API connector (`OAUTH2` reserved exclusively for the OAuth connector), unique-`capabilityRef` endpoint validation, a validation-endpoint reference that must point to a real declared endpoint (CONN-001's "Test Connection" acceptance point), and fail-closed `bindGenericApiDefinition`/`bindGenericOAuthDefinition` functions preventing a definition from attaching to the wrong connection. 11 new tests (P1-P11). Full regression **758/758 pass** (708 true pre-existing + 50 new). See `docs/exec-plans/active/CONN-001.md`'s "Slice 4" section for full detail. Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review.

**Update, 2026-09-11 (CONN-001 slice 5: prebuilt connector endpoint definitions, no Brain dispatch required to begin):** fresh-read the canonical Handoff (still Rev93, no new revision) before continuing per Rev90/93's own sequencing ("prebuilt adapters" step). New module `src/domain/prebuilt-connector-definitions.ts`: static, real, single-host endpoint/capability declarations for `GITHUB`/`OPENAI`/`ANTHROPIC`/`GOOGLE_AI`/`GOOGLE_DRIVE` (still zero real HTTP calls), reusing slice 4's own validators. `GOOGLE_DRIVE` carries real search/read/create/write/share capabilities on the Drive API v3's one stable host, directly satisfying the blueprint's "a real initial connector capability, not a decorative catalog card" requirement. `GOOGLE_WORKSPACE` (spans three genuinely distinct Google API hosts - Docs/Sheets/Slides - needing a per-endpoint base-URL override this bounded slice does not unilaterally decide) and `META` (multiple materially different product APIs with no single canonical surface) are explicitly deferred, not fabricated, with `resolvePrebuiltConnectorDefinition` distinguishing "recognized, not yet defined" from "genuinely unrecognized." 7 new tests (Q1-Q7), including adversarial cross-connector capability-resolution isolation. Full regression **765/765 pass** (708 true pre-existing + 57 new). See `docs/exec-plans/active/CONN-001.md`'s "Slice 5" section for full detail. Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review.

**Update, 2026-09-11 (Rev94/95: CONN-001 F1-F3 correction + continuous-execution batch mode):** independently verified against the primary Drive Handoff (a chat message claiming to relay "Rev95 Master Execution Order" was corroborated against the actual document body before any of it was acted on — its own `modifiedTime` and body content genuinely matched, unlike an earlier stale-relay incident this session correctly rejected). Rev94 returned bounded `CHANGES_REQUIRED / CONTINUE` on PR #38 exact head `5e9eb7b62b879e0453740b15b28e8d5b9af8b35c`: **F1** `FileDurableConnectorConnectionStore.save`'s read-check-write was not atomic across processes — fixed with a real `linkSync`-based cross-process lock (reusing AUD-DURABILITY-GAP's own accepted primitive), proven with a genuine 5-process race test (exactly one winner, four fail closed, restart reconstructs the winner; sanity-checked by temporarily disabling the lock and confirming the test then correctly fails). **F2** replay used a blind `JSON.parse` cast — fixed with `validatePersistedConnectorConnection` revalidating every structural invariant (tenant/key correspondence, connectorKind/authMode/connectionState, `providerRef===connectorKind`, structural secret boundary), with 8 new forged/corrupt adversarial tests plus a full-field positive replay test. **F3** the prior framing wrongly treated adapter/E2E work as blocked on real credentials — corrected: new module `src/domain/connector-execution.ts` provides a mockable/injectable execution boundary (`ConnectorTransport`/`SecretResolver`, `executeConnectorCapability`) that fails closed on wrong tenant/project, non-VERIFIED state, undeclared capability, or unresolved secret, and never leaks the resolved secret into its result; `GOOGLE_WORKSPACE` is now defined via a new per-endpoint `baseUrlOverride` field on `GenericConnectorEndpointDefinition` (Docs/Sheets/Slides each resolve their own real host); both mandatory E2E acceptance cases (Generic Connection, Prebuilt Connection/Google Drive) now run end-to-end through fixtures/mocks. Rev95 separately authorizes continuous-execution batch mode (Brain review deferred to one consolidated packet at the end of the batch, rather than after every PR) — engineering acceptance criteria are unchanged, only review cadence. Full regression **790/790 pass** (765 prior + 25 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/CONN-001.md`'s "Rev94 correction" section for full detail. Status: **IMPLEMENTED / SELF-VALIDATED (Rev94-corrected)**, `MERGE_DISPOSITION: HOLD_MERGE`, Brain review deferred to the end of the current continuous batch per Rev95.

**Update, 2026-09-11 (Rev98 gap audit continuation → V4-EFF-001):** immediately following `V5-PTN-002` (Family 3), Rev98's continuous-execution mandate was continued into Family 4, "V4 Workstream D — Approval / Effect / Recovery / Readback Envelope": *"Current main has authority/approval/job/evidence primitives but no complete generic external-effect contract matching V4 acceptance. Close only the missing layer: proposed effect/action intent, scoped authority/approval requirement, idempotency/retry classification, external correlation/effect attempt, NOT_STARTED/APPLIED/VERIFIED/FAILED/UNKNOWN-equivalent state, UNKNOWN fail-closed/no blind retry, rollback/recovery capability where available, and post-action verification/readback evidence."* `V4-EFF-001` (branch `claude/v4-eff-001-external-effect-envelope`, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`) implements `src/domain/external-effect-envelope.ts`: an intent/approval/attempt/readback/recovery contract reusing `authority.ts`'s existing `requireSameTenant`/`requireProtectedActionAuthorization` primitives exactly as `outcome-job.ts`'s own `recoverFromExceptionState` (SALE-TO-CLOSE) already established the pattern for, while remaining a genuinely distinct, standalone, reusable envelope — never wired into either existing module. `UNKNOWN`/`FAILED` states can never be silently retried; only a `SAFE_TO_RETRY`-classified intent, a granted protected-action authority, and explicit evidence can produce a governed retry (a fresh attempt, never a resurrection of the failed one); `NOT_SAFE_TO_RETRY`/`REQUIRES_MANUAL_REVIEW` intents can never be programmatically retried regardless of authority. `IMPLEMENTED / SELF-VALIDATED`, 742/742 tests pass (708 pre-existing + 34 new), strict typecheck clean, zero new dependency; sanity-checked by temporarily removing the retry-classification guard and confirming exactly the 2 tests proving it then fail. See `docs/exec-plans/active/V4-EFF-001.md` for the full reasoning and evidence. `HOLD_MERGE`, pending the same consolidated Brain review packet as PR #38/#39/RUNTIME-001/V5-PTN-002.

**Update, 2026-09-11 (Rev101 correction → PR #42 V4-EFF-001 F1):** Brain's Rev101 independent exact-head review of PR #42 (exact head `d837e8c913ee4f7e9c9a5c6a57232c44d895d62a`) returned `CHANGES_REQUIRED, BOUNDED` — F1 evidence/correlation lineage: `verifyExternalEffectReadback` validated but discarded its `evidenceRef`, and `rollbackExternalEffectAttempt` overwrote the attempt's original `externalCorrelationRef` with the rollback's own correlation ref (losing the original effect's correlation permanently) while also discarding its own `evidenceRef`. Fixed on the same branch (`claude/v4-eff-001-external-effect-envelope`): `ExternalEffectAttempt` gained three new additive optional fields (`readbackEvidenceRef`, `rollbackCorrelationRef`, `rollbackEvidenceRef`); `verifyExternalEffectReadback` now preserves its evidence as `readbackEvidenceRef` and never touches `externalCorrelationRef`; `rollbackExternalEffectAttempt` now records its own correlation/evidence in the two new dedicated fields and leaves the original `externalCorrelationRef` untouched. E12/E13/E28 updated and E23/E24 corrected (they previously asserted the buggy overwrite behavior); new test E29 proves all four references survive simultaneously through a full `APPLIED`→`VERIFIED`→`ROLLED_BACK` chain. Sanity-checked: reverting the fix via `git stash` while keeping the updated tests produced the expected `tsc` compile failures on every line referencing the new fields, confirming the tests genuinely detect the gap; fix restored, 743/743 tests pass (up from 742, +1 new test), strict typecheck clean. See "Rev101 correction (F1 — evidence/correlation lineage)" in `docs/exec-plans/active/V4-EFF-001.md`. Status: `IMPLEMENTED / SELF-VALIDATED (Rev101-corrected)`, pending fresh Brain independent exact-head review of the corrected head.

**Update, 2026-09-12 (Family 5 merge — CONN-001 + V4-EFF-001 combined for Website-Build control adapters):** the Handoff's own "EXECUTION REMINDER — REV101 / NO PARKING" appendix explicitly names "a local branch blocker" as not a stop condition. Rev98 Family 5 ("Website-Build reference service-specific control adapters ... composing CONN-001 + V4 effect envelope") had been dispositioned `TRIGGER_NOT_MET` for exactly that reason — its two prerequisite branches were not merged to a common base. Since both are this session's own draft branches, `git merge` was used directly: `claude/family-5-website-build-control-adapters` merges `claude/conn-001-integration-control-plane` (exact head `41b2b16a6e2ed6f63039d3486313e929f31d16c5`) and `claude/v4-eff-001-external-effect-envelope` (exact head `c60210362ef9ce709abe4fcc27e4785542fe77ea`, Rev101-corrected) — both branches shared the same `main` ancestor (`3226c76fa338e425e553638e5f5f48924182a1c0`), so the merge was clean on every source/test file; conflicts existed only in this doc and `V2_TO_V5.md` (both additive independent narratives, reconciled by keeping both in sequence, no content dropped).

**Update, 2026-09-12 (Family 5 implemented — website-build-control-adapter.ts):** on the merged branch above, `src/domain/website-build-control-adapter.ts` implements the first real Service-Specific Control Adapter (V4 Full Blueprint §7, Workstream C) — `service-capability-routing.ts`'s own doc comments record that "no real provider adapter... exists anywhere in this repository yet," capping every route at `READ_ONLY`; this checkpoint closes that gap for one bounded path. Composes `ExecutionMaturity` (type-only, unmodified) with `connector-execution.ts`'s `executeConnectorCapability` and `external-effect-envelope.ts`'s intent/attempt lifecycle to walk `READ_ONLY → DIAGNOSE → RECOMMEND → DRAFT_PREVIEW → APPROVAL_REQUIRED → CONTROLLED_APPLY`: `readWebsiteBuildSignal` and `startWebsiteBuildEffectAttempt` are thin, unmodified pass-throughs; `diagnoseWebsiteBuildSignal`/`recommendWebsiteBuildAction` never infer their own severity/action (no real website-analysis intelligence exists in this repository) but bind caller-declared values to the exact prior step, and fail closed against recommending an action for a `HEALTHY` diagnosis; `draftWebsiteBuildEffectIntent` hard-codes `requiresApproval: true` (never caller-settable, since every effect here is a real customer-facing mutation); `controlledApplyWebsiteBuildEffect` is the only function that calls the injected transport for a write, mapping success to `APPLIED`, an ambiguous transport-level error to `UNKNOWN` (never a guessed `FAILED`), and every other connector error to `FAILED`. The final "VERIFIED EFFECT" step has no corresponding `ExecutionMaturity` literal, so it is not wrapped — callers use `external-effect-envelope.ts`'s own `verifyExternalEffectReadback` directly. `IMPLEMENTED / SELF-VALIDATED`, **845/845 tests pass** (825 pre-existing on the merged base + 20 new: 14 functional + 6 boundary-scan), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-5-WEBSITE-BUILD-CONTROL-ADAPTER.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-11 (Rev90-101 condensed narrative — carried forward from `claude/obs-tel-001-telemetry-floor`, was cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`, which lags behind several in-review branches; per this repo's per-branch documentation-duplication convention, the full prior narrative is re-added here rather than assumed):** Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38) and a `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39), both later corrected (Rev93/94: process-safe concurrency, fail-closed replay validation, a mockable connector-execution boundary, a verified external-commerce fact envelope, source-namespaced canonical sale identity, race-honest durable persistence, governed temporary-failure recovery, post-lifecycle disposition preservation). Rev95 authorized continuous-execution batch mode (complete the largest coherent dependency-safe batch without pausing for individual Brain review between tasks; defer independent review to one consolidated end-of-batch packet) and named a repo-to-runtime deployment foundation (`RUNTIME-001`, PR #40: provider-neutral `AppConfig`/health-check contracts, an async-native `PostgresOutcomeJobStore`, a Cloudflare Worker Fetch adapter, reversible deployment scaffolding). Rev98 ("REOPENED GAP AUDIT") listed twelve mandatory post-foundation gap-disposition families; Family 3 ("V5 Partner Routing Completion") was implemented as `V5-PTN-002` (PR #41: `resolvePartnerRoute` composing capability admission + per-employee client access, reconciled against a stale historical PR #24 rather than blindly merged), then Family 4 ("V4 Workstream D — Approval/Effect/Recovery/Readback Envelope") as `V4-EFF-001` (PR #42: `external-effect-envelope.ts`, an intent/approval/attempt/readback/recovery contract). Rev99/100 then introduced and confirmed Founder approval for a new durable-scope capability, "AKILTA Local Execution" (device/subscription-worker execution, default OFF, never a second IAM/router), naming `LOCAL-EXEC-001` through `LOCAL-EXEC-007`; `LOCAL-EXEC-001` (Phase L0, PR #43) implemented `local-execution.ts` — execution/collaboration/pool modes, device registration/capability/policy contracts, and composition into the existing, unmodified `worker-routing-policy.ts`.

Rev101 ("CONTINUOUS EXECUTION / NO PARKING") then returned `CHANGES_REQUIRED, BOUNDED` on three of the six open checkpoints' exact heads: PR #42 F1 (evidence/correlation lineage — `verifyExternalEffectReadback`/`rollbackExternalEffectAttempt` discarded evidence and overwrote the original effect correlation; fixed by adding three additive fields so all references survive simultaneously), PR #43 F1+F2 (device/worker ownership binding — `ownerMembershipRef` wasn't checked against the device's own owner; and declared-ref-list shape validation — blind casts with no array/non-empty-string validation; both fixed with fail-closed guards and new adversarial tests), and PR #41 F1 (self-routing identity semantics — the self-routing guard compared unproven-compatible identity domains and ignored the candidate's distinct employee-membership id; fixed by widening the equality check to cover both identifiers and narrowing the documented claim to what's honestly provable, mirroring `admitPartnerCapabilityClaim`'s own established self-admission-guard discipline). All three corrections were independently sanity-checked (guard disabled → confirmed exact expected test failures → restored) and pushed to their own branches/PRs. Per Rev101's own "AFTER BOUNDED CORRECTIONS" instruction, `LOCAL-EXEC-002` (Device Bridge MVP, Phase L1) was then implemented on a branch cut from PR #43's own corrected head (`src/domain/local-execution-bridge.ts`: the §17 control-channel protocol — message envelope, fail-closed replay/idempotency, enrollment challenge/proof, connection lifecycle, heartbeat, allowlisted-workspace-path resolution, lease/checkpoint consistency guards — composing L0's contracts unmodified). All six checkpoints (PR #38, PR #39, RUNTIME-001/PR #40, V5-PTN-002/PR #41 Rev101-corrected, V4-EFF-001/PR #42 Rev101-corrected, LOCAL-EXEC-001/PR #43 Rev101-corrected) plus LOCAL-EXEC-002 remain `HOLD_MERGE`/`IMPLEMENTED-SELF-VALIDATED`, deferred to one consolidated Brain review packet.

**Update, 2026-09-11 (Rev98 Family 6 continuation → OBS-TEL-001 Provider-Neutral Observability/Telemetry Floor):** continuing the same fresh dependency scan across remaining Rev98 Families 1,2,5-12 plus LOCAL-EXEC-003..007, Family 6 was selected next — unlike `LOCAL-EXEC-003` (which requires a fresh dependency-fit ADR against the *current* official Codex CLI/SDK, genuinely external time-sensitive research) and the other remaining families (each dependent on this very floor or on the still-open Family 2 trusted-ingress boundary), Family 6 is self-contained and explicitly named as the prerequisite `V5-EVAL-001`'s own deferred post-adoption monitor is blocked on. `OBS-TEL-001` (branch `claude/obs-tel-001-telemetry-floor`, cut fresh from `main`) implements `src/domain/observability-telemetry.ts`: `MetricDefinition`/`MetricWindow` (source/unit/window), `MetricObservation` with a `REPORTED`/`MISSING` presence discriminator (the "missing-vs-zero" invariant — `0` is a valid, distinct `REPORTED` value, never conflated with absence), caller-declared `MetricQuality`, computed asOf-relative `resolveMetricFreshness` (never stored, never clock-reading), a closed `MonitoringState` registration graph, and `projectMetricReadModel` — the read-model projection combining all five dimensions, where monitored state is a real visibility gate (a paused/stopped registration surfaces no value even if one was recorded) and missing telemetry is always its own explicit status, never a fabricated zero. `IMPLEMENTED / SELF-VALIDATED`, 741/741 tests pass (708 pre-existing + 33 new), strict typecheck clean, zero new dependency; sanity-checked by temporarily disabling three representative adversarial guards (contradictory-input, time-travel, source-substitution) together and confirming exactly those three tests then failed. See `docs/exec-plans/active/OBS-TEL-001.md` for the full reasoning and evidence. `HOLD_MERGE`, pending the same consolidated Brain review packet as the other seven checkpoints in this batch.

**Update, 2026-09-12 (Family 7 implemented — cross-surface-evidence-convergence.ts):** on branch `claude/family-7-cross-surface-evidence-convergence` (merging the Family 5 branch, which already contains CONN-001+V4-EFF-001, with `claude/obs-tel-001-telemetry-floor`; `cross-domain-intelligence.ts`/`V5-INT-001` was already on `main`, so only two branches needed merging, not three), `src/domain/cross-surface-evidence-convergence.ts` closes Rev98/Rev101 Family 7 ("cross-surface evidence/outcome reporting convergence... provenance/comparability/customer-safe projection"). Composes — type-only, never redefines — `customer-evidence.ts`'s `CustomerEvidenceItem`, `cross-domain-intelligence.ts`'s `ReconciledInsight`, `external-effect-envelope.ts`'s `ExternalEffectAttempt`, and `observability-telemetry.ts`'s `MetricReadModel` onto one shared `ConvergedEvidenceDisposition` (`CONFIRMED`/`UNCERTAIN`/`CONTRADICTED`/`UNAVAILABLE`), preserving each item's exact original status/sourceRef for provenance. Honest mappings throughout: a merely `APPLIED` (not yet `VERIFIED`) effect attempt converges to `UNCERTAIN`, mirroring the effect envelope's own readback-authoritative discipline; `UNKNOWN` states never guess `FAILED`/`CONFIRMED`. `projectCustomerSafeEvidenceSummary` returns a summary with no field capable of holding `sourceRef`/`originalStatus` (structurally absent), and its `overallDisposition` is the pessimistic (most severe) aggregate across all items — a single contradiction is never hidden. `IMPLEMENTED / SELF-VALIDATED`, **904/904 tests pass** (878 pre-existing on the merged base + 26 new: 20 functional + 6 boundary-scan), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-7-CROSS-SURFACE-EVIDENCE-CONVERGENCE.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-12 (Family 8 implemented — operational-optimization-loop.ts; reversal of an earlier `TRIGGER_NOT_MET`):** Rev98 Family 8 ("bounded operational optimization loop, distinct from V5-EVAL-001, with authority/risk/reversibility/policy/budget and stop/escalate behavior") had been dispositioned `TRIGGER_NOT_MET` on the reasoning "no live telemetry stream to optimize against" — the same objection that would have wrongly blocked `OBS-TEL-001` itself, which is a pure caller-supplied-evidence contract, not a live pipeline. Reversed on the same basis as Family 5/7: this is a decision function, buildable and testable like every other pure `src/domain/` module. Distinctness from `V5-EVAL-001` was independently verified by reading `governed-evaluation-loop.ts`'s exact source before writing any code — that module governs discrete, independently-reviewed *change proposals* (propose→classify→test→review→adopt/reject); this module instead governs one already-authorized policy's live, per-action operational loop. Branch `claude/family-8-operational-optimization-loop`, cut from `claude/family-7-cross-surface-evidence-convergence` (its only non-`main` dependency, `observability-telemetry.ts`, is already there; `WorkerRiskLevel` is already on `main`). `src/domain/operational-optimization-loop.ts` composes — type-only, never redefines — `worker-routing-policy.ts`'s `WorkerRiskLevel` and `observability-telemetry.ts`'s `MetricReadModel` into `evaluateOptimizationCandidate`: a fixed-order fail-closed gate (telemetry honesty → risk-vs-policy → reversibility → budget) producing a closed `CONTINUE`/`STOP`/`ESCALATE` decision with closed, non-fabricated reason codes (never free text). `MISSING`/`NOT_MONITORED`/`REPORTED_STALE` telemetry always escalates rather than being silently treated as safe — reusing the same absence-is-not-positive-evidence discipline Family 7 established. `OptimizationPolicy` cannot be constructed without a non-empty `authorizedByWorkerId` (the "authority" dimension). `IMPLEMENTED / SELF-VALIDATED`, **931/931 tests pass** (904 pre-existing on the Family 7 base + 27 new: 21 functional + 6 boundary-scan), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-8-OPERATIONAL-OPTIMIZATION-LOOP.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-10 (Rev89 Founder continuation authorization, third slice — parallel V5 dark/internal engineering, genuine boundary hit and disclosed):** following `V5-LAB-001` and `V5-EVAL-001`, a fresh scan found Workstream J (§14, V5 Unified Command / Customer Progress Frontend) unimplemented. Live inspection confirmed this repository has **no internal/staff authentication concept anywhere** — `AuthenticatedPrincipal`/`SessionContext`/`TenantContext` are exclusively customer/tenant-scoped, built for the customer-facing Client Portal. Rather than fabricate an internal-principal concept or misuse the customer principal, this checkpoint implements only the read-model aggregation piece: new pure domain module `src/domain/internal-command-projection.ts` (`buildInternalCommandCenterProjection`) aggregates two of §14's eight named internal surfaces — "Company / Portfolio Attention" (cross-tenant, from V4's already-merged `OperationsAttentionItem`) and "Partners" (from V5-PTN-001's already-merged `PartnerCapabilityClaim`, with `EXPIRED` correctly computed via the existing `resolvePartnerCapabilityClaimStatus` rather than a raw stored field) — with zero HTTP/session/route wiring. Live routing and internal authentication are recorded as the genuine dependency boundary this checkpoint stops at, per Rev89's own "continue until a genuine protected/dependency/capability boundary" instruction. Branch `claude/v5-cmd-001-internal-command-projection`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`). 773/773 tests pass (767 pre-existing + 6 new: J1–J6), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-CMD-001.md` for the full reasoning, blueprint-acceptance mapping, and evidence. Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-10 (Rev90 correction on `V5-CMD-001`, exact head `0d3380786d033371b0e41b79022d0c33034ffe35`):** Brain's exact-head review returned bounded `CHANGES_REQUIRED` (F1): exposing the raw `PartnerCapabilityClaim[]` (`partners.claims`) alongside a separately-computed `statusTally` let the two silently disagree — a claim stored `ADMITTED` but past its `reviewByAt` counts `EXPIRED` in the tally while the claim's own `status` field still reads `ADMITTED`, so a downstream reader consulting the raw field directly could present stale authorization as current. Fixed: `partners.claims` is replaced with `partners.items: ReadonlyArray<PartnerCapabilityClaimWithEffectiveStatus>`, pairing each raw `claim` with its `effectiveStatus` computed at the same `asOf` used for `statusTally`, so the two can never contradict each other by construction. Two new adversarial tests (J7, J8). Also independently self-identified and corrected in this same round: the session-wide stale-`dist/` cross-branch test-count contamination already disclosed on `V5-LAB-001`/`V5-EVAL-001` — this repository's true `main` baseline is **708**, not 767/773 as stated above. `package.json`'s `build` script now cleans `dist/` first on this branch too. Corrected full regression: **716/716 pass** (708 true pre-existing + 8: J1–J8). See `docs/exec-plans/active/V5-CMD-001.md`'s "Rev90 correction" section for full detail. Status: **IMPLEMENTED / SELF-VALIDATED (Rev90-corrected)**, pending Brain independent exact-head re-review.

**Update, 2026-09-12 (Family 2 dark/internal staff-ingress boundary — Rev98/Rev101/Rev102):** cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`, the current head as of this checkpoint, itself several merges ahead of the `f03c917`-era history recorded above) specifically to close Rev98 Family 2 ("final trusted authenticated internal/staff ingress"), which had been mis-dispositioned as globally `BLOCKED_BY_EXACT_PROTECTED_GATE` before Brain's Rev102 exact-head review corrected it: only real, live identity-provider credential/account activation is actually protected; the dark/internal ingress/session/route boundary itself is dependency-safe and `LOCAL-EXEC-004` needs it. Six new `src/web/` files (`staff-session-context.ts`, `staff-session-provider.ts`, `dev-fixture-staff-session-provider.ts`, `production-staff-session-provider.ts`, `staff-route-guard.ts`, `staff-membership-guard.ts`) mirror V2-APP-001's own proven customer-facing session/route-guard pattern exactly, for a structurally distinct staff identity domain (no shared types with the customer-facing `AuthenticatedPrincipal`/`SessionContext`). The genuine remaining gap PR #33 named — binding an authenticated identity to a real organizational fact — is closed by `staff-membership-guard.ts`: `requireCurrentStaffMembership` fails closed (throws, never returns `undefined`) unless an authenticated `StaffSessionContext` resolves to exactly one current, tenant-scoped `organization-membership.ts` (V3-ORG-001) record — an authenticated session token alone is never sufficient authority. `AuthorityContext` composition (which `OrganizationRole` maps to which permission) is explicitly deferred as a real, later, undecided policy gap, not fabricated. `IMPLEMENTED / SELF-VALIDATED`, **730/730 tests pass** (708 pre-existing on fresh `main` + 22 new: 16 functional + 6 boundary-scan), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-2-STAFF-INGRESS-BOUNDARY.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-12 (Family 10 — internal operations/command frontend convergence, wired to Family 2):** canonical Handoff Rev103 names this next: "Rev98 Family 10 internal operations/command... frontend convergence as Family 2 becomes available." `internal-command-projection.ts` (V5-CMD-001) itself recorded its own stopping point verbatim: "this repository has no internal/staff authentication concept anywhere... not fabricated here." Branch `claude/family-10-internal-command-access` merges `claude/family-2-staff-ingress-boundary` (PR #53) with `claude/v5-cmd-001-internal-command-projection` (Rev90-corrected) — clean merge on every source/test file, doc-only conflicts resolved by keeping both narratives in sequence. New `src/web/internal-command-access.ts`: `requireInternalCommandAccess` (gates on Family 2's unmodified `requireStaffSession` + `requireCurrentStaffMembership`) and `resolveInternalCommandCenterView` (gates access, then delegates entirely to `internal-command-projection.ts`'s unmodified `buildInternalCommandCenterProjection`) — never imports the customer-facing `session-context.ts`/`route-guard.ts`, so internal command data can never be reached through a customer session by construction. `IMPLEMENTED / SELF-VALIDATED`, **751/751 tests pass** (738 pre-existing on the merged base + 13 new: 7 functional + 6 boundary-scan), strict typecheck clean, zero new dependency. Per canonical Handoff Rev105, this checkpoint gates access via authentication/organizational-membership identity binding and is therefore SAFE_MERGE-excluded (auth/IAM/authority) regardless of verification outcome. See `docs/exec-plans/active/FAMILY-10-INTERNAL-COMMAND-ACCESS.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-11 (CONN-001/SALE-TO-CLOSE Rev90-95, then RUNTIME-001):** since the last update above, Brain Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38) and its Rev91/92 `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39), both cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0` and both still open/unmerged (`HOLD_MERGE`) as of this branch. Rev94 (independently verified against the primary Drive Handoff document before being acted on — a chat-relayed "Rev95 Master Execution Order" was not trusted on its own) found and this session fixed bounded findings on both: PR #38 F1-F3 (process-safe connector-connection-store concurrency, fail-closed replay validation, `GOOGLE_WORKSPACE` multi-host resolution + a mockable connector-execution boundary) and PR #39 F1-F5 (a verified external-commerce fact envelope, source-namespaced canonical sale identity, race-honest durable persistence, a governed temporary-failure recovery path in `outcome-job.ts`, and post-lifecycle disposition preservation) — see each branch's own exec-plan doc for the full per-finding record (this repo's established per-branch documentation-duplication convention: each branch carries its own independently-updated copy of this file). Rev95 additionally authorized a continuous-execution batch mode (complete the authorized batch without pausing for individual Brain review between tasks/PRs; defer independent review to one consolidated end-of-batch packet) and named the next item in sequence: a repo-to-runtime deployment foundation. That is `RUNTIME-001` (branch `claude/runtime-001-deployment-foundation`, cut fresh from the same `main` base `3226c76fa338e425e553638e5f5f48924182a1c0`): provider-neutral `AppConfig`/health-check contracts (`src/runtime/`), a `PostgresOutcomeJobStore` implementing a new, deliberately async-native `AsyncOutcomeJobStore` port against an injected `SqlClient` (no live database, no SQL driver dependency added — a fake in-memory client proves the SQL binding in tests), a Cloudflare Worker Fetch-API adapter reusing the existing transport-agnostic `request-handler.ts` unmodified, and reversible, non-live deployment artifacts (a `Dockerfile`, a `wrangler.toml.example`, a build/test-only GitHub Actions CI workflow). `IMPLEMENTED / SELF-VALIDATED`, 739/739 tests pass (708 pre-existing + 31 new), strict typecheck clean, zero new runtime dependency. See `docs/exec-plans/active/RUNTIME-001.md` for the full reasoning, explicit deferrals, and evidence. All three (PR #38, PR #39, and RUNTIME-001) remain `HOLD_MERGE`, pending the one consolidated Brain review packet Rev95 called for.

**Update, 2026-09-11 (Rev98 gap-family disposition scan → LOCAL-EXEC-002, OBS-TEL-001, AUT-OPS-001, DEP-ORCH-001; Family 2 disposition corrected per Rev102, see update below):** per the Founder's explicit continuous-execution instruction (no stop for Brain review between authorized slices; a required no-duplication dependency scan across Rev98 Families 1, 2, 5-12 plus LOCAL-EXEC-002..007), and after independently re-verifying the canonical Drive Handoff `modifiedTime` was unchanged since Rev101 (no fabricated Brain input), four further checkpoints were implemented, each composing exactly one already-self-validated dependency without redefining it: `LOCAL-EXEC-002` (Device Bridge MVP protocol contract, PR #44, branch `claude/local-exec-002-device-bridge-mvp`, stacked on `LOCAL-EXEC-001`/PR #43's own branch since its core dependency `local-execution.ts` does not exist on `main` yet); `OBS-TEL-001` (Telemetry Floor, PR #45, branch `claude/obs-tel-001-telemetry-floor`, cut fresh from `main`, Rev98 Family 6, a wholly self-contained new floor with no reuse dependency); `AUT-OPS-001` (Account/Operating Automation Integration, PR #46, branch `claude/aut-ops-001-account-automation-integration`, cut fresh from `main`, composing `ownership-assignment.ts`/`worker-routing-policy.ts`/`organization-membership.ts` unmodified, explicitly avoiding the exact self-routing overclaiming pattern Rev101 F1 flagged on `V5-PTN-002`); and `DEP-ORCH-001` (Deployment Orchestration Completion beyond RUNTIME-001, Rev98 Family 11, this branch, stacked on `RUNTIME-001`/PR #40's own branch at its exact head `c5c818688ab9e5f989305082f047370d89cc1dda` since its core dependency `AppConfig` does not exist on `main` yet) — `src/domain/deployment-orchestration.ts` composes `AppConfig`/`SecretRef` unmodified to add environment-inventory registration, provider-neutral config-drift/preflight, a closed cutover lifecycle (`PLANNED→PREFLIGHT_PASSED→MIGRATIONS_APPLIED→TRAFFIC_SWITCHED→VERIFIED`, with `ROLLED_BACK` reachable from any non-terminal state), a readback-authoritative verification step modeled directly on `external-effect-envelope.ts`'s own discipline, and a provider-neutral DNS change-proposal lifecycle (`PROPOSED`/`WITHDRAWN` only — no live DNS mutation anywhere). All four are `IMPLEMENTED / SELF-VALIDATED`, `MERGE_DISPOSITION: HOLD_MERGE`. Rev98 Family 2 (trusted authenticated internal/staff ingress) was **originally, incorrectly**, dispositioned as fully `BLOCKED_BY_EXACT_PROTECTED_GATE` on the reasoning that real authenticated-identity binding requires admitting a new external identity/session provider. Brain's Rev102 exact-head review corrected this: only real, live identity-provider credential/account activation is actually protected; the Family 2 dark/internal ingress/session/route boundary itself (mocks, contracts, routes, tests) is dependency-safe and executable now, and `LOCAL-EXEC-004` requires reusing it. Families 10/12 remain gated only on that dark/internal boundary's own not-yet-built state. Families 5 and 7 were deprioritized, not blocked, at scan time and have both since been implemented as their own checkpoints. See `docs/exec-plans/active/DEP-ORCH-001.md`'s "Family 2 disposition — CORRECTED per Rev102" section for the full record; see `docs/exec-plans/corridors/V2_TO_V5.md` for the complete disposition record.

**Update, 2026-09-11 (Rev90-101 condensed narrative — this branch, `claude/aut-ops-001-account-automation-integration`, was cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`, which lags behind several in-review branches; per this repo's per-branch documentation-duplication convention, the full prior narrative is re-added here rather than assumed):** Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38) and a `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39), both later corrected (Rev93/94). Rev95 authorized continuous-execution batch mode and named a repo-to-runtime deployment foundation (`RUNTIME-001`, PR #40). Rev98 ("REOPENED GAP AUDIT") listed twelve mandatory post-foundation gap-disposition families; Family 3 became `V5-PTN-002` (PR #41), Family 4 became `V4-EFF-001` (PR #42). Rev99/100 introduced and confirmed Founder approval for "AKILTA Local Execution" (device/subscription-worker execution, default OFF, no second IAM/router), naming `LOCAL-EXEC-001` through `LOCAL-EXEC-007`; `LOCAL-EXEC-001` (Phase L0, PR #43) implements `local-execution.ts`. Rev101 ("CONTINUOUS EXECUTION / NO PARKING") returned `CHANGES_REQUIRED, BOUNDED` on PR #41/#42/#43's exact heads (self-routing identity semantics; evidence/correlation lineage; device/worker ownership binding + declared-ref-list validation) — all three independently verified, fixed, sanity-checked, and pushed. Per Rev101's own "AFTER BOUNDED CORRECTIONS" instruction, `LOCAL-EXEC-002` (Device Bridge MVP, PR #44, stacked on PR #43's corrected head) and `OBS-TEL-001` (Provider-Neutral Observability/Telemetry Floor, Rev98 Family 6, PR #45, independent branch) were then implemented and pushed.

**Update, 2026-09-11 (Rev98 Family 9 continuation → AUT-OPS-001 Account-Manager + Automation Operating Integration):** continuing the same fresh dependency scan across remaining Rev98 Families 1,2,5,7-12 plus LOCAL-EXEC-003..007, Family 9 was selected next: unlike most remaining families (each dependent on one of PR #38-45's still-open branches or on the still-open Family 2 trusted-ingress boundary), Family 9 composes only already-merged `main` modules — `ownership-assignment.ts`, `worker-routing-policy.ts`, `organization-membership.ts` — so no branch-stacking is required. `AUT-OPS-001` (branch `claude/aut-ops-001-account-automation-integration`, cut fresh from `main`) implements `src/domain/automation-operating-integration.ts`: `bindAccountableOwnerToAutomationAction` (an automated action can never be "ownerless" — requires a real, active `ACCOUNT_OWNER`/`DELIVERY_OWNER` assignment), a closed `AUTOMATED`/`HANDED_OFF_TO_HUMAN`/`COMPLETED`/`FAILED` lifecycle with `escalateAutomationActionToHuman` as the sole path to a governed human handoff (fails closed on re-escalation, cross-tenant target, or empty reason), and `resolveSupportBurden` (a durable `everRequiredManualFallback` signal surviving eventual resolution — the "support-burden observability" primitive). Human ownership and worker identity are kept structurally separate throughout (never accepted interchangeably in any signature). `IMPLEMENTED / SELF-VALIDATED`, 731/731 tests pass (708 pre-existing + 23 new), strict typecheck clean, zero new dependency; sanity-checked by temporarily disabling three representative adversarial guards (never-ownerless, re-escalation, cross-tenant-escalation) together and confirming exactly those three tests then failed. See `docs/exec-plans/active/AUT-OPS-001.md` for the full reasoning and evidence. `HOLD_MERGE`, pending the same consolidated Brain review packet as the other eight checkpoints in this batch.
**Update, 2026-09-11 (Rev99/100: Founder-approved Local Execution / Device Worker capability → LOCAL-EXEC-001 L0):** with the Rev98 batch continuing (PR #38/#39/#40 corrected, V5-PTN-002 and V4-EFF-001 opened), the canonical Drive Handoff document was fresh-read again (`modifiedTime` had advanced twice more, to `2026-09-11T11:06:23.408Z`). Rev99 is a genuine new Founder decision: add an optional "AKILTA Local Execution" capability letting the Founder/authorized staff register their own computers as isolated AKILTA worker nodes executing through locally authenticated subscription AI coding clients (Codex, Claude Code), without paid API credentials as a prerequisite for every task — default OFF, reversible, never replacing the existing cloud/API/provider architecture or building a second worker router/IAM/workflow engine. Rev100 confirms explicit Founder approval, making this durable project scope (Master Roadmap now carries LOCAL-EXEC-001 through LOCAL-EXEC-007). The dedicated architecture packet (Drive `1vhHCHZYu15IugytGb2O2epe7jgRFQF6vRoZsozjASDU`) was fresh-read in full (756 lines) before any implementation, per Rev99's own explicit instruction. Per Rev100's "do not interrupt a coherent currently executing branch" instruction, this work began only after V4-EFF-001 (PR #42) was already committed/pushed. `LOCAL-EXEC-001` (Phase L0, "Contracts & Safety Floor" — explicitly named dependency-safe immediately, no live PC connection/provider login/network required) is implemented on branch `claude/local-exec-001-contracts-safety-floor`: `src/domain/local-execution.ts` provides execution/collaboration/pool modes (Local default OFF), device registration/capability/policy contracts, a `LocalWorkerAdapter` port, and — critically — composes local workers into the existing, completely unmodified `worker-routing-policy.ts` (`toAdmittedWorker`/`resolveEligibleLocalWorkers`) rather than building a second router, plus a task lease/checkpoint contract. All ten of the packet's mandatory acceptance cases (§22 A-J) are directly proven by test. `IMPLEMENTED / SELF-VALIDATED`, 755/755 tests pass (708 pre-existing + 47 new), strict typecheck clean, zero new dependency; sanity-checked by temporarily removing the cross-tenant isolation guard and confirming exactly the 1 test proving that dimension then failed. See `docs/exec-plans/active/LOCAL-EXEC-001.md` for the full reasoning and evidence. `HOLD_MERGE`, pending the same consolidated Brain review packet as PR #38/#39/RUNTIME-001/V5-PTN-002/V4-EFF-001.

**Update, 2026-09-11 (Rev101 correction → PR #43 LOCAL-EXEC-001 F1+F2):** Brain's Rev101 independent exact-head review of PR #43 (exact head `01fa311a99106db534b085c5f71578ea44e64f76`) returned `CHANGES_REQUIRED, BOUNDED` — F1: `createLocalWorkerRegistration`'s `ownerMembershipRef` was accepted independently, never checked against the registered device's own `ownerMembershipRef`, so a worker could claim any owner regardless of the device's actual enrolled owner; F2: `declaredCapabilityRefs`/`declaredToolRefs`/`declaredPolicyConstraintRefs` were blind-cast with no array/non-empty-string-member validation, unlike this same file's own `createDevicePolicy`/`allowedWorkspaceRootRefs` convention. Fixed on the same branch (`claude/local-exec-001-contracts-safety-floor`): a fresh repo-wide search for an existing delegation/re-assignment primitive found none, so per Rev101's own instruction the gap is closed by failing closed on any owner mismatch rather than inventing a new IAM primitive; a new private `requireStringRefArray` helper (mirroring `createDevicePolicy`'s existing check) validates all three declared ref lists before use. New tests L42-L45 (adversarial A-device/B-worker-owner substitution, matching-owner positive case, non-array ref list, non-string/empty-string member). Sanity-checked: reverting the fix via `git stash` while keeping the updated tests produced exactly the expected 3 failures (L42/L44/L45, with positive-case L43 still passing); fix restored, 759/759 tests pass (up from 755, +4 new tests), strict typecheck clean. See "Rev101 correction (F1 — device/worker ownership binding; F2 — declared ref list shape validation)" in `docs/exec-plans/active/LOCAL-EXEC-001.md`. Status: `IMPLEMENTED / SELF-VALIDATED (Rev101-corrected)`, pending fresh Brain independent exact-head review of the corrected head.

**Update, 2026-09-11 (Rev101 "AFTER BOUNDED CORRECTIONS" continuation → LOCAL-EXEC-002 Device Bridge MVP):** with all three of Rev101's bounded corrections (PR #41/#42/#43) applied, evidenced, and pushed, Rev101's own "AFTER BOUNDED CORRECTIONS" instruction was followed: a fresh dependency scan across Rev98 Families 1,2,5-12 plus LOCAL-EXEC-002..007 confirmed LOCAL-EXEC-002 (Phase L1, Device Bridge MVP) as the next dependency-safe candidate — Rev101 itself explicitly names LOCAL-EXEC-002/003 as proceedable "without Founder PC login." Because L1 directly composes L0's own `local-execution.ts` contracts (not yet on `main`), this checkpoint's branch (`claude/local-exec-002-device-bridge-mvp`) is cut from PR #43's own corrected head (`fe297fb5140ce89d5471dc0c7a220e31003ce429`), not from bare `main` — the usual per-Rev98-family "fresh from main" convention does not apply to a directly-dependent phase. `src/domain/local-execution-bridge.ts` implements the §17 control-channel protocol contracts (message envelope/replay-idempotency/enrollment challenge-proof/connection-lifecycle/heartbeat/allowlisted-workspace-path/lease-checkpoint-consistency), composing L0's `markDeviceOnline`/`isWorkspaceRootAllowed` unmodified rather than duplicating them — no live socket/transport, no real crypto, no native provider credential scraping, matching the packet's own explicit deferrals. `IMPLEMENTED / SELF-VALIDATED`, 802/802 tests pass (759 pre-existing on this base + 43 new), strict typecheck clean, zero new dependency; sanity-checked by temporarily disabling three representative adversarial guards (replay strictly-greater, foreign-challenge-id, path-traversal) and confirming exactly those three tests then failed. See `docs/exec-plans/active/LOCAL-EXEC-002.md` for the full reasoning and evidence. `HOLD_MERGE`, pending the same consolidated Brain review packet as the other six checkpoints in this batch.

**Update, 2026-09-11 (LOCAL-EXEC-003 Codex Local Adapter, dependency-fit ADR + implementation):** continuing the same Rev101 continuation ("LOCAL-EXEC-002/003 proceedable without Founder PC login"), this checkpoint (branch `claude/local-exec-003-codex-adapter`, cut from LOCAL-EXEC-002's own head `c28349226c72f61e7990a716c1fa93afe099ff74` since it directly composes L1's bridge protocol) required genuine, time-sensitive external research before any implementation — Rev101 explicitly calls for "a fresh dependency-fit ADR against current official OpenAI Codex CLI/SDK/app-server behavior," which this session cannot fabricate from memory. `docs/architecture/ADR/0002-local-exec-003-codex-adapter-fit.md` records that research (dated today, sourced from developers.openai.com/codex and related public documentation): Codex's App Server is a bidirectional JSON-RPC protocol where the server can pause a turn to request approval; exactly three sandbox modes (`read-only`/`workspace-write`/`danger-full-access`) and three approval policies (`untrusted`/`on-request`/`never`) exist as independent dimensions; network access defaults off; `codex exec resume <id>`/`--last`/`--all` resume prior sessions persisted under `~/.codex/sessions`; authentication is via `codex login` (ChatGPT session) or `codex login --with-api-key`, with `codex login status` reporting readiness locally — this repository's cloud side never ingests the resulting local auth state; usage is tracked in a rolling 5-hour window plus a weekly quota. `src/domain/codex-local-adapter.ts` models exactly these dimensions as closed, validated types and composes — never redefines — LOCAL-EXEC-001's `DevicePolicy`/`isWorkspaceRootAllowed`/`LocalTaskCheckpoint`/`createLocalTaskCheckpoint`/`LocalWorkerAdapterResult` and LOCAL-EXEC-002's `BridgeProtocolMessageKind`/`isRecognizedBridgeMessageKind`: `createCodexRunRequest` validates START-vs-RESUME session-ref requirements and the working directory via the existing unmodified workspace-allowlist gate; `mapCodexRunEventKindToBridgeMessageKind` deterministically projects a closed, honestly-labeled Codex event-kind enum onto the bridge's own existing message kinds (never a new one); `resolveCodexRunOutcome` binds a terminal event to the existing adapter result shape; `resolveCodexRunReadiness`/`CodexAuthReadinessStatus` accept only a locally-reported readiness status, with no field anywhere capable of carrying a real credential. `IMPLEMENTED / SELF-VALIDATED`, 835/835 tests pass (802 pre-existing on the LOCAL-EXEC-002 base + 33 new: 27 functional + 6 boundary-scan), strict typecheck clean, zero new dependency — every test uses caller-supplied mock inputs; no real Codex CLI process, JSON-RPC client, or filesystem access exists anywhere in this module. See `docs/exec-plans/active/LOCAL-EXEC-003.md` for the full reasoning and evidence. `HOLD_MERGE`, pending the same consolidated Brain review packet as the other checkpoints in this batch.

**Update, 2026-09-12 (Family 2 dark/internal staff-ingress boundary — Rev98/Rev101/Rev102):** cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`, the current head as of this checkpoint, itself several merges ahead of the `f03c917`-era history recorded above) specifically to close Rev98 Family 2 ("final trusted authenticated internal/staff ingress"), which had been mis-dispositioned as globally `BLOCKED_BY_EXACT_PROTECTED_GATE` before Brain's Rev102 exact-head review corrected it: only real, live identity-provider credential/account activation is actually protected; the dark/internal ingress/session/route boundary itself is dependency-safe and `LOCAL-EXEC-004` needs it. Six new `src/web/` files (`staff-session-context.ts`, `staff-session-provider.ts`, `dev-fixture-staff-session-provider.ts`, `production-staff-session-provider.ts`, `staff-route-guard.ts`, `staff-membership-guard.ts`) mirror V2-APP-001's own proven customer-facing session/route-guard pattern exactly, for a structurally distinct staff identity domain (no shared types with the customer-facing `AuthenticatedPrincipal`/`SessionContext`). The genuine remaining gap PR #33 named — binding an authenticated identity to a real organizational fact — is closed by `staff-membership-guard.ts`: `requireMatchingStaffMembership` (renamed from `requireCurrentStaffMembership` by the Rev106 correction below, since `organization-membership.ts` carries no temporal-lifecycle field to prove currentness with) fails closed (throws, never returns `undefined`) unless an authenticated `StaffSessionContext` resolves to exactly one tenant-scoped `organization-membership.ts` (V3-ORG-001) record — an authenticated session token alone is never sufficient authority. `AuthorityContext` composition (which `OrganizationRole` maps to which permission) is explicitly deferred as a real, later, undecided policy gap, not fabricated. `IMPLEMENTED / SELF-VALIDATED`, **730/730 tests pass** (708 pre-existing on fresh `main` + 22 new: 16 functional + 6 boundary-scan), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-2-STAFF-INGRESS-BOUNDARY.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-12 (LOCAL-EXEC-004 Phase L3 — staff/team/pool wiring reusing Family 2 + V3 membership/authority):** canonical Handoff Rev103 ("NEXT GRAPH, NOT A SINGLE NEXT PR") explicitly names this as the next unit now that Family 2 exists. Branch `claude/local-exec-004-staff-binding` merges `claude/family-2-staff-ingress-boundary` (PR #53) with `claude/local-exec-003-codex-adapter` (PR #49, the full L0-L2 chain) — both trace to the same `main` ancestor, clean merge on every source/test file, doc-only conflicts resolved by keeping both narratives in sequence. New `src/web/local-execution-staff-binding.ts`: three thin composition functions (`registerDeviceForAuthenticatedStaff`, `createLocalWorkerRegistrationForAuthenticatedStaff`, `resolveEligibleLocalWorkersForAuthenticatedStaff`) that resolve an authenticated `StaffSessionContext` to a real `OrganizationMembership` via Family 2's unmodified `requireMatchingStaffMembership`, then feed that membership's `membershipId` into `local-execution.ts`'s existing, unmodified constructors as the trusted `ownerMembershipRef`/`requestingOwnerMembershipRef` — closing the exact dependency `local-execution.ts`'s own doc comments named ("deliberately opaque caller-supplied strings, never resolved against any real authentication system"). `local-execution.ts`'s own Rev101 F1 ownership-mismatch guard is proven still intact underneath (a different authenticated staff member's own resolved membership still fails closed against a device it doesn't own). `IMPLEMENTED / SELF-VALIDATED`, **872/872 tests pass** (857 pre-existing on the merged base + 15 new: 10 functional + 5 boundary-scan), strict typecheck clean, zero new dependency. Per canonical Handoff Rev105, this checkpoint composes authentication/worker-authority identity binding and is therefore SAFE_MERGE-excluded (auth/IAM/authority) regardless of verification outcome. See `docs/exec-plans/active/LOCAL-EXEC-004.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-12 (LOCAL-EXEC-005 Phase L4 — Collaboration & Cross-Worker Handoff, merges V4-EFF-001 + LOCAL-EXEC-004):** see the LOCAL-EXEC-005 entry below for full detail; branch `claude/local-exec-005-collaboration-handoff` merges `claude/local-exec-004-staff-binding` (PR #54) with `claude/v4-eff-001-external-effect-envelope` (PR #42, Rev101-corrected) so this checkpoint's failover gate can reuse `external-effect-envelope.ts`'s real `ExternalEffectAttemptState` unmodified, per §15's own instruction to consult "the V4 external-effect recovery/readback envelope" before any failover — both branches trace to the same `main` ancestor (`3226c76fa338e425e553638e5f5f48924182a1c0`), clean merge on every source/test file, doc-only conflicts resolved by keeping both independently-true narratives in sequence.

**Update, 2026-09-12 (Brain Rev106 periodic HIGH-RISK sweep — corrections to PR #53/#54/#55):** Brain's fresh periodic sweep found three genuine bounded auth/authority findings, each independently re-verified against source before fixing. PR #53 (Family 2): `requireCurrentStaffMembership`/`resolveCurrentStaffMembership` overclaimed "current" membership when `organization-membership.ts` has no temporal/active/revoked field at all — renamed to `requireMatchingStaffMembership`/`resolveMatchingStaffMembership`, docs/errors narrowed to the provable fact only, no behavior change. PR #54 (LOCAL-EXEC-004): a real privilege-escalation bug — `createLocalWorkerRegistrationForAuthenticatedStaff` forwarded caller-controlled `trustStatus`/`authorityLevel` unchecked, letting a merely authenticated staff member mint an `ADMITTED`/`ELEVATED` local worker — fixed by removing those two fields as caller-supplied parameters entirely; a new worker is now always born `UNTRUSTED`/`STANDARD`. PR #55 (Family 10): a real cross-tenant leak — membership in one tenant was treated as authorization to read the internal command center's intentionally cross-tenant company-wide data — fixed by tenant-filtering attention items and always returning empty partner data (no tenant field exists on `PartnerCapabilityClaim` to filter honestly). Each fix carries adversarial tests and was sanity-checked by temporarily reverting it and confirming the intended tests fail. See each branch's own exec-plan "Rev106 correction" section for full detail.

**Update, 2026-09-12 (LOCAL-EXEC-006 Phase L5 — Claude Local Adapter; original `POLICY_SAFE` finding superseded by Rev110's `POLICY_REVIEW_REQUIRED`, see below):** fresh provider-policy research dated today (`docs/architecture/ADR/0003-local-exec-006-claude-adapter-fit.md`) originally found `POLICY_SAFE` for local, single-machine, officially-authenticated `claude -p` CLI automation — Anthropic's own documented headless-mode surface; not evaluated for any future distributed/shared-backend use. Branch `claude/local-exec-006-claude-adapter`, cut from `claude/local-exec-005-collaboration-handoff`'s own head. `src/domain/claude-local-adapter.ts` mirrors LOCAL-EXEC-003's own Codex adapter shape exactly, composing L0/L1's existing contracts unmodified: a structural policy-check admission gate, the CLI's real six permission-mode values, auth readiness, START/RESUME session requests, usage-limit disposition, event-to-bridge-kind mapping, run outcome binding, and checkpoint records. Both readiness functions check the policy gate strictly before auth readiness — a `POLICY_BLOCKED` disposition can never be bypassed by a valid session. `IMPLEMENTED / SELF-VALIDATED`, **983/983 tests pass** (940 pre-existing + 43 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/LOCAL-EXEC-006.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

**Update, 2026-09-14 (LOCAL-EXEC-006 Rev110/Rev112 correction — current disposition is `POLICY_REVIEW_REQUIRED`, activation-gated):** official Anthropic Legal and Compliance documentation makes running Claude Code in a product/service conditional on applicable Commercial Terms and end-user-owned authentication, a business/use-case classification this checkpoint cannot resolve unilaterally. `ClaudeAdapterPolicyDisposition` now includes `POLICY_REVIEW_REQUIRED`, and both `resolveClaudeRunReadiness`/`resolveClaudeAdapterCapabilityReadiness` fail closed on it identically to `POLICY_BLOCKED`. **1002/1002 tests pass**, strict typecheck clean. See `docs/exec-plans/active/LOCAL-EXEC-006.md`'s "Rev110 correction" section for full detail. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.

**Update, 2026-09-12 (Brain Rev107 incremental re-review — wording-only corrections to PR #53/#55):** Rev107 confirmed the Rev106 currentness/cross-tenant fixes substantially correct but found two remaining source-level doc sentences still overclaiming. PR #53: `staff-membership-guard.ts`'s header said an authenticated principal is "trusted for internal/staff action" once bound to a membership — narrowed to identity/binding language only. PR #55: `internal-command-access.ts`'s header still said "real, current `OrganizationMembership`" — replaced with `staff-membership-guard.ts`'s own exact wording. Both wording-only, no behavior change; each branch's own test suite passes unchanged.

**Update, 2026-09-12 (Brain Rev108 exact-head review — corrections to PR #56 LOCAL-EXEC-005):** Rev108 reviewed LOCAL-EXEC-005's materially-advanced head and found two real HIGH-RISK gaps, both independently re-verified before fixing. F1: `resolveLocalExecutionFailover` accepted a bare checkpoint with no authoritative lease to bind it to, so it couldn't prove the checkpoint belonged to the outgoing worker/device/tenant/task nor that the lease was deterministically ended before a replacement was selected — fixed with a required `currentLease`/`expectedTaskRef` parameter pair, five fail-closed lineage checks, and representing the lease-end via the existing `transitionLocalTaskLease` (`CHECKPOINTED` → `CANCELLED`). F2: `planCollaborationModeTransition` permitted a widening transition with an empty visibility/resource set, and `claimSharedRepoBranch` proved only mutual exclusion, never real per-worker repository authorization — fixed by requiring non-empty visibility/resource lists on widening and reusing the existing `LocalWorkerRegistration.boundProjectOwnerships` field to gate branch claims (no second IAM invented). 12 new adversarial tests; both fixes sanity-checked by reverting and confirming the intended tests fail. **952/952 tests pass** on `claude/local-exec-005-collaboration-handoff`; merged forward into `claude/local-exec-006-claude-adapter` (**995/995 pass**). See each branch's own exec-plan "Rev108 correction" section for full detail.

**Update, 2026-09-12 (Brain Rev109 periodic HIGH-RISK sweep — corrections to PR #56/#57):** Rev109 reviewed the Rev108-corrected heads and found F1/F2-A already resolved, with one remaining finding on each PR. PR #56 F2-B: Rev108's `claimSharedRepoBranch` fix wrongly treated `LocalWorkerRegistration.boundProjectOwnerships` as an existing authorization fact, but it is itself a bare caller-supplied parameter in LOCAL-EXEC-004 — an authenticated member can self-declare any project binding. `OwnershipAssignment` was considered and rejected as a reuse candidate (a business ownership role, not repository-access authority). Fixed by making `SharedRepoBranchClaim.accessVerified` a structural `false` literal type — `claimSharedRepoBranch` remains coordination-only (mutex + declared-scope consistency), and no input can ever produce a claim asserting verified access; real per-user/device repository-access admission stays explicitly open. PR #57 F1/F2: the ADR's provider-policy sourcing (permission-mode values corroborated only against this environment's own session-creation tool schema; a Consumer Terms distinction asserted without citation) was insufficiently authoritative — fixed by directly re-fetching the official Claude Code permission-modes documentation (confirms all six values by exact literal, satisfying Rev109's own stated fallback, so no narrowing was needed) and citing a real, dated Anthropic compliance-page clarification (2026-02-20) for the terms distinction; no type/behavior change. Both fixes pushed in place; **997/997 tests pass** on `claude/local-exec-006-claude-adapter`. See each branch's own exec-plan "Rev109 correction" section for full detail.

**Update, 2026-09-12 (Claude escalation — LOCAL-EXEC-007/Phase L6 deferred, not attempted):** fresh-read the architecture packet's §21 "LOCAL-EXEC-007 / Phase L6 — Hybrid / UX / Hardening" before considering it as the next dependency-safe unit. Unlike every prior LOCAL-EXEC phase, L6 has no named contracts/types, no explicit dependency line, and is absent from §23's own Claude execution/sequencing enumeration entirely; several of its bullets (routing UI, installer/signing, soak tests) are self-gated on infrastructure this repository does not have (no "frontend" reference exists anywhere in the packet). Implementing it as literally scoped would mean inventing the missing contracts unilaterally, outside Claude's role boundary — so it is escalated in this execution record rather than resolved unilaterally, and not attempted this batch. See `docs/exec-plans/corridors/V2_TO_V5.md`'s "Claude escalation" section for full detail. Pivoting to Family 12 (Website Build Cold-Start convergence) instead.

## Active corridor

This repository operates inside the DEC-160 V2→V5 Autonomous Engineering Corridor. See `docs/exec-plans/corridors/V2_TO_V5.md` for the repo-native corridor contract (condensed; Drive `113M8-J5mYAUPM_BbMlOp0-EgDD4OpCVhNad2yk4crVA` is canonical) and `docs/architecture/VERSION_EVOLUTION_MAP.md` for what is actually built per version.

**Correction (superseding a prior entry in this section):** after V2-CDO-008, this record briefly stated the corridor-admitted candidate pool was "exhausted" because no further separately-published `Prepared candidate: Drive <id>` task packet existed. Brain's handoff `HANDOFF_HEAD_REV: 10` explicitly rejected that interpretation: absence of a separately-named prepared packet is not a valid DEC-160/DEC-163 exhaustion predicate — Claude is authorized and required to progressively elaborate the Master Program Blueprint's and version Full Blueprints' own PROVISIONAL dependency spine into concrete repo-native tasks. That correction was independently verified by fresh-reading the actual handoff/`02` documents in full (not accepted on a chat paraphrase) before acting on it. Following that correction, a real dependency scan was run against the V3 Full Blueprint's own Definition-of-Ready (§2) and dependency spine (§3): all V3 entry predicates relevant to Workstream A (organization/role/assignment foundation) were satisfied by existing closed work (`V2-APP-001_TO_V2-CDO-006`'s verified identity/session/tenant substrate), so `V3-ORG-001` was elaborated and implemented — see `docs/exec-plans/active/V3-ORG-001.md` for the full reasoning and evidence. `V2-CDO-007` remains superseded (its goal was already delivered under `V2-CDO-002`).

**Post-PR#4-merge continuation (first scan):** with V3 Workstreams A–D and the V2-APP-001_TO_V2-CDO-006 train merged into `main`, a fresh dependency/authority reconciliation was run directly against merged `main`. Three dependency-safe, honestly-bounded units were selected and implemented, each declaring-but-not-fabricating the parts of its governing blueprint section with no real observable data source in this repository yet (same DEC-153 Activation Discipline already applied to `ContractualSlaStatus`): `V3-COM-001` (Commission/Pricing/Discount Authority Read Models, Workstream E floor — `DiscountAuthorityLevel` produces only `APPROVAL_REQUIRED`, zero numeric/financial fields), `V4-SVC-001` (Service-to-Capability Routing, Workstream A floor — `ExecutionMaturity` declares all 10 blueprint rungs but produces only `UNAVAILABLE`/`READ_ONLY`, since no real provider adapter exists), and `V4-OPS-001` (Unified Operations Attention/Exception Model, Workstream B floor — `AttentionSourceDomain` declares only the one real observable source, `DELIVERY_OUTCOME_JOB`, out of eleven blueprint-named candidate families). None invents a commission %, discount threshold, payout, reseller economics, or binding SLA value. All three are now Brain-PASSed and merged into `main` (PR #5/#6/#7, see "Active tasks" below for exact merge SHAs).

**Post-PR#4-merge continuation, second scan (this section's current head):** with `V3-COM-001`/`V4-SVC-001`/`V4-OPS-001` open but unmerged at the time, a fresh dependency scan of V4 Workstreams C-H (all require real provider adapters/external effects that do not exist) and V5 Workstreams A-D (meta-engineering-process concepts that would duplicate the existing `ENG-ORCH-001` durable-state primitives) found no further honest unit without either merging one of those three PRs or a new architectural decision. The Founder explicitly authorized one such decision as a reversible, architecture-within-envelope technical call inside Claude's delegated authority: a new provider-neutral shell read-port surfacing already-merged V3 A-D substrate (organization membership, ownership assignment, attention state) into the web shell, without consuming PR #5/#6/#7. `V3-F-001` (Team & Attention Shell Projection, V3 Workstream F floor slice) implements exactly that, and is being merged into `main` now as `PR #8` — see `docs/exec-plans/active/V3-F-001.md`.

## Repository identity

- Owner/name: `Jay-prodesign/akil-main`
- Visibility: Private
- Default branch: `main` — as of 2026-09-05 `main` is at exact final SHA `f03c917e66283fcd63736234d81b166465496d7c`, carrying PR #4 through PR #20 (merge SHAs `4ba5231309ac2fc72dd089cc947030a958eab77e`, `5c91f18034a7ec435a4d83fccac856d354f8394f`, `c93a9cc1e8caf5da3c13d102bdb9fa7c778a5982`, `6f403db130434b0228783584487dfdf43f3544f7`, `837214dc863970bdb05f29bf8611a3438735b3f4`, `6758de759bf17ecda49da7f4f8e1d07faa526655`, `90e989935a443462f372fbbb9ff1caa6987c82ce`, `19df961498896427223842f7de82df2d9f6a8304`, `1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb`, `460871cba43273f89bbf8004e547664c40b90794`, `839aca0f6dd830c46dcf4576e46035c3fdbd9207`, `f03c917e66283fcd63736234d81b166465496d7c`). New engineering work uses a fresh task-scoped branch cut from current `main`, opens/updates a coherent PR, and merges only on separately granted protected owner-gate authority — never direct-push to `main`.
- Base commit at bootstrap: `8a95c721024da4bfee99e082a01e34b86321637f` ("Create README.md")

## What exists

- `README.md`, the AKI-GIT-001 governance scaffolding (`AGENTS.md`, `CLAUDE.md`, `docs/engineering/`, `docs/architecture/ADR/`, `docs/specs/`, `docs/exec-plans/`).
- A secret-free TypeScript domain/application kernel under `src/domain/`, `src/application/`, `src/ports/`, `src/fixtures/`, built across three tasks (see "Completed tasks" / "Active tasks" below):
  - `AKI-BE-001` — `TenantScope`, `Customer`, `Project`, `OutcomeJob` (+ lifecycle/transitions), `EvidenceReference`, `VerificationResult`, `AuthorityContext`, `AuditEvent`, plus in-memory ports/adapters.
  - `ENG-ORCH-001` — `EngineeringEventEnvelope`, `EngineeringRunState` (pure reducer, fencing/idempotency/replay), `FileDurableEngineeringStore`, `WorkerInvoker`/`invokeSafely`, DEC-138 provenance.
  - `DEL-003` — `OfferBlueprintVersion`, `SoldScope`, `CustomerEvidenceItem`, `ProjectPlanVersion`/`compilePlan`, `PlanValidationResult`/`validatePlan`, `OutcomeJobSpec`, `ProjectPlanDiff`, `ApprovalReference`; second slice adds `PlanAdmissionResult`/`admitPlan`/`admitJobs`, `EvidenceReadinessAssertion`/`evaluateReadiness`, `wireAdmittedOutcomeJobs`, and durable admission/job stores (`durable-plan-admission-store.ts`, `durable-outcome-job-store.ts`).
- `tests/` — Node's built-in `node:test` runner exercises all of the above (228 tests at the current checkpoint; `npm run test`).
- `package.json`/`package-lock.json`/`tsconfig.json` — strict-mode TypeScript, zero runtime dependencies, `devDependencies` limited to `typescript` + `@types/node`.

## What does not exist yet

- Any HTTP/API server, database, ORM, queue, cache, object storage, or cloud service — this remains a domain/application kernel, not a deployable backend.
- Any CI, cloud, or deployment configuration.
- Any real secret, credential, or vault integration (secret-free by design; see `docs/engineering/PERMISSION_POLICY.md`).
- Any ADRs or specs beyond index scaffolding (see `docs/architecture/ADR/README.md` and `docs/specs/README.md`).
- Durable production runtime dispatch, cross-project (AI Commerce) request/result flows, broad AI/model routing, and FAS-001 S1/S2 adversarial activation — the *broader* forms of INT-001/CONN-001/AI-004 remain stage/trigger/dependency-gated per the canonical roadmap and the DEC-144 V1 Fast Lane contract's anti-scope-creep filter; not pulled forward. (Their minimum V2 slices — first Delivery Recipe, first account/connection-ownership contract — are separately marked `V2 Active Requirement` under the 21 August 2026 DEC-149/150 roadmap reconciliation and consumed only where V2-CDO-series work needs them; see `docs/exec-plans/active/V2-CDO-001.md`.)
- A public-facing website, payment/legal/commercial surface — the live AKILTA commercial website (`akilta.com`) is a separate, already-live Shopify-hosted surface outside this repository's scope; this repository does not build or touch it.

## Active tasks

**`REV98-CORRIDOR-CONSOLIDATION` (2026-09-15, branch `claude/rev98-corridor-consolidation`, cut from `claude/family-12-sale-service-routing-convergence`):** every one of Rev98's twelve mandatory gap families now exists, implemented and self-validated, inside one single coherent tree — a clean sequential merge of `claude/family-8-operational-optimization-loop` (itself already a stack of CONN-001 + Families 4/5/6/7/8), `claude/family-10-internal-command-access` (V5-CMD-001 + Family 10), `claude/dep-orch-001-environment-config-cutover` (RUNTIME-001 + Family 11), and `claude/aut-ops-001-account-automation-integration` (Family 9), on top of the Family-12 branch's own Families 1/3/12. Every merge step was independently retested: 1070 → 1116 → 1175 → **1198/1198 tests pass**, strict typecheck clean throughout. The only conflicts across all four merges were in `CURRENT_STATE.md`'s own append-only narrative, resolved by keeping every independently-true entry in sequence — zero source/test file conflicts anywhere. See `docs/exec-plans/active/REV98-CORRIDOR-CONSOLIDATION.md` for the full family-by-family disposition matrix and evidence. `MERGE_DISPOSITION: HOLD_MERGE` — this is the single consolidated end-of-batch review packet Rev95's "FINAL RETURN" text calls for, superseding the need to review PR #37/#38/#39/#40/#41/#42/#45/#46/#47/#48/#50/#51/#52/#55/#58 individually.

`RUNTIME-001` (PR #40), `CONN-001` (PR #38), `SALE-TO-CLOSE` (PR #39), `LOCAL-EXEC-001` (PR #43), `LOCAL-EXEC-002` (PR #44, stacked on PR #43), `OBS-TEL-001` (PR #45), `AUT-OPS-001` (PR #46), and `DEP-ORCH-001` (this branch, stacked on PR #40) are all `IMPLEMENTED / SELF-VALIDATED`, `MERGE_DISPOSITION: HOLD_MERGE`, awaiting the one consolidated Brain review packet per Rev95/101's continuous-execution batch mode — see the 2026-09-11 update notes above and each checkpoint's own `docs/exec-plans/active/*.md` for full detail. Rev98 Family 2 (trusted authenticated staff ingress): only real, live identity-provider credential/account activation is `BLOCKED_BY_EXACT_PROTECTED_GATE` — a genuine external-identity-provider admission decision. The dark/internal ingress/session/route boundary itself is dependency-safe and remains a real, currently-unbuilt gap, per Brain's Rev102 correction (see `DEP-ORCH-001.md`'s "Family 2 disposition — CORRECTED per Rev102" section).

`V2-CDO-008`, `V3-ORG-001`/`V3-OWN-001`/`V3-PTR-001`/`V3-SLA-001` (via PR #4), `V3-COM-001` (via PR #5), `V4-SVC-001` (via PR #6), `V4-OPS-001` (via PR #7), `V3-F-001` (via PR #8), the V3 §14/Rev35-correction docs (via PR #10), `V5-ENG-001` (via PR #12), `V5-BOOT-001` (via PR #14), `V5-ENG-002` (via PR #16), `V5-INT-001` (via PR #18), and `V5-WRK-001` (via PR #20) below are all now durably part of `main` — their branch references below are historical (pre-merge) provenance, not current working branches. Brain's merge-gating review for each was an exact-head §12A evidence/audit-matrix review of that PR's full bundle; each slice's own status line below is unchanged from its original self-validation. With all six V3/V4 slices merged, Brain Rev34 (fresh-read, independently verified) closed the V3 Full Blueprint §14 version-end integration audit edge — see `docs/exec-plans/corridors/V2_TO_V5.md` "V3 §14 version-end integration audit — CLOSED" for the full record. Brain Rev35 then corrected the immediate post-closure `CORRIDOR_EXHAUSTED` finding's V5 A/D reasoning as invalid and located the concrete next candidate. PR #10, PR #12, PR #14, PR #16, PR #18, and PR #20 all merged. `V5-PTN-001` (below, V5 Workstream H floor) remains the one open source-bearing checkpoint, PR #21 — its branch has been rebased onto post-PR#20 `main` (`f03c917e66283fcd63736234d81b166465496d7c`) per explicit Founder instruction, since its prior exact-head evidence was bound to the now-stale base `839aca0f6dd830c46dcf4576e46035c3fdbd9207`.

- `FAMILY-12-ROUTING-EXECUTION-CONVERGENCE` (Rev98 Family 12 — sale/service/routing sibling-branch merge + admitted-routing ↔ authorized-execution glue) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/family-12-sale-service-routing-convergence`, three-way merge of `claude/sale-to-close-e2e-floor` (PR #39), `claude/svc-adm-001-trusted-service-catalog` (PR #48), and `claude/v5-ptn-002b-partner-routing-decision` (PR #41) — all three trace to the same `main` ancestor (`3226c76fa338e425e553638e5f5f48924182a1c0`) with zero source/test file overlap, so every merge was clean. New pure composition module `src/domain/outcome-job-routing-execution.ts`: `RoutedExecutionAssignment` binds a `ROUTED` `WorkerRoutingDecision` to a specific `OutcomeJob`'s tenant/customer/project/job identity (mirroring `outcome-job-closure-approval.ts`'s own scope-binding discipline); `authorizeOutcomeJobExecutionFromRouting` requires a valid, matching assignment before delegating the actual `READY -> EXECUTING` transition entirely to the existing, unmodified `transitionOutcomeJob` — closing the real gap where a routing decision carried no job identity and a job could begin executing without ever having gone through admitted routing. `src/application/authorized-outcome-job-operations.ts` gains `authorizedTransitionOutcomeJobToExecutingViaRouting`, the properly-glued application-boundary path. Deliberately does NOT narrow the existing `authorizedTransitionOutcomeJob`'s own `EXECUTING` path (no equivalent bypass finding exists there, unlike the Family-12 closure-approval-gate's `CLOSED` narrowing) — this is purely additive glue, not a rewrite. `PartnerRoutingDecision` binding is explicitly out of scope (a distinct domain concept). 847/847 tests pass (825 pre-existing from the merge + 22 new), strict typecheck clean, zero new dependency, sanity-checked by temporarily removing the fail-closed assignment check and confirming exactly the 5 tests proving that dimension then failed. See `docs/exec-plans/active/FAMILY-12-ROUTING-EXECUTION-CONVERGENCE.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.
- `CONN-001` (Integration & Credential Control Plane, slices 1-5 — architecture/domain contract + persistence + admin read-model + generic adapters + prebuilt adapter definitions) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/conn-001-integration-control-plane`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`). Slice 1, `src/domain/integration-connector-catalog.ts` (composing entirely on top of the already-merged, unmodified `connection-authority.ts`, `V2-CDO-004`): a connector catalog and the `test/health/reconnect/rotate` lifecycle operations `connection-authority.ts` does not itself provide. Slice 2, `src/domain/durable-connector-connection-store.ts`: an optimistic-concurrency-gated durable store preventing lost updates between concurrent lifecycle operations. Slice 3, `src/domain/integration-admin-view.ts`: an admin read-model (same disclosed no-internal-auth boundary as `V5-CMD-001`) whose connection-summary type has no field capable of holding a `SecretRef` at all. Slice 4, `src/domain/generic-connector-definition.ts`: pure declarative Generic Custom API/OAuth connector data contracts. Slice 5, `src/domain/prebuilt-connector-definitions.ts`: static, real, single-host endpoint declarations for `GITHUB`/`OPENAI`/`ANTHROPIC`/`GOOGLE_AI`/`GOOGLE_DRIVE` (`GOOGLE_WORKSPACE`/`META` explicitly deferred as genuine multi-host/ambiguous-surface architecture questions, not fabricated). 765/765 tests pass (708 true pre-existing + 57 new), strict typecheck clean, zero new dependency; every slice still makes zero real HTTP calls. Real credential entry/OAuth exchange and the two mandatory E2E acceptance cases remain deferred (protected gate). See `docs/exec-plans/active/CONN-001.md` for the full reasoning and evidence.
- `V5-CMD-001` (Unified Command Frontend floor: internal command-center read-model aggregation, V5 Workstream J / §14) — Status: **IMPLEMENTED / SELF-VALIDATED (Rev90-corrected)**, branch `claude/v5-cmd-001-internal-command-projection`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`). Pure aggregation module (`src/domain/internal-command-projection.ts`): `buildInternalCommandCenterProjection` combines already-merged `OperationsAttentionItem` (cross-tenant "Company / Portfolio Attention") and `PartnerCapabilityClaim` ("Partners", exposed as `partners.items` pairing each raw claim with its computed `effectiveStatus` via `resolvePartnerCapabilityClaimStatus` — Rev90 F1 correction, so the raw stored `status` can never be mistaken for current) into one internal read-model, with zero HTTP/session/route wiring — this repository has no internal/staff authentication concept, and inventing one is a genuine architectural decision explicitly deferred rather than fabricated. 716/716 tests pass (708 true pre-existing + 8 new: J1–J8), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-CMD-001.md` for the full reasoning and evidence.
- `V5-PTN-001` (Partner Capability Claim Admission, V5 Workstream H) — Status: **IMPLEMENTED / SELF-VALIDATED, Brain Rev56 PASS**, branch `claude/v5-ptn-001-partner-capability-admission`, base `f03c917e66283fcd63736234d81b166465496d7c` (PR #20, unchanged since the rebase), exact head `4b78e4b3cd6134b5da0db8e799d47a8bf8795134`, PR #21 (open/draft, `HOLD_MERGE`, `OWNER_GATE: OPEN` — awaiting live Founder merge confirmation, NOT yet given). Pure lifecycle module (`src/domain/partner-capability-admission.ts`) extending `V3-PTR-001`'s `PartnerOrganization`: `createPartnerCapabilityClaim` always produces `UNVERIFIED` (accepts no status/evidence field, so a claim can never be born admitted); `admitPartnerCapabilityClaim` is the only function that can produce `ADMITTED`, requiring a non-empty `evidenceRef`, a `reviewByAt` strictly after `admittedAt`, and (Rev55 F1 correction) a non-empty `admittingAuthorityId` fail-closed rejected whenever it equals the claim's own `partnerOrganizationId` — self-admission is now a structural rejection, not a caller convention, and the accepted authority is persisted as `admittedByAuthorityId` for audit; `revokePartnerCapabilityClaim` transitions `ADMITTED` to terminal `REVOKED` only; `resolvePartnerCapabilityClaimStatus` resolves `EXPIRED` as a computed (never stored) disposition once a caller-supplied `asOf` passes `reviewByAt`. Directly satisfies §12's "capability claim cannot self-certify" and "requires evidence/admission and may expire/review" acceptance directions. Exactly one type-only import (`PartnerOrganization`), no system-clock read, no capability catalog/taxonomy invented, no wiring into client-access resolution (out of scope for this floor). See `docs/exec-plans/active/V5-PTN-001.md` for the full reasoning, the Rev55 correction note, and current test/evidence counts.
- `V5-WRK-001` (Worker / Model Routing Policy, V5 Workstream B) — Status: **IMPLEMENTED / SELF-VALIDATED, Brain Rev53 PASS, merged** (PR #20, `f03c917e66283fcd63736234d81b166465496d7c`), branch `claude/v5-wrk-001-worker-routing-policy` (historical; merged into `main`), cut fresh from merged `main` (`839aca0f6dd830c46dcf4576e46035c3fdbd9207`, PR #18). Pure routing-decision module (`src/domain/worker-routing-policy.ts`): `resolveWorkerRoute` selects an authorized executor (and, when required, an independent reviewer) from a caller-declared `AdmittedWorker` registry — fails closed on `UNTRUSTED`/`REVOKED` trust status, non-`AVAILABLE` availability, missing required capability/tool/policy-constraint refs, missing evaluation evidence, insufficient `authorityLevel`, or insufficient `maxRiskLevel` for `HIGH_RISK` work; candidate order never overrides the eligibility filter; a required independent reviewer can never be the executor; every decision carries a reason. Zero imports, no system-clock read, no hard-coded provider/model/worker-identity literal. 666/666 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-WRK-001.md` for the full reasoning and evidence.
- `V5-INT-001` (Cross-Domain Company Intelligence reconciliation floor, V5 Workstream F) — Status: **IMPLEMENTED / SELF-VALIDATED, Brain Rev50 PASS, merged** (PR #18, `839aca0f6dd830c46dcf4576e46035c3fdbd9207`), branch `claude/v5-int-001-cross-domain-intelligence` (historical; merged into `main`), cut fresh from merged `main` (`460871cba43273f89bbf8004e547664c40b90794`, PR #16). Pure reconciliation module (`src/domain/cross-domain-intelligence.ts`): `buildCrossDomainIntelligenceSnapshot` combines opaque cross-domain "insight" pointers (closed six-surface `IntelligenceSourceDomain` enum matching the blueprint's own SALES/DELIVERY/OPERATIONS/ENGINEERING/COMMERCIAL/PRODUCT list) about the same subject into a governed snapshot, reusing the existing `TenantScope` isolation primitive. Fails closed on a mismatched `tenantScope`/`projectRef` (no cross-client leakage), unrecognized `domain`, empty/whitespace `sourceRef`, or a `capturedAt` in the future relative to `asOf` (the last two added in the Rev49 correction — see below); a subject with no insight fresh enough (relative to a caller-supplied `asOf`, never `Date.now()`) resolves `STALE`; disagreeing fresh insights resolve `CONFLICTING`, never silently arbitrated; agreeing fresh insights resolve `CURRENT`. `OBSERVED`/`DERIVED` evidence kinds are preserved verbatim, never blurred. Exactly one import (`TenantScope`), no filesystem/network/child_process coupling, no execution of what an insight's `sourceRef` points to. **Rev49 correction**: Brain's exact-head review of the original submission (`0a7715d03c7d4e89c648433de79058f203cd0b38`) found (F1) no fail-closed check on empty/whitespace `sourceRef`, and (F2) a `capturedAt` in the future relative to `asOf` could wrongly resolve `CURRENT` (negative age satisfies any non-negative threshold) — both independently confirmed via an empirical probe against the compiled `dist/` output before being fixed with two new rejection checks and 4 new adversarial tests (F16-F19). 629/629 tests pass (605 pre-existing + 20 original + 4 Rev49), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-INT-001.md` for the full reasoning and evidence.
- `V5-ENG-002` (RESOLVE Authority Separation, extending ENG-ORCH-001/V5-ENG-001, V5 Workstream A) — Status: **IMPLEMENTED / SELF-VALIDATED, Brain Rev46 PASS, merged** (PR #16, `460871cba43273f89bbf8004e547664c40b90794`), branch `claude/v5-eng-002-resolve-authority-separation` (historical; merged into `main`), cut fresh from merged `main` (`1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb`, PR #14). Extends `src/domain/engineering-run-state.ts`'s `applyEvent` `RESOLVE` case with one guard: a `RESOLVE` event whose `fromRole` is not `"BRAIN"` or `"OWNER"` is rejected as a safe no-op (same treatment as every other RESOLVE-path rejection already in the reducer), never producing a `BrainResolution` or moving the run to `PASS`/`CHANGES_REQUIRED`. Directly satisfies the V5 blueprint §5 acceptance criterion "agent cannot self-mark final VERIFIED," which an empirical probe against the compiled reducer confirmed was a real, exploitable gap before this fix (a `WORKER`-authored `RESOLVE(PASS)` was previously accepted and produced a durable PASS resolution). No new event type, no second orchestration engine. 605/605 tests pass (601 pre-existing + 4 new; all 14 pre-existing `RESOLVE` tests updated to pass `fromRole: "BRAIN"` explicitly, no assertion changed), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-ENG-002.md` for the full reasoning and evidence.
- `V5-BOOT-001` (Project/Repository Bootstrap Template Resolution, V5 Workstream C) — Status: **IMPLEMENTED / SELF-VALIDATED (Rev43-corrected)**, branch `claude/v5-boot-001-bootstrap-template`, cut fresh from merged `main` (`19df961498896427223842f7de82df2d9f6a8304`, PR #12), PR #14. Pure planning module (`src/domain/project-bootstrap-template.ts`): `resolveProjectBootstrapPlan` copies only structural assets from a closed, blueprint-verbatim `BootstrapAssetKind` enum (no credential/provider-secret kind exists to select), stamps every copied asset `supersedesLocalAuthority: false` (a literal type, not a settable field), fails closed on a colliding `targetProjectNamespace`, and forwards `contentRef` as an opaque, never-interpreted pointer. Zero imports — no filesystem/network/child_process/sibling-domain coupling; a pure, side-effect-free planning function only, no execution of the resolved plan. Cross-project isolation proven via two synthetic projects sharing one template source, without needing a real second live repository (correcting Rev35's earlier "architecturally larger" framing). Brain Rev43 exact-head review of the original submission returned `CHANGES_REQUIRED`: (F1) an asset with a recognized `kind` but empty/whitespace `sourceProjectRef`/`sourceVersion` could be copied — fixed by rejecting assets with empty provenance; (F2) no field represented the §7-required local-applicability revalidation decision, so a recognized `kind` alone was sufficient for acceptance — fixed by adding a required `localApplicability: "ACCEPTED" | "NOT_APPLICABLE"` field, only `"ACCEPTED"` assets can be copied. Both findings independently verified against the live source before fixing, same branch, no redesign. 601/601 tests pass (597 prior + 4 new: B9-B12), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-BOOT-001.md` for the full reasoning and evidence.
- `V5-ENG-001` (CHECKPOINT-after-PASS Stale-Approval Invalidation, extending ENG-ORCH-001, V5 Workstream A/D) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/v5-eng-001-checkpoint-invalidation` (historical; merged into `main` via PR #12, `19df961498896427223842f7de82df2d9f6a8304`). Extends `src/domain/engineering-run-state.ts`'s pure reducer: `EngineeringRunState.supersededResolutions` (new field, durable history of superseded `BrainResolution`s) + `CHECKPOINT_ACCEPTING_STATUSES` now includes `PASS`, so a fresh `CHECKPOINT` (post-review push) with a NEW `checkpointSha` explicitly reopens a `PASS` run into `CHECKPOINT_RECEIVED`, clears the stale `resolution`, and appends it to `supersededResolutions` — directly satisfying the V5 blueprint §5 acceptance criteria "stale SHA approval cannot merge" / "post-review push invalidates old merge approval." A duplicate `CHECKPOINT` delivering the exact same reviewed `checkpointSha` remains a safe no-op (does not invalidate a still-current approval); an EQUAL-fenced `CHECKPOINT` with a NEW `checkpointSha` is also a strict no-op — only a STRICTLY higher fencing token proves a genuinely newer push (Rev39 correction: this was found to be a real gap on independent verification, not just a missing test — fixed and proven); `COMPLETED` remains terminal (unaffected, checked before this guard); the global fencing floor is unchanged and still checked first. No new event type, no PR/merge-execution side effect, no second orchestration engine. 584/584 tests pass (580 pre-existing, one prior test rewritten into five that verify the new behavior and its preserved guardrails), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-ENG-001.md` for the full reasoning and evidence.

- `V3-F-001` (Team & Attention Shell Projection, V3 Workstream F floor slice) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/v3-f-001-team-attention-shell-projection` (historical; merged into `main` via PR #8, `837214dc863970bdb05f29bf8611a3438735b3f4`), independent of siblings `#5`/`#6`/`#7` (structurally cannot import any of their files at the time it was built — none existed on this branch). New provider-neutral shell read port (`src/web/team-attention-view-state.ts`, `TeamAttentionSource`) surfaces a pure projection (`src/domain/team-attention-projection.ts`, `buildTeamAttentionProjection`) composing only already-merged V3-ORG-001/V3-OWN-001/V3-SLA-001 substrate — organization/client context, current viewer role, each of LeadOwner/DealOwner/AccountOwner/DeliveryOwner rendered separately, and internal attention/exception state — into the existing shell (`shell-render.ts`'s new `renderTeamAttentionSection`, wired via an optional `teamAttentionSource` dependency on `createRequestHandler`/`createHttpServer`, backward-compatible with every existing caller). Commercial/commission/discount state renders as a fixed "Unavailable" literal — no commercial domain type invented, no consumption of the `V3-COM-001` checkpoint. No new persistence, auth/session/tenant architecture, or provider SDK/OAuth connector. 514/514 tests pass (483 pre-existing/unchanged + 31 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V3-F-001.md` for the full reasoning and evidence.
- `V4-OPS-001` (Unified Operations Attention/Exception Model, single-domain floor, V4 Workstream B) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/v4-ops-001-operations-attention` (historical; merged into `main` via PR #7, `6f403db130434b0228783584487dfdf43f3544f7`). Pure domain module (`src/domain/operations-attention.ts`): `AttentionSourceDomain` (single literal `"DELIVERY_OUTCOME_JOB"` — the only one of the V4 blueprint's eleven candidate attention families with a real observable source in this repo) + `OperationsAttentionItem`/`toOperationsAttentionItem` (reuses `AttentionState`, V3-SLA-001, verbatim; `isActive` always recomputed, never cached) + `resolveActiveOperationsAttention` (pure tenant/project-scoped active-item filter). Deliberately excludes `contractualSlaStatus` (keeps internal-vs-contractual SLA truths separate) and exports no escalation/dismiss/resolve function. No import of `outcome-job.ts`, `authority.ts`, `capability-admission.ts`, `connection-authority.ts`, `service-capability-routing.ts`, or `partner-organization.ts`. 496/496 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V4-OPS-001.md` for the full reasoning and evidence.
- `V4-SVC-001` (Service-to-Capability Routing — execution-maturity floor, V4 Workstream A) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/v4-svc-001-service-capability-routing` (historical; merged into `main` via PR #6, `c93a9cc1e8caf5da3c13d102bdb9fa7c778a5982`). Pure domain module (`src/domain/service-capability-routing.ts`): `ExecutionMaturity` (ten literals declared per the V4 blueprint's own ladder, but this checkpoint's `resolveServiceCapabilityRoute` can only ever produce `UNAVAILABLE` or `READ_ONLY` — no real provider adapter exists anywhere in this repository yet, so claiming `DIAGNOSE`/`RECOMMEND`/`APPROVAL_REQUIRED`/`CONTROLLED_APPLY` etc. would be fabricating executable capability) + `ServiceCapabilityRoute`, reusing `CapabilityAdmission` (V2-CDO-004) directly rather than duplicating it. Fail-closed on cross-tenant/cross-project admission substitution. 497/497 tests pass (483 pre-existing/unchanged + 14 new: 8 functional, 6 boundary-scan — the boundary scan statically greps every `executionMaturity: "..."` assignment in source and asserts each is `UNAVAILABLE` or `READ_ONLY`), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V4-SVC-001.md` for the full selection reasoning and evidence.
- `V3-COM-001` (Commission / Pricing / Discount Authority Read Models, V3 Workstream E) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/v3-com-001-commercial-authority-readmodel` (historical; merged into `main` via PR #5, `5c91f18034a7ec435a4d83fccac856d354f8394f`). Pure domain module (`src/domain/commercial-authority.ts`): `CommissionLedgerStatus` (single literal `"UNKNOWN"` — no commission/billing/settlement system exists anywhere in this repository) + `DiscountAuthorityLevel` (`VIEW_ONLY`/`RECOMMEND_ONLY`/`APPROVAL_REQUIRED`/`COMMIT_ALLOWED`, declared but this slice's `buildCommercialAuthoritySnapshot` can only ever produce `APPROVAL_REQUIRED` — fail-closed absent a real DEC-146 pricing-policy integration) + `CommercialAuthoritySnapshot`, reusing `resolveCurrentOwner` (V3-OWN-001) for an optional `dealOwnerMembershipId`. Zero numeric/currency field on the type (structurally cannot invent a commission %, discount threshold, payout, reseller/wholesale economics or binding SLA term — all explicitly deferred per the V3 Full Blueprint §8). Does not import `outcome-job.ts`, `authority.ts`, or `partner-organization.ts`. 498/498 tests pass (483 pre-existing/unchanged + 15 new: 8 in `tests/commercial-authority.test.ts`, 7 in `tests/v3-com-001-boundary-scan.test.ts`), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V3-COM-001.md` for the full selection reasoning and evidence.
- `V3-SLA-001` (SLA / Escalation / Attention Model, V3 Workstream D) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/akilta-continuity-readback-5hm3ed`. Pure domain module (`src/domain/attention-state.ts`): `InternalAttentionLevel` (`NORMAL`/`EXCEPTION`/`ESCALATED`, a direct honest mapping of the existing `OutcomeJobState` exception-state signal — no invented AT_RISK heuristic) + `ContractualSlaStatus` (always `UNKNOWN` — no real commitment data source exists) + `AttentionState`/`buildAttentionState`, reusing `resolveCurrentOwner` (V3-OWN-001) for `responsibleOwnerMembershipId`. Imports `outcome-job.ts` as types only and nothing from `authority.ts` — cannot transition a job or grant permission. 483/483 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V3-SLA-001.md` for the full reasoning and evidence.
- `V3-PTR-001` (Agency / Partner Multi-Organization Boundary, V3 Workstream C) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/akilta-continuity-readback-5hm3ed`. Pure domain module (`src/domain/partner-organization.ts`): `PartnerOrganization`/`PartnerRelationshipType` (representable, never consulted for access) + `PartnerEmployeeMembership` (deliberately distinct from `OrganizationMembership` — see exec-plan for why) + `PartnerClientAssignment`/`revokePartnerClientAssignment` (provenance-preserving) + fail-closed `resolvePartnerClientAccess` (`AUTHORIZED`/`REVOKED`/`UNAUTHORIZED`, cross-client substitution rejected). 469/469 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V3-PTR-001.md` for the full reasoning and evidence.
- `V3-OWN-001` (Multi-Owner Sales/Account/Delivery Semantics, V3 Workstream B) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/akilta-continuity-readback-5hm3ed`. Pure domain module (`src/domain/ownership-assignment.ts`): `OwnerRole` (`LEAD_OWNER`/`DEAL_OWNER`/`ACCOUNT_OWNER`/`DELIVERY_OWNER`) + `OwnershipAssignment`/`createOwnershipAssignment` + provenance-preserving `reassignOwnership` (never mutates/erases prior records) + fail-closed `resolveCurrentOwner` (throws on ambiguity rather than guessing). No import of `outcome-job.ts`/`project-plan.ts`/`approval-reference.ts` — cannot manufacture transition/approval authority. Depends directly on `V3-ORG-001`'s `OrganizationMembership`. 451/451 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V3-OWN-001.md` for the full reasoning and evidence.
- `V3-ORG-001` (Organization Membership / Role / Assignment Authority Foundation, V3 Workstream A) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/akilta-continuity-readback-5hm3ed`. Pure domain module (`src/domain/organization-membership.ts`): `OrganizationMembership`/`OrganizationRole` (`STAFF`/`JUNIOR`/`STUDENT`/`CLIENT_ASSOCIATE`, deliberately no owner role) + `AssignmentReference`/`resolveAssignmentStatus` (fail-closed, no cross-tenant leakage). No import of `authority.ts` — a role/membership can never itself grant permission/protected-action authority. See `docs/exec-plans/active/V3-ORG-001.md` for the full Definition-of-Ready check and evidence.
- `V2-CDO-008` (Delivery Project Advisor — L0 Observe / L1 Recommend minimum slice) — Status: **IMPLEMENTED / SELF-VALIDATED**, branch `claude/akilta-continuity-readback-5hm3ed` (continued on the current session branch, not a new task-scoped branch). Pure domain contract (`src/domain/delivery-advisor.ts`) composing the existing `ClientProjectSnapshot` (V2-CDO-005) and `DeliveryRecipe` (V2-CDO-002) fixtures, plus its presentation-layer wiring into the existing V2-APP-001/V2-CDO-006 shell (`src/web/shell-render.ts`'s `renderAdvisorSection`) — completing this same task's own A12/A13 acceptance items now that the portal it depends on exists. No new port, route or dependency. 421/421 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V2-CDO-008.md` for the full selection reasoning and evidence.
- `DEL-003` (second bounded slice: runtime admission + OutcomeJob wiring) — Status: **IMPLEMENTED / SELF-VALIDATED / PENDING NEXT V1 BRAIN AUDIT** at commit `72e2d614e8a6347ca9f3a75f6f0558593caff637`, branch `claude/DEL-003-runtime-admission-task-packet`. Round-1 correction closed Brain's F1–F4 CHANGES_REQUIRED findings; the DEC-144 final Brain verification pass then accepted F2/F3/F4 and VQA-001's Contact correction but found one remaining F1 defect (readiness gate could treat any current-version FACT as positive readiness regardless of what it actually asserted), closed by the round-2 correction (structural `readinessOutcome` field). See `docs/exec-plans/active/DEL-003.md`. Not reopened by V2-CDO-001 absent concrete regression evidence.
- `AKI-GIT-001` — Status: **IMPLEMENTED — READY FOR CHATGPT VERIFICATION**, draft PR `#1` open, not merged. See `docs/exec-plans/active/AKI-GIT-001.md`.

## Completed tasks

- `V2-APP-001` / `V2-CDO-006` (DEC-158 `V2-APP-001_TO_V2-CDO-006` train) — Brain **PASS / VERIFIED / CLOSED** at checkpoint `33f95a3dc1ae1fdae02992b781002b92267f3110`, branch `claude/akilta-continuity-readback-5hm3ed`, cut from the verified `V2-CDO-005` closure SHA `b5e5afb255e5e0134cca8f3f17cb20fa843d4c4e`. `V2-APP-001` (checkpoint `fd6aa89c190658978ae09d3e3ffc6950ff76f550`) adds `src/web/` — provider-neutral `AuthenticatedPrincipal`/`SessionContext`/`TenantContext`, fail-closed route/tenant guards, a transport-agnostic request-handler/shell-render pair, and a thin `node:http` adapter (ADR-0001), resolving the architecture edge V2-CDO-006 hit at the prior checkpoint. Its live re-preflight then satisfied V2-CDO-006's own start predicate, so `V2-CDO-006` (checkpoint `8f9f4b66273139758ffdb7d8ee18d00e1b6b8229`) was implemented directly on that shell: the minimum logged-in Client Portal UI (next-action, timeline, working-artifact, capabilities, recent-updates sections) consuming `ClientProjectSnapshot` verbatim. Zero new runtime dependency across both segments. 404/404 tests pass. Brain PASS followed two rounds of CR-E1/CR-E2/CR-E3 evidence-only correction (source unchanged throughout). `MERGE_DISPOSITION: HOLD_MERGE / DO_NOT_MERGE` remains controlling for PR `#4` (draft) independent of the evidence PASS. See `docs/exec-plans/completed/V2-APP-001.md`.
- `V2-CDO-005` (Customer-Safe Project & Outcome Projection Contract, DEC-157 continuous-train head, + V2-CDO-006 edge-stop finding) — Brain **PASS / VERIFIED / CLOSED** at `b5e5afb255e5e0134cca8f3f17cb20fa843d4c4e`. See `docs/exec-plans/completed/V2-CDO-005.md`.
- `V2-CDO-004` (First Bounded Account/Connection Ownership & Admitted Capability Consumption Contract, CONN-001 Minimum V2 Slice) — Brain **VERIFIED / PASS / CLOSED** at `abfb2a1c8487df8e3e240752286b1207989773cb` after CR-1 correction. See `docs/exec-plans/completed/V2-CDO-004.md`.
- `V2-CDO-003` (First Bounded Customer Ownership & Project Communications Contract, OPS-010 Minimum V2 Slice) — Brain **VERIFIED / PASS / CLOSED** at `b9430b74fdc580ce66530899e1510bc4c5ddf2cb` after CR-1 correction. See `docs/exec-plans/completed/V2-CDO-003.md`.
- `V2-CDO-002` (First Bounded Delivery Recipe Contract, `WEBSITE_BUILD_v1`) — Brain **VERIFIED / PASS / CLOSED** at `f99cd2f46b178d2190ac94ff6052c955d3e5cd43`. See `docs/exec-plans/completed/V2-CDO-002.md`.
- `V2-CDO-001` (first bounded V2 Client & Delivery OS slice: project/service status + unified delivery timeline) — Brain **VERIFIED / PASS / CLOSED** at `b5a8c4fd422b61c91ba656e910a73ba6165727b7`. See `docs/exec-plans/completed/V2-CDO-001.md`.
- `AKI-BE-001` — Brain **VERIFIED / COMPLETED** at `c2e7e7dee579b2fab614485631fa3cd1f53c6070`. See `docs/exec-plans/completed/AKI-BE-001.md`.
- `ENG-ORCH-001` — Brain **VERIFIED / COMPLETED** at `a804ebe73822883122f3dfabd869a4651f94dd38`. See `docs/exec-plans/completed/ENG-ORCH-001.md`.
- `DEL-003` first bounded slice (project compiler) — Brain **VERIFIED / COMPLETED** at `abb5ea954e338d82cf1583d83359e469d6959599`. Recorded inside `docs/exec-plans/active/DEL-003.md` (the DEL-003 parent task itself remains active/stage-gated; only its first slice is closed).

## Known conflicts / blockers

- None found. `git status` is clean at every checkpoint above; the full history is one linear chain from bootstrap through the current checkpoint (`AKI-GIT-001` → `AKI-BE-001` → `ENG-ORCH-001` first slice → `DEL-003` first slice → `ENG-ORCH-001` round-3 correction → `DEL-003` second slice round-1 correction (`3253439`) → V1 Fast Lane Stage B documentation pass (`ff832ce`) → `DEL-003` second slice round-2 correction (`72e2d61`), current HEAD `72e2d61`), verifiable via `git log --oneline` on this branch.
