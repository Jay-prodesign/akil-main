# V4-OPS-001 — Unified Operations Attention/Exception Model (Single-Domain Floor)

## Provenance

- Governing authority: DEC-160 V2→V5 Autonomous Engineering Corridor; V4 Full Blueprint (Drive `1M5vANxdfuH7Vro_CAFenioAmWitGPS3TRlzd06d5DNs`, R22 + R32/R39 currentness overlays) §4 spine item C / §6 Workstream B.
- **Selection reasoning (continuation of the same dependency scan that selected V3-COM-001 and V4-SVC-001):** the V4 blueprint's own R32 hot-buffer overlay names Workstream B ("unified Operations Attention/Exception aggregation") as the preferred readiness path immediately after Workstream A, explicitly instructing it to "reuse or extend the existing V3 attention semantics where natural." `V3-SLA-001`'s `AttentionState` (merged, `main`) is exactly that reusable substrate, so this slice is dependency-safe now.
- **Bounded-scope determination (the substantive finding of this task record):** §6 names eleven candidate attention families (blocked dependency, customer approval required, provider wait, credential/access problem, SLA/operating risk, failed external effect, telemetry anomaly, policy restriction, data-quality uncertainty, recovery required, optimization opportunity). A repo-wide check confirmed **exactly one of these families has a real, observable source-domain signal in this repository**: internal delivery/job exception state (`AttentionState`, V3-SLA-001). The other ten would each require a real data source that does not exist yet (no provider-wait telemetry, no credential/access system, no optimization-loop evidence, etc.). Declaring an `AttentionSourceDomain` union with only the one real, observable literal — rather than inventing ten more — is the same "declared honestly, not fabricated" discipline used throughout this checkpoint train.
- Branch: `claude/v4-ops-001-operations-attention`, cut fresh from merged `main` directly (independent of, and not stacked on, `V3-COM-001`/PR #5 or `V4-SVC-001`/PR #6 — none of the three depends on another's review).
- `BASE_PROVENANCE_SHA`: `4ba5231309ac2fc72dd089cc947030a958eab77e` (merged main).

## Goal

Provide a single, deterministic aggregation envelope over the one real attention source-domain this repository has, without flattening its distinct domain truth, without granting action authority, and without inventing the nine unrepresentable attention families or a next-safe-action reference no data source supports.

## Scope (this checkpoint — single-domain aggregation floor only)

- `AttentionSourceDomain` (single literal `"DELIVERY_OUTCOME_JOB"`) + `OperationsAttentionItem` + `toOperationsAttentionItem` + `resolveActiveOperationsAttention` (`src/domain/operations-attention.ts`) — pure functions composing the existing `AttentionState` (V3-SLA-001) into a deterministic aggregation item and a tenant/project-scoped active-item filter.
- One canonical `WEBSITE_BUILD_v1` fixture pair (`src/fixtures/website-build-v1-operations-attention.ts`) — a fresh job placed into a `BLOCKED` exception state via the existing `outcome-job.ts` functions, wrapped through `buildAttentionState` (V3-SLA-001) and then `toOperationsAttentionItem`.

## Explicitly deferred (not invented)

Nine of §6's eleven candidate attention families (customer approval required, provider wait, credential/access problem, SLA/operating risk, failed external effect, telemetry anomaly, policy restriction, data-quality uncertainty, recovery required, optimization opportunity) have no real observable source anywhere in this repository and are therefore not represented on `AttentionSourceDomain` at all — adding a real second source domain later is additive, not a breaking change. Grouping/deduplication of correlated events (§6: "may be grouped") is not implemented — with only one source domain, there is nothing genuinely cross-domain to correlate yet. `nextSafeActionRef` is omitted entirely — no such source exists. Dismiss/resolve semantics are not implemented at all (no mutation capability exists in this module), so "dismiss/resolve semantics cannot rewrite source domain truth unless the source contract permits it" is satisfied by absence rather than by a permission check.

## Architecture / semantic invariants

- `toOperationsAttentionItem` reads every field directly off the given `AttentionState` — it cannot promote/demote urgency, invent an owner, or fabricate a reason/timestamp the source state does not already carry ("UI aggregation cannot manufacture status," §6).
- `isActive` is always recomputed from the exact given `AttentionState.internalAttentionLevel` at call time — never cached or independently toggled, so a stale/resolved source cannot remain flagged active ("stale resolved source cannot remain active," §6 acceptance).
- `contractualSlaStatus` is deliberately not carried into the aggregation — conflating it with internal attention would blur the exact internal-vs-contractual distinction V3-SLA-001 exists to preserve.
- `resolveActiveOperationsAttention` is a pure filter — it never mutates an item, and only ever narrows to the exact requested tenant/project scope, verified by an adversarial cross-project test.
- This module exports no escalation/dismiss/resolve function — visibility never grants action authority ("escalation and next action are separately authorized," §6), verified by a boundary-scan test asserting the module's exact export set.
- No import of `authority.ts`, `capability-admission.ts`, `connection-authority.ts`, `service-capability-routing.ts`, or `partner-organization.ts` — and `outcome-job.ts` itself is not imported at all (only `attention-state.ts`'s own already-composed types are used), keeping this module strictly one layer removed from job-transition capability.

## Hard Non-Scope

No provider-wait/credential/telemetry/policy/data-quality/recovery/optimization attention family; no cross-domain grouping/deduplication; no `nextSafeActionRef`; no dismiss/resolve/escalation mutation; no persistence/database; no HTTP/UI surface; no AI Commerce mutation; no merge/deploy/release/production/publication/customer-binding/legal/financial action anywhere in this checkpoint's source.

## Test Coverage

| Test | §6 acceptance direction | Covered in |
|---|---|---|
| Reference proof: deterministic, reuses existing attention-state fixture | reuse V3 attention semantics rather than duplicate | `tests/operations-attention.test.ts` |
| `NORMAL` state → `isActive: false` | honest, non-fabricated status | `tests/operations-attention.test.ts` |
| `BLOCKED` state → `isActive: true`, reason/timestamp preserved verbatim | UI aggregation cannot manufacture status | `tests/operations-attention.test.ts` |
| No `contractualSlaStatus` field carried through | internal vs. contractual SLA remain separate truths | `tests/operations-attention.test.ts` |
| `resolveActiveOperationsAttention` returns only active items in-scope | correct aggregation, no over-broad match | `tests/operations-attention.test.ts` |
| Cross-project adversarial substitution excluded, not coerced | cross-client attention leakage rejects | `tests/operations-attention.test.ts` |
| Module exports exactly the two read-only functions, nothing else | escalation/next action separately authorized | `tests/operations-attention.test.ts` |
| No secret/commerce-provider coupling | general hygiene | `tests/v4-ops-001-boundary-scan.test.ts` |
| No model/provider hard-coding | hard non-scope | `tests/v4-ops-001-boundary-scan.test.ts` |
| `AttentionSourceDomain` declares exactly one literal | no fabricated second source domain | `tests/v4-ops-001-boundary-scan.test.ts` |
| No import of `authority.ts`/`capability-admission.ts`/`connection-authority.ts`/`service-capability-routing.ts`/`partner-organization.ts` | isolation from authority/capability/connection/partner systems | `tests/v4-ops-001-boundary-scan.test.ts` |
| No import of `outcome-job.ts`, or from `src/web/`/`src/application/` | one layer removed from job-transition capability; domain isolation | `tests/v4-ops-001-boundary-scan.test.ts` |
| Zero new runtime dependency | dependency delta disclosed | `tests/v4-ops-001-boundary-scan.test.ts` |

## Evidence

- `npx tsc -p tsconfig.json --noEmit`: exit 0, strict mode, zero errors.
- `npm run test`: **496/496 pass** (483 pre-existing/unchanged + 13 new: 7 in `operations-attention.test.ts`, 6 in `v4-ops-001-boundary-scan.test.ts`), 0 fail, 0 skipped.
- `package.json`: zero runtime `dependencies`; `devDependencies` unchanged (`@types/node`, `typescript`).
- Files touched this round: `src/domain/operations-attention.ts` (new), `src/fixtures/website-build-v1-operations-attention.ts` (new), `tests/operations-attention.test.ts` (new), `tests/v4-ops-001-boundary-scan.test.ts` (new), this exec-plan, `docs/engineering/CURRENT_STATE.md`, `docs/architecture/DEPENDENCY_MAP.md`, `docs/architecture/VERSION_EVOLUTION_MAP.md`, `docs/exec-plans/corridors/V2_TO_V5.md`. No existing source file modified.

## Status

**IMPLEMENTED / SELF-VALIDATED** — pending Brain independent verification. Not yet `VERIFIED`/`PASS`/`CLOSED`; Claude's authority ends at this status per `AGENTS.md` §10. Normal task-scoped PR against `main`; merge requires a separately granted protected owner-gate, exactly as PR #4 did.

## Brain review pointer

To be recorded after commit/push/PR (see PR description and Drive evidence bridge).
