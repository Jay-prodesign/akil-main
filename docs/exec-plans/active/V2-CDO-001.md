# V2-CDO-001 — Client & Delivery OS — First Bounded Engineering Slice

## Provenance

- Version authority: "AKILTA + AI Commerce — Master Release, Version & Frontend Evolution Plan v1.0" (Drive `1GtYQ90h-3Nw8jNvu7z1-9olMlBIgTiLObLUEdkToS5w`), Section 5 "AKILTA v2.0 — CLIENT & DELIVERY OS": client portal; project/service status; unified delivery timeline; AI Project Advisor; account/customer ownership; structured Delivery Recipes; customer communications routing; AI engineering task orchestration foundations; AI Commerce capability consumption where admitted.
- Sequencing authority: DEC-149 "VERSION-TRANSITION VERIFICATION ECONOMY + V1→V2 FAST-TRACK" and DEC-150 "CONTINUOUS ENGINEERING / OBSERVABLE VERSION CHECKPOINT OPERATING MODEL", both dated 21 August 2026.
- Roadmap tracking: `04 — AKILTA Master Roadmap & Task Tracker` (Drive `1VRDtcYNK1PqNyAk_vLr2T90bkuNrI83BGUFf_1d__P4`). As of the 21 August 2026 DEC-149/150 reconciliation pass (independently re-verified by Claude by reading the live rows directly, not trusting summary text), the minimum V2 slices of `POR-001`, `AI-004`, `OPS-010`, `CONN-001`, and `CXP-001` are each marked `V2 Active Requirement — <named minimum slice> / Broader <parent scope> Stage-Gated`, with each row explicitly noting: "NON-SELECTOR: this row is requirement/acceptance authority only and MUST NOT be interpreted as current task selection, current cursor, next action or independent WIP. Sole mutable current engineering selection resolves from 02 — AKILTA Current Project State." These rows are therefore *requirement/acceptance authority* that bounds and informs V2-CDO-001's scope — not separate task selections to start.
- Live-state precondition independently verified before this slice started: Shopify theme `gid://shopify/OnlineStoreTheme/161739243744` ("AKILTA — V1 FOOTER ROUTE FIX — 2026-08-20") is `role=MAIN`, `processing=false`, `processingFailed=false`.
- Branch: `claude/V2-CDO-001-task-packet`, created from `9d21e14` (212/212 tests passing at that commit).
- No pre-authored bounded task packet existed for V2 when this slice started; Claude self-scoped the smallest safe first slice under the DEC-149/150 "Engineering Execution Ownership Reconciliation" delegation. Brain verification is required before this slice can move past `IMPLEMENTED / SELF-VALIDATED`.

## Purpose

Implement the smallest concrete, provider-neutral, secret-free slice of "project/service status" and "unified delivery timeline" (v2.0 scope items 2–3) as a pure read-model projection over the already-VERIFIED/COMPLETED AKI-BE-001 domain kernel (`OutcomeJob`, `AuditEvent`, `Project`, `Customer`, `TenantScope`) — the customer-observable core a client portal would eventually render, built with zero new architecture, zero new dependency, and zero HTTP/frontend surface.

## Scope (Minimum Domain Objects)

- `DeliveryStatusLabel` / `DeliveryJobStatus` / `DeliveryStatusView` / `computeDeliveryStatus` (`src/domain/delivery-status.ts`) — aggregates a project's own `OutcomeJob` records into one customer-safe status label (`NOT_STARTED` / `IN_PROGRESS` / `BLOCKED` / `COMPLETE`) plus a per-job breakdown, derived directly from the existing canonical `OutcomeJobState` lifecycle (main path vs. the four exception states) — no new state machine invented.
- `CustomerTimelineCategory` / `DeliveryTimelineEntry` / `DeliveryTimeline` / `buildDeliveryTimeline` (`src/domain/delivery-timeline.ts`) — chronological (timestamp-sorted) projection of a project's own `AuditEvent` records into a minimal customer-facing entry (`eventId`, `jobId`, `category`, `timestamp`). Round 1 correction (below): `category` is a deterministic, closed-set mapping (`STATUS_UPDATE` / `BLOCKER` / `VERIFICATION` / `ACTION_REQUIRED` / `WORKING_ARTIFACT` / `APPROVAL`) from the raw `AuditEvent.eventType`; raw `reason`, `actorRef`, `relatedRefs`, and free-form `eventType` never reach the customer-facing entry. An event whose `eventType` has no mapping fails closed by omission (excluded from the timeline), never guessed.
- Both functions fail closed (throw) on any input record whose `tenantId` or `projectId` does not match the given `Project` — matching the DEL-003 T7 "cross-tenant/wrong-project ... binding fails closed" discipline already established in this repository.

## Non-Scope (explicitly not implemented this slice)

Client portal UI/frontend or any navigation/IA surface (Section 2 of the Master Release Plan classifies "client portal" as a major-version mental-model change requiring "mandatory IA/frontend review" — not something to build unilaterally in a backend-only bounded slice); the *broader* forms of AI Project Advisor / structured Delivery Recipes / customer communications routing / account-connection ownership / AI Commerce capability consumption beyond the minimum V2 slices the roadmap now marks active (see Provenance) — specifically excluded per CR-2 (Round 1 correction): `OPS-005` existing-customer migration/managed-service registry, full CRM/backoffice/billing/invoicing/payment/subscription/licensing/renewal-dunning/e-Fatura, the broader `OPS-010` operations platform, the broader `CONN-001` integration architecture / production cutover automation, a generic AI factory/workflow platform beyond the first Delivery Recipe slice, any claim of autonomy beyond what's evidenced, and any AI Commerce repository/state mutation; any HTTP/API server, database, queue, or deployment infrastructure (this remains a domain/application kernel); any customer-visibility/redaction logic beyond the closed-set `CustomerTimelineCategory` mapping.

## Round 1 Correction (CR-1 / CR-2 / CR-3) — 21 August 2026

Brain independent disposition on checkpoint `b37440dab6f4eb8b7fa36a0ecdaba178ad50f09c`: **CHANGES_REQUIRED** (delivered as document-body `BRAIN_RESPONSE` on the native-Google-Doc handoff v2, after an initial comment-based reply proved unreadable by Claude's tooling on the plain-text v1 handoff — see the "Transport" note in Status below).

- **CR-1 (customer-safe timeline boundary)**: closed. `delivery-timeline.ts` rewritten to strip `reason`/`actorRef`/`relatedRefs`/free-form `eventType` from the customer-facing entry; replaced with a deterministic closed-set `category` mapping, unmapped events omitted. See Scope above and the T1–T4 test rows below.
- **CR-2 (stale authority correction)**: closed by this document's Provenance/Non-Scope sections above, reflecting that the minimum V2 slices of POR-001/AI-004/OPS-010/CONN-001/CXP-001 are active requirements (not untriggered/stage-gated in full) while their broader/full forms, and `OPS-005`, remain explicitly out of scope.
- **CR-3 (evidence closure)**: closed via a fresh SHA-bound evidence bundle superseding the prior one (see Status below).

Before applying this correction, Claude independently re-verified the live `04 — AKILTA Master Roadmap & Task Tracker` rows directly (not the write-lock document's summary of them) to confirm CR-2's premise was actually true in the canonical source, since an earlier write-lock lease entry had made the same claim before the roadmap tracker itself showed any edit — that earlier claim was correctly not acted on until the roadmap tracker's own cells changed.

## Test Coverage

| Test | Requirement | Covered in |
|---|---|---|
| No jobs / no events | Empty input yields `NOT_STARTED` / empty timeline, correctly tenant/project-scoped | `tests/delivery-status.test.ts`, `tests/delivery-timeline.test.ts` |
| Status derivation | All-`CLOSED` → `COMPLETE`; non-terminal mix → `IN_PROGRESS`; any exception-state job → `BLOCKED` with only that job marked `isBlocking` | `tests/delivery-status.test.ts` |
| T1/T2 | Customer timeline entry never carries `reason`, `actorRef`, or `relatedRefs` | `tests/delivery-timeline.test.ts` |
| T3 | An unmapped/unknown `eventType` is omitted from the customer timeline, not guessed | `tests/delivery-timeline.test.ts` |
| T4 | Known `eventType`s map deterministically to a closed-set `CustomerTimelineCategory` | `tests/delivery-timeline.test.ts` |
| Timeline ordering | Entries sorted ascending by timestamp regardless of input order | `tests/delivery-timeline.test.ts` |
| T5 | Fail-closed tenant/project binding — cross-tenant and same-tenant-wrong-project input records are rejected, not silently filtered | `tests/delivery-status.test.ts`, `tests/delivery-timeline.test.ts` |
| T6 | `computeDeliveryStatus` unchanged/regression-free (no defect found, no change made) | `tests/delivery-status.test.ts` |
| T7 / Boundary | Full suite + strict `tsc` + secret/commerce-provider/model-provider/workflow-engine scans pass; zero new runtime dependency | `tests/v2-cdo-001-boundary-scan.test.ts` |

## Evidence (this checkpoint)

- `npm run test`: **228/228 pass** (221 pre-existing/unchanged + 7 in the rewritten `delivery-timeline.test.ts`), 0 fail.
- `npx tsc -p tsconfig.json --noEmit`: pass, strict mode, zero errors.
- Secret / commerce-provider / model-provider / workflow-engine scan across the 2 domain files: 0 matches (`tests/v2-cdo-001-boundary-scan.test.ts`).
- `package.json`: zero runtime `dependencies`; `devDependencies` unchanged (`@types/node`, `typescript`) — no new dependency introduced.
- Files touched this round: `src/domain/delivery-timeline.ts` (rewritten), `tests/delivery-timeline.test.ts` (rewritten), this exec-plan, `docs/engineering/CURRENT_STATE.md`. `delivery-status.ts` and its tests unchanged (T6).

## Status

`IMPLEMENTED / SELF-VALIDATED / PENDING BRAIN RE-AUDIT`. Not self-declared VERIFIED, COMPLETED, Commercial Ready, or GA. No merge, no deploy, no release, no frontend/UI built, no AI/model routing, no external provider/connector, no AI Commerce mutation. `V2-CDO-002` (First Bounded Delivery Recipe Contract, per Brain's response) explicitly must not start until this checkpoint receives Brain PASS.

Durable evidence/handoff surfaces (Brain cannot probe this private repository directly per the "AKILTA BRAIN PRIVATE REPOSITORY OBSERVATION BOUNDARY" reconciliation):
- v1 (plain text, not comment-readable): "V2-CDO-001 — Engineering Evidence Bundle — b37440d — 2026-08-21", Drive `1lSt4MV-eQrLeBzrimkteXDsItyEk8qNh`; "V2-CDO-001 — Claude Handoff — PENDING_BRAIN_RESPONSE — 2026-08-21", Drive `1DEVtGMrDbL2OdhvpEvqOZwFkz15zc01I`.
- v2 (native Google Doc, comment-readable): "V2-CDO-001 — Claude Handoff v2" (now `ANSWER_RECEIVED / RESUME`), Drive `1zLf0twZu4qgqnnfUowRCUhopS6czYIr6fWFPmNipgOw` — carries the full `BRAIN_RESPONSE` (CR-1/CR-2/CR-3, disposition, required test contract) that this correction round implements.
- Round 1 correction evidence: "V2-CDO-001 — Round 1 Correction Evidence Bundle — b5a8c4f — 2026-08-21", Drive `1DiL0bK5kl5FvY9t5YSlzKctwGtYu0BnX` — full diff/acceptance mapping against the CR-1/CR-2/CR-3 request, raw test/tsc/scan output, dependency delta, non-scope confirmation. Superseded on raw-format grounds by the artifact below.
- Final raw evidence transcript (responding to Brain's `FINAL_EVIDENCE_FORMAT_GAP_ONLY` disposition, handoff v2 rev 4, 2026-08-21T23:19:00+03:00): "V2-CDO-001 — Final Raw Evidence Transcript — b5a8c4f — 2026-08-21", Drive `1EycwcdUibnF50uNdBxee1NgKopHcXCeq` — the complete, unedited `npm run test` transcript (all 228 per-test `duration_ms`/`type` YAML blocks, unstripped), the explicit `NPM_TEST_EXIT_CODE: 0`, and an explicit statement/mapping that tests 210–212 (`tests/v2-cdo-001-boundary-scan.test.ts`) are the sole boundary/secret/provider/dependency scan mechanism — no separate scan command exists in this repository. Evidence-only artifact; does not carry any Stable Engineering Handoff / Task Head status.
- **Transport note**: an initial Brain reply was posted as a Google Docs comment on the v1 (plain-text) files; Claude's Drive tooling cannot read comments back from non-native file types, so that reply was structurally unreadable. The v2 native-Doc handoff exists specifically to fix this, and the operative `BRAIN_RESPONSE` was reproduced there as document body text, which Claude could read.

## Next Exact Action

Awaiting Brain re-audit of this correction round. Do not self-declare PASS. `V2-CDO-002` (First Bounded Delivery Recipe Contract, `WEBSITE_BUILD_v1`) is the pre-named next task but is explicitly gated on this checkpoint receiving Brain PASS first.
