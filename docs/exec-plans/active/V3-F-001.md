# V3-F-001 — Team & Attention Shell Projection (V3 Workstream F Floor Slice)

## Provenance

- Governing authority: DEC-160 V2→V5 Autonomous Engineering Corridor + V3 Full Blueprint (Drive `1joeMcCRtnDSotEmWa63fFG6uxGvbeU1LZApm7_5ap50`) §3 spine item G / §9 Workstream F (V3 logged-in frontend / role-aware IA), R39 currentness overlay ("a bounded frontend slice using only already-authoritative fields... Claude resolves this from fresh repo truth").
- **Selection reasoning:** after PR #4 merged into `main` (`4ba5231309ac2fc72dd089cc947030a958eab77e`) and three independent floor-slice checkpoints (V3-COM-001/PR #5, V4-SVC-001/PR #6, V4-OPS-001/PR #7) were opened but remained unmerged, a fresh dependency scan of V4 Workstreams C-H (all require real provider adapters or external effects that do not exist) and V5 Workstreams A-D (meta-engineering-process concepts that would duplicate the existing `ENG-ORCH-001` durable-state primitives, which the V5 blueprint's own R32 hot buffer explicitly warns against re-creating) found no further honest domain-module unit reachable without either merging one of the three open PRs or introducing a new architectural decision. That architectural decision — a new provider-neutral shell read-port surfacing already-merged V3 A-D substrate into the web shell — was explicitly authorized by the Founder as a reversible, architecture-within-envelope technical decision inside Claude's existing delegated authority, per DEC-160's "Claude may make reversible technical/architecture-within-envelope decisions" role definition.
- **Bounded-scope determination:** this checkpoint consumes **only already-merged `main` substrate** — `OrganizationMembership`/`resolveAssignmentStatus` (V3-ORG-001), `OwnershipAssignment`/`resolveCurrentOwner` (V3-OWN-001), `AttentionState`/`buildAttentionState` (V3-SLA-001) — all merged via PR #4. It does **not** import from, depend on, or wait for PR #5 (V3-COM-001, commercial-authority), PR #6 (V4-SVC-001), or PR #7 (V4-OPS-001); none of those files exist on this branch (cut fresh from merged `main`), so consuming them is structurally impossible, not merely avoided by discipline. Commercial/commission/discount state is rendered as a fixed "Unavailable" literal, never a computed value — no commercial domain type is invented.
- Branch: `claude/v3-f-001-team-attention-shell-projection`, cut fresh from merged `main` (`4ba5231309ac2fc72dd089cc947030a958eab77e`).
- `BASE_PROVENANCE_SHA`: `4ba5231309ac2fc72dd089cc947030a958eab77e` (merged main).

## Goal

Extend the existing V2-APP-001/V2-CDO-006 logged-in shell with a role-aware "Team & attention" section — organization/client context, current viewer role, each of the four V3 owner roles rendered separately, and internal attention/exception state — using only already-merged, already-authoritative V3 substrate, without creating a second identity/session/auth system, a new persistence layer, a provider SDK/OAuth connector, or a new commercial/financial policy.

## Scope (this checkpoint)

- `src/domain/team-attention-projection.ts` — `TeamAttentionProjection`/`buildTeamAttentionProjection`, a pure function composing `OrganizationMembership`, `OwnershipAssignment` history (via `resolveCurrentOwner`, once per owner role), and an optional `AttentionState` into one deterministic, tenant/project-scoped projection.
- `src/web/team-attention-view-state.ts` — `TeamAttentionSource` (the one new provider-neutral read port) + `TeamAttentionViewState` (closed `FORBIDDEN_TENANT_SCOPE | UNAVAILABLE | READY` union) + `resolveProtectedTeamAttentionView`, mirroring `snapshot-view-state.ts`'s exact fail-closed tenant-ownership-check-before-source-query pattern.
- `src/fixtures/website-build-v1-team-attention.ts` — one canonical fixture reusing `WEBSITE_BUILD_V1_OWNERSHIP` verbatim, populating all four owner roles and a real `BLOCKED` `AttentionState` (built from a fresh `OutcomeJob`, never importing the unmerged V4-OPS-001 fixture).
- `src/web/shell-render.ts` — `renderTeamAttentionSection`, wired into `renderSnapshotBody` for both `READY` and `BLOCKED` shell states; `ShellPageContent`'s `READY`/`BLOCKED` variants gain an optional `teamAttention?: TeamAttentionViewState` field.
- `src/web/request-handler.ts` — `createRequestHandler` gains an optional `teamAttentionSource?: TeamAttentionSource` dependency; when present, the resolved view is attached to `READY`/`BLOCKED` content using the same session/ownership already validated for the snapshot. Omitting it (all existing callers) degrades to an honest "Unavailable" render, never a crash or fabricated data.
- `src/web/http-server.ts` — threads the same optional `teamAttentionSource` through to `createRequestHandler`.
- `src/fixtures/web-shell.ts` — `createWebShellFixtureTeamAttentionSource()`, a dev-fixture `TeamAttentionSource` mirroring `createWebShellFixtureSnapshotSource()` exactly.

## Explicitly deferred (not invented)

Commission/commission-ledger/discount-authority state (Workstream E, a separate unmerged checkpoint) — rendered as a fixed "Commercial: Unavailable — pending separate commercial-authority checkpoint" literal, not computed. Service-capability-routing/execution-maturity and operations-attention aggregation (V4 Workstreams A/B, also separate unmerged checkpoints) are not consumed. Multi-client/partner context switching (V3 Workstream C's own frontend implication) is not implemented — only the single-project ownership/attention view is in scope. No new authentication/session/tenant architecture, no persistence, no provider SDK/OAuth/connector.

## Rev28 bounded correction (2026-09-02) — CHANGES_REQUIRED_SOURCE_SECURITY

Brain Handoff Rev28 finding on PR #8: "The resolver checks requested ownership before querying the source but does not fail closed if the returned projection itself carries a different tenant/customer/project scope. A faulty or compromised source could therefore yield READY with foreign-scope data."

Required bounded correction: "validate returned projection tenantId/customerId/projectId against requested ownership before READY; fail closed on any returned-scope mismatch; add adversarial tests for foreign tenant/customer/project projections; verify viewerRole cannot be emitted from a non-current membership under the existing membership semantics, proving via tests if the input contract already guarantees currentness."

Root cause: `resolveProtectedTeamAttentionView`'s `requireTenantOwnership` check proves the *caller's session* is allowed to see the requested tenant/customer/project scope. It proved nothing about what `source.getTeamAttentionProjection` actually returned — a `TeamAttentionSource` implementation that is faulty (wrong lookup key) or compromised (deliberately substitutes data) could hand back a `TeamAttentionProjection` for a completely different tenant/customer/project, and the resolver would wrap it in `READY` unexamined.

Fix (`src/web/team-attention-view-state.ts`): after obtaining a defined `projection` from the source, `resolveProtectedTeamAttentionView` now independently compares `projection.tenantId`/`projection.customerId`/`projection.projectId` against `input.requestedOwnership`'s own three fields; any mismatch resolves `UNAVAILABLE` (the same honest "no trustworthy data" outcome the type already used for a missing/throwing source) rather than `READY`. No new `TeamAttentionViewState` union member was added — `renderTeamAttentionSection` (`shell-render.ts`) already renders every non-`READY` kind identically as "Unavailable," so this fail-closed path needed no render-layer change.

Second required item — "verify viewerRole cannot be emitted from a non-current membership": `OrganizationMembership` (V3-ORG-001, merged) carries exactly four fields (`membershipId`/`tenantId`/`principalRef`/`role`) and no status/supersede/revoke/timestamp field of any kind in this bounded slice (unlike `OwnershipAssignment`, which does have provenance-preserving supersede semantics). There is therefore no representable "non-current" `OrganizationMembership` value for `buildTeamAttentionProjection`'s existing tenant-match check to fail to catch — this is proven structurally by a new test asserting the type's exact closed field set, plus a corroborating behavioral test, rather than by adding a check against a state dimension that does not exist. No source change was required for this half of the correction.

## Architecture / semantic invariants

- `buildTeamAttentionProjection` is pure and reuses `resolveCurrentOwner` (V3-OWN-001) independently for each of the four `OwnerRole`s — one role is never inferred from another (§9: "render separately").
- `viewerRole` is surfaced only when the supplied `viewerMembership.tenantId` exactly matches the requested `ownership.tenantId` — a cross-tenant membership is never surfaced (adversarial test coverage). (Rev28) `OrganizationMembership`'s closed four-field shape carries no status/currency dimension at all, so this tenant-match check already covers every representable case — proven structurally, not just tested behaviorally.
- `attention` is surfaced only when the supplied `AttentionState.tenantId`/`projectId` exactly match the requested `ownership` — mirroring `buildAttentionState`'s own existing two-field fail-closed check verbatim (no new isolation rule invented).
- `resolveProtectedTeamAttentionView` performs the identical `requireTenantOwnership` check as `resolveProtectedSnapshotView` before ever querying `source` — a tenant/project mismatch never reaches the new port at all. (Rev28) It additionally verifies the *returned* `projection`'s own tenant/customer/project identity against the requested ownership before ever returning `READY` — a faulty or compromised source's foreign-scope projection resolves `UNAVAILABLE`, never `READY`.
- `TeamAttentionViewState`'s `FORBIDDEN_TENANT_SCOPE` and `UNAVAILABLE` render the identical "Unavailable" message in the shell — no boundary-detail difference is ever exposed to the page.
- `team-attention-projection.ts` imports nothing from `outcome-job.ts`, `authority.ts`, `capability-admission.ts`, `connection-authority.ts`, or `partner-organization.ts` — visibility cannot transition a job or grant permission.
- No file in this checkpoint imports `commercial-authority.ts`, `service-capability-routing.ts`, or `operations-attention.ts` — structurally impossible, since none of those files exist on this branch (cut from merged `main`, prior to any of PR #5/#6/#7 merging).
- `teamAttentionSource` is optional everywhere it is threaded (`createRequestHandler`, `createHttpServer`) — every existing caller/test that omits it continues to compile and behave exactly as before (verified: all 483 pre-existing tests still pass unchanged).

## Hard Non-Scope

No commission/discount/payout/currency field of any kind; no persistence/database; no provider SDK/OAuth/connector; no new auth/session/tenant architecture; no consumption of PR #5/#6/#7 (structurally impossible on this branch); no merge/deploy/release/production/publication/customer-binding/legal/financial commitment anywhere in this checkpoint's source.

## Test Coverage

| Test | §9 / §7 acceptance direction | Covered in |
|---|---|---|
| Reference proof: fixture reuses existing ownership, populates all 4 owner roles + attention | reuse existing identities | `tests/team-attention-projection.test.ts` |
| No history/membership/attentionState supplied → every optional field honestly absent | never guessed | `tests/team-attention-projection.test.ts` |
| Each owner role resolves independently | "render separately" | `tests/team-attention-projection.test.ts` |
| Cross-tenant viewerMembership never surfaces as viewerRole (adversarial) | no cross-tenant leakage | `tests/team-attention-projection.test.ts` |
| Cross-project attentionState never coerced into scope (adversarial) | isolation fails closed | `tests/team-attention-projection.test.ts` |
| NORMAL attentionState surfaces isActive:false, not omitted | UI cannot manufacture status | `tests/team-attention-projection.test.ts` |
| No commission/discount/payout-shaped field name anywhere | no invented commercial value | `tests/team-attention-projection.test.ts` |
| READY view + tenant mismatch → FORBIDDEN_TENANT_SCOPE without querying source | fail-closed isolation | `tests/web-team-attention-view-state.test.ts` |
| No source wired / source returns undefined / source throws → UNAVAILABLE | honest unavailable, no fabrication | `tests/web-team-attention-view-state.test.ts` |
| Source returns a projection for a foreign tenant/customer/project → UNAVAILABLE, never READY (adversarial/compromised source) | Rev28: fail closed on returned-scope mismatch | `tests/web-team-attention-view-state.test.ts` (3 tests) |
| Source returning the exact matching scope still resolves READY | Rev28 correction narrows nothing for honest sources | `tests/web-team-attention-view-state.test.ts` |
| OrganizationMembership's closed field set proves no "non-current" state is representable, so viewerRole cannot leak from one | Rev28: viewerRole/non-current-membership proof | `tests/team-attention-projection.test.ts` |
| Only READY/BLOCKED render the section; UNAVAILABLE/FORBIDDEN render identical "Unavailable" | scoped, no boundary-detail leak | `tests/web-shell-render.test.ts` |
| READY view renders each owner role, viewer role, attention, fixed commercial-unavailable literal | full IA truth surfaced | `tests/web-shell-render.test.ts` |
| Unassigned owner role renders "not assigned", never a guessed id | canonical state, not UI default | `tests/web-shell-render.test.ts` |
| Handler without teamAttentionSource still renders 200 + honest Unavailable | backward-compatible, no crash | `tests/web-request-handler.test.ts` |
| Handler with teamAttentionSource renders resolved fields end-to-end | full wiring proof | `tests/web-request-handler.test.ts` |
| No secret/commerce-provider/model-provider coupling | general hygiene | `tests/v3-f-001-boundary-scan.test.ts` |
| No invented commercial field; zero `: number` field | hard non-scope | `tests/v3-f-001-boundary-scan.test.ts` |
| No persistence/database/provider-SDK coupling | hard non-scope | `tests/v3-f-001-boundary-scan.test.ts` |
| No import of sibling-PR-only modules or authority/outcome-job/capability-admission/connection-authority/partner-organization | isolation | `tests/v3-f-001-boundary-scan.test.ts` |
| No new session/auth primitive introduced | no auth architecture change | `tests/v3-f-001-boundary-scan.test.ts` |
| `teamAttentionSource` is optional on `createRequestHandler` | backward compatibility | `tests/v3-f-001-boundary-scan.test.ts` |
| Zero new runtime dependency | dependency delta disclosed | `tests/v3-f-001-boundary-scan.test.ts` |

## Evidence

- `npx tsc -p tsconfig.json --noEmit`: exit 0, strict mode, zero errors, at the Rev28-corrected head.
- `npm run test`: **519/519 pass**, 0 fail, 0 skipped, at the Rev28-corrected head (514 from the original implementation + 5 new this round: 4 in `tests/web-team-attention-view-state.test.ts`, 1 in `tests/team-attention-projection.test.ts`).
- `package.json`: zero runtime `dependencies`; `devDependencies` unchanged (`@types/node`, `typescript`).
- Files touched at original implementation: `src/domain/team-attention-projection.ts` (new), `src/web/team-attention-view-state.ts` (new), `src/fixtures/website-build-v1-team-attention.ts` (new), `tests/team-attention-projection.test.ts` (new), `tests/web-team-attention-view-state.test.ts` (new), `tests/v3-f-001-boundary-scan.test.ts` (new), `src/web/shell-render.ts`, `src/web/request-handler.ts`, `src/web/http-server.ts`, `src/fixtures/web-shell.ts`, `tests/web-shell-render.test.ts`, `tests/web-request-handler.test.ts`, this exec-plan, `docs/engineering/CURRENT_STATE.md`, `docs/architecture/DEPENDENCY_MAP.md`, `docs/architecture/VERSION_EVOLUTION_MAP.md`, `docs/exec-plans/corridors/V2_TO_V5.md`.
- Files touched this Rev28 correction round: `src/web/team-attention-view-state.ts`, `tests/web-team-attention-view-state.test.ts`, `tests/team-attention-projection.test.ts`, this exec-plan. No other existing source file modified.

## Status

**IMPLEMENTED / SELF-VALIDATED** — Rev28 bounded correction applied; pending fresh Brain independent verification at the new exact head. Not yet `VERIFIED`/`PASS`/`CLOSED`; Claude's authority ends at this status per `AGENTS.md` §10. `MERGE_DISPOSITION: HOLD_MERGE` remains in force per Rev28 — normal task-scoped PR against `main`; merge requires a separately granted protected owner-gate, never inferred from a prior PR's grant.

## Brain review pointer

Rev27 reviewed the original implementation at head `4e918fa92499f8f3ad31923144aba4cac93f9676` and returned `CHANGES_REQUIRED — EVIDENCE_ONLY / EXACT_HEAD_PACKAGE_INCOMPLETE` (missing complete §12A diff+raw-transcript package, not a source defect). Rev28 then independently reviewed the completed Rev27 evidence package for that same head and returned `CHANGES_REQUIRED_SOURCE_SECURITY` (this correction's subject). The corrected head and its complete new exact-head evidence package (diff + typecheck + full regression transcript + criterion map) are recorded in Drive; see the Rev28 PR #8 correction evidence documents for the exact fileIds and the new head SHA.
