# V2-CDO-003 — First Bounded Customer Ownership & Project Communications Contract — OPS-010 Minimum V2 Slice

## Provenance

- Governing authority: DEC-149 / DEC-150 continuous V2 Client & Delivery OS corridor + Roadmap `OPS-010` `ACTIVE_V2_REQUIREMENT` minimum ownership/communications acceptance authority. Not the broader `OPS-010` operations platform.
- Start-authorized only because `V2-CDO-002` received Brain **PASS** at checkpoint `f99cd2f46b178d2190ac94ff6052c955d3e5cd43` — see `docs/exec-plans/completed/V2-CDO-002.md`.
- Canonical task head: "V2-CDO-003 — Stable Engineering Handoff / Task Head", Drive `1IKVfUyuQrpVNWglIcJVAbSG46ZVwjjIr9KsMWgq-dug`, head rev 1. This document's scope/invariants/test contract are transcribed from that canonical source, not invented.
- `BASE_PROVENANCE_SHA` per the task head: `f99cd2f46b178d2190ac94ff6052c955d3e5cd43` (verified predecessor V2-CDO-002 closure).
- Pre-execution reconciliation (performed before source mutation, per the task head's explicit instruction): live-read this repository and confirmed no existing `Organization` type or `service` identity, and no existing communication/message primitive. The closest existing organizational-boundary identifier is `TenantScope.tenantId` (`tenant-scope.ts`'s own doc comment: "a boundary identifier, not a full IAM/organization product") — reused as the "organization" mapping (see Mapping below) rather than inventing a parallel `Organization` type. `Customer.customerId` and `Project.projectId` are reused directly.
- Branch: `claude/V2-CDO-003-task-packet`.

## Goal

Implement the first pure, provider-neutral, machine-consumable domain contract for stable customer/organization/project/service ownership boundaries and customer-safe project-linked communication history/routing semantics required by the minimum `OPS-010` V2 slice. Identity, classification, routing/observation and validation semantics only — no communications runtime, CRM, or persistence layer.

## Scope (Minimum Domain Objects)

- `ProjectOwnershipRef` / `createProjectOwnershipRef` (`src/domain/project-ownership.ts`) — stable reference tuple: `tenantId` (organization mapping), `customerId`, `projectId`, optional `serviceRef`. Reference only; does not create a second customer/project lifecycle.
- `ProjectCommunicationRecord` / `createProjectCommunicationRecord` (`src/domain/project-communication.ts`) — `communicationId`, `ownership`, `direction`, `classification`, `requiredActor`, `observationState`, optional `evidenceRef`/`relatedPlanId`/`relatedPlanVersion`/`relatedJobId`/`relatedArtifactRef`, `timestamp`.
- `CommunicationDirection` — `CUSTOMER_TO_AKILTA` | `AKILTA_TO_CUSTOMER`.
- `CommunicationClassification` — `INFORMATIONAL` | `ACTION_REQUIRED`.
- `RequiredActor` — `NONE` | `CUSTOMER` | `AKILTA`, with deterministic compatibility: `INFORMATIONAL` ⇒ `NONE`; `ACTION_REQUIRED` ⇒ `CUSTOMER` or `AKILTA`.
- `CommunicationObservationState` — `RECORDED_ONLY` | `DELIVERY_UNVERIFIED` | `DELIVERY_VERIFIED` | `DELIVERY_FAILED`; `DELIVERY_VERIFIED` requires a non-empty `evidenceRef`.
- `ProjectCommunicationHistory` / `buildProjectCommunicationHistory` — a validated, ownership-bound collection of records (fail-closed cross-tenant/cross-project binding + duplicate-id rejection).
- One canonical fixture (`src/fixtures/website-build-v1-communication.ts`) reusing the existing `WebsiteBuildV1Fixture`'s project identity (DEL-003) for ownership, with two example communications (one informational/verified, one action-required/unverified).

## Mapping (existing identity reuse, per the task head's explicit instruction)

| Contract field | Value | Source |
|---|---|---|
| `ProjectOwnershipRef.tenantId` ("organization") | reused directly | `TenantScope.tenantId` (`tenant-scope.ts`) — no separate `Organization` type exists in this repository |
| `ProjectOwnershipRef.customerId` | reused directly | `Customer.customerId` (`customer.ts`) |
| `ProjectOwnershipRef.projectId` | reused directly | `Project.projectId` (`project.ts`) |
| `ProjectCommunicationRecord.relatedPlanId` / `relatedPlanVersion` | typed as | `ProjectPlanVersion["planId"]` / `ProjectPlanVersion["version"]` (`project-plan.ts`, `import type` only) — same two-field convention as `OutcomeJobSpec.planId`/`OutcomeJobSpec.planVersion` (`outcome-job-spec.ts`); CR-1 correction below |
| `ProjectCommunicationRecord.relatedJobId` | typed as | `OutcomeJob["jobId"]` (`outcome-job.ts`, `import type` only) |

`ProjectOwnershipRef.serviceRef` is the one genuinely new, optional field — no existing "service" identity exists to reuse. No second customer/project/message lifecycle was created.

## Invariants

- Customer, organization (`tenantId`) and project references are explicit, non-empty stable refs; `serviceRef`, when present, is explicit (empty string rejected — only `undefined` omits it).
- A communication record binds to one customer/organization/project tuple; `relatedPlanId`/`relatedPlanVersion`/`relatedJobId`/`relatedArtifactRef` remain contextual/reference-only and cannot rebind ownership.
- Communication IDs are unique within any validated `ProjectCommunicationHistory`.
- Direction is explicit and provider/channel-neutral — no email/SMS/Slack/vendor implementation is encoded.
- `INFORMATIONAL` requires `requiredActor` `NONE`; `ACTION_REQUIRED` requires `CUSTOMER` or `AKILTA`. No implicit actor is inferred.
- `ACTION_REQUIRED` is attention semantics only — it grants no approval/execution/resume/`OutcomeJob`-transition/customer-binding authority (no such function exists in this module; `project-plan.ts`/`outcome-job.ts` are `import type`-only dependencies, so this module has zero runtime coupling to their transition/admission functions).
- `DELIVERY_VERIFIED` requires an explicit `evidenceRef`; `RECORDED_ONLY`/`DELIVERY_UNVERIFIED` can never be silently promoted — there is no "mark delivered" mutation function; a different observation state requires constructing a new record.
- The customer-safe shape exposes no internal notes, margins/unit economics, prompts, credentials/secrets, or provider payloads.
- Existing `ProjectPlanVersion`/`OutcomeJob` verification, evidence, approval and resume semantics remain authoritative and are not duplicated or bypassed.
- Contract creation/validation has no external effect.

## Round 1 Correction (CR-1) — 22 August 2026

Brain independent disposition on checkpoint `48fc5e63beea09612a6df6ce823882cb6180eaa2`: **CHANGES_REQUIRED — CR-1 ONLY**, same task/branch, no Founder relay required.

- **CR-1 (exact `ProjectPlanVersion` identity)**: closed. `relatedPlanVersionId?: ProjectPlanVersion["planId"]` modeled an optional plan reference by `planId` alone, which can conflate two versions of the same plan — a `ProjectPlanVersion` is identified by `planId` *plus* `version` (see the existing `OutcomeJobSpec.planId`/`OutcomeJobSpec.planVersion` two-field convention in `outcome-job-spec.ts`, independently confirmed against source before applying the fix). Corrected to `relatedPlanId?: ProjectPlanVersion["planId"]` + `relatedPlanVersion?: ProjectPlanVersion["version"]`, with construction-time validation that the two must both be present or both be absent (a partial reference is rejected as not exact). New regression: `tests/project-communication.test.ts` "CR-1: relatedPlanId/relatedPlanVersion together preserve an exact ProjectPlanVersion reference - two versions of the same plan remain distinguishable" plus 3 negative tests (partial reference either direction rejects; non-positive-integer version rejects).
- **Accepted carry-forward** (per Brain's disposition, unaffected by CR-1): ownership tuple validation, direction/classification/required-actor compatibility, delivery-observation evidence requirement, history ownership isolation, duplicate communication rejection, customer-safe/provider-neutral boundary, no `OutcomeJob` transition authority, no `ConnectionBinding`/runtime/persistence/provider implementation, zero-runtime-dependency result.

## Non-Scope (explicitly not implemented this slice)

No CRM/backoffice application; no omnichannel inbox; no email/SMS/WhatsApp/Slack/provider SDK or sending; no queue/event bus/job runner/webhook; no auto-reply; no automatic wait/resume; no customer portal/UI/HTTP endpoint; no new database/persistence/migration; no account/`ConnectionRequirement`/`ConnectionBinding`/`SecretRef` implementation (reserved to `CONN-001`); no billing/invoicing/payment/subscription/licensing/renewal/dunning/e-Fatura/e-Arşiv; no support-ticket runtime; no generic messaging/workflow framework; no AI/model gateway; no AI Commerce repository/schema/runtime/state mutation; no merge/deploy/release/production/publication/provider/customer-binding/legal/financial action.

## Test Coverage (O1–O11)

| Test | Requirement | Covered in |
|---|---|---|
| O1 | Valid minimal ownership/communication contract is deterministic; reuses existing identity via the `WEBSITE_BUILD_v1` fixture | `tests/project-ownership.test.ts`, `tests/project-communication.test.ts` |
| O2 | Empty/missing customer, organization, or project ref rejects | `tests/project-ownership.test.ts` |
| O3 | Invalid direction/classification/required-actor/observation-state value rejects | `tests/project-communication.test.ts` |
| O4 | `INFORMATIONAL`⇒`NONE`; `ACTION_REQUIRED`⇒`CUSTOMER`/`AKILTA` compatibility | `tests/project-communication.test.ts` |
| O5 | `DELIVERY_VERIFIED` requires evidence; other states never implicitly promoted | `tests/project-communication.test.ts` |
| O6 | Mismatched ownership tuple within a bounded history rejects (cross-tenant, cross-project) | `tests/project-communication.test.ts` |
| O7 | Duplicate `communicationId` within one history rejects | `tests/project-communication.test.ts` |
| O8 | No transition/admission/verification capability over `OutcomeJob`/`ProjectPlanVersion` | `tests/project-communication.test.ts` (functional), `tests/v2-cdo-003-boundary-scan.test.ts` (import boundary) |
| O9 | Customer-safe shape has no internal/margin/prompt/credential fields; no provider/channel dependency | `tests/project-communication.test.ts` (field-set), `tests/v2-cdo-003-boundary-scan.test.ts` (scan) |
| O10 | No `ConnectionBinding`/commerce connector/CRM/persistence/HTTP/UI/queue/messaging-provider introduced | `tests/v2-cdo-003-boundary-scan.test.ts` |
| O11 | Strict typecheck + full regression + applicable scans pass; zero new dependency | Evidence below |

## Evidence (this checkpoint)

- `npm run test`: **286/286 pass** (282 from the prior checkpoint + 4 new CR-1 regressions), 0 fail.
- `npx tsc -p tsconfig.json --noEmit`: pass, strict mode, zero errors.
- Secret / commerce-provider / model-provider / workflow-engine / messaging-provider scan across the 3 domain/fixture files: 0 matches.
- `package.json`: zero runtime `dependencies`; `devDependencies` unchanged (`@types/node`, `typescript`) — no new dependency introduced.
- Files touched this round: `src/domain/project-communication.ts` (CR-1 fix), `tests/project-communication.test.ts` (4 new CR-1 tests), this exec-plan. `src/domain/project-ownership.ts`, `src/fixtures/website-build-v1-communication.ts`, `tests/project-ownership.test.ts`, `tests/v2-cdo-003-boundary-scan.test.ts` unchanged from the prior checkpoint.

## Status

`IMPLEMENTED / SELF-VALIDATED / PENDING BRAIN RE-AUDIT (CR-1)`. Not self-declared VERIFIED, COMPLETED, Commercial Ready, or GA. No merge, no deploy, no release, no frontend/UI built, no AI/model routing, no external provider/connector, no AI Commerce mutation, no messaging/CRM runtime, no persistence.

## Evidence Delivered

- Prior checkpoint (superseded on CR-1 grounds): "V2-CDO-003 — SHA-Bound Transferable Evidence Package — 48fc5e6 — 2026-08-22", Drive `1Aeujlxm02gy6fRWXW--IgcidxEb2bxl7`.
- This checkpoint's CR-1 evidence: "V2-CDO-003 — CR-1 SHA-Bound Evidence Package — b9430b7 — 2026-08-22", Drive `1yEPCy8xjK1LeaB9Fw7p-jXfh3AyzGn2F` — CR-1-only diff, raw full `npm run test` transcript (286/286 pass, `NPM_TEST_EXIT_CODE: 0`), raw strict `tsc --noEmit` output (`TSC_EXIT_CODE: 0`), the specific revalidation regression Brain's disposition required, dependency delta. Delivered per the canonical task head's `CHECKPOINT EVIDENCE`/`REVALIDATION` instruction (Drive `1IKVfUyuQrpVNWglIcJVAbSG46ZVwjjIr9KsMWgq-dug`).

## Next Exact Action

Await Brain re-audit of this CR-1 checkpoint on the canonical task head. Do not self-declare PASS.
