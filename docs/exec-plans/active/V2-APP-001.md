# V2-APP-001 — Minimum First-Party Logged-in Application Shell + Provider-Neutral Auth/Session/Tenant Bootstrap — DEC-158 Train Head (Segment 1 of 2)

## Provenance

- Governing authority: DEC-158 (authorizing this task as the new sole `CURRENT_ENGINEERING_SELECTOR`/train head after V2-CDO-006's own preauthorized edge-only stop), applying the DEC-157 continuous-train execution model (one consolidated Brain audit at the train boundary, not a stop at each intermediate segment).
- Start-authorized because the prior train (`V2-CDO-005` self-validated, `V2-CDO-006` correctly edge-stopped — "no suitable logged-in shell/auth/session/tenant boundary exists") received Brain **PASS/VERIFIED/CLOSED** at `b5e5afb255e5e0134cca8f3f17cb20fa843d4c4e`, with V2-CDO-006 explicitly kept queued as the conditional continuation once that predicate is satisfied. See `docs/exec-plans/active/V2-CDO-005.md`.
- Canonical task head: "V2-APP-001 — minimum first-party logged-in application shell + provider-neutral auth/session/tenant bootstrap", Drive `1gjK-R58WPtUDBJPZY7x1NhivV1nkYlr9WV0qKEFb1p0`. Scope/invariants/acceptance contract (A1–A16) transcribed from that canonical source, not invented.
- `BASE_PROVENANCE_SHA`: `b5e5afb255e5e0134cca8f3f17cb20fa843d4c4e` (verified V2-CDO-005/006 train checkpoint). Branch cut from `claude/V2-CDO-005-task-packet` at commit `1fe4d09faa353e31a0154b293c87b5b787fc6be1` — confirmed `b5e5afb2` is an ancestor via `git merge-base --is-ancestor`.
- Pre-execution repo preflight: `package.json` declared zero runtime dependencies and exactly two devDependencies (`@types/node`, `typescript`); repository-wide search for `.tsx`/`.jsx`/`.html`/`.vue`/`.svelte` found zero matches; `src/` contained only `domain/`, `fixtures/`, `application/`, `ports/` — no HTTP/frontend/auth/session framework anywhere in the dependency tree or source tree. Node v22.22.2 confirmed (built-in `fetch`/`http`).
- Framework/placement selection (delegated engineering authority, task head IN SCOPE preamble): recorded in `docs/architecture/ADR/0001-v2-app-001-web-shell-framework.md` — Node's built-in `node:http` plus a small hand-written, transport-agnostic request-routing layer in `src/web/`, with **zero new runtime dependencies**. Alternatives (Express, Fastify, Hono, Next.js) considered and rejected in the ADR.
- Branch: `claude/akilta-continuity-readback-5hm3ed` (harness-designated branch for this session; carries forward the identical work originally built on `claude/V2-APP-001-task-packet`, confirmed by ancestry that the prior designated-branch pointer was itself an ancestor of this work before being fast-forwarded).

## Goal

Implement the minimum first-party, server-capable TypeScript web-application shell with a provider-neutral `AuthenticatedPrincipal`/`SessionContext`/`TenantContext` model, fail-closed route admission, and tenant/customer/project scope enforcement derived only from a trusted session — with no external identity provider, no persistence, and no production deployment — so that V2-CDO-006's own edge-stop predicate (an existing logged-in application shell / auth / session / tenant boundary) can be re-evaluated truthfully.

## Scope (Minimum Bootstrap, `src/web/`)

- `session-context.ts` — `AuthenticatedPrincipal`, `TenantContext`, `SessionContext`; `createAuthenticatedPrincipal`/`createSessionContext` factories. `TenantContext` is always derived from the given `principal`'s own `tenantId`/`customerId` — never independently caller-supplied.
- `session-provider.ts` — `SessionProvider` port (`resolveSession(token) -> SessionContext | undefined`).
- `production-session-provider.ts` — `createProductionSessionProvider()`: unconditionally returns `undefined` for any token. No real IdP integration exists to back anything else (A8).
- `dev-fixture-session-provider.ts` — `createDevFixtureSessionProvider({ fixtures, isProduction })`: throws `DevFixtureSessionProviderInProductionError` at **construction time** (not per-request) if `isProduction` is true; otherwise a deterministic in-memory token → `SessionContext` lookup (A7).
- `route-guard.ts` — `requireSession` (fail-closed on missing/invalid token, `UnauthenticatedError`), `requireTenantOwnership` (field-by-field tenant/customer/project match against the session's own scope, `TenantScopeMismatchError` on any mismatch, including a session-side `projectId` that is absent or different) (A4/A5).
- `snapshot-view-state.ts` — `ClientProjectSnapshotSource` port and `resolveProtectedSnapshotView`: checks tenant ownership **before** ever calling the snapshot source; maps to `FORBIDDEN_TENANT_SCOPE` / `NOT_FOUND` / `ERROR` / `READY` (A9/A10).
- `shell-render.ts` — `ShellPageContent` discriminated union (`LOADING`/`EMPTY`/`UNAVAILABLE`/`UNSUPPORTED`/`BLOCKED`/`ERROR`/`NOT_FOUND`/`UNAUTHENTICATED`/`FORBIDDEN_TENANT_SCOPE`/`READY`) and `renderShellPage`: one shared accessible HTML layout (skip link, semantic landmarks, viewport meta, `:focus-visible` outline, responsive breakpoint, always-visible text status label alongside a tone class) (A11/A12).
- `request-handler.ts` — `createRequestHandler`: pure `IncomingRequestLike -> OutgoingResponseLike` function with zero `node:http` dependency; rejects non-GET as `UNSUPPORTED`, parses `/portal/:tenantId/:customerId/:projectId`, enforces session/tenant checks, maps delivery status to the correct shell state.
- `http-server.ts` — `createHttpServer`: the only file importing `node:http`; translates a real socket request/response to/from the plain shapes and selects the fail-closed production provider whenever `isProduction` is true.
- `src/fixtures/web-shell.ts` — one deterministic dev-only session fixture scoped to the existing `WEBSITE_BUILD_v1` ownership tuple, and one fixture `ClientProjectSnapshotSource` backed by the existing verified `WEBSITE_BUILD_V1_CLIENT_PROJECT_SNAPSHOT` (V2-CDO-005) — no new domain identity invented.

## Mapping (existing identity/logic reuse)

| Contract element | Reused from | File |
|---|---|---|
| Customer-safe projection consumed by the shell | `ClientProjectSnapshot` (verbatim, no bypass) | `src/domain/client-project-snapshot.ts` (V2-CDO-005) |
| Ownership tuple | `ProjectOwnershipRef` (reused directly) | `src/domain/project-ownership.ts` (V2-CDO-003) |
| Reference fixture identity | `WEBSITE_BUILD_V1_OWNERSHIP` / `WEBSITE_BUILD_V1_CLIENT_PROJECT_SNAPSHOT` (reused directly, not re-derived) | `src/fixtures/website-build-v1-communication.ts`, `src/fixtures/website-build-v1-snapshot.ts` |

No second domain/customer/project lifecycle was created; `AuthenticatedPrincipal`/`SessionContext`/`TenantContext` are the only genuinely new identities, and they are explicit, provider-neutral, and framework-owned by nothing but this repository's own code.

## Architecture Invariants (per task head)

Web framework is an adapter, not a new domain authority; no external provider SDK/OAuth/token exchange/hosted auth product selected; no persistence/database/data-plane activation; `ClientProjectSnapshot` remains the sole customer-safe read boundary; visibility grants no approval/resume/verify/execute/mutate authority; framework/package choice is replaceable/portable with no hard-coded cloud/PaaS deployment; dev/test fixture auth is mechanically excluded from production; public-site (Shopify) and logged-in-app release gates remain independent.

## Non-Scope (explicitly not implemented this slice)

No external IdP/OAuth/OIDC integration or connection; no real credentials/secrets; no database/ORM/migration/durable session store; no cloud/PaaS/hosting/provider selection; no DNS/hostname change; no production deploy/merge/release/publication/Commercial-Ready/GA claim; no customer invitation/live account creation; no approval/input command endpoint; no billing/payment/subscription/support/CRM/backoffice build; no AI Project Advisor/Delivery Recipe breadth beyond already-verified contracts; no provider-specific commerce runtime; no AI Commerce project mutation.

## Acceptance Mapping (A1–A16)

| # | Requirement | Covered in |
|---|---|---|
| A1 | Repo preflight/lineage recorded | Provenance section above |
| A2 | Repo-local ADR justifying framework choice, no second web framework | `docs/architecture/ADR/0001-v2-app-001-web-shell-framework.md` |
| A3 | Shell compiles/builds/starts/renders locally without a production external provider | `tests/web-http-server.test.ts` (real `server.listen(0)` + built-in `fetch`) |
| A4 | Fail-closed protected-route admission | `tests/web-route-guard.test.ts`, `tests/web-request-handler.test.ts` |
| A5 | Tenant/customer/project mismatch rejects; caller-supplied identifiers cannot widen scope | `tests/web-route-guard.test.ts`, `tests/web-request-handler.test.ts` |
| A6 | Explicit/testable Principal/Session/Tenant context, not framework-owned | `tests/web-session-context.test.ts` |
| A7 | Dev/test identity adaptation mechanically excluded/fail-closed in production | `tests/web-session-provider.test.ts`, `tests/web-request-handler.test.ts` (representative production-mode test), `tests/web-http-server.test.ts` |
| A8 | Production mode with no real provider fails closed truthfully | `tests/web-session-provider.test.ts`, `tests/web-http-server.test.ts` |
| A9 | Server adapter consumes `ClientProjectSnapshot` without bypass | `tests/web-snapshot-view-state.test.ts`, `tests/web-request-handler.test.ts` |
| A10 | Tenant/project mismatch fails before snapshot rendering; no secret/internal field ever appears | `tests/web-snapshot-view-state.test.ts` (`callCount` spy) |
| A11 | Deterministic loading/empty/error/unavailable/not-found states, never masquerading as success | `tests/web-shell-render.test.ts` |
| A12 | Minimum responsive/accessibility checks | `tests/web-shell-render.test.ts` |
| A13 | Strict TypeScript + lint/build/tests/regression pass, no new regressions | Evidence below |
| A14 | Dependency delta explicit/justified — zero new runtime dependency | `tests/v2-app-001-boundary-scan.test.ts`, Evidence below |
| A15 | Bounded scans: no secret material/provider coupling/fixture-to-production path/private-record bypass/cross-tenant leakage | `tests/v2-app-001-boundary-scan.test.ts` |
| A16 | No persistence/deployment/DNS/customer-invite/payment/AI-Commerce/protected external effect | Non-Scope section; no such code exists anywhere in this diff |

## Evidence (this checkpoint)

- `npm run test`: **398/398 pass** (354 from the prior checkpoint + 44 new: 5 `web-session-context.test.ts` + 4 `web-session-provider.test.ts` + 8 `web-route-guard.test.ts` + 4 `web-snapshot-view-state.test.ts` + 7 `web-shell-render.test.ts` + 7 `web-request-handler.test.ts` + 3 `web-http-server.test.ts` + 6 `v2-app-001-boundary-scan.test.ts`), 0 fail, `NPM_TEST_EXIT_CODE: 0`.
- `npx tsc -p tsconfig.json --noEmit`: pass, strict mode (including `exactOptionalPropertyTypes`), zero errors, `TSC_EXIT_CODE: 0`.
- Secret / commerce-provider / identity-provider(OAuth-OIDC/Auth0/Clerk/Supabase/etc.) / HTTP-UI-framework / persistence-ORM / cloud-SDK / model-provider scan across all 10 new `src/web/*.ts` + `src/fixtures/web-shell.ts` files: 0 matches.
- Import-boundary scan: `production-session-provider.ts` never imports the dev-fixture module; only `http-server.ts` imports `node:http`; `request-handler.ts`/`snapshot-view-state.ts` never import `OutcomeJob`/`ProjectPlanVersion`/`ConnectionBinding` domain modules directly.
- `package.json`: zero runtime `dependencies`; `devDependencies` unchanged (`@types/node`, `typescript` only) — no new dependency introduced.
- One false-positive scan match was found and fixed in-flight: a doc comment in `session-provider.ts` literally named prohibited third-party identity vendors (Auth0/Clerk/Supabase) while documenting the Hard Non-Scope list; reworded to remove the vendor names (the ADR and this exec-plan already carry that list) rather than weakening the scan.
- Files touched this round: `docs/architecture/ADR/0001-v2-app-001-web-shell-framework.md` (new), `docs/architecture/ADR/README.md` (index update), `src/web/session-context.ts`, `src/web/session-provider.ts`, `src/web/production-session-provider.ts`, `src/web/dev-fixture-session-provider.ts`, `src/web/route-guard.ts`, `src/web/snapshot-view-state.ts`, `src/web/shell-render.ts`, `src/web/request-handler.ts`, `src/web/http-server.ts` (all new), `src/fixtures/web-shell.ts` (new), `tests/web-session-context.test.ts`, `tests/web-session-provider.test.ts`, `tests/web-route-guard.test.ts`, `tests/web-snapshot-view-state.test.ts`, `tests/web-shell-render.test.ts`, `tests/web-request-handler.test.ts`, `tests/web-http-server.test.ts`, `tests/v2-app-001-boundary-scan.test.ts` (all new), this exec-plan.

## Status

`IMPLEMENTED / SELF-VALIDATED` at checkpoint `fd6aa89c190658978ae09d3e3ffc6950ff76f550`. Per DEC-157/158, this is an **intermediate train state, not a Brain/conversation stop** — the V2-CDO-006 live re-preflight against this newly built shell/session/tenant boundary follows immediately below.

---

# V2-CDO-006 — Minimum Logged-in Client Portal UI Slice — DEC-157/158 Train Segment 2 of 2

## R3 Live Preflight (performed immediately after V2-APP-001 self-validation, per the DEC-158 reactivation-path continuation rule)

Per the canonical V2-CDO-006 task head's own ACTIVATION PRECONDITIONS #3/#4 and the DEC-158 `START_TRIGGER` ("a fresh live-repository preflight against this packet ... application shell, routing/server boundary, provider-neutral authenticated session/principal/tenant boundary, tenant-isolation enforcement and V2-CDO-005 ClientProjectSnapshot consumption boundary"), live-read this repository at checkpoint `fd6aa89c190658978ae09d3e3ffc6950ff76f550`:

- Application shell + routing/server boundary: `src/web/http-server.ts` (the sole `node:http` adapter) + `src/web/request-handler.ts` (routes `/portal/:tenantId/:customerId/:projectId`). **Present.**
- Provider-neutral authenticated session/principal/tenant boundary: `src/web/session-context.ts` (`AuthenticatedPrincipal`/`SessionContext`/`TenantContext`), `src/web/session-provider.ts`. **Present.**
- Tenant-isolation enforcement, fail-closed: `src/web/route-guard.ts` (`requireSession`, `requireTenantOwnership`). **Present.**
- V2-CDO-005 `ClientProjectSnapshot` consumption boundary, no bypass: `src/web/snapshot-view-state.ts`. **Present.**

**Result: predicate satisfied.** Per the DEC-158 reactivation-path `START_TRIGGER`/`CONTINUATION` rule, this segment starts immediately, without a Brain/Founder relay at this boundary.

## Goal

Implement the minimum responsive/accessibility-safe Client Portal view that consumes the verified `ClientProjectSnapshot` and lets a customer answer the task head's five questions ("what is happening now", "what's completed and verified", "what happens next", "do I need to do anything", "is there a safe artifact/evidence/update I can inspect") — reusing the V2-APP-001 shell rather than inventing a second frontend, with no interactive approval/input mutation and no external provider/persistence/deployment.

## Scope (this segment)

Expanded `src/web/shell-render.ts` from V2-APP-001's minimal placeholder body into the task head's minimum IA, all driven only by fields already on `ClientProjectSnapshot` (V2-CDO-005):

- Project identity header (customerId/projectId) + overall delivery status.
- Next Action / Needs Attention section: a distinct, always-visible text badge per `NextActionOwner` (`NO_ACTION_NEEDED`/`CLIENT_ACTION_REQUIRED`/`AKILTA_ACTION_REQUIRED`/`EXTERNAL_WAIT`) — never color alone.
- Blocker card (rendered only on the `BLOCKED` page state): sourced only from the customer-safe `DeliveryTimeline`'s `BLOCKER`-category entries (timestamp only) — never a raw internal `reason` string, which this layer never receives.
- Timeline summary: every customer-safe `DeliveryTimeline` entry (category + timestamp), or an explicit "No updates recorded yet."
- Working Artifact section (only when `workingArtifact` is present): current version, an `Approved`/`Preview — not yet approved` label driven exactly by `isCurrentVersionApproved`, and a staleness notice when `lastApprovedVersion` differs from `currentVersion`.
- Connections & capabilities section (only when non-empty): `requiredCapabilityRef` + a human status label — never `connectionBindingId`/`evidenceRef`/provider detail.
- Recent updates section (only when non-empty): `classification`/`requiredActor`/`timestamp` + an opaque `evidenceRef`/`relatedArtifactRef` when present — no message body field exists on `ProjectCommunicationRecord` to leak.
- Verified-completed-work count (carried over from V2-APP-001).
- `request-handler.ts`'s `BLOCKED` branch now passes the underlying `snapshot` through (previously a bare marker) so the blocker card and the rest of the project view render together.

No new file, no new domain type, no new dependency, no interactive/approve/submit control, no second frontend framework.

## UX / Claim Rules Compliance

- The next-action badge and working-artifact approval label are structurally driven by the snapshot's own fields — this layer cannot fabricate `NO_ACTION_NEEDED`, `Approved`, or a percentage/ETA (there is none to render; `eta` stays the untouched `UNKNOWN` literal from V2-CDO-005).
- Loading/empty/unavailable/unsupported/blocked/error/not-found/unauthenticated/forbidden all remain first-class, structurally distinct `ShellPageContent` kinds (unchanged from V2-APP-001) — none can render the project body.
- No approve/submit control exists anywhere in this render layer; approval status is read-only text.
- Internal notes, prompts, margins, secrets, and raw internal exception reasons are structurally unreachable — this layer's only inputs are `ClientProjectSnapshot` fields, all already proven customer-safe by V2-CDO-005's own P3/P9/O9 tests.

## Non-Scope (explicitly not implemented this segment)

No new auth provider/session architecture; no backend/domain lifecycle change; no approval/input/feedback mutation or command endpoint; no persistence/database/migration; no payment/billing; no provider OAuth/connect runtime; no AI Commerce connector/runtime; no generic dashboard framework; no public Shopify website change; no production publication; no customer invitation/send; no release/GA claim; no i18n framework (repo has none to reuse — TR/EN coverage is N/A at this bootstrap).

## Acceptance Mapping (U1–U20)

| # | Requirement | Covered in |
|---|---|---|
| U1 | Keyboard-reachable controls, visible focus | `tests/web-shell-render.test.ts` (`:focus-visible` rule on every page) |
| U2 | Semantic headings/landmarks/status messaging; action-required not color-only | `tests/web-shell-render.test.ts` |
| U3 | Desktop/mobile layouts avoid horizontal overflow/clipped content | Rendered evidence (desktop 1280px / mobile 390px screenshots), responsive `@media` rule (carried from V2-APP-001) |
| U4 | Long labels/refs wrap safely | Rendered mobile screenshot (long `evidenceRef` string wraps without overflow) |
| U5 | Loading/empty/error/unsupported/blocked states deterministic, never masquerade as success | `tests/web-shell-render.test.ts`, `tests/web-request-handler.test.ts` (EMPTY/BLOCKED via a real handler) |
| U6 | Action-required/approval-required status has clear labels; no interactive approve control | `tests/web-shell-render.test.ts`, `tests/web-request-handler.test.ts`; no approve/submit markup exists anywhere in `shell-render.ts` |
| U7 | No console/runtime errors on representative rendered checks | Playwright-rendered evidence: zero `pageerror` events on any captured route; the only `console` entries are the browser's own non-2xx top-level navigation advisories on the intentionally 401/403 routes, not an application defect |
| U8 | Localization follows existing repo conventions | N/A — repository has no i18n framework to reuse; not invented here |
| U9 | UI renders only the customer-safe projection; no private-record bypass | `tests/v2-app-001-boundary-scan.test.ts` (import-boundary scan), carried-over A9 tests |
| U10 | Tenant/project mismatch fails closed, cannot render another customer/project | `tests/web-route-guard.test.ts`, `tests/web-request-handler.test.ts`, rendered `desktop-forbidden` evidence |
| U11 | Secret/prompt/internal-margin fields cannot render through any path, including error/empty | `tests/v2-app-001-boundary-scan.test.ts`, `tests/web-shell-render.test.ts` (blocker card omits the raw internal reason) |
| U12 | Fixture/mock markers unreachable through the production path | `tests/web-session-provider.test.ts`, `tests/web-http-server.test.ts` (production-mode server test) |
| U13 | Preview/working artifact cannot render as verified/done when unverified | `tests/web-shell-render.test.ts` |
| U14 | Approval-required UI references exact artifact/version identity; no silent stale-approval reuse | `tests/web-shell-render.test.ts` (staleness notice), reuses V2-CDO-005's `isApprovalValidForPlan` verbatim |
| U15 | Connection/capability rendering cannot widen authority or expose SecretRef material | `tests/web-shell-render.test.ts`, `tests/v2-app-001-boundary-scan.test.ts` |
| U16 | Reuse existing stack/conventions; dependency delta 0 | `tests/v2-app-001-boundary-scan.test.ts` — zero new runtime dependency |
| U17 | Focused component/view tests for state/action/preview/approval/error/tenant-safe cases | `tests/web-shell-render.test.ts`, `tests/web-request-handler.test.ts` |
| U18 | Typecheck/build/test suite passes | Evidence below |
| U19 | Rendered evidence for representative desktop/mobile states | Playwright screenshots: `desktop-ready`, `mobile-ready`, `desktop-unauthenticated`, `mobile-unauthenticated`, `desktop-forbidden` (see evidence package) |
| U20 | No merge/deploy/release/publication/production activation | No such code exists anywhere in this diff; not performed |

## Evidence (this checkpoint)

- `npm run test`: **404/404 pass** (398 from the V2-APP-001 checkpoint + 6 net new: `web-shell-render.test.ts` grew from 7 to 11 tests as it was rewritten for the expanded IA (+4), `web-request-handler.test.ts` grew from 7 to 9 (+2)), 0 fail, `NPM_TEST_EXIT_CODE: 0`.
- `npx tsc -p tsconfig.json --noEmit`: pass, strict mode, zero errors, `TSC_EXIT_CODE: 0`.
- `package.json`: zero runtime `dependencies`; `devDependencies` unchanged — no new dependency introduced (the Playwright rendering pass below used the environment's pre-installed, out-of-repo Playwright/Chromium purely as an evidence-capture tool; it is not a project dependency and nothing in `src/`/`package.json` references it).
- Rendered evidence: a real `createHttpServer` instance (`isProduction: false`, dev fixture, `WEBSITE_BUILD_V1_CLIENT_PROJECT_SNAPSHOT` source) was started on an ephemeral port and driven with Chromium at 1280×900 (desktop) and 390×844 (mobile) viewports for the `READY` state, plus 1280/390 for `UNAUTHENTICATED` and 1280 for `FORBIDDEN_TENANT_SCOPE` (via a cross-tenant path). Zero `pageerror` events on any route; the only console entries were the browser's own non-2xx top-level-navigation advisories on the 401/403 routes. No horizontal overflow at either width; the long `evidenceRef` string wraps correctly on mobile.
- Files touched this round: `src/web/shell-render.ts` (expanded), `src/web/request-handler.ts` (`BLOCKED` now carries `snapshot`), `tests/web-shell-render.test.ts` (rewritten for the expanded IA), `tests/web-request-handler.test.ts` (added EMPTY/BLOCKED end-to-end cases), `tests/web-http-server.test.ts` (assertion text updated for the renamed status heading), this exec-plan.

## Status

`IMPLEMENTED / SELF-VALIDATED`. This is the **train-end checkpoint** for `V2-APP-001_TO_V2-CDO-006` per DEC-157/158 — normal consolidated Brain audit is requested now. No merge, no deploy, no release, no external provider/connector, no persistence, no AI Commerce mutation, no customer activation.

## Next Exact Action

Await Brain audit of this consolidated train checkpoint (V2-APP-001 + V2-CDO-006) on the canonical V2-APP-001/V2-CDO-006 task heads. Do not self-declare VERIFIED/PASS/CLOSED. Do not start further frontend/backend breadth (e.g. the V3 lookahead, remaining WEB-002 breadth, or CXP-001/AI-004 recipe/advisor surfaces) absent a new Founder-issued or canonical-doc-issued selector.
