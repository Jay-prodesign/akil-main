# V5-PTN-001 — Partner Capability Claim Admission (Governed Non-Self-Certification Floor)

## Provenance

- Governing authority: DEC-160 V2→V5 Autonomous Engineering Corridor; V5 Full Blueprint (Drive `1HfQ8Mzbos63LZPE_vZryd4TiLyfa8GFP_AdtmLR-el8`) §12 Workstream H (Partner Network Operations), spine item I (§4).
- **Selection reasoning**: this is a self-paced continuation of the standing post-merge dependency-scan authorization, triggered by a scheduled corridor check-in after PR #18 (`V5-INT-001`) merged and PR #20 (`V5-WRK-001`) was left awaiting fresh Brain review at its Rev52-corrected exact head. A fresh scan of remaining V5 workstreams found: Workstream A (finding recorded, pending Brain adjudication), C, and F all landed; B (`V5-WRK-001`) submitted and awaiting review; E (Infrastructure/Provider/DNS) blocked on a real external effect/provider that does not exist; G (Product Lab) and I (Governed Evaluation Loop) both require real validated-outcome evidence history that does not exist yet; J (Unified Command Frontend) needs more backend substrate first. Workstream H (Partner Network Operations, §12) was re-read in full against the already-merged `V3-PTR-001` (`src/domain/partner-organization.ts`).
- Direct inspection of `partner-organization.ts` found four of §12's five acceptance directions already satisfied by that module: "partner cannot access unrelated client/project" and "revoked assignment removes access" (both proven by `resolvePartnerClientAccess`'s fail-closed exact-ownership-match/status logic), "confidential internal data remains restricted" (a presentation-layer concern deferred to a future Workstream F-style slice, not a domain gap), and "no implicit authority to bind AKILTA/customer" (`relationshipType` is representable-only, never read for any authority decision). The one genuine, unaddressed gap: **"partner capability claims require evidence/admission and may expire/review"** and the matching acceptance bullet **"capability claim cannot self-certify"** — no such concept exists anywhere in `src/`. This mirrors the exact "extend the acceptance criteria into a bounded floor" reasoning already used for `V5-INT-001`, `V5-BOOT-001`, and `V5-WRK-001`.
- Branch: `claude/v5-ptn-001-partner-capability-admission`, cut fresh from merged `main` after PR #18, later rebased onto post-PR#20 `main` (see "Rebased onto post-PR#20 main" below).
- `BASE_PROVENANCE_SHA` (original, now stale): `839aca0f6dd830c46dcf4576e46035c3fdbd9207` (merged main, PR #18).
- `BASE_PROVENANCE_SHA` (current, post-rebase): `f03c917e66283fcd63736234d81b166465496d7c` (merged main, PR #20).

## Goal

Provide a pure, side-effect-free capability-claim lifecycle for partner organizations in which a claim can never be self-certified into an admitted state, admission always requires evidence, and admission is always subject to a caller-supplied review date rather than standing indefinitely.

## Scope (this checkpoint)

- `src/domain/partner-capability-admission.ts`:
  - `PartnerCapabilityClaim` — scoped to an existing `PartnerOrganization` (imported type-only from `partner-organization.ts`, not duplicated), carrying an opaque `capabilityRef`, a `PartnerCapabilityClaimStatus` (`UNVERIFIED` | `ADMITTED` | `EXPIRED` | `REVOKED`), and admission provenance (`evidenceRef`/`admittedAt`/`reviewByAt`) present only once admitted.
  - `createPartnerCapabilityClaim(input)` — the only construction function; accepts no status or evidence field, so every claim is born `UNVERIFIED`.
  - `admitPartnerCapabilityClaim(input)` — the only function that can produce `ADMITTED`; requires a non-empty `evidenceRef`, a `reviewByAt` strictly after `admittedAt`, and (Rev55 F1 correction) a non-empty `admittingAuthorityId` that is fail-closed rejected if it equals the claim's own `partnerOrganizationId` — making "capability claim cannot self-certify" a structural guarantee rather than caller convention. The accepted authority is persisted on the claim as `admittedByAuthorityId` for audit/provenance. Rejects any claim not currently `UNVERIFIED`.
  - `revokePartnerCapabilityClaim(input)` — transitions `ADMITTED` → `REVOKED`; rejects a claim that was never admitted or is already revoked.
  - `resolvePartnerCapabilityClaimStatus(input)` — resolves the claim's effective status as of a caller-supplied `asOf`; an `ADMITTED` claim whose `reviewByAt` has passed resolves `EXPIRED` (computed, never stored); `UNVERIFIED`/`REVOKED` pass through unchanged.

## Explicitly deferred (not invented)

No filesystem/network/child_process coupling, no persistence layer, no system-clock read anywhere (the module has exactly one type-only import, `PartnerOrganization`, and every status resolution is bound to a caller-supplied `asOf`, never `Date.now()`). No capability catalog or capability-taxonomy validation (`capabilityRef` is an opaque pointer, exactly like `sourceRef`/`capabilityRef` conventions elsewhere in `src/domain/`). No wiring of claim status into `resolvePartnerClientAccess` or any client-assignment gate — §12 does not require capability claims to gate client access, only to exist as a governed, non-self-certifiable lifecycle; conflating the two would be inventing scope. No commercial/legal relationship modeling (§12: "commercial/legal relationship state is separate from technical eligibility" — untouched, consistent with `PartnerRelationshipType`'s existing representable-only discipline). No partner routing/reason/fallback recording (§12's last acceptance bullet) — that is a distinct, larger routing-decision concern (structurally similar to `V5-WRK-001`'s worker routing, but for partners) intentionally left for a separate future checkpoint rather than bundled in here.

## Architecture / semantic invariants (verified by test)

- A freshly created claim is always `UNVERIFIED` — there is no construction path, and no way to pass extra fields through `createPartnerCapabilityClaim`, that can produce any other status.
- Only `admitPartnerCapabilityClaim` can produce `ADMITTED`, and only when given a non-empty `evidenceRef`, a `reviewByAt` strictly after `admittedAt`, and a non-empty `admittingAuthorityId` distinct from the claim's own `partnerOrganizationId`; it rejects a claim that is not currently `UNVERIFIED`.
- Self-admission is a structural, fail-closed rejection: an `admittingAuthorityId` equal to the claimant's own `partnerOrganizationId` is refused outright regardless of what evidence/dates accompany it (Rev55 F1 correction).
- Only `revokePartnerCapabilityClaim` can produce `REVOKED`, and only from `ADMITTED`; a never-admitted or already-revoked claim rejects.
- `EXPIRED` is never a stored value — it is a `resolvePartnerCapabilityClaimStatus` computation relative to the caller-supplied `asOf`, so the same stored claim can honestly resolve `ADMITTED` at one `asOf` and `EXPIRED` at a later one with no mutation.
- `REVOKED` and `UNVERIFIED` pass through `resolvePartnerCapabilityClaimStatus` unchanged regardless of `asOf` — expiry only ever applies to an `ADMITTED` claim.

## Hard Non-Scope

No filesystem/network/child_process import (verified by boundary scan — exactly one type-only import, `PartnerOrganization`); no provider/model hard-coding; no secret/credential material; no system-clock read (verified by boundary scan); no capability-catalog validation or taxonomy; no wiring into client-access resolution; no commercial/legal/payment modeling; no merge/deploy/release/production/publication/customer-binding action anywhere in this checkpoint's source.

## Test Coverage

| Test | §12 acceptance direction | Covered in |
|---|---|---|
| Freshly created claim is always UNVERIFIED | governed lifecycle floor | `tests/partner-capability-admission.test.ts` H1 |
| Construction accepts no status/evidence field, even if smuggled | "capability claim cannot self-certify" | H2 |
| Valid admission with evidence + reviewByAt transitions to ADMITTED | "partner capability claims require evidence/admission" | H3 |
| Empty evidenceRef rejected | evidence required | H4 |
| reviewByAt at/before admittedAt rejected | "may expire/review" requires a genuine forward-looking review date | H5 |
| Re-admitting an already-ADMITTED claim rejected | no re-processing a decided claim | H6 |
| Revoke transitions ADMITTED → REVOKED | "revoked assignment removes access deterministically" (same discipline) | H7 |
| Revoking a never-admitted (UNVERIFIED) claim rejected | revocation requires a genuine prior admission | H8 |
| Revoking an already-REVOKED claim rejected | terminal state, no re-processing | H9 |
| Status resolution passes UNVERIFIED through unchanged | non-time-relative statuses stay stable | H10 |
| ADMITTED resolves ADMITTED before reviewByAt | correct pre-expiry resolution | H11 |
| ADMITTED resolves EXPIRED after reviewByAt, no stored mutation | "may expire" | H12 |
| Expiry boundary: asOf exactly at reviewByAt resolves EXPIRED | boundary correctness | H13 |
| REVOKED passes through unchanged regardless of asOf | revocation is not time-relative | H14 |
| Invalid timestamp fails closed | fail-closed input validation | H15 |
| Empty id/capabilityRef fails closed at construction | fail-closed input validation | H16 |
| Self-admission (admittingAuthorityId equals claimant's own partnerOrganizationId) fails closed | "capability claim cannot self-certify" (Rev55 F1 correction) | H17 |
| Empty admittingAuthorityId rejected | fail-closed input validation (Rev55 F1 correction) | H18 |
| Admission by a distinct authority succeeds and admittedByAuthorityId is persisted, distinct from partnerOrganizationId | independent, auditable admission provenance (Rev55 F1 correction) | H19 |
| No secret material / provider-model hard-coding | general hygiene | `tests/v5-ptn-001-boundary-scan.test.ts` |
| Exactly one type-only import, no filesystem/network/child_process | pure lifecycle function, no side effect | boundary-scan |
| No `Date.now()` call | deterministic, replayable from input alone | boundary-scan |
| Exactly the four expected exported functions construct/mutate a claim | no hidden self-certification path | boundary-scan |
| Module exports exactly the expected surface | no execution/persistence/clock-reading function | boundary-scan |
| Zero new runtime dependency | dependency delta disclosed | boundary-scan |

## Rebased onto post-PR#20 main (Founder-directed, no owner-gate action)

- After PR #20 (`V5-WRK-001`) merged, this checkpoint's original exact-head evidence (bound to base `839aca0f6dd830c46dcf4576e46035c3fdbd9207`) was stale — Brain could no longer treat it as a live exact-head review target once `main` advanced to `f03c917e66283fcd63736234d81b166465496d7c`. The Founder explicitly directed continuing on this same PR #21 / branch (no new PR or branch), rebasing onto current `main`, resolving any conflicts without scope expansion, re-running the full test/regression suite, and producing a fresh exact-head evidence package bound to the new base/head. `FOUNDER_ACTION / OWNER_GATE: NONE` for this step — a bounded rebase/re-evidence correction, not a merge decision.
- `git rebase origin/main` on `claude/v5-ptn-001-partner-capability-admission` (onto `f03c917e66283fcd63736234d81b166465496d7c`) produced conflicts only in the three corridor documentation files (`docs/engineering/CURRENT_STATE.md`, `docs/exec-plans/corridors/V2_TO_V5.md`, `docs/architecture/VERSION_EVOLUTION_MAP.md`) — every source and test file in this checkpoint (`src/domain/partner-capability-admission.ts`, `tests/partner-capability-admission.test.ts`, `tests/v5-ptn-001-boundary-scan.test.ts`) applied cleanly with zero conflict, since PR #20 never touched partner/capability code.
- The doc conflicts were resolved by reconciling both branches' independently-true additive history into one coherent, non-duplicated account (PR #20's own Rev52-correction/merge narrative plus this checkpoint's own V5-PTN-001 narrative) — no new claim, acceptance-criteria coverage, field, or function was introduced during conflict resolution; this is a pure base-currency correction.
- New exact head after rebase: `d9e59543e2bc97d18ce366f0a1cdefc20ad3ec63` (single commit, rebased onto `f03c917e66283fcd63736234d81b166465496d7c`).
- Full test/regression suite and strict typecheck re-run on the rebased tree (with a clean `rm -rf dist && npx tsc -p .` rebuild first, to rule out any cross-branch `dist/` artifact contamination): see the updated Evidence section below.

## Rev55 F1 correction: explicit independent admission authority, fail-closed self-admission

- Brain (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 55`) reviewed the rebased exact head `9137450725bf3f0f78e7a119f48c1e57d7089e4b` on base `f03c917e66283fcd63736234d81b166465496d7c` and returned `BRAIN_REVIEW_RESULT: CHANGES_REQUIRED`.
- **F1**: `evidenceRef`/`admittedAt`/`reviewByAt` alone did not prove the admission came from an authority independent of the claimant — nothing in `admitPartnerCapabilityClaim` prevented a partner organization from supplying its own evidence/dates and admitting its own claim. V5 R22's "capability claim cannot self-certify" was therefore only a caller convention, not a structurally enforced invariant.
- **AUTHORIZED_CORRECTION** (same PR #21 / same branch only, no new PR/branch, no merge — `FOUNDER_RELAY_REQUIRED: NO`, `OWNER_GATE: NONE / NOT OPEN`): make admission authority/provenance explicit and auditable, reject claimant/self-admission fail-closed, add negative regression, rerun the full suite, regenerate exact-head evidence with a fresh HEAD_SHA, and return to Brain review.
- Independently verified the finding directly against the source before fixing: confirmed `admitPartnerCapabilityClaim` had no concept of an admitting identity at all, only claim-scoped fields — the finding is correct and not a false positive.
- Fix: added a required `admittingAuthorityId` input to `admitPartnerCapabilityClaim`, validated non-empty, and fail-closed rejected whenever it equals the claim's own `partnerOrganizationId` (self-admission). The accepted authority is persisted on the returned claim as `admittedByAuthorityId` (a new, dedicated `AdmittingAuthorityId` branded type, distinct from `PartnerOrganizationId`) for audit/provenance. Added negative regression H17 (self-admission rejected), H18 (empty authority rejected), and positive regression H19 (admission by a distinct authority succeeds and is persisted).
- No new PR/branch was opened; the correction is a single additional commit on the same `claude/v5-ptn-001-partner-capability-admission` branch. `main` (`f03c917e66283fcd63736234d81b166465496d7c`) did not move, so `BASE_SHA` is unchanged — only `HEAD_SHA` changed.

## Evidence

- Original submission (base `839aca0f6dd830c46dcf4576e46035c3fdbd9207`, now stale): `npx tsc --noEmit -p .` exit 0; `npm run test` 651/651 pass (629 pre-existing + 22 new).
- Rebased onto post-PR#20 main (base `f03c917e66283fcd63736234d81b166465496d7c`, head `d9e59543e2bc97d18ce366f0a1cdefc20ad3ec63`, now stale per Rev55 `CHANGES_REQUIRED`): `npx tsc --noEmit -p .` exit 0; `npm run test` 688/688 pass.
- **Rev55 F1-corrected (current)**: base `f03c917e66283fcd63736234d81b166465496d7c` (unchanged), head `e7013fac72989380568726f750518b9ddee39046`. `npx tsc --noEmit -p .` exit 0, strict mode, zero errors, on a clean `dist/` rebuild. `npm run test`: **691/691 pass**, 0 fail, 0 skipped, 0 cancelled, 0 todo (688 pre-existing/unchanged + 3 new: H17/H18/H19 in `partner-capability-admission.test.ts`). Independently re-verified twice: once on the local post-commit tree, once on a fresh detached-HEAD checkout of the pushed remote head in an isolated git worktree with a fresh `npm ci`.
- `package.json`: zero new runtime or dev dependency (unchanged by this correction).
- Files touched by this correction: `src/domain/partner-capability-admission.ts` (added `AdmittingAuthorityId` type, `admittedByAuthorityId` field, `admittingAuthorityId` param + fail-closed self-admission check in `admitPartnerCapabilityClaim`), `tests/partner-capability-admission.test.ts` (added `admittingAuthorityId` to all 12 existing `admitPartnerCapabilityClaim` calls, added H17/H18/H19, extended H3's assertion), this exec-plan.

## Rev56: Brain PASS, Founder owner-gate now OPEN

- Brain Rev56 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 56`) reviewed exact head `4b78e4b3cd6134b5da0db8e799d47a8bf8795134` on base `f03c917e66283fcd63736234d81b166465496d7c` (the doc-record commit added on top of the Rev55 source fix — no further source/test change) and returned `BRAIN_REVIEW_RESULT: PASS`.
- `REV55_F1_CLOSURE: SATISFIED` — Brain confirmed the corrected candidate makes admission authority/provenance explicit and auditable, rejects self-admission fail-closed, rejects empty authority input, and adds negative/positive regression coverage, closing the Rev55 finding.
- `SCOPE_CHECK: PASS` (bounded to the existing PR #21/branch, no unauthorized new branch/PR, no scope expansion, no merge occurred) and `REGRESSION_CHECK: PASS` (691/691 in two clean validations).
- `MERGE_STATUS: NOT MERGED`. `OWNER_GATE: OPEN` — Brain states explicit Founder authorization is required before PR #21 may be merged; `NEXT_EXPECTED_ACTOR: FOUNDER`. Per this session's own standing conduct rule, a document-recorded Brain PASS is not itself Founder authorization: merging requires a separate, explicit Founder confirmation given directly in a live conversation turn in response to the specific exact-head merge question, never inferred from a document's claim alone. Live PR state was independently re-verified to match the reviewed head/base exactly (draft, open, not merged, `mergeable_state: clean`) before surfacing this to the Founder.
- No merge has occurred. Awaiting a live Founder decision.

## Self-identified error: post-Rev56 head change, Founder-directed resolution

- Immediately after recording Rev56's PASS, a documentation-only commit (`6003b1c5003ab3e2829a950e8047567f68cad0a4`) was mistakenly pushed on top of the Brain-reviewed exact head `4b78e4b3cd6134b5da0db8e799d47a8bf8795134`. This changed PR #21's head, which — per Brain's own explicit condition in Rev56 and this exec-plan's own standing exact-head-voiding rule — immediately voided the Rev56 PASS as a merge-authorizing review. This was caught and disclosed before any merge action; no merge occurred at any point during this sequence.
- Presented live with the choice of (a) a corrective `force-push` restoring the exact reviewed head, or (b) keeping the new head and returning it to Brain for a fresh review round, the Founder explicitly chose (b) and explicitly declined to approve merge at that time.
- The diff between the Rev56-reviewed head and `6003b1c...` was documentation-only (zero source/test change); re-verified `npx tsc --noEmit -p .` exit 0 and `npm run test` 691/691 pass, identical to Rev56's reviewed head. This head was submitted to Brain as a fresh exact-head review request (see the superseding evidence doc referenced below).

## Rev57: fresh exact-head PASS + Founder routine-technical-merge policy; merged

- Brain Rev57 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 57`) freshly reviewed exact head `6003b1c5003ab3e2829a950e8047567f68cad0a4` on base `f03c917e66283fcd63736234d81b166465496d7c` and returned `BRAIN_REVIEW_RESULT: PASS` — confirming the delta from the Rev56-reviewed head was documentation-only (3 docs changed, 0 source, 0 test files) and that Rev56's substantive F1 closure remained satisfied. Fresh validation: strict TypeScript `BUILD_OK`, full regression 691/691 pass.
- Rev57 also recorded a new **Founder policy decision** (durable, independently corroborated both in this live conversation and in the canonical Drive document): routine, reversible, low-risk technical PR merges inside an already-authorized roadmap/task corridor no longer require a separate per-PR Founder merge approval after exact-head Brain PASS (`FOUNDER_UTTERANCE: "Evet otomatik yap"`). A protected owner-gate remains required for deploy/release/production/publication; credentials/secrets/access widening; payment/financial/legal-binding commitment; destructive/irreversible action; material data migration/deletion; or any product/architecture/strategic authority change — none of which apply to this checkpoint.
- Per Rev57's `EXACT_NEXT_ACTION`, live PR #21 state was independently re-verified to match the reviewed head/base exactly (open, draft, unmerged, `mergeable_state: clean`, head `6003b1c...`, base `f03c917e...`) before proceeding. PR #21 was then merged into `main` via the normal protected non-destructive merge path (GitHub merge API, standard merge commit, no protection bypass) — merge commit `912e66927e432c955fcd3135d302e0a8c941cc0c` (parents `f03c917e66283fcd63736234d81b166465496d7c` + `6003b1c5003ab3e2829a950e8047567f68cad0a4`).
- Post-merge, independently re-verified: `git diff` between the merge commit and the reviewed exact head showed zero difference; fresh strict typecheck (exit 0, clean `dist/` rebuild) and full regression (**691/691 pass**, exit 0) re-confirmed directly on the exact merge commit in an isolated worktree with a fresh `npm ci`; `package.json` re-confirmed zero new runtime/dev dependency.

## Status

**IMPLEMENTED / SELF-VALIDATED, Brain Rev57 PASS, MERGED** — merge commit `912e66927e432c955fcd3135d302e0a8c941cc0c` on `main` (parents `f03c917e66283fcd63736234d81b166465496d7c` + `6003b1c5003ab3e2829a950e8047567f68cad0a4`). `VERIFIED`/`PASS` per Brain's independent review at the exact merged head; `CLOSED` remains Brain's determination, not Claude's to declare, per `AGENTS.md` §10. Merged under the Rev57 Founder routine-technical-merge policy — no separate per-PR owner-gate was required or sought for this merge, consistent with that policy's explicit scope (this checkpoint triggers none of the categories the policy reserves for a protected owner-gate).
