# V5-WRK-001 — Worker / Model Routing Policy (Governed Executor/Reviewer Selection Floor)

## Provenance

- Governing authority: DEC-160 V2→V5 Autonomous Engineering Corridor; V5 Full Blueprint (Drive `1HfQ8Mzbos63LZPE_vZryd4TiLyfa8GFP_AdtmLR-el8`) §6 Workstream B (Worker/Model Routing Policy), spine item C (§4).
- **Selection reasoning**: after PR #18 (`V5-INT-001`) merged into `main` (`839aca0f6dd830c46dcf4576e46035c3fdbd9207`) with Brain Rev50 PASS and a live Founder "I confirm" on the exact head, the Founder's follow-up instruction to continue automatically applied the standing post-merge dependency-scan authorization. A fresh DEC-160/V5 dependency scan against merged `main` found V5 Workstreams A (finding recorded, pending Brain adjudication), C, and F all now have a landed floor slice. Re-reading the full V5 blueprint's §6 (Workstream B) found its acceptance criteria — "unavailable/untrusted worker cannot be selected as authorized executor; cost optimization cannot bypass policy; reviewer independence preserved where required; provider outage degrades gracefully" — describe a pure routing-decision function over a caller-declared registry of admitted workers, with no real provider/model API required. This mirrors the same "extend the acceptance criteria into a bounded floor, not a second architecture" reasoning already applied to `V5-INT-001` (Workstream F) and `V5-BOOT-001` (Workstream C).
- The existing `src/domain/worker-invoker.ts` (from `ENG-ORCH-001`) already resolves a narrow reviewer question (`resolvePreV1Reviewer`) for a single fixed `WorkerRole` (`"CLAUDE_PRIMARY_ENGINEER"`) — it is not a general routing policy across an open set of admitted workers, so this checkpoint is a new, additive module rather than an extension of that one; it does not modify or duplicate `worker-invoker.ts`'s existing DEC-142 pre-V1 reviewer-eligibility logic.
- Branch: `claude/v5-wrk-001-worker-routing-policy`, cut fresh from merged `main` after PR #18.
- `BASE_PROVENANCE_SHA`: `839aca0f6dd830c46dcf4576e46035c3fdbd9207` (merged main, PR #18).

## Goal

Provide a pure, side-effect-free routing-decision function that selects an authorized executor (and, where required, an independent reviewer) from a caller-declared registry of admitted workers, without ever calling a real provider/model API or letting cost/availability/position override policy.

## Scope (this checkpoint)

- `src/domain/worker-routing-policy.ts`:
  - `AdmittedWorker` — a caller-declared record: `workerId` (opaque string — no hard-coded vendor/model name anywhere in this module), `declaredCapabilityRefs`, `trustStatus` (`ADMITTED` | `UNTRUSTED` | `REVOKED`), `availability` (`AVAILABLE` | `UNAVAILABLE` | `DEGRADED`), `maxRiskLevel` (`STANDARD` | `HIGH_RISK`), `costWeight` (caller-supplied, opaque relative-cost figure), `evaluationEvidenceRef` (non-empty provenance pointer).
  - `resolveWorkerRoute(request)` — pure function. Fails closed (`InvalidWorkerRoutingRequestError`) only on malformed structural input (empty `requiredCapabilityRef`, invalid `riskLevel`, or `HIGH_RISK`/`requiresIndependentReview` with no `reviewerCandidates` supplied at all). Selects the first candidate in caller-supplied order that is `ADMITTED`, `AVAILABLE`, declares the required capability, carries non-empty evidence, and (for `HIGH_RISK` work) is authorized for that risk level. When independent review is required, selects the first reviewer candidate meeting the same filter AND distinct from the selected executor. Any policy failure (no eligible executor, or no eligible independent reviewer) returns an explicit `REJECTED` decision with a reason — never a thrown error.

## Explicitly deferred (not invented)

No real provider/model API call, no worker registry persistence, no filesystem/network/child_process coupling — the module has **zero imports** and never reads the system clock (routing is fully deterministic from caller-supplied input alone, matching §6's "provider-specific memory is never canonical company memory"). No cost-based tie-breaking beyond caller-supplied candidate order (the module does not sort by `costWeight` itself — a caller choosing to present candidates cost-ascending is the caller's own ordering choice, not logic this module owns). No cross-worker capability negotiation, no automatic worker discovery/admission (that remains a separate, out-of-scope admission process this module only consumes the declared output of), no evaluation-evidence interpretation (the module requires `evaluationEvidenceRef` to be present, never reads or scores its contents).

## Architecture / semantic invariants (verified by test)

- A worker whose `trustStatus` is not `ADMITTED` is never selected as executor or reviewer, regardless of cost, position, or availability.
- A worker whose `availability` is not `AVAILABLE` is never selected — a caller degrading to a fallback candidate after an unavailable primary exercises the same uniform filter, never a relaxed one (§6: "provider outage degrades gracefully" without silently reducing requirements).
- A worker lacking the required capability, or lacking a non-empty `evaluationEvidenceRef`, is never selected.
- A worker whose `maxRiskLevel` is `STANDARD` is never selected for `HIGH_RISK` work.
- Candidate order is caller preference only (e.g. cost-ascending) — an ineligible, cheaper, earlier-listed worker never wins over an eligible, later-listed one.
- When independent review is required, the selected reviewer is always distinct from the selected executor — an executor is never permitted to review its own work through this function.
- Every returned decision (`ROUTED` or `REJECTED`) carries a non-empty `reason`.

## Hard Non-Scope

No filesystem/network/child_process import (verified by boundary scan — the module has zero imports at all); no provider/model hard-coding (no vendor/model name literal anywhere in source); no secret/credential material; no system-clock read (verified by boundary scan); no worker admission/registry mutation; no merge/deploy/release/production/publication/customer-binding/legal/financial action anywhere in this checkpoint's source.

## Test Coverage

| Test | §6 acceptance direction | Covered in |
|---|---|---|
| ADMITTED/AVAILABLE/capable worker routes as executor | governed executor selection | `tests/worker-routing-policy.test.ts` A1 |
| UNTRUSTED/REVOKED worker never selected | "unavailable/untrusted worker cannot be selected as authorized executor" | A2, A3 |
| UNAVAILABLE/DEGRADED worker never selected; graceful fallback to a fully-eligible candidate | "provider outage degrades gracefully" | A4, A5, A6 |
| Cheaper ineligible candidate never beats an eligible later one | "cost optimization cannot bypass policy" | A7 |
| Missing capability / missing evidence rejects | declared capabilities/evidence required | A8, A9 |
| STANDARD-only worker rejected for HIGH_RISK work; matching workers route | risk-level authorization | A10, A11 |
| Same worker cannot be both executor and reviewer (HIGH_RISK and STANDARD+requiresIndependentReview) | "reviewer independence preserved where required" | A12, A13, A14, A15 |
| No eligible executor rejects gracefully, not via thrown error | fail-closed without crashing | A16, A21 |
| Malformed structural input throws | fail-closed input validation | A17, A18, A19, A20 |
| Every decision carries a reason | "routing decisions retain reason/provenance" | A22 |
| No secret material / provider-model hard-coding / worker-identity literal | general hygiene, provider-neutrality | `tests/v5-wrk-001-boundary-scan.test.ts` |
| Zero imports, no system-clock read | pure decision function, no side effect, no memory | boundary-scan |
| Module exports exactly the expected surface | no invocation/persistence/provider-call function | boundary-scan |
| Zero new runtime dependency | dependency delta disclosed | boundary-scan |

## Evidence

- `npx tsc --noEmit -p .`: exit 0, strict mode, zero errors.
- `npm run test`: **656/656 pass**, 0 fail, 0 skipped (629 pre-existing/unchanged + 27 new: 22 in `worker-routing-policy.test.ts`, 5 in `v5-wrk-001-boundary-scan.test.ts`).
- `package.json`: zero new runtime or dev dependency.
- Files touched: `src/domain/worker-routing-policy.ts` (new), `tests/worker-routing-policy.test.ts` (new), `tests/v5-wrk-001-boundary-scan.test.ts` (new), this exec-plan, `docs/engineering/CURRENT_STATE.md`, `docs/exec-plans/corridors/V2_TO_V5.md`, `docs/architecture/VERSION_EVOLUTION_MAP.md`.

## Status

**IMPLEMENTED / SELF-VALIDATED** — pending fresh Brain independent verification at the exact head. Not yet `VERIFIED`/`PASS`/`CLOSED`; Claude's authority ends at this status per `AGENTS.md` §10. `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; merge requires a separately granted protected owner-gate, never inferred from any prior PR's grant.
