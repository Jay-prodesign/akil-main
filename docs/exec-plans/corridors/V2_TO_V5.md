# V2 → V5 Autonomous Engineering Corridor (repo-native projection)

This is the repository-native projection of the canonical corridor contract. It is a condensed pointer, not a replacement — the full contract lives in Drive and is the authority of record if this projection and the Drive original ever disagree.

- **Canonical source**: Drive `113M8-J5mYAUPM_BbMlOp0-EgDD4OpCVhNad2yk4crVA` — "AKILTA — V2→V5 Autonomous Engineering Corridor — Repository Projection & Deep Execution Packet v1.0".
- **Decision authority**: DEC-160 (corridor), reinforced by DEC-150/153/157/158/159; DEC-161 (public-site parallel lane), DEC-162 (version-ahead readiness + parallel audit), DEC-163 (full-program-ahead readiness) apply where non-conflicting.
- **Status of this projection**: created after Brain's `PASS / VERIFIED / CLOSED` disposition on the `V2-APP-001_TO_V2-CDO-006` transition checkpoint (`33f95a3dc1ae1fdae02992b781002b92267f3110`), per the canonical packet's own §17 instruction to project this model into repo-native files once that transition checkpoint clears.

## What the corridor is

One bounded, multi-version engineering corridor from AKILTA v2.0 (Client & Delivery OS) through v5.0 (AI-Native Technology Company OS), removing routine per-task Brain↔Claude permission ping-pong without weakening canonical scope, independent verification, tenant/security boundaries, project isolation, or protected external/live gates. The corridor is the single active engineering selector; a `CURRENT_WORK_SUBCURSOR` (see `docs/engineering/CURRENT_STATE.md`) is mutable execution state inside it, not a second selector. Roadmap is globally `NON_SELECTOR`; inside this corridor its admitted V2→V5 task/dependency graph is the candidate pool Claude may choose the next dependency-safe work unit from.

## Version capability map (outcomes only — see the Drive packet §3 for full detail)

| Version | Outcomes (target) | Status | Canonical blueprint |
|---|---|---|---|
| V2.0 — Client & Delivery OS | Logged-in client workspace; provider-neutral identity/session/tenant boundary; customer/project status & attention projection; Delivery Recipe capability; bounded AI Project Advisor; communication/evidence projection; accountable outcome delivery; admitted capability/connection boundaries; truthful loading/empty/unavailable/blocked/error states. | **Active** — see "Current V2 state" below. | Master program blueprint, Drive `1OjPfxJaBJCWsZrillMJk9hXdjrPkRqz6pSsDR-ICrps` (R22, `PREPARED / NON_SELECTOR / FULL-PROGRAM-AHEAD`) |
| V3.0 — Team, Sales & Agency OS | Roles/ownership models; sales ownership & commission semantics; SLA/escalation; agency/partner org model, dashboards, context switching; multi-org controls; tenant-safe customer/project authority. | **Active** (Workstream A only) — see "Current V3 state" below. | Drive `1joeMcCRtnDSotEmWa63fFG6uxGvbeU1LZApm7_5ap50` |
| V4.0 — 360 Digital Operations | Unified operational routing/approval/reporting across admitted Website/CMS, Commerce, Social, Paid Media, AI/Content, CRM/Email, Listings/Maps, Hosting/DNS/Development surfaces; unsupported providers stay explicit. | **Active** (floor slices of Workstreams A and B only) — see "Current V4 state" below. | Drive `1M5vANxdfuH7Vro_CAFenioAmWitGPS3TRlzd06d5DNs` |
| V5.0 — AI-Native Technology Company OS | Company intelligence; cross-client reusable capability learning under evidence/policy controls; Product Lab; AI-native internal delivery; orchestration across AKILTA modules and the isolated versioned AI Commerce boundary. | `PREPARED / NON_SELECTOR` | Drive `1HfQ8Mzbos63LZPE_vZryd4TiLyfa8GFP_AdtmLR-el8` |

Version names are capability/release checkpoints, not engineering permission stops. `Commercial Ready` / `GA` / `RELEASE_ELIGIBLE` / `LIVE` and public/customer activation remain separately governed and are never implied by reaching a version boundary.

## Current V2 state

- `V2-APP-001` → `V2-CDO-006` (DEC-158 continuous train) reached checkpoint `33f95a3dc1ae1fdae02992b781002b92267f3110`, `IMPLEMENTED / SELF-VALIDATED`, and — after two rounds of evidence-integrity correction on the CR-E3 rendered captures — Brain returned `PASS / VERIFIED / CLOSED` at that same checkpoint. See `docs/exec-plans/completed/V2-APP-001.md` for the full evidence trail.
- **Merged (2026-08-29):** after Brain's exact-head §12A evidence-review PASS on PR #4's full accumulated history (through `V3-SLA-001`, checkpoint `fdcb4a29f26a616664ec07cffdfb6a1ec92e41a3`), the Founder explicitly granted the protected merge owner-gate scoped only to this merge. PR #4 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `4ba5231309ac2fc72dd089cc947030a958eab77e` (parents `8a95c721024da4bfee99e082a01e34b86321637f` + `fdcb4a29f26a616664ec07cffdfb6a1ec92e41a3`), independently re-verified by fresh `git fetch`. That owner-gate is now fully consumed; no standing merge/deploy/release/production authority carries forward from it. Further work uses independent new task-scoped branches cut from current `main`, never the retired `claude/akilta-continuity-readback-5hm3ed` branch.
- **Merged (2026-09-03):** after Brain's exact-head §12A evidence-review PASS on PR #5 (`V3-COM-001`, checkpoint `4ff2bca5ab2a62f0e0941a9dd978917665ef81f2`), PR #6 (`V4-SVC-001`, checkpoint `42530f9e31ddd08898aa6fd0c33e4a12a82fe07f`), and PR #7 (`V4-OPS-001`, checkpoint `cf3c329c94417b95829207d19c57723c63cde59c`), the Founder explicitly granted the protected merge owner-gate for each PR (part of the same round that also covers PR #8, each merged independently on its own exact-head PASS — no single grant is treated as blanket standing authority for PRs it wasn't scoped to). PR #5 was merged into `main` at `5c91f18034a7ec435a4d83fccac856d354f8394f`; PR #6 at `c93a9cc1e8caf5da3c13d102bdb9fa7c778a5982`; PR #7 at `6f403db130434b0228783584487dfdf43f3544f7`. PR #8 (`V3-F-001`) is being merged next in this same round.
- Completed V2 slices carried forward unchanged: `V2-CDO-001` through `V2-CDO-005` (see `docs/exec-plans/completed/`), `AKI-BE-001`, `ENG-ORCH-001`, and the `DEL-003` bounded slices.
- **Correction to the prior version of this note**: both candidates' actual canonical task packets exist and were fetched and read in full (they were previously only known by name, via a secondary reference inside the roadmap tracker) — Drive `1WWRKNk7IgL51Mkb-9qK-eFTZpG-exMvC3IN7zVzitWQ` (V2-CDO-007) and Drive `18bMxG_76s3n2axD3jsSZT3sY_DUQiWqDB4geMZHtKNY` (V2-CDO-008), both meeting §9's bar (goal, dependencies, entry conditions, in-scope/hard-non-scope, acceptance criteria, evidence contract). Live repo preflight against those packets' own stated preconditions found:
  - `V2-CDO-007` ("First Delivery Recipe Contract Slice — WEBSITE_BUILD_v1") is **superseded**: its own Activation Precondition #3 ("no equivalent DeliveryRecipe contract has already landed") fails — `V2-CDO-002` (Brain VERIFIED/PASS/CLOSED at `f99cd2f46b178d2190ac94ff6052c955d3e5cd43`) already implemented exactly this contract (`src/domain/delivery-recipe.ts` + `src/fixtures/website-build-v1-recipe.ts`). Selecting V2-CDO-007 as specified would duplicate already-verified work.
  - `V2-CDO-008` (Delivery Project Advisor, L0 Observe / L1 Recommend) had both of its stated prerequisites satisfied by existing closed work (`V2-APP-001_TO_V2-CDO-006`'s verified portal/projection; `V2-CDO-002`'s verified recipe/capability substrate) and no equivalent Advisor code existed anywhere in `src/`. It is now `IMPLEMENTED / SELF-VALIDATED` (domain contract + presentation-layer wiring) — see `docs/exec-plans/active/V2-CDO-008.md` for the full reasoning and evidence.

- **Superseded finding, corrected by Brain `HANDOFF_HEAD_REV: 10`**: after V2-CDO-008, this section stated that a repo-wide search for `Prepared candidate: Drive <id>` packets found only the V2-CDO-007/008 entries above, and treated that absence of a further named packet as a corridor-exhaustion finding. **Brain explicitly rejected this as a valid DEC-160/DEC-163 exhaustion predicate** (independently verified by fresh-reading the actual Rev-10 handoff document in full, not accepted on a chat paraphrase): absence of a separately-published prepared packet does not mean no dependency-safe work exists — Claude is authorized and required to progressively elaborate the Master Program Blueprint's and version-specific Full Blueprints' own PROVISIONAL dependency spine into concrete repo-native tasks itself. A "no prepared packet found" search is therefore not, by itself, a valid stop condition under this corridor; see "Current V3 state" below for what that progressive-elaboration scan produced.

## Current V3 state

- Following Brain's Rev-10 correction, a real dependency scan was run against the V3 Full Blueprint (Drive `1joeMcCRtnDSotEmWa63fFG6uxGvbeU1LZApm7_5ap50`, R21) — not limited to `Prepared candidate`-named documents. Its §2 Definition-of-Ready predicates relevant to §3 Workstream A (organization/role/assignment authority foundation) were checked against live repo truth and found satisfied: the one-time `33f95a3` transition has Brain PASS, and the V2 identity/session/tenant substrate (`src/web/session-context.ts`) it extends is stable. Workstream A has no dependency on Delivery Recipe/capability/evidence semantics per the blueprint's own text, so it does not wait on any further V2 substrate.
- `V3-ORG-001` (Organization Membership / Role / Assignment Authority Foundation, spine item B) was progressively elaborated and implemented: `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-ORG-001.md`.
- `V3-OWN-001` (Multi-Owner Sales/Account/Delivery Semantics — LeadOwner/DealOwner/AccountOwner/DeliveryOwner, spine item C / §5 Workstream B) continued the same loop immediately: its only prerequisite (`OrganizationMembership` from `V3-ORG-001`) had just landed, so it was elaborated and implemented in the same checkpoint session without waiting for Brain review of the prior slice, per Rev10 item 8's "continue the DEC-160 loop... CONTINUE immediately" instruction. `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-OWN-001.md`.
- `V3-PTR-001` (Agency / Partner Multi-Organization Boundary, spine item D / §6 Workstream C) continued the same loop immediately per Brain `HANDOFF_HEAD_REV: 12`'s explicit next-action directive (independently fetched and read in full before acting). `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-PTR-001.md`.
- **Long-run execution contract (Brain `HANDOFF_HEAD_REV: 11`)**: this session operates under a run-until-genuine-stop directive — completing a task, writing a checkpoint, or persisting an evidence-bridge pointer is explicitly NOT a valid stop condition. The exclusive valid stop conditions are: a genuine Founder/protected-owner gate; a material product/scope/architecture/security/privacy/legal/payment/provider/credential/access decision outside delegated authority; a destructive/irreversible or production/deploy/release/publication/DNS/live-customer action; an unresolved canonical conflict; a required-unavailable capability blocking ALL admitted work; durable enumerated proof that ALL V2→V5 paths are exhausted/revoked/blocked (absence of a named packet is explicitly insufficient); or an execution-surface (context/time/tool/session) limit, in which case a `PAUSED_BY_EXECUTION_LIMIT` checkpoint is persisted instead of a plain stop.
- `V3-SLA-001` (SLA / Escalation / Attention Model, spine item E / §7 Workstream D) continued the same long-run loop immediately per Brain `HANDOFF_HEAD_REV: 11`'s run-until-genuine-stop contract. Reuses the existing `OutcomeJobState` exception-state signal and `AuditEvent` rather than inventing an AT_RISK heuristic (no due-date/SLA-clock source exists in this repository). `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-SLA-001.md`.
- **Post-PR#4-merge continuation (Brain handoff Rev26, independently fresh-read before acting; explicitly rejects a/b/c-style waiting for Founder choice)**: with `main` now carrying V3 Workstreams A-D, a fresh dependency/authority reconciliation was run directly against merged `main`. `V3-COM-001` (Commission/Pricing/Discount Authority Read Models, spine item F / §8 Workstream E) was elaborated to its policy-neutral/read-only/fail-closed floor only — `CommissionLedgerStatus` single literal `UNKNOWN`, `DiscountAuthorityLevel` producing only `APPROVAL_REQUIRED`, zero numeric/financial fields. No real commission %, payout, discount threshold, reseller economics, or binding SLA value was invented or activated (DEC-146/DEC-153 remain controlling for any of those). `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #5 (`5c91f18034a7ec435a4d83fccac856d354f8394f`). See `docs/exec-plans/active/V3-COM-001.md`.
- **Second post-merge scan**: with `V3-COM-001`/`V4-SVC-001`/`V4-OPS-001` (PRs #5/#6/#7) open but unmerged at the time, a further scan of V4 Workstreams C-H and V5 Workstreams A-D found no further honest unit without either merging one of those three PRs or a new architectural decision (see "Current V4 state" below for the V4/V5 side of that scan). The Founder explicitly authorized one such decision — a new provider-neutral shell read-port surfacing already-merged V3 A-D substrate into the web shell, not consuming any unmerged sibling PR — as a reversible, architecture-within-envelope technical call inside Claude's delegated authority. `V3-F-001` (Team & Attention Shell Projection, spine item G / §9 Workstream F floor slice) was elaborated on that authority: organization/client context, viewer role, and each of LeadOwner/DealOwner/AccountOwner/DeliveryOwner rendered separately in the existing shell, plus internal attention/exception state, using only merged-main substrate; commercial state renders as a fixed "Unavailable" literal rather than consuming the `V3-COM-001` checkpoint. `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #8 in this same round. See `docs/exec-plans/active/V3-F-001.md`.
- **Correction (2026-09-03) — "V3 exit audit" is not a Claude engineering task**: this document and `docs/architecture/VERSION_EVOLUTION_MAP.md` previously called the final V3 spine item "Workstream G (cross-slice integration + V3 exit audit)" as if it were a numbered implementation workstream awaiting elaboration, parallel to A-F. A fresh read of the V3 Full Blueprint (Drive `1joeMcCRtnDSotEmWa63fFG6uxGvbeU1LZApm7_5ap50`) shows this is inaccurate: §3's dependency spine lists eight lettered items (A: verified V2 substrate; B-G map onto blueprint §4-§9's six numbered "WORKSTREAM A-F" sections, i.e. `V3-ORG-001`/`V3-OWN-001`/`V3-PTR-001`/`V3-SLA-001`/`V3-COM-001`/`V3-F-001`; H is the final item), and spine item H has no corresponding numbered "WORKSTREAM" section with its own acceptance-direction/required-semantics text at all. Its actual definition lives in blueprint §14 ("V3 VERSION-END FULL AUDIT / EXIT CRITERIA"), which opens: *"At the V3 capability checkpoint **Brain performs** a comprehensive integration audit using accumulated delta evidence plus fresh cross-slice verification."* This is explicitly a Brain review activity consuming evidence Claude has already produced across the six landed slices — not a Claude-authored source module, domain type, or test suite to elaborate. With V3 Workstreams A-F (`V3-ORG-001` through `V3-F-001`, i.e. PR #4's four slices + PR #5 + PR #8) all now merged into `main`, there is no further V3 engineering unit for Claude to select here: the dependency-safe next action on this edge is Brain's own §14 exit audit against the evidence already on `main`, not a new "V3-EXIT-001"-style task. Inventing one would fabricate scope the blueprint does not define.

## Current V4 state

**Note (2026-09-03): this section was briefly absent from this document during PR #5/#6's merge reconciliation** — a doc-only conflict-resolution slip folded V4-SVC-001's own addition of this section into the "Current V3 state" text above without preserving its header. No source file was affected; this is a repo-doc-only correction restoring the section, reconstructed from PR #6's, PR #7's, and PR #8's own branch content (each independently added this section when cut from the same post-PR#4 `main`).

- The same continuation directive (Brain handoff Rev26) also authorized selecting the next dependency-safe unit from V4 once its own §2 Definition-of-Ready predicates were live-checked, without waiting on V3 Workstreams F/G. `V4-SVC-001` (Service-to-Capability Routing, §5 Workstream A) was elaborated to its honest floor only: the blueprint's full 10-rung `ExecutionMaturity` ladder assumes real provider adapters that do not exist anywhere in this repository, so the type declares all 10 rungs (additive-safe for later) but `resolveServiceCapabilityRoute` only ever produces `UNAVAILABLE`/`READ_ONLY`, fail-closed on ownership mismatch. `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #6 (`c93a9cc1e8caf5da3c13d102bdb9fa7c778a5982`). See `docs/exec-plans/active/V4-SVC-001.md`. (This narrowing from "V4 Workstream A" to "its floor only" was disclosed in that PR's own description and exec-plan, not silently absorbed.)
- `V4-OPS-001` (Unified Operations Attention/Exception Model, §6 Workstream B) continued the same loop immediately, per the V4 blueprint's own R32 hot-buffer overlay naming Workstream B as the preferred readiness path right after Workstream A and instructing it to reuse the existing V3 attention semantics. A repo-wide check found exactly one of the blueprint's eleven candidate attention families (blocked dependency, customer approval required, provider wait, credential/access problem, SLA/operating risk, failed external effect, telemetry anomaly, policy restriction, data-quality uncertainty, recovery required, optimization opportunity) has a real observable source in this repository today — internal delivery/job exception state (`AttentionState`, V3-SLA-001) — so `AttentionSourceDomain` declares only that one literal (`DELIVERY_OUTCOME_JOB`) rather than fabricating the other ten. `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #7 (`6f403db130434b0228783584487dfdf43f3544f7`). See `docs/exec-plans/active/V4-OPS-001.md`.
- **Second scan finding (before the V3-F-001 authorization above)**: V4 Workstreams C-H were each checked directly against their §7-§12 text. C (Service-Specific Control Adapters) requires real provider SDKs; D (Approval/Effect/Recovery) depends on C; E (Evidence/Outcome Reporting) requires real effect/telemetry data; F (Bounded Optimization Loop) depends on D+E; G (Account-Manager Operating Model) requires an "automation worker identity" concept with no real source; H (frontend) consumes C-G. None has an honest floor reachable without either a real external provider/effect or fabricating scope — none was selected. V5 Workstreams A-D were also checked: they are meta-engineering-process concepts (WorkUnit/Task/PR/Evidence identity, worker routing policy, repo bootstrap templates) that the V5 blueprint's own R32 hot buffer warns against re-creating as "a second workflow engine, second canonical state system" — `ENG-ORCH-001` (V1, merged) already provides the durable engineering-state contract V5 Workstream A describes, so building a parallel one would be non-additive fabricated scope, not genuine new capability. This finding was re-confirmed live as still current as of the Rev31/Rev32 dependency-scan rounds (2026-09-03), prior to the Founder's merge-round authorization below.
- `V3-COM-001` (PR #5), `V4-SVC-001` (PR #6), `V4-OPS-001` (PR #7), and `V3-F-001` (PR #8) were each cut fresh from merged `main` on independent branches — none depended on, or was blocked by, any sibling's review status. All four are now Brain-PASSed and merged into `main` under one explicit Founder-granted merge round (see "Current V2 state" above for exact merge SHAs). V4 Workstreams C-H and V5 remain not-yet-elaborated for the reasons above; the V3 spine's final item (H, blueprint §14 exit audit) is Brain's own review activity, not a further Claude engineering unit — see the correction above in "Current V3 state". The next dependency scan should re-check live repo truth against this newly-merged state rather than assume a fixed order.

## V3 §14 version-end integration audit — CLOSED (Brain Rev34)

- PR #9 (the "Workstream G" mislabel correction above) was merged into `main` at `6758de759bf17ecda49da7f4f8e1d07faa526655` (parents `837214dc863970bdb05f29bf8611a3438735b3f4` + `63da4ab839f231ca6f13ce2256ca7542bf87504f`). Its merge followed a conversational Founder authorization exchange in the engineering session, consistent with the PR #5–#8 merge round; per the same `OWNER_GATE_PROOF_MISSING` provenance standard already applied to that round (see the Rev33 evidence package referenced below), that conversational record is accepted as historical merge-execution context but does not itself constitute a durable, independently-verifiable proof artifact of Founder approval. No such artifact exists for PR #9 specifically — this section previously stated PR #9 "was Founder-approved and merged" as flat fact without carrying that caveat forward; corrected here per Brain Rev35's `GOVERNANCE_PROVENANCE_CONFLICT` finding. The historical merge itself is not disputed, only the unqualified approval-provenance phrasing. A compact, transferable, SHA-bound post-merge evidence package for PR #5 through PR #9 (repo identity, full merge-chain ancestry, proof that every reconciliation/correction commit touched documentation only, fresh strict-typecheck + 580/580 regression at that exact SHA, dependency/lockfile/protected-boundary readback, and an honest `OWNER_GATE_PROOF_MISSING` statement rather than a fabricated durable approval artifact) was composed, uploaded to Drive, and read-back-diff-verified clean — main document Drive `1ezXwkj8zYLqwk7A-qxFVRwy8PVVLGx5C1H9hk339rEQ`.
- Brain independently reviewed that package (Handoff `HANDOFF_HEAD_REV: 34`, fresh-read before acting, not accepted on a chat paraphrase) and returned `BRAIN_REVIEW_DISPOSITION: PASS — POST_MERGE_TECHNICAL_INTEGRATION_EVIDENCE / OWNER_GATE_PROVENANCE_EXCEPTION_RECORDED`. Rev34 explicitly closes the V3 Full Blueprint §14 version-end integration audit edge (`V3_§14_AUDIT_STATE: PASS / VERSION_END_INTEGRATION_EDGE_CLOSED`) at exact final main `6758de759bf17ecda49da7f4f8e1d07faa526655`, using the already-accepted exact-head semantics of each of the six landed slices plus this merged-main proof. It records the owner-gate provenance exception honestly (a historical governance/provenance gap, not a source defect) rather than retroactively fabricating a durable approval artifact.
- Rev34's continuation instruction (`STOP_STATUS: CONTINUE`, `NEXT_EXPECTED_ACTOR: CLAUDE`, `PENDING_BRAIN_RESPONSE: NO`, `OWNER_GATE: NONE` for ordinary fresh-read/dependency-scan/bounded-already-authorized engineering) directed a fresh DEC-160 dependency/authority scan across V4 Workstreams C-H and V5 Workstreams A-D for the highest-value dependency-safe V5-first slice. That fresh scan (this checkpoint) reconfirmed V4 Workstreams C-H remain blocked: no new provider/effect adapter or connector SDK has been added anywhere in `src/` since the last scan (`providerRef`/`connection-authority.ts`/`worker-invoker.ts` remain structurally opaque string references by design, per their own doc comments — no field anywhere in the repo can hold a provider SDK object, OAuth token, or invocation config), so those workstreams still depend on a real external provider/effect that does not exist. It also concluded, for V5 Workstreams A/D, that "no field anywhere holds a provider object" is not itself their blocking edge — that reasoning wrongly treated the *existence* of the already-merged `ENG-ORCH-001` durable engineering-state contract as grounds to call any further V5-A/D work a forbidden duplicate, when the V5 Full Blueprint's own R32 hot-buffer overlay (§5, "V5 Reuse Foundation Now Visible") states the opposite: the V5 AI Engineering Delivery Engine **must extend** `ENG-ORCH-001` and the DEC-160 repo-native execution package, not avoid touching it. That prior 10/10 BLOCKED conclusion is withdrawn for V5 A/D per Brain Rev35's `CORRIDOR_EXHAUSTION_DEPENDENCY_ANALYSIS_INVALID` finding (independently fresh-read and verified against both the canonical Handoff document and the V5 Full Blueprint itself before acting — see "V5 A/D corrected re-scan" below).

## V5 A/D corrected re-scan + V5 C bootstrap re-evaluation (Brain Rev35)

- **V5 Workstream A (AI Engineering Delivery Engine) / D (Spec/Task/Test/PR/Review Orchestration) — ELIGIBLE (extend, not duplicate)**: the blueprint's §5 acceptance criteria require, among other things, that "stale SHA approval cannot merge" and that a "post-review push invalidates old merge approval." Fresh reading of the already-merged ENG-ORCH-001 primitives (`src/domain/engineering-run-state.ts`, `src/domain/engineering-event-envelope.ts`) shows this specific acceptance criterion is not yet enforced as an explicit, tested transition: `applyEvent`'s `CHECKPOINT` handling only accepts a new checkpoint from `NONE`/`CHANGES_REQUIRED`/`RESUME_AUTHORIZED` status (`CHECKPOINT_ACCEPTING_STATUSES`, `src/domain/engineering-run-state.ts:93-97`); a fresh push (new `CHECKPOINT` event) arriving while status is `PASS` is a silent no-op that leaves the stale `BrainResolution.checkpointSha` in place with no explicit authorized path to reopen the run for the new SHA. This is not an oversight bug — it is a deliberately-scoped, already-tested boundary of the V1 ENG-ORCH-001 slice (see `tests/engineering-run-state.test.ts:459`, test name: *"a fresh higher-fenced CHECKPOINT cannot reopen/regress an already-PASS run **without an explicit authorized path**"* — the italicized qualifier is the exact gap V5-A/D's acceptance criterion asks to close). Closing it is a narrow, additive extension of the existing reducer/event vocabulary (most likely: a new explicit event/transition that lets a fresh `CHECKPOINT` on a `PASS`-resolved run reopen into `CHECKPOINT_RECEIVED` with the prior `BrainResolution` explicitly retained as superseded history rather than silently dropped) — not a second orchestration engine, not a parallel canonical-state system. **This is the resolved highest-value dependency-safe V5-first candidate.** Exact task/file scope remains LIVE-RESOLVED at implementation time, per the corridor's own convention; it is named and located here, not yet implemented in this documentation-only correction (see below).
- **V5 Workstream C (Project/Repository Bootstrap) — re-evaluated, not BLOCKED by target-count**: the prior scan's "only one project/repository exists, therefore blocked" reasoning is withdrawn. The blueprint's §7 candidate reusable assets — compact AGENTS/CLAUDE navigation, `CURRENT_STATE`, `SYSTEM_MAP`/`VERSION_EVOLUTION_MAP`, a `DEPENDENCY_MAP`, an execution contract, task templates, evidence manifests, and a review/finding schema — already exist as this repository's own scaffolding (`AGENTS.md`, `CLAUDE.md`, `docs/engineering/CURRENT_STATE.md`, `docs/architecture/VERSION_EVOLUTION_MAP.md`, `docs/exec-plans/`). A second live customer repository is not a stated entry predicate anywhere in §7; the real predicate is the ability to extract/parameterize this scaffolding into a reusable, namespace-isolated template with cross-project-contamination tests proving it (§7 acceptance: "cross-project state contamination tests; stale template authority cannot override project truth; secret/customer artifact cloning prohibited"). This is technically ELIGIBLE in principle, but is architecturally larger (it defines a new "template" artifact/contract, not an extension of an existing typed domain module) than the V5-A/D gap above, so it is not the candidate selected for the next implementation round — it is recorded here as ELIGIBLE_NOT_SELECTED rather than BLOCKED.
- **V5 Workstream B (Worker/Model Routing)**: not part of Rev35's requested re-scan; its prior "not yet elaborated" status is unchanged and not re-asserted as BLOCKED here.
- Per Brain Rev35's explicit step 5 bound for this documentation-only PR #10 correction ("do not mutate product source"), the V5-A/D slice named above is resolved/located, not yet implemented, in this correction.

## V5-ENG-001 implemented (Brain Rev38 confirmed candidate)

- PR #10 (carrying the V3 §14 closure and the Rev35 correction above) merged into `main` at `90e989935a443462f372fbbb9ff1caa6987c82ce` on the Founder's own direct, explicit approval given in the engineering conversation. Brain Rev38 (independently fresh-read from the canonical Handoff document before acting) accepted the merge as technically PASS (merged tree byte-identical to the reviewed exact head, fresh typecheck exit 0, fresh 580/580 regression), corrected Rev37's own incorrect "Dvm = merge approval" claim to `INVALID / RETRACTED` (Claude's independently-asked-and-answered "Yes" is recorded as `OWNER_GATE_PROOF_UNVERIFIED_AT_BRAIN_SURFACE` — accepted as historically real, not fabricated into independently-proven authority), and confirmed the V5-A/D candidate from the section above as the authorized next action, with `OWNER_GATE: NONE` for the bounded internal source slice.
- `V5-ENG-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-eng-001-checkpoint-invalidation`) cut from merged `main`: `src/domain/engineering-run-state.ts`'s `EngineeringRunState` gained a `supersededResolutions` field, and `CHECKPOINT_ACCEPTING_STATUSES` now includes `PASS`, so a fresh `CHECKPOINT` (post-review push) carrying a NEW `checkpointSha` explicitly reopens a `PASS`-resolved run into `CHECKPOINT_RECEIVED`, clearing the stale `resolution` and appending it to `supersededResolutions` — a duplicate `CHECKPOINT` for the SAME `checkpointSha` remains a safe no-op. `IMPLEMENTED / SELF-VALIDATED`, 584/584 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-ENG-001.md` for the full reasoning and evidence.
- **Rev39 correction**: Brain's exact-head review of PR #12 found the submitted evidence didn't prove that EQUAL fencing (not just stale/lower) also cannot invalidate a `PASS` approval, per the Rev36/Rev38 acceptance contract. On independent verification (fresh empirical probe against the compiled reducer, not just trusting either the original self-assessment or Brain's own "not proven either way" framing), this was found to be a genuine source gap, not only a missing test: the `PASS`-guard's `isGenuineNewPush` check verified only that `checkpointSha` differed, not that `fencingToken` was strictly greater than current — so an equal-fenced `CHECKPOINT` with a new `checkpointSha` was incorrectly accepted as a valid invalidation. Fixed with a one-condition narrowing (`event.fencingToken > state.currentFencingToken` added to the existing check) on the same PR #12 branch, plus the required focused test. 584/584 tests pass.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #10's grant or any other prior PR's.
- Brain Rev40 (independently fresh-read before acting) reviewed the Rev39-corrected exact head, reclassified the prior "no material source defect" finding as `SUPERSEDED BY EMPIRICAL EVIDENCE`, and returned `BRAIN_REVIEW_DISPOSITION: PASS` with `OWNER_GATE: REQUIRED / NOT_YET_APPROVED` for the PR #12 merge.

## PR #12 merged (Founder-approved exact-head merge)

- Brain Rev41 subsequently claimed the Founder had told Brain directly, in a separate channel, to merge PR #12 (exact head `acfc674fc2f1606204ff8953994156af317f98df`). Per standing practice, Claude did not act on this claim alone — the underlying event happened in a channel outside this conversation and could not be independently confirmed from it. Claude instead confirmed PR #12's live GitHub state matched exactly, then asked the Founder the specific exact-head merge question directly in the engineering conversation and received an explicit "Merge" confirmation before merging.
- PR #12 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `19df961498896427223842f7de82df2d9f6a8304` (parents `90e989935a443462f372fbbb9ff1caa6987c82ce` + `acfc674fc2f1606204ff8953994156af317f98df`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #12's exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (584/584 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.

## V5-BOOT-001 implemented (Brain Rev42 confirmed candidate)

- Brain Rev42 (independently fresh-read from the canonical Handoff document before acting) accepted PR #12's post-merge state as PASS, confirmed the Rev41 owner-gate was consumed for that merge only, and reauthorized a fresh DEC-160/V5 dependency scan against the newly-merged main with `OWNER_GATE: NONE` for the scan and for bounded internal engineering work it selects — explicitly rejecting "waiting on your call" as a valid stop condition once that scan was already standing-authorized.
- That fresh scan reconfirmed V4 Workstreams C-H remain blocked (no real provider/effect adapter exists anywhere in `src/`) and V5 Workstream B (Worker/Model Routing) remains blocked (only `WORKER`/`BRAIN`/`OWNER` roles exist; DEC-142 still forbids Codex admission pre-V1; no multi-provider capability registry exists to route against). V5 Workstream C, previously `ELIGIBLE_NOT_SELECTED` per Rev35's "architecturally larger" framing, was reconsidered: its §7 acceptance criteria (cross-project contamination tests, stale-template-cannot-override-truth, secret-cloning prohibition, unique identifiers) can be modeled and adversarially tested as a pure planning function over synthetic project namespaces — a real second live repository was never actually a stated entry predicate.
- `V5-BOOT-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-boot-001-bootstrap-template`) cut from merged `main`: `src/domain/project-bootstrap-template.ts`'s `resolveProjectBootstrapPlan` resolves which structural assets (from a closed, blueprint-verbatim `BootstrapAssetKind` enum with no credential/provider-secret kind) a new project may reuse from an existing template, stamping every copied asset `supersedesLocalAuthority: false`, failing closed on a colliding project namespace, and forwarding `contentRef` as an opaque, never-interpreted pointer. `IMPLEMENTED / SELF-VALIDATED`, 597/597 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-BOOT-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.

## Rev43: PR #14 CHANGES_REQUIRED — provenance/local-applicability gaps fixed same-branch

- Brain Rev43 (independently fresh-read from the canonical Handoff document) reviewed PR #14's exact head (`4df83393002c62323fb5c277c5b1766eb5d14b62`) and returned `BRAIN_REVIEW_DISPOSITION: CHANGES_REQUIRED — MATERIAL ACCEPTANCE COMPLETENESS`, with `NO_MERGE_AUTHORITY: YES`. Both findings were independently verified against the live source before acting, not accepted from the framing alone: F1 (the per-asset loop never validated `sourceProjectRef`/`sourceVersion` for emptiness — an asset with a recognized `kind` but blank provenance could be copied) and F2 (no field represented the §7-required local-applicability revalidation decision — a recognized `kind` alone was sufficient for acceptance) were both confirmed as real gaps by reading the actual `src/domain/project-bootstrap-template.ts`.
- Fixed same-branch (`claude/v5-boot-001-bootstrap-template`), no new PR, no redesign: added fail-closed provenance validation (empty/whitespace `sourceProjectRef`/`sourceVersion` now rejected) and a required `localApplicability: "ACCEPTED" | "NOT_APPLICABLE"` field — only `"ACCEPTED"` assets can enter `copiedAssets`. 4 new focused tests (B9-B12); all prior invariants (duplicate-namespace fail-closed, secret/provider boundary, opaque `contentRef`, cross-project isolation, zero imports, zero new dependency) preserved. Strict typecheck exit 0; full regression 601/601 pass.
- `OWNER_GATE: NONE` for this bounded same-branch correction per Rev43; no Founder relay required. PR #14 remains `OPEN / DRAFT / UNMERGED / HOLD_MERGE`, returned for Brain exact-head re-review at the corrected head.

## PR #14 merged (Founder-approved exact-head merge)

- Brain Rev44 (independently fresh-read from the canonical Handoff document) reviewed the Rev43-corrected exact head `88f84e5d5c0e98ee1a2a9e83d39d3ae18655df64` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming F1/F2 closed, 601/601 tests pass, zero new dependency.
- Brain Rev45 subsequently claimed the Founder had told Brain directly, in a separate channel ("Ok edilsin"), to merge PR #14 exact head `88f84e5d5c0e98ee1a2a9e83d39d3ae18655df64`. Per standing practice, Claude did not act on this claim alone — the underlying event happened in a channel outside this conversation and could not be independently confirmed from it. Claude instead confirmed PR #14's live GitHub state matched exactly, then asked the Founder the specific exact-head merge question directly in the engineering conversation and received an explicit "Merge et devam et" confirmation before merging.
- PR #14 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb` (parents `19df961498896427223842f7de82df2d9f6a8304` + `88f84e5d5c0e98ee1a2a9e83d39d3ae18655df64`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #14's exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (601/601 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.

## V5-ENG-002 implemented (post-PR#14 dependency scan, Founder "devam et, sen seç")

- After PR #14 merged, the Founder authorized ("Devam et, sen seç" — continue, you choose) the standing post-merge continuation: per Rev42/Rev45's instruction, ordinary bounded internal engineering work following a merge does not require another Founder gate. A fresh DEC-160/V5 dependency scan was run against merged `main` (`1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb`).
- The scan reconfirmed V4 Workstreams C-H and V5 Workstream B remain blocked (no real provider/effect adapter or multi-provider capability registry exists anywhere in `src/`). Re-reading the V5 blueprint §5 (Workstream A) acceptance list in full against the already-merged `src/domain/engineering-run-state.ts` found one criterion not yet enforced: *"agent cannot self-mark final VERIFIED."* `applyEvent`'s `RESOLVE` case never inspected `event.fromRole` — only run status and `checkpointSha` binding gated it. This was independently, empirically confirmed as a real, exploitable defect (not merely inferred from reading the code): compiling the reducer and probing it directly showed a `fromRole: "WORKER"` `RESOLVE(PASS)` event was accepted and produced a durable `BrainResolution` with `decision: "PASS"`.
- `V5-ENG-002` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-eng-002-resolve-authority-separation`) cut from merged `main`: `applyEvent`'s `RESOLVE` case now rejects (as a safe no-op, same treatment as its other RESOLVE-path rejections) any event whose `fromRole` is not `"BRAIN"` or `"OWNER"`. All 14 pre-existing `RESOLVE`-emitting tests were updated to pass `fromRole: "BRAIN"` explicitly (they were relying on the shared test helper's `fromRole: "WORKER"` default, which the fix now closes) with no change to any assertion; 4 new tests prove the self-certification rejection for both `PASS` and `CHANGES_REQUIRED`, confirm an `OWNER`-authored `RESOLVE` still works, and confirm the rejected attempt still advances the fencing floor per the existing E7 convention. `IMPLEMENTED / SELF-VALIDATED`, 605/605 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-ENG-002.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.

## PR #16 merged (Founder-approved exact-head merge)

- Brain Rev46 (independently fresh-read from the canonical Handoff document) reviewed PR #16's exact head `3c4f20aadb1d43e6f93d58b5c6010301d884aa38` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming the empirical pre-fix defect finding, the bounded correction, and 605/605 tests passing. `OWNER_GATE: REQUIRED / NOT_YET_APPROVED`.
- The Founder said, unprompted, "Merge edebilirsin bu arada" (you can merge it, by the way) before Brain's review had landed. Per standing practice, Claude did not merge on that alone — it flagged that Brain had not yet independently reviewed PR #16 (a distinct gate from Founder approval, and notably the very principle PR #16 itself encodes: independent verification, not self-certification), and asked whether to merge anyway or wait for Brain first. The Founder said to wait. Once Brain's Rev46 PASS arrived, Claude re-verified PR #16's live GitHub state matched the audited head exactly, then asked the Founder the specific exact-head merge question directly in the engineering conversation and received an explicit "I confirm" before merging.
- PR #16 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `460871cba43273f89bbf8004e547664c40b90794` (parents `1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb` + `3c4f20aadb1d43e6f93d58b5c6010301d884aa38`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #16's exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (605/605 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.
- Subsequently, Brain Revs 47 and 48 (fresh-read at a later check-in) were found to describe a Founder approval/merge-execution sequence for PR #16 that had already completed live in this conversation — Brain's connected GitHub surface returned a 404 on this private repo and so could not observe the already-merged state. Rev47's quoted Founder utterance ("Edilsin bence") does not match the actual live exchange that authorized the merge (the real sequence is recorded above); this was noted as a Rev37-style misattribution but required no corrective action since the underlying merge had already happened correctly.

## V5-INT-001 implemented (post-PR#16 dependency scan, Founder continuation nudge)

- After PR #16 merged, the Founder's "Dvm edemiyor musun" (can't you continue?) applied the standing Rev42/Rev45/Rev46/Rev48 continuation authorization: ordinary bounded internal engineering work following a merge needs no further Founder gate. A fresh DEC-160/V5 dependency scan was run against merged `main` (`460871cba43273f89bbf8004e547664c40b90794`).
- The scan reconfirmed V4 Workstreams C-H and V5 Workstream B remain blocked. Re-reading V5 blueprint §5 (Workstream A) in full against the now-twice-extended `engineering-run-state.ts` found all five named acceptance criteria satisfied: stale-SHA invalidation (`V5-ENG-001`), wrong-context fail-closed (pre-existing `ENG-ORCH-001` guard), no self-certification (`V5-ENG-002`), auditable finding lifecycle (`supersededResolutions`), and durable-truth resumability (`FileDurableEngineeringStore`/`reconstructState`) — a finding recorded here, not a status Claude claims authority to close. With V5 A/C now built and B/D exhausted at the currently-elaboratable level, the scan located V5 Workstream F (§10, Cross-Domain Company Intelligence) as buildable via a pure reconciliation function over opaque insight pointers, reusing the existing `TenantScope` isolation convention — the same "real second system was never the actual entry predicate" reasoning already applied to `V5-BOOT-001`.
- `V5-INT-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-int-001-cross-domain-intelligence`) cut from merged `main`: `src/domain/cross-domain-intelligence.ts`'s `buildCrossDomainIntelligenceSnapshot` combines insights from a closed six-surface domain enum (SALES/DELIVERY/OPERATIONS/ENGINEERING/COMMERCIAL/PRODUCT, matching the blueprint's own §10 text) about the same subject, failing closed on tenant/project scope mismatch or an unrecognized domain, and reconciling by freshness (against a caller-supplied `asOf`, never the system clock) into `CURRENT`/`STALE`/`CONFLICTING` per subject — disagreement is never silently arbitrated, and stale evidence never masquerades as current. `IMPLEMENTED / SELF-VALIDATED`, 625/625 tests pass, strict typecheck clean, zero new dependency. PR #18 opened, `HOLD_MERGE`. See `docs/exec-plans/active/V5-INT-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.

## Rev49 correction: PR #18 (V5-INT-001) exact-head review, CHANGES_REQUIRED

- Brain Rev49 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 49`, confirmed genuine before acting) reviewed PR #18's exact submitted head `0a7715d03c7d4e89c648433de79058f203cd0b38` and returned `BRAIN_REVIEW_DISPOSITION: CHANGES_REQUIRED` / `MERGE_DISPOSITION: HOLD_MERGE / DO_NOT_MERGE`, with two findings:
  - **F1 — provenance fail-closed gap**: `IntelligenceInsight` carries `sourceRef` as its provenance pointer, but the audited implementation never validated it before accepting an insight — an empty/whitespace `sourceRef` could masquerade as governed evidence.
  - **F2 — future-timestamp freshness gap**: the freshness comparison (`asOfMs - capturedAtMs <= freshnessThresholdMs`) never checked `capturedAtMs <= asOfMs`; a `capturedAt` later than the caller-supplied `asOf` produces a negative age, which satisfies any non-negative threshold and could wrongly resolve `CURRENT`.
- Consistent with this corridor's standing empirical-verification discipline, both findings were independently confirmed against the compiled `dist/src/domain/cross-domain-intelligence.js` output (not accepted from Brain's framing alone) before any source change: a probe insight with `sourceRef: ""` was accepted with zero rejections; a probe insight dated `2099` against a 2026 `asOf` and a 1000ms threshold still resolved `CURRENT`. Both were genuine, exploitable gaps.
- Fixed on the **same branch/PR** (`claude/v5-int-001-cross-domain-intelligence`, PR #18) per Rev49's own explicit bounded-correction authorization — no new branch, no redesign: two new rejection checks added to the per-insight validation loop in `buildCrossDomainIntelligenceSnapshot` (reject empty/whitespace `sourceRef`; reject `capturedAt` in the future relative to `asOf`, checked before the freshness filter so a negative age can never be reached), plus 4 new adversarial tests (F16-F19 in `tests/cross-domain-intelligence.test.ts`) covering both fixes and the exact-equality boundary (`capturedAt === asOf` is accepted, not "in the future").
- `V5-INT-001` is now `IMPLEMENTED / SELF-VALIDATED (Rev49-corrected)`: strict typecheck exit 0, full regression 629/629 pass (625 prior + 4 new), zero new dependency. `MERGE_DISPOSITION: HOLD_MERGE` unchanged — Brain's fresh exact-head review of the corrected head is the next step, not merge.
- Rev49 also reconfirmed the V5 Workstream A five-criterion finding remains `FINDING_PENDING_BRAIN_ADJUDICATION` — Claude's recorded finding is accepted as directionally noted but not yet independently adjudicated by Brain.

## PR #18 merged (Rev50 PASS, Founder-approved exact-head merge)

- Brain Rev50 (independently fresh-read from the canonical Handoff document) reviewed PR #18's Rev49-corrected exact head `76a352b9fc0ed782b5112bff581e578e4cabaca9` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming both Rev49 findings (F1 sourceRef, F2 future-capturedAt) were closed and 629/629 tests passing. `OWNER_GATE: REQUIRED / NOT_YET_APPROVED`.
- Rev51 (fresh-read at the next check-in) recorded a Founder utterance ("Ok merge") authorizing the merge for that exact head. Per standing practice, this document-relayed utterance was not treated as sufficient on its own — Claude independently re-verified PR #18's live GitHub state matched the reviewed head/base exactly (open, draft, unmerged, `mergeable_state: clean`), then asked the Founder the specific exact-head merge question directly in the live engineering conversation and received an explicit "I confirm" before merging.
- PR #18 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `839aca0f6dd830c46dcf4576e46035c3fdbd9207` (parents `460871cba43273f89bbf8004e547664c40b90794` + `76a352b9fc0ed782b5112bff581e578e4cabaca9`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #18's reviewed exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (629/629 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.
- Rev51's `WORKSTREAM_A_FINDING` line reconfirms the V5 Workstream A five-criterion finding is not implicitly closed by this merge — it remains `FINDING_PENDING_BRAIN_ADJUDICATION`.

## V5-WRK-001 implemented (post-PR#18 dependency scan, Founder continuation instruction)

- After PR #18 merged, the Founder's explicit instruction to fresh-read the Handoff and continue applied the standing post-merge dependency-scan authorization. A fresh DEC-160/V5 dependency scan was run against merged `main` (`839aca0f6dd830c46dcf4576e46035c3fdbd9207`).
- The scan reconfirmed V5 Workstreams A (finding recorded, pending Brain adjudication), C, and F all have a landed floor slice. Re-reading V5 blueprint §6 (Workstream B, Worker/Model Routing Policy) in full found its acceptance criteria — "unavailable/untrusted worker cannot be selected as authorized executor; cost optimization cannot bypass policy; reviewer independence preserved where required; provider outage degrades gracefully" — describe a pure routing-decision function over a caller-declared registry of admitted workers, buildable without any real provider/model API, mirroring the same reasoning already applied to `V5-INT-001` and `V5-BOOT-001`.
- `V5-WRK-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-wrk-001-worker-routing-policy`) cut from merged `main`: `src/domain/worker-routing-policy.ts`'s `resolveWorkerRoute` selects the first caller-supplied candidate that is `ADMITTED`, `AVAILABLE`, capable, evidenced, and risk-authorized as executor, and (when independent review is required) the first such candidate distinct from the executor as reviewer — never letting cost/position/availability override policy, never letting an executor review its own work, always returning an explicit `REJECTED` decision with a reason rather than throwing when no eligible worker exists. `IMPLEMENTED / SELF-VALIDATED`, 656/656 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-WRK-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #18's grant or any other prior PR's.

## Rev52 correction: PR #18 post-merge acceptance + PR #20 (V5-WRK-001) CHANGES_REQUIRED

- Brain Rev52 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 52`, confirmed genuine before acting) formally accepted PR #18's post-merge state as `TECHNICAL_DISPOSITION: PASS / POST-MERGE ACCEPTED`, confirming the merge commit/parentage/zero-diff/typecheck/regression evidence, and closed Rev51's owner-gate for the PR #18 exact-head merge only. Rev52 also noted that Rev51's directly-observed Founder "Ok merge" utterance is itself sufficient durable merge authority on its own terms, and that Brain does not need to independently prove or elevate the additional live "I confirm" this session separately obtained — that live confirmation was this corridor's own standing practice (never merge from a document claim alone) and remains a valid additional safeguard, not a claim that Brain itself verified it.
- Rev52 also reviewed PR #20 (`V5-WRK-001`) and returned `CHANGES_REQUIRED`, with two findings, both independently confirmed against the module's own source before fixing:
  - **F1 — required worker policy dimensions not modeled**: §6's required semantics ("each worker has declared capabilities/tools/policy constraints/evaluation evidence") and "provider availability != admission != authority" require tool access, policy/privacy-security constraints, and an explicit authority dimension to be modeled as separate routing inputs. The submitted `AdmittedWorker` had no field for any of the three — confirmed true by direct inspection of the source.
  - **F2 — exact-head transferability gap**: the original evidence recorded only the abbreviated head `c106161`, not a full 40-character SHA, so Brain could not bind an exact-head review to it — confirmed true (the evidence document literally read "c106161 (short)").
- Fixed on the **same branch/PR** (`claude/v5-wrk-001-worker-routing-policy`, PR #20) per Rev52's own explicit bounded-correction authorization: `AdmittedWorker` gained `declaredToolRefs`, `declaredPolicyConstraintRefs`, and `authorityLevel` (`STANDARD` | `ELEVATED`); `WorkerRoutingRequest` gained matching `requiredToolRefs`, `requiredPolicyConstraintRefs`, and `requiredAuthorityLevel`; the eligibility filter (applied uniformly to executor selection, reviewer selection, and every fallback candidate — fallback can never route on a relaxed subset) now requires all three. 10 new adversarial tests (A23-A32). New full exact head: `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79`.
- `V5-WRK-001` is now `IMPLEMENTED / SELF-VALIDATED (Rev52-corrected)`: strict typecheck exit 0, full regression 666/666 pass (656 prior + 10 new), zero new dependency — re-verified via fresh detached-HEAD checkout of the exact pushed head. `MERGE_DISPOSITION: HOLD_MERGE` unchanged — Brain's fresh exact-head review of the full-SHA-bound corrected head is the next step, not merge.
- Rev52 also directed that PR #19 (the PR #18 post-merge documentation edge) remain an independent, unreviewed doc-only edge — its merge authority is never inferred from PR #18's, and any of its prose treating the worker-reported live "I confirm" as independently Brain-proven Founder authority must be corrected to cite Rev51's "Ok merge" as the canonical durable authority record instead.
- Rev52 reconfirmed the V5 Workstream A five-criterion finding remains `FINDING_PENDING_BRAIN_ADJUDICATION`, not closed as a side effect of either PR #18's acceptance or PR #20's review.

## V5-PTN-001 implemented (self-paced corridor continuation, no new Rev beyond 52 found)

- A scheduled corridor check-in fresh-read the canonical Handoff document and confirmed no genuine new Brain response existed beyond Rev52 (PR #20 remained awaiting review at its Rev52-corrected exact head). Per the standing post-merge continuation authorization, a fresh scan of the remaining V5 workstreams was run: E (blocked, no real external provider), G/I (blocked, no real validated-outcome evidence history exists), J (needs more backend substrate first) — H (Partner Network Operations, §12) was re-read in full against the already-merged `V3-PTR-001` (`src/domain/partner-organization.ts`).
- Direct inspection found four of §12's five acceptance directions already satisfied by `partner-organization.ts`'s `resolvePartnerClientAccess`/`PartnerRelationshipType` design. The one genuine gap: "partner capability claims require evidence/admission and may expire/review" and "capability claim cannot self-certify" — no such concept existed anywhere in `src/`.
- `V5-PTN-001` was elaborated and implemented on `claude/v5-ptn-001-partner-capability-admission`, cut from merged `main`: `src/domain/partner-capability-admission.ts` extends `PartnerOrganization` (type-only import) with a `PartnerCapabilityClaim` lifecycle — always born `UNVERIFIED`; only an explicit `admitPartnerCapabilityClaim` call (requiring non-empty evidence and a forward-looking `reviewByAt`) can produce `ADMITTED`; `EXPIRED` is a computed, never-stored disposition relative to a caller-supplied `asOf`; `REVOKED` is terminal. `IMPLEMENTED / SELF-VALIDATED`, 651/651 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-PTN-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #18's grant or any other prior PR's.

## PR #20 merged (Rev53 PASS, Founder-approved exact-head merge)

- Brain Rev53 (independently fresh-read from the canonical Handoff document) reviewed PR #20's Rev52-corrected exact head `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming both Rev52 findings (F1 tool/policy/authority dimensions, F2 full-SHA binding) closed and 666/666 tests passing.
- Rev54 recorded a Founder "Ok" utterance authorizing the merge for that exact head — the canonical, Brain-accepted durable authority record for this merge on its own terms. Per this corridor's own standing practice (never merge from a document claim alone), Claude additionally re-verified PR #20's live GitHub state matched the reviewed head/base exactly, then asked the Founder the specific exact-head merge question directly in the live conversation and received an explicit "I confirm" before merging — a self-applied extra safeguard, consistent with Rev52's own clarification that Brain does not require or independently verify this additional step.
- PR #20 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `f03c917e66283fcd63736234d81b166465496d7c` (parents `839aca0f6dd830c46dcf4576e46035c3fdbd9207` + `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #20's reviewed exact head showed zero difference; fresh strict typecheck (exit 0, on a clean `dist/` rebuild) and full regression (666/666 pass, exit 0) were re-confirmed on the exact merge commit.
- Rev54 reconfirmed the V5 Workstream A five-criterion finding remains `FINDING_PENDING_BRAIN_ADJUDICATION`, and that PR #19 remains an independent edge inheriting no approval from PR #20's merge.

## PR #21 (V5-PTN-001) rebased onto post-PR#20 main, per explicit Founder instruction

- With PR #20 merged, PR #21's exact-head evidence (bound to base `839aca0f6dd830c46dcf4576e46035c3fdbd9207`) was stale — Brain could no longer treat it as an exact-head review target once `main` advanced to `f03c917e66283fcd63736234d81b166465496d7c`. The Founder explicitly directed rebasing the existing branch onto current `main` rather than opening a new branch or PR, resolving any conflicts without scope expansion, re-running the full test/regression suite, and producing a fresh exact-head evidence package bound to the new base/head — with `MERGE_DISPOSITION: HOLD_MERGE` unchanged (no Founder/owner-gate action at this stage).
- `git rebase origin/main` on `claude/v5-ptn-001-partner-capability-admission` produced conflicts only in the three corridor documentation files (`CURRENT_STATE.md`, `V2_TO_V5.md`, `VERSION_EVOLUTION_MAP.md`) — every source and test file (`src/domain/partner-capability-admission.ts`, `tests/partner-capability-admission.test.ts`, `tests/v5-ptn-001-boundary-scan.test.ts`) applied cleanly with zero conflict, since PR #20's own changes never touched partner/capability code. The doc conflicts were resolved by reconciling both branches' independently-true additive history (PR #20's Rev52-correction/merge narrative plus PR #21's own V5-PTN-001 narrative) into one coherent, non-duplicated account — no new claim, scope, or acceptance-criteria coverage was introduced during resolution.
- See `docs/exec-plans/active/V5-PTN-001.md` for the new `BASE_SHA`/`HEAD_SHA` and the re-run test/typecheck evidence bound to them.

## PR #21 (V5-PTN-001) Rev55 F1 correction: explicit independent admission authority

- Brain Rev55 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 55`) reviewed the rebased exact head `9137450725bf3f0f78e7a119f48c1e57d7089e4b` on base `f03c917e66283fcd63736234d81b166465496d7c` and returned `BRAIN_REVIEW_RESULT: CHANGES_REQUIRED`.
- **F1**: independent admission authority/provenance was implicit, not modeled/enforced — `evidenceRef` + `reviewByAt` and a separate `admitPartnerCapabilityClaim` function did not prove the admission came from an authority independent of the claimant; V5 R22's "capability claim cannot self-certify" was therefore not yet structurally satisfied, only a caller convention.
- `AUTHORIZED_CORRECTION`: same PR #21 and same branch only, no new PR/branch, no merge (`FOUNDER_RELAY_REQUIRED: NO`, `OWNER_GATE: NONE / NOT OPEN`). The finding was independently verified against source before fixing (confirmed `admitPartnerCapabilityClaim` had no admitting-identity concept at all) — then corrected: a required, non-empty `admittingAuthorityId` is now fail-closed rejected whenever it equals the claim's own `partnerOrganizationId`, and the accepted authority is persisted as `admittedByAuthorityId` for audit. Added negative regression (H17: self-admission rejected, H18: empty authority rejected) and positive regression (H19: distinct-authority admission succeeds and is persisted).
- New exact head `e7013fac72989380568726f750518b9ddee39046` on the unchanged base `f03c917e66283fcd63736234d81b166465496d7c` (main did not move for this correction). `npx tsc --noEmit -p .` exit 0 on a clean `dist/` rebuild; `npm run test` **691/691 pass** (688 pre-existing + 3 new), independently re-verified on a fresh detached-HEAD checkout of the pushed remote head in an isolated worktree with fresh `npm ci`.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged. See `docs/exec-plans/active/V5-PTN-001.md` "Rev55 F1 correction" section for the full record.

## PR #21 (V5-PTN-001) Rev56: Brain PASS, Founder owner-gate OPEN

- Brain Rev56 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 56`) reviewed exact head `4b78e4b3cd6134b5da0db8e799d47a8bf8795134` on base `f03c917e66283fcd63736234d81b166465496d7c` and returned `BRAIN_REVIEW_RESULT: PASS`. `REV55_F1_CLOSURE: SATISFIED` — the corrected candidate makes admission authority/provenance explicit and auditable, rejects self-admission fail-closed, and adds negative/positive regression coverage. `SCOPE_CHECK: PASS`, `REGRESSION_CHECK: PASS` (691/691).
- `MERGE_STATUS: NOT MERGED`. `OWNER_GATE: OPEN` — Brain states explicit Founder authorization is required before PR #21 may be merged; `NEXT_EXPECTED_ACTOR: FOUNDER`.
- Consistent with this corridor's standing conduct rule (demonstrated on PR #18's Rev51 and PR #20's Rev54 merges): a document-recorded Brain PASS, even with an owner-gate note, is not itself a Founder authorization. Merging requires a separate, explicit Founder confirmation given directly in a live conversation turn in response to the specific exact-head merge question — never inferred from a document's claim alone. Live PR #21 GitHub state was independently re-verified to match the reviewed head/base exactly (draft, open, not merged, `mergeable_state: clean`) before surfacing this to the Founder. No merge has occurred as of this record; awaiting a live Founder decision.

## V5-PTN-001 Rev62 non-blocking hardening: revokedAt temporal-integrity fix (self-selected, no Brain input required to begin)

- Context: this branch (`claude/v5-ptn-001-revokedat-temporal-hardening`) was cut fresh from `main` at `fe2cbc02f20e800b309e127288cc32a72176c109`, the same base `AUD-DURABILITY-GAP` (PR #30) was cut from — the two corrections are independent and touch disjoint files.
- With PR #30 open and awaiting Brain review, and no new Handoff revision yet available, a further scan of Brain's still-open Rev62 "NON-BLOCKING / HARDENING OBSERVATIONS" found a second actionable, dependency-safe item.
- Brain's exact text (Rev62, verbatim): *"V5 partner capability `revokedAt` currently validates non-empty string but not timestamp/order relative to admission; retain as bounded temporal-integrity hardening unless a stronger canonical invariant promotes it."*
- Independently verified against source: `revokePartnerCapabilityClaim` in `src/domain/partner-capability-admission.ts` validated `revokedAt` only via `requireNonEmptyString` (not the module's own `requireValidTimestamp` helper, already used by `admitPartnerCapabilityClaim`) — so a non-timestamp value, or a `revokedAt` predating the claim's own `admittedAt`, was silently accepted.
- Fixed: `revokedAt` now validates via `requireValidTimestamp`, and is rejected if strictly before `admittedAt` (equal is accepted — an immediate revocation is not "before" its own admission). Three new adversarial/positive tests added.
- 708/708 tests pass (705 pre-existing on this base + 3 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-PTN-001.md` "Rev62 non-blocking hardening" section for full detail.
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; Status: `IMPLEMENTED / SELF-VALIDATED`, pending Brain independent exact-head review; Claude's authority ends here per `AGENTS.md` §10.
- `DOWNSTREAM`: independent of `AUD-DURABILITY-GAP` (PR #30); both may be reviewed/merged in either order without conflict (disjoint file sets).
- **Rev74 update:** Brain independently reviewed exact head `b0b748e3f85ebb80d2d3f4d6b682e7647d480ac9` and returned `PASS / MERGE AUTHORIZED UNDER STANDING REV57 ROUTINE-TECHNICAL POLICY`. Merged (squash) into `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`. PR #31 is now `CLOSED/MERGED`.

## Rev70/PR #25 merged; AUD-V5-GAP-02 floor implemented (post-F1-closure Cold-Start continuation)

- Brain Rev70 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 70`) authorized merging PR #25 (`V5-WRK-001` Rev60 F1 correction, `AUD-V5-01`) using exact-head fencing, `OWNER_GATE: NONE`/`FOUNDER_RELAY_REQUIRED: NO` — PR #25 was merged into `main` at `fe2cbc02f20e800b309e127288cc32a72176c109`, independently confirmed live via GitHub (`merged: true`, that commit is current `main`'s tip). This is the same base PR #30 (`AUD-DURABILITY-GAP`) and PR #31 (`V5-PTN-001` Rev62 non-blocking hardening) were already cut from — both independently verified `mergeable_state: clean` against it.
- With `AUD-V5-01` closed, Brain's Rev62 Handoff's own "AUTHORIZED CORRECTION ORDER — NO FOUNDER RELAY" step 3 continuation note directly authorized the next action without further Founder relay: *"After F1 closes, fresh dependency scan the V5 Cold-Start gap. The smallest likely next convergence slice is Commercial Order → Canonical Service/Recipe Resolution → Conditional Intake/Readiness, reusing existing repo primitives and creating no second orchestration system."* This was independently re-read in full from the Rev62 Handoff text (not accepted from a chat paraphrase) before selecting it, together with the underlying finding it closes — `AUD-V5-GAP-02`, "WEBSITE BUILD COLD-START NORTH-STAR NOT YET END-TO-END PROVEN": *"Commercial Order → Canonical Service Resolution: MISSING on audited main; no Commercial Order start/resolver found... the current WEBSITE_BUILD_v1 fixture starts from preconstructed tenant/customer/project/soldScope/evidence and therefore is not the required zero-history commercial-order cold start."*
- Implemented on a fresh task-scoped branch cut from merged `main` (`claude/v5-cold-start-commercial-order-resolution`, base `fe2cbc02f20e800b309e127288cc32a72176c109`): a new pure domain module `src/domain/commercial-order.ts` (`CommercialOrder` — deliberately no `projectId`, since a project does not exist yet at order time; `ServiceCatalogEntry`, reusing `OfferBlueprintVersion`/`DeliveryRecipe` identifiers verbatim rather than inventing a parallel product/SKU concept; `resolveCanonicalServiceFromOrder`, fail-closed to `UNRESOLVED_SERVICE` on no catalog match and throwing on an ambiguous catalog, never guessing a "closest" match) plus a genuine zero-history `WEBSITE_BUILD_v1` order fixture proving no `Project`/`SoldScope`/evidence is required to reach `RESOLVED`.
- **Same-checkpoint extension, same branch/PR**: Brain names this as one convergence slice ("Commercial Order → Canonical Service/Recipe Resolution → Conditional Intake/Readiness"), so rather than stop after the first link and idle on Brain review, the loop continued immediately (same precedent as the V3-ORG-001→V3-SLA-001 continuous chain) to close the remaining two. `src/domain/commercial-order-intake.ts` (new): `intakeSoldScopeFromResolution` closes the audit's own named "Conditional Intake" gap ("sold-scope/evidence/blueprint primitives exist but no order-driven intake compiler") — fails closed unless the caller's `blueprint` identity/version exactly matches the order's own resolution, then delegates entirely to the already-verified `createSoldScope`, duplicating none of its logic. `src/fixtures/website-build-v1-commercial-order.ts` gained `buildWebsiteBuildV1ColdStartFixture()`, proving the full chain end to end from one genuinely zero-history order — order → resolution → intake-compiled `SoldScope` → `compilePlan` → `validatePlan` returning `COMPLETE` — closing the audit's "Brief Completeness / Readiness: PARTIAL end-to-end" finding. Evidence-based admission (`evaluateReadiness`/`admitPlan`) and project-bootstrap-template composition (V5-BOOT-001) both remain explicitly out of scope, not silently absorbed — no customer evidence is fabricated to force an admission result, and bootstrap-template wiring stays a separate, untouched concern.
- **Third same-checkpoint extension, still same branch/PR**: closes the audit's *"Required Approval: PRESENT primitive / PARTIAL end-to-end"* finding by proof-by-composition alone — `tests/commercial-order-cold-start-admission.test.ts` (new, 2 tests) composes `createApprovalReference`, the existing `buildFullReadinessAssertions` test helper, `admitPlan`, `deriveOutcomeJobSpecs`, `admitJobs`, and `wireAdmittedOutcomeJobs` (all pre-existing, separately-tested primitives — none modified) on top of the same zero-history fixture's compiled plan, proving the chain reaches an `ADMITTED` plan and wires `DRAFT` `OutcomeJob`s given a valid approval and full readiness evidence, and proving the negative (no approval → `WAITING`, zero jobs). This does not claim "Delivery Closure" or "Authorized Execution" are closed — real job execution/worker invocation/QA-evidence/handover requires worker/effect capability this repository does not have, same restraint as V4 Workstreams C-H; jobs are proven to reach `DRAFT`, never further.
- `IMPLEMENTED / SELF-VALIDATED`, **729/729 tests pass** (705 pre-existing on this base + 13 resolution tests + 9 intake tests + 2 admission-chain tests), strict typecheck clean, zero new dependency, 6 new files total across all three rounds (825 insertions, 0 deletions/modifications to any existing file). See `docs/exec-plans/active/AUD-V5-GAP-02.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #25's or any other PR's grant.
- **Rev74 update:** Brain independently reviewed exact head `d2cd161e6c41bfed884fdd9feb894383ebd49271` and returned `CHANGES_REQUIRED`. F1: `resolveCanonicalServiceFromOrder` accepts a caller-supplied `ServiceCatalogEntry[]` with no admission/provenance/authority binding — a caller can fabricate a `serviceRef` → blueprint/version/recipe mapping and obtain `RESOLVED`; this is deterministic caller-supplied lookup, not canonical resolution. Acceptance: bind resolution to an existing admitted/trusted repository-native catalog authority/provenance boundary and add a forged/unadmitted-mapping negative test, OR narrow the claim honestly to "deterministic caller-supplied lookup" and leave the canonical-resolution audit gap explicitly OPEN — do not create a second catalog/orchestration system. A repository-wide search confirmed no admitted/trusted catalog authority primitive exists anywhere in this codebase (the closest candidates, `OfferBlueprintVersion`/`DeliveryRecipe`, are validated-construction domain objects, not an admission/provenance system), so inventing one here would be exactly the forbidden "second catalog/orchestration system." The correction therefore takes the honest-narrowing path: see the "Rev74 correction" entry in `docs/exec-plans/active/AUD-V5-GAP-02.md`.

## SVC-ADM-001 closes the Rev74 canonical-resolution gap (Rev98/Rev101 Family 1)

- Condensed catch-up: since the Rev74 entry above, this repository progressed through Rev77 (renamed `resolveCanonicalServiceFromOrder`/`CanonicalServiceResolution` to `resolveDeclaredServiceFromOrder`/`DeclaredServiceLookupResult` on this same PR #32, naming-only) and Rev79 (matching exec-plan/PR framing correction), while numerous independent checkpoints landed on other branches: `V5-PTN-001` (PR #21/#25), `AUD-DURABILITY-GAP` (PR #30), `AUD-V5-AUTHORITY-INGRESS` (PR #33, confirmed `BLOCKED_BY_EXACT_PROTECTED_GATE`), `AKILTA-IP-001` (PR #34), `V5-LAB-001`/`V5-EVAL-001`/`V5-CMD-001` (PR #35-37), `CONN-001`/`SALE-TO-CLOSE` (PR #38/#39), `RUNTIME-001` (PR #40), `V5-PTN-002` (PR #41), `V4-EFF-001` (PR #42), `LOCAL-EXEC-001`/`002`, `OBS-TEL-001`, `AUT-OPS-001`, `DEP-ORCH-001` (PR #43-47) — none touch `commercial-order.ts`/`commercial-order-intake.ts` and are not duplicated here; see each branch's own docs for full detail.
- Handoff Rev101's "MANDATORY REMAINING FAMILIES" list names Family 1 verbatim: *"trusted canonical service/delivery knowledge boundary resolving commercial service identity to admitted service/version/intake/recipe/capabilities/work-plan inputs/acceptance/approvals/gap behavior."* This is the exact Rev74 F1 gap recorded above, still open.
- Implemented on `claude/svc-adm-001-trusted-service-catalog`, stacked on this same branch's `AUD-V5-GAP-02`/PR #32 exact head `8b95aa337da5e4fb5498f8ebf8f0b2ced8ae93bc`, since its core dependency (`commercial-order.ts`'s `CommercialOrder`/`ServiceCatalogEntry`/`DeclaredServiceLookupResult`) does not exist on `main`. `src/domain/service-catalog-admission.ts` composes those types unmodified (type-only imports, never redefined) with a new fail-closed admission gate:
  - `admitServiceCatalogEntry` is the sole construction path for a `ServiceCatalogAdmission` (mirrors `capability-admission.ts`'s single-gate discipline, no separate promotion step) — fails closed unless `catalogEntry.recipeId` exactly matches the supplied, separately-validated `recipe.recipeId`, and requires non-empty `admittedByAuthorityId`/`evidenceRef`/`admittedAt`.
  - `revokeServiceCatalogAdmission` is a terminal, one-way transition reusing `partner-capability-admission.ts`'s own Rev62 temporal-integrity discipline verbatim: `revokedAt` must not be strictly before `admittedAt` (equal is accepted as an immediate revocation).
  - `resolveTrustedServiceForOrder` composes `resolveDeclaredServiceFromOrder`'s own output (never re-implemented) with the admitted catalog and returns the Family-1-required gap disposition: `RESOLVED` only when exactly one currently-`ADMITTED` admission matches the declared lookup's exact blueprintId/blueprintVersion/recipeId triple (never a stale-version or `REVOKED` admission, never a different serviceRef's admission); `NOT_ADMITTED` — a first-class, honest disposition, never a thrown error — for a declared-but-unadmitted service; `UNRESOLVED_SERVICE` passed straight through when the declared lookup itself failed; throws (never guesses) on an ambiguous multi-admission match, matching `resolveDeclaredServiceFromOrder`'s own "throw rather than guess" convention.
- `tests/service-catalog-admission.test.ts` (new, SA1-SA16) and `tests/svc-adm-001-boundary-scan.test.ts` (new, 6 tests, including an explicit assertion that `CommercialOrder`/`ServiceCatalogEntry`/`DeclaredServiceLookupResult` are never redefined). `IMPLEMENTED / SELF-VALIDATED`, **755/755 tests pass** (733 pre-existing on this base + 22 new), strict typecheck clean, zero new dependency. `MERGE_DISPOSITION: HOLD_MERGE`.
- Build-hygiene note: this branch's `package.json` predated the `clean`-script fix already applied on later branches (`"clean": "rm -rf dist"` wired into `build`) — its absence surfaced live when a stale `dist/` directory carried over from switching branches caused 5 unrelated `DEP-ORCH-001` test failures on the first run here (those files/tests do not exist on this branch's source tree at all). Reapplied the same fix; `rm -rf dist` plus a fresh `npm run test` confirmed 755/755 with zero source changes needed.
- See `docs/exec-plans/active/SVC-ADM-001.md` for the full reasoning and evidence.

## CONN-001 (Integration & Credential Control Plane, Rev90) + SALE-TO-CLOSE addendum (Rev91/92/93)

- Following PR #25 (Rev60 F1, merged) and the Rev62 `revokedAt` hardening above, Brain Rev90 (independently fresh-read from the canonical Handoff document) returned `CHANGES_REQUIRED` bounded findings on PR #35/#36/#37 (each independently verified against source and fixed on its own branch — see those PRs' own exec-plan docs) and separately authorized a new task: `CONN-001` (Integration & Credential Control Plane), to be progressively elaborated from architecture/domain contract → persistence/secret-ref boundary → admin UI → generic adapters → prebuilt adapters → adversarial E2E, explicitly avoiding speculative provider breadth.
- `CONN-001` was implemented across five slices on branch `claude/conn-001-integration-control-plane` (PR #38), composing entirely on top of the already-merged, unmodified `connection-authority.ts` (V2-CDO-004): `integration-connector-catalog.ts` (connector descriptors, connection lifecycle, reconnection, health checks), `durable-connector-connection-store.ts` (optimistic-concurrency persistence), `integration-admin-view.ts` (structurally secret-free admin read-model), `generic-connector-definition.ts` (Generic Custom API/OAuth adapter contracts), and `prebuilt-connector-definitions.ts` (GitHub/OpenAI/Anthropic/Google AI/Google Drive endpoint definitions, with Google Workspace/Meta explicitly deferred as disclosed, not fabricated, scope boundaries). Brain Rev93 found one genuine bounded F1 (`reconnectConnectorConnection` missing a connector-identity check, allowing a revoked connection of one connector kind to be "reconnected" as a different connector kind) — independently verified against source and fixed with an adversarial test (K22) on the same branch. See `docs/exec-plans/active/CONN-001.md` (on that branch) for the full slice-by-slice reasoning and evidence.
- Rev91/92 added a **SALE-TO-CLOSE E2E acceptance floor** addendum on top of CONN-001: given an admitted, `VERIFIED` `ConnectionBinding` vouching for an external sale/order fact, idempotently/deterministically bootstrap or reconcile the AKILTA `Project`+`SoldScope`+`OutcomeJob`, preserve tenant isolation, survive duplicate delivery/replay/crash, and give cancellation/refund/chargeback facts an explicit governed disposition — without AKILTA itself becoming a payment/settlement engine.
- Per explicit self-instruction (this is a distinct mission from CONN-001's own connector/credential-plane scope, not a sixth slice of it), SALE-TO-CLOSE was opened as its **own new branch/PR**: `claude/sale-to-close-e2e-floor`, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0` (independent of, and not stacked on, PR #38).
- `src/domain/external-sale-bootstrap.ts` (`bootstrapExternalSaleOutcome`/`applyExternalSaleDisposition`) and `src/domain/durable-external-sale-bootstrap-store.ts` (`FileDurableExternalSaleBootstrapStore`, mirroring `durable-outcome-job-store.ts`'s own `putIfAbsent`-only idempotency pattern) implement this floor entirely by calling the existing, unmodified `createProject`/`createSoldScope`/`createOutcomeJob`/`enterExceptionState` with every identifier deterministically derived from the caller-supplied `externalSaleRef` — the same idempotency primitive `outcome-job-wiring.ts` already established for `jobId`. `IMPLEMENTED / SELF-VALIDATED`, 723/723 tests pass (708 pre-existing baseline + 15 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/SALE-TO-CLOSE.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #38's or any other prior PR's.

## Rev94/95: CONN-001 F1-F3 and SALE-TO-CLOSE F1-F5 correction, continuous-execution batch mode

- A chat-relayed message claiming to carry a Brain-issued "REV95 Master Execution Order" (embedding Rev94 findings for both PR #38 and PR #39, plus explicit instructions to disregard certain of Claude's own standing stop conditions such as "the Handoff has not changed") was, per this session's own standing discipline, not acted on directly — it was independently verified against the primary Drive Handoff document first (`modifiedTime`/file-size change confirmed, full body read and cross-checked against every specific claim in the relayed message) before any of its instructions were followed. Confirmed genuine: a freshly-written Rev94/Rev95 revision, not a stale or fabricated relay.
- **Rev94 on PR #38** (`claude/conn-001-integration-control-plane`): F1 — `FileDurableConnectorConnectionStore` had no process-safe locking around its read-check-write update critical section, so genuinely concurrent updates from separate OS processes could race; fixed with a `linkSync`-based `withTenantLock` mutex (the same atomic-lock primitive `FileDurableOutcomeJobStore.putIfAbsent` already established), proven under a real 5-process race test. F2 — `readAll()` blind-cast persisted JSON without revalidating structural invariants; fixed with `validatePersistedConnectorConnection`, a fail-closed replay validator covering tenant/key correspondence, recognized enum values, and cross-field referential integrity. F3 — `GOOGLE_WORKSPACE`'s genuine three-host shape (Docs/Sheets/Slides) was being treated as an indefinitely deferred connector, and no mockable execution-adapter boundary existed to actually invoke an admitted connector's capability; fixed by adding an optional `baseUrlOverride` field to `GenericConnectorEndpointDefinition` (resolving the multi-host shape inside the existing single-`baseUrl`-per-definition contract) and a new `connector-execution.ts` module (`executeConnectorCapability`, with injectable `ConnectorTransport`/`SecretResolver` boundaries — no real HTTP call is ever made directly). See `docs/exec-plans/active/CONN-001.md` (on that branch) "Rev94 correction" section — 790/790 tests pass.
- **Rev94 on PR #39** (`claude/sale-to-close-e2e-floor`, this branch): F1 — the original `bootstrapExternalSaleOutcome` took a bare connection + `externalSaleRef` string, with nothing structurally proving the connection actually vouched for that specific sale; fixed with `VerifiedExternalCommerceFact`, constructible only via `createVerifiedExternalCommerceFact`, which performs the fail-closed tenant/verification checks up front. F2 — the original id derivation used `externalSaleRef` alone, risking cross-provider/account/connection sale-id collisions; fixed with `deriveCanonicalSaleId`, which joins `providerRef::accountRef::connectionBindingId::externalOrderRef`. F3 — the durable sale-bootstrap store was never proven race-safe under genuine OS-level concurrency and blind-cast persisted records without revalidation; fixed with the same `linkSync`-based creation-lock pattern used elsewhere in this session plus `validatePersistedSaleBootstrapRecord`, proven under a real 5-process race test. F4 — no governed path existed to recover a job stuck in a temporary `BLOCKED`/`RECOVERING` exception state back into active execution; fixed by extending the previously closed/VERIFIED `outcome-job.ts` (explicitly authorized by Rev94/95 as a bounded, in-scope extension) with `recoverFromExceptionState`, reusing `authority.ts`'s existing `requireSameTenant`/`requireProtectedActionAuthorization` primitives, restricted to safe source/target state pairs so verification can never be bypassed and `ESCALATED`/`STOPPED` (governed terminal dispositions) can never be silently undone. F5 — a disposition (cancellation/refund/chargeback) against a job that already left the lifecycle (already `CLOSED` or already in any exception state) previously threw and silently dropped the fact; fixed with a try/catch fallback that records a `POST_LIFECYCLE_DISPOSITION_RECORDED:{kind}` audit event with no lifecycle rollback. See `docs/exec-plans/active/SALE-TO-CLOSE.md` "Rev94 correction" section — 741/741 tests pass.
- **Rev95 continuous-execution batch mode**: explicitly authorizes executing the maximum currently authorized dependency-safe, reversible, dark/internal batch of work (both PR corrections above, then the repo-to-runtime deployment foundation, then the next dependency-safe V2-V5 slice) continuously, without pausing between individual tasks/PRs to request Brain review of each one; independent Brain exact-head review of the entire batch is deferred to a single consolidated packet at the end. Completing a task, pushing an exact head, or a PR sitting in pending-Brain-review status are explicitly not, on their own, stop conditions during this batch. This does not relax any of the corridor's own standing Hard Stops (see above) — only the cadence of requesting individual Brain review is affected.
- `MERGE_DISPOSITION: HOLD_MERGE` on both PR #38 and PR #39 — unchanged; merge still requires its own separately-granted protected owner-gate.

## Rev96/97/98: post-foundation gap audit → V5-PTN-002

- With PR #38's Rev94 correction, PR #39's Rev94 correction, and RUNTIME-001 all complete, the canonical Drive Handoff document was fresh-read again per standing discipline (its `modifiedTime` had advanced past the last verified Rev95 read). It is now Rev98. Rev97 clarified that dependency-safe dark/internal engineering is not "intentionally waiting" and that PR #38 + PR #39 + the deployment foundation are not sufficient by themselves as a batch endpoint. Rev98 is a "REOPENED GAP AUDIT" listing twelve mandatory post-foundation disposition families (trusted canonical service/delivery knowledge boundary; internal/staff authenticated authority ingress; V5 partner routing completion; V4 Workstream D approval/effect/recovery/readback envelope; V4 Workstream C reference service-specific control adapter floor; provider-neutral observability/telemetry floor; V4 Workstream E cross-surface evidence/outcome reporting convergence; V4 Workstream F bounded operational optimization loop; V4 Workstream G account-manager/automation operating integration; V4 H + V5 J frontend convergence; V5 Workstream E deployment orchestration completion beyond basic scaffolding; Cold-Start First Customer convergence for Website Build) — explicitly a gap-closure list, not a rebuild list, with the instruction to fresh dependency-scan them and execute the largest coherent currently dependency-safe reversible batch, recording precise blockers for anything genuinely gated rather than leaving the whole list unexamined.
- Family 3 ("V5 Partner Routing Completion") was selected first: the most narrowly bounded, no dependency on the still-open families (2, 6, etc.), and a concrete historical reference to reconcile rather than design from scratch. Rev98's own text: *"Live main contains partner organization + capability admission but not partner-routing-decision.ts. Old PR #24 contains a historical implementation but is stale/non-controlling and must not be blindly merged. Reconcile its semantics against current main and current authority/effective-status rules; port/reimplement only the still-valid minimal delta on a fresh current-main branch, with tenant/client/project/capability/revocation/effective-status and fallback adversarial coverage."*
- Reconciliation against PR #24 found its `resolvePartnerRoute` modeled eligibility on capability admission alone, never checking §6's "explicit agency employee → client/account assignment" authority (`resolvePartnerClientAccess`) — an admitted organization-level capability claim never proves a specific partner employee is authorized against the specific tenant/customer/project a routing decision is actually for. `V5-PTN-002` (branch `claude/v5-ptn-002b-partner-routing-decision`, a fresh branch cut from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0` — deliberately not a reset of the stale PR #24 branch, which is left untouched as historical record) implements `resolvePartnerRoute` composing both `resolvePartnerCapabilityClaimStatus` and `resolvePartnerClientAccess` into one routing decision, with per-employee (not merely per-partner-organization) granularity. See `docs/exec-plans/active/V5-PTN-002.md` for the full reasoning and evidence — 737/737 tests pass (708 pre-existing + 29 new), strict typecheck clean, zero new dependency, sanity-checked (the new client-assignment check was temporarily removed and confirmed exactly the 5 tests proving that dimension then failed).
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate. Per Rev95's continuous-execution batch mode (still controlling per Rev97/98), independent Brain exact-head review is deferred to the one consolidated end-of-batch packet alongside PR #38, PR #39, and RUNTIME-001.
- Remaining Rev98 disposition families (1, 2, and 4 through 12) remain to be fresh dependency-scanned and executed as further dependency-safe units in continuation of this same batch.

## Rev101: bounded correction on PR #41 (V5-PTN-002) — self-routing identity semantics

- Brain's Rev101 independent exact-head review (fresh-read from the canonical Drive Handoff) reviewed PR #38 through PR #43's exact pushed heads and returned `CHANGES_REQUIRED, BOUNDED` on three of them; this entry covers PR #41's finding (the other two, PR #42 and PR #43, are corrected on their own branches per this repo's per-branch documentation-duplication convention — see each branch's own exec-plan/CURRENT_STATE entries).
- PR #41 F1 (exact head `87b777cc8747ea367d2a4d98f964525957fa9b21`): the original `partnerOrganization.partnerOrganizationId === decidedByOwnerId` self-routing check was never proven to compare values from the same semantic identity domain (`decidedByOwnerId` is a bare, unbranded, caller-supplied string with no repository-enforced binding to any identity type), and it never accounted for the candidate's own distinct `partnerEmployeeMembershipId`. A fresh repo-wide search for an existing delegation/cross-domain identity-mapping primitive found none (`project.ts`'s own `ownerRef` is equally bare and conceptually unrelated), so per Rev101's own explicit disjunction ("bind to an existing semantically compatible identity ... or narrow/remove the unsupported self-certification claim and fail closed where independence cannot be proven ... must not invent a new IAM"), the fix narrows the documented claim and widens the code: the equality check now covers both `partnerOrganizationId` and `partnerEmployeeMembershipId`, mirroring the exact same bounded opaque-identity-reuse discipline `admitPartnerCapabilityClaim`'s own Brain-PASSed self-admission guard (Rev55 F1) already uses, with doc comments corrected to stop overclaiming proof of independent authority. New test P24 proves a `decidedByOwnerId` equal to a candidate's employee-membership id (not its org id) is rejected exactly as the pre-existing org-id case (P6, retitled to the same bounded framing) already was. Sanity-checked by reverting the fix via `git stash` while keeping the updated tests, confirming exactly P24 fails (P6 still passes), then restoring and reconfirming 738/738 pass. See "Rev101 correction (F1 — self-routing identity semantics)" in `docs/exec-plans/active/V5-PTN-002.md`.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged; per Rev95/97/98/99/100/101's continuous-execution batch mode, independent Brain exact-head review of the corrected head is deferred to the one consolidated end-of-batch packet alongside the other five checkpoints (PR #38, PR #39, RUNTIME-001, V4-EFF-001 Rev101-corrected, LOCAL-EXEC-001 Rev101-corrected).

## CONN-001 implemented, slice 1 (Rev90 authorization — no Brain dispatch required to begin)

- Context: Rev90 (2026-09-10) returned bounded `CHANGES_REQUIRED` on PR #35/#36/#37 (all three corrected on their own branches - see the `V5-LAB-001`/`V5-EVAL-001`/`V5-CMD-001` sections above) and, in the same revision, authorized a large new continuation task: **CONN-001, Integration & Credential Control Plane**, with an explicit instruction not to stop for another Brain dispatch once the three corrections were pushed.
- CONN-001's 13-point spec is large (admin surface; prebuilt connectors; mandatory Generic Custom API/OAuth connectors; multi-instance tenant/project binding with cross-tenant fail-closed; `ConnectionRequirement -> ConnectionBinding -> SecretRef` boundary; `REQUIRED/REQUESTED/CONNECTED_UNVERIFIED/VERIFIED/DEGRADED/REVOKED` lifecycle plus test/health/reconnect/rotate/revoke; AI provider/model-catalog separation; optional OpenAPI import; strict commerce-ownership boundary). Rev90's own text: "progressively elaborate from architecture/domain contract -> persistence/secret-ref boundary -> admin UI -> generic adapters -> prebuilt adapters -> adversarial E2E, avoiding speculative provider breadth."
- This checkpoint is slice 1: new pure domain module `src/domain/integration-connector-catalog.ts`, composing entirely on top of the already-merged, already-`VERIFIED`/`PASS`/`CLOSED` `connection-authority.ts` (`V2-CDO-004`) rather than modifying or duplicating its `ConnectionRequirement`/`ConnectionBinding`/`SecretRef` boundary - verified unmodified by a dedicated boundary-scan test.
- Adds: a connector catalog (`ConnectorDescriptor`, closed prebuilt-kind list `GOOGLE_DRIVE`/`GOOGLE_WORKSPACE`/`OPENAI`/`ANTHROPIC`/`GOOGLE_AI`/`GITHUB`/`META` plus mandatory `GENERIC_CUSTOM_API`/`GENERIC_CUSTOM_OAUTH` with fail-closed cross-field auth-mode rules, `isAiModelProvider` for AI-provider/model-catalog separation); and the lifecycle operations CONN-001 names that `connection-authority.ts` itself does not provide - `rotateConnectorSecret` (demotes a `VERIFIED` binding to `DEGRADED` on rotation, never silently stays `VERIFIED` against an unverified credential), `reconnectConnectorConnection` (only from `REVOKED`, fail-closed cross-tenant ownership check, always a genuinely new binding id per this repository's "revoked requires an entirely new record, never resurrection" discipline), `recordConnectorHealthCheck` (pure observational recording only, never an automatic state transition). `revoke` reuses the existing, unmodified `transitionConnectionBinding(..., "REVOKED")` directly. `REQUIRED` is represented via the pre-existing `ConnectionRequirement`/`ConnectionBinding` split rather than a redundant new enum value.
- Explicitly deferred, not fabricated: real credential entry/OAuth exchange/live network calls, admin UI, prebuilt adapter implementations (the named prebuilt kinds exist only as catalog descriptors), OpenAPI import accelerator, persistence/durable storage, real AI model-catalog data, and the two mandatory E2E acceptance cases (both require the admin UI/adapter layer this slice deliberately does not build yet).
- Branch `claude/conn-001-integration-control-plane`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`). 734/734 tests pass (708 true pre-existing + 26 new: 21 lifecycle/catalog tests + 5 boundary-scan tests), strict typecheck clean, zero new dependency.
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; Status: `IMPLEMENTED / SELF-VALIDATED`, pending Brain independent exact-head review; Claude's authority ends here per `AGENTS.md` §10.
- `DOWNSTREAM`: independent of `V5-LAB-001`/`V5-EVAL-001`/`V5-CMD-001` and PR #30/#32/#33/#34 (disjoint files); may be reviewed/merged in any order. Per Rev90's own instruction, the next dependency-safe elaboration step (persistence interface and/or admin-UI scaffolding) continues without a further Brain dispatch.

### Rev93 correction on `CONN-001` slice 1 (exact head `b525f6f7f1d2da08041f142e3896337963a60e55`)

- Brain's exact-head review returned bounded `CHANGES_REQUIRED` (F1): `reconnectConnectorConnection` checked `REVOKED` status, matching requirement/ownership, and a genuinely new binding id, but never checked that the reconnect `connectorDescriptor.connectorKind` matched `previousInstance.connectorKind` - two descriptors sharing a declared capability could let a revoked provider-A connection be silently "reconnected" as provider-B.
- Fixed: fail-closed rejection on connector-identity mismatch, checked before any other reconnect work. One new adversarial test (K22).
- In the same revision, Brain also independently PASSed PR #37's `V5-CMD-001` fix (head `391b964d0fdb56540d7e39cde19317c4b2ea608f`) - Rev90/92's F1 finding is confirmed resolved there; no further action.
- Corrected full regression: **735/735 pass** (708 true pre-existing + 27: 22 K-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED (Rev93-corrected)**, pending Brain independent exact-head re-review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `ENGINEERING NEXT ACTION` per Rev93: push this correction, then continue automatically through the CONN-001 progressive sequence (slice 2) and, at the first dependency-safe integration point, implement the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor (P0 adversarial tests only), without a further Brain dispatch.

## CONN-001 slice 2: persistence interface (no Brain dispatch required to begin)

- Per Rev93's own instruction, immediately continued into CONN-001's next progressive-elaboration step ("persistence/secret-ref boundary") after the Rev93 F1 correction was pushed.
- New module `src/domain/durable-connector-connection-store.ts`: `DurableConnectorConnectionStore`/`FileDurableConnectorConnectionStore`. Unlike `durable-outcome-job-store.ts`'s put-once/never-mutated record, a `ConnectorConnectionInstance` genuinely changes state over its lifecycle, so this store supports real updates gated by optimistic concurrency (`save(instance, expectedVersion?)`) rather than blind overwrite - a missing/stale `expectedVersion` against an existing record is fail-closed rejected (`ConnectorConnectionVersionConflictError`), preventing a lost update between two concurrent lifecycle operations on the same binding.
- Reference implementation uses only `node:fs`/`node:path` (zero new dependency), one JSON file per tenant, mirroring the existing durable-store discipline in this repository (full-file reconstruction, no separate in-memory index to diverge). Cross-tenant isolation is structural via per-tenant file keying.
- The persisted `secretRef` can only ever be an opaque reference id per `connection-authority.ts`'s own `SecretRef` contract - persisting the full instance never persists real secret material.
- 7 new tests (M1-M7): create-at-version-1, conflict on an `expectedVersion` with no existing record, correct-version update (+1), adversarial lost-update prevention (missing/stale version rejected, record provably untouched), unknown-id/cross-tenant `get()` isolation, cross-tenant `list()` isolation, restart-safety.
- Full regression: **742/742 pass** (708 true pre-existing + 34: 22 K-tests + 7 M-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: admin read-model surface, then generic/prebuilt adapters, then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## CONN-001 slice 3: admin read-model surface (no Brain dispatch required to begin)

- Fresh-read the canonical Handoff before continuing (still Rev93 as of this checkpoint - no new revision) - confirmed via `modifiedTime` unchanged since the Rev93 read, and PR #38's live GitHub state unchanged since the slice-2 push.
- New module `src/domain/integration-admin-view.ts`: `buildIntegrationsAdminView` aggregates the connector catalog and stored connections into the "Settings -> Integrations / AI & Providers" admin read-model CONN-001's spec names.
- Same disclosed genuine boundary as `V5-CMD-001`'s `internal-command-projection.ts`: no internal/staff authentication concept exists anywhere in this repository, so this module has zero HTTP/session/route wiring; deliberately cross-tenant, matching that same precedent.
- Structural secret-masking guarantee: `IntegrationConnectionSummary` has no field capable of holding a `SecretRef` at all - not omitted by convention, but structurally absent from the type.
- 5 new tests (N1-N5), including an adversarial proof that a connection's real `secretRef` never appears anywhere in the serialized admin view (checked both structurally and via a full-JSON substring scan for the literal secret value).
- Full regression: **747/747 pass** (708 true pre-existing + 39: 22 K-tests + 7 M-tests + 5 N-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: generic/prebuilt adapters, then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## CONN-001 slice 4: Generic Custom API/OAuth connector definitions (no Brain dispatch required to begin)

- Fresh-read the canonical Handoff before continuing (still Rev93, no new revision - confirmed via unchanged `modifiedTime` and unchanged PR #38 live GitHub state since the slice-3 push).
- New module `src/domain/generic-connector-definition.ts`: pure, in-memory declarative data contracts for CONN-001's two mandatory generic connectors - no real HTTP call, no DNS resolution, no request ever executed.
- `GenericApiConnectorDefinition`: validated `baseUrl`, `authMode` restricted to `API_KEY`/`BEARER_TOKEN`/`BASIC`/`CUSTOM_HEADER` (`OAUTH2` structurally excluded), unique-`capabilityRef` endpoints, a `validationEndpointCapabilityRef` fail-closed required to reference a real declared endpoint (CONN-001's "Test Connection" acceptance point).
- `GenericOAuthConnectorDefinition`: validated `authorizationUrl`/`tokenUrl`, non-empty deduplicated `scopes`; `authMode` is `OAUTH2` by construction, not a settable field.
- `resolveEndpointForCapability` fail-closed throws for an undeclared capability (never returns `undefined`, matching `resolveConnectorDescriptor`'s own discipline); `bindGenericApiDefinition`/`bindGenericOAuthDefinition` fail-closed reject a definition attaching to the wrong connector kind, binding id, or auth mode.
- 11 new tests (P1-P11), including adversarial OAUTH2-on-API-connector rejection and multi-instance independence.
- Full regression: **758/758 pass** (708 true pre-existing + 50: 22 K-tests + 7 M-tests + 5 N-tests + 11 P-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: prebuilt adapters (still no real network call), then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## CONN-001 slice 5: prebuilt connector endpoint definitions (no Brain dispatch required to begin)

- Fresh-read the canonical Handoff before continuing (still Rev93, no new revision - confirmed via unchanged `modifiedTime` and unchanged PR #38 live GitHub state since the slice-4 push).
- New module `src/domain/prebuilt-connector-definitions.ts`: static, real, single-host endpoint/capability declarations for `GITHUB`, `OPENAI`, `ANTHROPIC`, `GOOGLE_AI`, and `GOOGLE_DRIVE` - still zero real HTTP calls, reusing slice 4's `validateConnectorEndpoints`/`requireAbsoluteHttpUrl` validators.
- `GOOGLE_DRIVE` carries real search/read/create/write/share capabilities on the Drive API v3's one stable host, directly satisfying the blueprint's "a real initial connector capability, not a decorative catalog card" requirement.
- `GOOGLE_WORKSPACE` and `META` are explicitly deferred, not fabricated: Workspace's Docs/Sheets/Slides capabilities span three genuinely distinct Google API hosts, which this module's one-`baseUrl`-per-definition shape cannot honestly represent without a genuine architecture decision (a per-endpoint base-URL override) this bounded slice does not unilaterally make; Meta exposes multiple materially different product APIs with no single canonical surface to default to. `resolvePrebuiltConnectorDefinition` fail-closed distinguishes "recognized, not yet defined" from "genuinely unrecognized."
- 7 new tests (Q1-Q7), including adversarial cross-connector capability-resolution isolation (no connector's capability ever resolves against another connector's definition).
- Full regression: **765/765 pass** (708 true pre-existing + 57: 22 K-tests + 7 M-tests + 5 N-tests + 11 P-tests + 7 Q-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: the two mandatory E2E acceptance cases (protected gate - need real credentials/admin-UI wiring), then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## Rev90-98: CONN-001/SALE-TO-CLOSE → deployment foundation → post-foundation gap audit → V4-EFF-001

- Brain Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38) and Rev91/92 added a `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39), both cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`. Rev93 found and fixed a bounded F1 on PR #38 (reconnection connector-identity gap). A chat-relayed "Rev95 Master Execution Order" was independently verified against the primary Drive Handoff document before being acted on. Rev94 found bounded findings on both PRs (PR #38 F1-F3: process-safe connector-connection-store concurrency, fail-closed replay validation, Google Workspace multi-host resolution + a mockable connector-execution boundary; PR #39 F1-F5: a verified external-commerce fact envelope, source-namespaced canonical sale identity, race-honest durable persistence, a governed temporary-failure recovery path in `outcome-job.ts`, and post-lifecycle disposition preservation) — both corrected on their own branches (see each branch's own exec-plan doc for the full record, per this repo's per-branch documentation-duplication convention).
- Rev95 authorized a continuous-execution batch mode (complete the authorized batch without pausing for individual Brain review between tasks/PRs; defer independent review to one consolidated end-of-batch packet) and named the next item: a repo-to-runtime deployment foundation. `RUNTIME-001` (branch `claude/runtime-001-deployment-foundation`, cut fresh from the same `main` base) implements provider-neutral `AppConfig`/health-check contracts, a `PostgresOutcomeJobStore` behind a deliberately async-native port (no live database/SQL driver dependency), a Cloudflare Worker Fetch-API adapter reusing the existing transport-agnostic `request-handler.ts` unmodified, and reversible deployment scaffolding (Dockerfile, `wrangler.toml.example`, build/test-only CI). See `docs/exec-plans/active/RUNTIME-001.md`.
- Rev97 then clarified that PR #38 + PR #39 + the deployment foundation are not by themselves a valid batch endpoint, and Rev98 ("REOPENED GAP AUDIT / MANDATORY POST-FOUNDATION DISPOSITION") lists twelve mandatory post-foundation gap-disposition families to fresh dependency-scan and execute the largest coherent currently dependency-safe batch from (not a rebuild list — reuse/extend existing primitives). Family 3, "V5 Partner Routing Completion," was implemented as `V5-PTN-002` (branch `claude/v5-ptn-002b-partner-routing-decision`): `resolvePartnerRoute` composes both existing partner-authority dimensions (capability admission via `resolvePartnerCapabilityClaimStatus`, and per-employee tenant/client/project access via `resolvePartnerClientAccess`) into one routing decision, reconciled against the stale historical PR #24 (which modeled capability admission alone) rather than blindly merging it. See `docs/exec-plans/active/V5-PTN-002.md`.
- Family 4, "V4 Workstream D — Approval/Effect/Recovery/Readback Envelope," was implemented next as `V4-EFF-001` (branch `claude/v4-eff-001-external-effect-envelope`): `src/domain/external-effect-envelope.ts` closes the missing generic layer between an authorized decision to act and an actual external-provider mutation — proposed intent, scoped authority/approval requirement, idempotency/retry classification, mandatory external correlation on every attempt outcome, a `NOT_STARTED`/`APPLIED`/`VERIFIED`/`FAILED`/`UNKNOWN`/`ROLLED_BACK` state machine, fail-closed no-blind-retry out of `UNKNOWN`, and readback-authoritative verification. Reuses `authority.ts`'s existing primitives exactly as `outcome-job.ts`'s own `recoverFromExceptionState` already established the pattern for, while remaining a distinct, standalone, unwired envelope — no second workflow engine, no real provider mutation. See `docs/exec-plans/active/V4-EFF-001.md`.
- `MERGE_DISPOSITION: HOLD_MERGE` on all five (PR #38, PR #39, RUNTIME-001, V5-PTN-002, V4-EFF-001) — normal task-scoped PRs against `main`; merge requires its own separately-granted protected owner-gate. Per Rev95, independent Brain exact-head review of all five is deferred to one consolidated end-of-batch packet.
- Remaining Rev98 disposition families (1, 2, and 5 through 12) remain to be fresh dependency-scanned and executed as further dependency-safe units in continuation of this same batch.

## Rev101: bounded correction on PR #42 (V4-EFF-001) — evidence/correlation lineage

- Brain's Rev101 independent exact-head review (fresh-read from the canonical Drive Handoff) reviewed PR #38 through PR #43's exact pushed heads and returned `CHANGES_REQUIRED, BOUNDED` on three of them; this entry covers PR #42's finding (the other two, PR #41 and PR #43, are corrected on their own branches per this repo's per-branch documentation-duplication convention — see each branch's own exec-plan/CURRENT_STATE entries).
- PR #42 F1 (exact head `d837e8c913ee4f7e9c9a5c6a57232c44d895d62a`): `verifyExternalEffectReadback` validated but discarded its `evidenceRef`, and `rollbackExternalEffectAttempt` overwrote the attempt's original `externalCorrelationRef` with the rollback's own correlation ref, permanently losing the original effect's correlation, while also discarding its own `evidenceRef`. Fixed on the same branch (`claude/v4-eff-001-external-effect-envelope`): three new additive optional fields on `ExternalEffectAttempt` (`readbackEvidenceRef`, `rollbackCorrelationRef`, `rollbackEvidenceRef`) so readback evidence, rollback evidence, rollback correlation, and the original effect correlation can all survive on the same record simultaneously — no existing field renamed or removed, no exported symbol added or removed. New test E29 proves the full `APPLIED`→`VERIFIED`→`ROLLED_BACK` chain retains all four references at once; E12/E13/E28 updated and E23/E24 corrected (they had asserted the pre-fix overwrite behavior). Sanity-checked by reverting the fix via `git stash` while keeping the updated tests, confirming the expected `tsc` compile failures on every new-field assertion, then restoring and reconfirming 743/743 pass. See "Rev101 correction (F1 — evidence/correlation lineage)" in `docs/exec-plans/active/V4-EFF-001.md`.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged; per Current Project State Rev95/97-100's continuous-execution batch mode and canonical Handoff Rev101's own dispatch, independent Brain exact-head review of the corrected head is deferred to the one consolidated end-of-batch packet alongside the other checkpoints (PR #38, PR #39, RUNTIME-001, V5-PTN-002 Rev101-corrected, LOCAL-EXEC-001 Rev101-corrected).

## Family 5 — Website-Build reference service-specific control adapters (CONN-001 + V4-EFF-001 merged)

- Rev98/Rev101's Family 5: *"V4 reference service-specific control-adapter floor for the Website Build path on top of CONN-001, using mocks/injected transports and no duplicate AI Commerce runtime,"* with the acceptance shape "READ→DIAGNOSE→RECOMMEND→DRAFT/PREVIEW→APPROVAL-BOUND APPLY→VERIFIED EFFECT, composing CONN-001 + V4 effect envelope." Handoff Rev101's own "EXECUTION REMINDER — REV101 / NO PARKING" appendix (added 2026-09-12) explicitly names "a local branch blocker" as not a stop condition — this family had been dispositioned `TRIGGER_NOT_MET` for exactly that reason (its two prerequisite branches were not merged to a common base), which is corrected here since both are this session's own draft branches and merging them is a reversible action within existing authority.
- Branch `claude/family-5-website-build-control-adapters` merges `claude/conn-001-integration-control-plane` (exact head `41b2b16a6e2ed6f63039d3486313e929f31d16c5`, Rev94-corrected) and `claude/v4-eff-001-external-effect-envelope` (exact head `c60210362ef9ce709abe4fcc27e4785542fe77ea`, Rev101-corrected) — both cut from the same `main` ancestor (`3226c76fa338e425e553638e5f5f48924182a1c0`), so every source/test file merged cleanly; the only conflicts were in this document and `CURRENT_STATE.md` (both additive, independent narratives, reconciled by keeping both in sequence with no content dropped).
- `src/domain/website-build-control-adapter.ts` implements the module: `service-capability-routing.ts`'s own doc comments record "no real provider adapter... exists anywhere in this repository yet," capping every route at `READ_ONLY` — this is that adapter's first real instance. Composes `ExecutionMaturity` (type-only, unmodified) with `connector-execution.ts`'s `executeConnectorCapability` and `external-effect-envelope.ts`'s intent/attempt lifecycle: `readWebsiteBuildSignal` (`READ_ONLY`, unmodified pass-through) → `diagnoseWebsiteBuildSignal` (`DIAGNOSE`, caller-declared severity/evidence bound to the exact prior signal — no real website-analysis intelligence exists in this repository, so severity is never inferred) → `recommendWebsiteBuildAction` (`RECOMMEND`, fails closed against recommending an action for a `HEALTHY` diagnosis) → `draftWebsiteBuildEffectIntent` (`DRAFT_PREVIEW`, `requiresApproval` hard-coded `true`, never caller-settable) → `startWebsiteBuildEffectAttempt` (`APPROVAL_REQUIRED`, unmodified pass-through, approval always mandatory) → `controlledApplyWebsiteBuildEffect` (`CONTROLLED_APPLY`, the only function calling the injected transport for a write — success → `APPLIED`; a `ConnectorExecutionTransportError` → `UNKNOWN`, never a guessed `FAILED`; every other connector error → `FAILED`). The final "VERIFIED EFFECT" step has no corresponding `ExecutionMaturity` literal, so it is not wrapped — callers use `verifyExternalEffectReadback` directly, proven end-to-end by a full-chain test.
- `tests/website-build-control-adapter.test.ts` (14 tests: W1-W14) and `tests/family-5-boundary-scan.test.ts` (6 tests). `IMPLEMENTED / SELF-VALIDATED`, **845/845 tests pass** (825 pre-existing on the merged base + 20 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-5-WEBSITE-BUILD-CONTROL-ADAPTER.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.

## Rev90-101 condensed narrative (carried forward from `claude/obs-tel-001-telemetry-floor`, cut fresh from `main`)

- Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38) and a `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39); Rev93/94 found and fixed bounded findings on both (process-safe concurrency, fail-closed replay validation, a mockable connector-execution boundary; a verified external-commerce fact envelope, source-namespaced canonical sale identity, race-honest durable persistence, governed temporary-failure recovery, post-lifecycle disposition preservation).
- Rev95 authorized continuous-execution batch mode and named a repo-to-runtime deployment foundation: `RUNTIME-001` (PR #40) implements provider-neutral `AppConfig`/health-check contracts, an async-native `PostgresOutcomeJobStore`, a Cloudflare Worker Fetch adapter, and reversible deployment scaffolding.
- Rev98 ("REOPENED GAP AUDIT") listed twelve mandatory post-foundation gap-disposition families. Family 3 ("V5 Partner Routing Completion") became `V5-PTN-002` (PR #41: `resolvePartnerRoute` composing capability admission + per-employee client access, reconciled against a stale historical PR #24). Family 4 ("V4 Workstream D") became `V4-EFF-001` (PR #42: `external-effect-envelope.ts`, an intent/approval/attempt/readback/recovery contract).
- Rev99/100 introduced and confirmed Founder approval for "AKILTA Local Execution" (device/subscription-worker execution, default OFF, no second IAM/router), naming `LOCAL-EXEC-001` through `LOCAL-EXEC-007`. `LOCAL-EXEC-001` (Phase L0, PR #43) implements `local-execution.ts` composing into the existing, unmodified `worker-routing-policy.ts`.
- Rev101 ("CONTINUOUS EXECUTION / NO PARKING") returned `CHANGES_REQUIRED, BOUNDED` on PR #41/#42/#43's exact heads (self-routing identity semantics; evidence/correlation lineage; device/worker ownership binding + declared-ref-list validation, respectively) — see each branch's own exec-plan for full detail. All three were independently sanity-checked and corrected. Per Rev101's own "AFTER BOUNDED CORRECTIONS" instruction, `LOCAL-EXEC-002` (Device Bridge MVP, Phase L1) was then implemented on a branch cut from PR #43's own corrected head: `local-execution-bridge.ts` implements the §17 control-channel protocol (message envelope, fail-closed replay/idempotency, enrollment challenge/proof, connection lifecycle, heartbeat, allowlisted-workspace-path resolution, lease/checkpoint consistency), composing L0's contracts unmodified.
- All six prior checkpoints (PR #38, PR #39, RUNTIME-001/#40, V5-PTN-002/#41 Rev101-corrected, V4-EFF-001/#42 Rev101-corrected, LOCAL-EXEC-001/#43 Rev101-corrected) plus LOCAL-EXEC-002 remain `HOLD_MERGE`, deferred to one consolidated Brain review packet.

## Rev98 Family 6: OBS-TEL-001 Provider-Neutral Observability/Telemetry Floor

- Continuing the fresh dependency scan across remaining Rev98 Families 1,2,5-12 plus LOCAL-EXEC-003..007, Family 6 was selected next: unlike `LOCAL-EXEC-003` (requires a fresh dependency-fit ADR against the *current* official Codex CLI/SDK — genuinely external, time-sensitive research) and the other remaining families (each dependent on this very floor or the still-open Family 2 trusted-ingress boundary), Family 6 is self-contained and is explicitly the prerequisite `V5-EVAL-001`'s own deferred post-adoption monitor is blocked on.
- Rev98's own text: *"Implement the smallest repo-native telemetry/health/evidence contract and read model needed to represent source, metric definition/unit/window, freshness, quality/confidence, missing-vs-zero and monitored state without fabricating provider data. No vendor monitoring account or live ingestion is required for the dark floor."*
- `OBS-TEL-001` (branch `claude/obs-tel-001-telemetry-floor`, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0` — an independent Rev98 family, unlike `LOCAL-EXEC-002`) implements `src/domain/observability-telemetry.ts`: `MetricDefinition`/`MetricWindow` (source/unit/window), `MetricObservation` with a `REPORTED`/`MISSING` presence discriminator enforcing "missing-vs-zero" (a reported `0` is valid and distinct from absence), caller-declared `MetricQuality`, computed asOf-relative `resolveMetricFreshness` (never stored, never clock-reading), a closed `MonitoringState` registration graph, and `projectMetricReadModel` composing all five dimensions with monitored state as a real visibility gate. See `docs/exec-plans/active/OBS-TEL-001.md` for the full reasoning and evidence — 741/741 tests pass (708 pre-existing + 33 new), strict typecheck clean, zero new dependency, sanity-checked (three representative adversarial guards were temporarily disabled together and confirmed exactly those three tests then failed).
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`. Per Rev95/97/98/99/100/101's continuous-execution batch mode, independent Brain exact-head review is deferred to the one consolidated end-of-batch packet alongside the other seven checkpoints.
- Remaining work: Rev98 disposition families 1, 2, 5, 7 through 12, plus LOCAL-EXEC-003 through LOCAL-EXEC-007.

## Family 7 — Cross-Surface Evidence/Outcome Reporting Convergence (Family 5 + OBS-TEL-001 merged)

- Rev98/Rev101's Family 7: *"cross-surface evidence/outcome reporting convergence using existing customer-evidence/cross-domain/effect evidence"* with acceptance shape "provenance/comparability/customer-safe projection." Had been dispositioned `TRIGGER_NOT_MET`, but independent re-verification found `cross-domain-intelligence.ts` (`V5-INT-001`) already merged into `main` — only `V4-EFF-001` and `OBS-TEL-001` actually needed merging, a two-branch merge rather than three. Per Handoff Rev101's "EXECUTION REMINDER — REV101 / NO PARKING" appendix, "a local branch blocker" is not a stop condition, and both remaining branches are this session's own draft branches.
- Branch `claude/family-7-cross-surface-evidence-convergence` merges `claude/family-5-website-build-control-adapters` (already containing CONN-001+V4-EFF-001) with `claude/obs-tel-001-telemetry-floor` (exact head `4117c715b5a0dfe79956f372f2b9e9ecd0247e10`) — both trace to the same `main` ancestor, so every source/test file merged cleanly; only doc conflicts, resolved by keeping both branches' independently-true narratives in sequence.
- `src/domain/cross-surface-evidence-convergence.ts` composes — type-only, never redefines — `CustomerEvidenceItem`, `ReconciledInsight`, `ExternalEffectAttempt`, and `MetricReadModel` onto one shared `ConvergedEvidenceDisposition` (`CONFIRMED`/`UNCERTAIN`/`CONTRADICTED`/`UNAVAILABLE`) via four pure per-surface converge functions, each preserving the exact original status/sourceRef for provenance. A merely `APPLIED` effect attempt converges to `UNCERTAIN` (mirrors the effect envelope's own readback-authoritative discipline, never `CONFIRMED` on a claimed-only outcome); `UNKNOWN` states never guess `FAILED`/`CONFIRMED`; `ROLLED_BACK` converges to `CONTRADICTED`. `buildConvergedEvidenceSnapshot` is pure aggregation over caller-supplied, already-converged items. `projectCustomerSafeEvidenceSummary`'s `CustomerSafeEvidenceSummary` has no field capable of holding `sourceRef`/`originalStatus` (structurally absent), and its `overallDisposition` is the pessimistic (most severe) aggregate — a single contradicted item is never hidden behind otherwise-good evidence; an empty snapshot is honestly `UNAVAILABLE`, never a fabricated `CONFIRMED`.
- `tests/cross-surface-evidence-convergence.test.ts` (20 tests: X1-X20) and `tests/family-7-boundary-scan.test.ts` (6 tests). `IMPLEMENTED / SELF-VALIDATED`, **904/904 tests pass** (878 pre-existing on the merged base + 26 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-7-CROSS-SURFACE-EVIDENCE-CONVERGENCE.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.

## Family 8 — Bounded Operational Optimization Loop

- Rev98/Rev101's Family 8: *"bounded operational optimization loop, distinct from V5-EVAL-001, with authority/risk/reversibility/policy/budget and stop/escalate behavior."* Had been dispositioned `TRIGGER_NOT_MET` ("no live telemetry to optimize against") — the same objection that would have wrongly blocked `OBS-TEL-001` itself, a pure caller-supplied-evidence contract. Reversed on the same basis as Family 5/7: a decision function, not a live system.
- Distinctness from `V5-EVAL-001` independently verified by reading `governed-evaluation-loop.ts`'s exact source (`git show` against `origin/claude/v5-eval-001-governed-evaluation-loop`) before writing code: that module governs discrete, independently-reviewed *change proposals* (`PROPOSED→CLASSIFIED→TESTED→REVIEWED→ADOPTED/REJECTED`); this module governs one already-authorized policy's live, per-action operational execution loop. No shared type, no shared lifecycle.
- Branch `claude/family-8-operational-optimization-loop`, cut from `claude/family-7-cross-surface-evidence-convergence` (already carries this checkpoint's only non-`main` dependency, `observability-telemetry.ts`; `worker-routing-policy.ts`'s `WorkerRiskLevel` is already on `main`).
- `src/domain/operational-optimization-loop.ts` composes — type-only, never redefines — `WorkerRiskLevel` and `MetricReadModel` into `evaluateOptimizationCandidate`: a fixed-order fail-closed gate (telemetry honesty → risk-vs-policy → reversibility → budget) producing a closed `CONTINUE`/`STOP`/`ESCALATE` decision with closed, non-fabricated `OptimizationEscalationReason`/`OptimizationStopReason` enums (never free text). `MISSING`/`NOT_MONITORED`/`REPORTED_STALE` telemetry always escalates — reusing Family 7's own absence-is-not-positive-evidence discipline. `OptimizationPolicy` cannot be constructed without a non-empty `authorizedByWorkerId` (the "authority" leg of Rev98's five named dimensions).
- `tests/operational-optimization-loop.test.ts` (21 tests: O1-O21) and `tests/family-8-boundary-scan.test.ts` (6 tests). `IMPLEMENTED / SELF-VALIDATED`, **931/931 tests pass** (904 pre-existing on the Family 7 base + 27 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-8-OPERATIONAL-OPTIMIZATION-LOOP.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.
- Remaining Rev98 dispositions: families 1, 2, 9 through 12 (each still depends on the still-open Family 2 trusted-ingress boundary or on nearly everything, per Rev98's own text), plus LOCAL-EXEC-003 (implemented, PR #49) through LOCAL-EXEC-007.

## V5-CMD-001 implemented (Rev89 Founder continuation authorization, third slice — genuine boundary hit and disclosed)

- Context: third continuation slice under the same Rev89 Founder continuation authorization that selected `V5-LAB-001` and `V5-EVAL-001` (see those sections above). A fresh scan of the remaining V5 workstreams found Workstream J (§14, "V5 UNIFIED COMMAND / CUSTOMER PROGRESS FRONTEND") unimplemented.
- Live inspection of `src/web/session-context.ts` confirmed this repository has **no internal/staff authentication concept anywhere** — `AuthenticatedPrincipal`/`SessionContext`/`TenantContext` are exclusively customer/tenant-scoped, built for the customer-facing Client Portal (`V2-APP-001`/`V2-CDO-006`). §14's own required UX truths demand internal and customer projections stay separate; reusing the customer principal for an internal command surface would blur exactly that boundary, and inventing a new internal-principal concept is a genuine new architectural decision, not something a bounded floor slice should decide unilaterally.
- Implemented only the honest floor available without that decision: `src/domain/internal-command-projection.ts`, a pure, unwired read-model aggregation covering two of §14's eight named internal surfaces using already-merged `main` data — "Company / Portfolio Attention" (deliberately cross-tenant, from V4's `OperationsAttentionItem`, unlike the existing customer-scoped `resolveActiveOperationsAttention` filter) and "Partners" (from `V5-PTN-001`'s `PartnerCapabilityClaim`, with the never-stored `EXPIRED` disposition correctly computed via the existing `resolvePartnerCapabilityClaimStatus` rather than read off a raw field).
- This checkpoint records live routing and internal authentication as the genuine dependency boundary it stops at, per Rev89's own "continue automatically... until a genuine protected/dependency/capability boundary" instruction — not a completed §14, and not fabricated.
- Branch `claude/v5-cmd-001-internal-command-projection`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`) — deliberately independent of the still-unmerged `V5-LAB-001`/`V5-EVAL-001` branches (not stacked on my own not-yet-reviewed work). 773/773 tests pass (767 pre-existing + 6 new: J1–J6, including a genuine cross-tenant test built via the real `createOutcomeJob`/`enterExceptionState`/`buildAttentionState`/`toOperationsAttentionItem` pipeline, not hand-faked types), strict typecheck clean, zero new dependency.
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; Status: `IMPLEMENTED / SELF-VALIDATED`, pending Brain independent exact-head review; Claude's authority ends here per `AGENTS.md` §10.
- `DOWNSTREAM`: independent of `V5-LAB-001`, `V5-EVAL-001`, and PR #30/#32/#33/#34 (disjoint files); may be reviewed/merged in any order.

### Rev90 correction on `V5-CMD-001` (exact head `0d3380786d033371b0e41b79022d0c33034ffe35`)

- Brain's exact-head review returned bounded `CHANGES_REQUIRED` (F1): exposing the raw `PartnerCapabilityClaim[]` (`partners.claims`) alongside a separately-computed `statusTally` let the two silently disagree — a claim stored `ADMITTED` but past its `reviewByAt` counts `EXPIRED` in the tally while the claim's own `status` field still reads `ADMITTED`.
- Fixed: `partners.claims` replaced with `partners.items: ReadonlyArray<PartnerCapabilityClaimWithEffectiveStatus>`, pairing each raw `claim` with its `effectiveStatus` computed at the same `asOf` used for `statusTally` — the two can never contradict each other by construction. Module remains unwired; the no-internal-auth boundary is unaffected. Two new adversarial tests: J7 (raw status vs. effective status shown side by side, never one masking the other) and J8 (tally recomputed from `items` matches `statusTally` exactly).
- Also self-identified and corrected in this same round: the session-wide stale-`dist/` test-count contamination already disclosed on `V5-LAB-001`/`V5-EVAL-001` — this repository's true `main` baseline is **708**, not the 767/773 stated above; `package.json`'s `build` script now cleans `dist/` first on this branch too.
- Corrected full regression: **716/716 pass** (708 true pre-existing + 8: J1–J8), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED (Rev90-corrected)**, pending Brain independent exact-head re-review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged; per Rev95/97/98/99/100/101's continuous-execution batch mode, independent Brain exact-head review of the corrected head is deferred to the one consolidated end-of-batch packet alongside the other five checkpoints (PR #38, PR #39, RUNTIME-001, V5-PTN-002 Rev101-corrected, LOCAL-EXEC-001 Rev101-corrected).

## Rev99/100: Founder-approved Local Execution / Device Worker capability → LOCAL-EXEC-001 L0

- With the Rev98 batch continuing (PR #38/#39/#40 corrected/opened, V5-PTN-002 and V4-EFF-001 opened as PR #41/#42), the canonical Drive Handoff was fresh-read again (`modifiedTime` had advanced twice more, to `2026-09-11T11:06:23.408Z`, since the last verified Rev98 read). Rev99 is a genuine new Founder decision, not a chat relay: add an optional "AKILTA Local Execution" capability so the Founder and authorized staff can use local computers and already-authenticated supported AI coding clients (Codex, Claude Code) as execution workers, without paid API credentials as a prerequisite for every task. Default OFF, reversible, removable by a switch; never replacing the existing cloud/API/provider architecture, never a second IAM/workflow/router. Rev100 confirms explicit Founder approval, making this durable project scope — Master Roadmap now carries `LOCAL-EXEC-001` through `LOCAL-EXEC-007`.
- Rev99 names a dedicated architecture packet (Drive `1vhHCHZYu15IugytGb2O2epe7jgRFQF6vRoZsozjASDU`, "AKILTA — Local Execution Bridge / Subscription Worker System — Architecture & Execution Packet — R1") and explicitly requires it be fresh-read in full before any LOCAL-EXEC selection or implementation. That packet (756 lines) was read in its entirety before this checkpoint began, confirming Founder sign-off via its own R2 approval note.
- Per Rev100's own "CLAUDE CONTINUATION ORDER" ("do not interrupt a coherent currently executing branch solely to start Local Execution"), this work began only after the in-flight `V4-EFF-001` (PR #42) was already committed and pushed. The packet's own §21 Implementation Program names `LOCAL-EXEC-001` (Phase L0, "Contracts & Safety Floor") as "dependency-safe immediately when selected... No live PC connection, provider login or network service required for this floor" — the same pure-domain-contract-plus-adversarial-tests shape as every other checkpoint this session, and Rev100 explicitly authorizes implementing it "without another Founder prompt" if it is among the largest coherent dependency-safe slices.
- `LOCAL-EXEC-001` (branch `claude/local-exec-001-contracts-safety-floor`, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`): `src/domain/local-execution.ts` implements execution/collaboration/pool modes (Local default OFF, the three dimensions structurally independent), device registration/capability-snapshot/policy contracts (closed status transition graphs, workspace-root allowlisting, kill switch), a `LocalWorkerAdapter` port (no mock in domain code, mirroring `connector-execution.ts`'s own boundary discipline), and — critically, per the packet's own repeated explicit instruction — composes local workers into the existing, completely unmodified `worker-routing-policy.ts` (`toAdmittedWorker` projects a `LocalWorkerRegistration` into the exact `AdmittedWorker` shape; `resolveEligibleLocalWorkers` is the pool/tenant/project/execution-mode gate that runs before `resolveWorkerRoute` ever sees a candidate) rather than building a second router, plus a task lease/checkpoint contract. All ten of the packet's own mandatory acceptance cases (§22 A-J) are directly proven by test. `IMPLEMENTED / SELF-VALIDATED`, 755/755 tests pass (708 pre-existing + 47 new), strict typecheck clean, zero new dependency, sanity-checked (the cross-tenant isolation guard was temporarily removed and confirmed exactly the 1 test proving it then failed). See `docs/exec-plans/active/LOCAL-EXEC-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate. Per Rev95's continuous-execution batch mode (still controlling per Rev97/98/99/100), independent Brain exact-head review is deferred to the one consolidated end-of-batch packet alongside PR #38, PR #39, RUNTIME-001, V5-PTN-002, and V4-EFF-001.
- Remaining work: Rev98 disposition families 1, 2, and 5 through 12, plus LOCAL-EXEC-002 through LOCAL-EXEC-007 (Phases L1-L6), all fresh-mapped together as one continuous dependency-safe batch per Rev99/100's own explicit sequencing.

## Rev101: bounded correction on PR #43 (LOCAL-EXEC-001) — device/worker ownership binding + declared ref list validation

- Brain's Rev101 independent exact-head review (fresh-read from the canonical Drive Handoff) reviewed PR #38 through PR #43's exact pushed heads and returned `CHANGES_REQUIRED, BOUNDED` on three of them; this entry covers PR #43's findings (the other two, PR #41 and PR #42, are corrected on their own branches per this repo's per-branch documentation-duplication convention — see each branch's own exec-plan/CURRENT_STATE entries).
- PR #43 F1+F2 (exact head `01fa311a99106db534b085c5f71578ea44e64f76`): F1 — `createLocalWorkerRegistration`'s `ownerMembershipRef` was accepted independently of `input.device.ownerMembershipRef`, so a worker could be registered claiming an owner other than the device's own enrolled owner; existing tests never exercised the mismatch case because the shared `makeWorker` test helper always defaulted to the device's own owner. F2 — `declaredCapabilityRefs`/`declaredToolRefs`/`declaredPolicyConstraintRefs` were blind-cast with no array/non-empty-string-member validation, unlike this same file's own `createDevicePolicy`/`allowedWorkspaceRootRefs` convention.
- Fixed on the same branch (`claude/local-exec-001-contracts-safety-floor`): a fresh repo-wide search for an existing delegation/re-assignment primitive that could otherwise justify an owner mismatch found none, so per Rev101's own instruction the gap is closed by fail-closed rejection of any mismatch rather than inventing a new IAM primitive; a new private `requireStringRefArray` helper (mirroring `createDevicePolicy`'s own check exactly) validates all three declared ref lists. No duplicate-handling convention exists elsewhere in this codebase for such ref lists, so duplicates are left as supplied. New tests L42-L45 prove: an A-device/B-worker-owner substitution fails closed (L42), a matching owner still succeeds (L43), a non-array ref list on any of the three fields fails closed (L44), and a non-string/empty-string member fails closed while an empty array is accepted (L45). Sanity-checked by reverting the fix via `git stash` while keeping the updated tests, confirming exactly L42/L44/L45 fail (L43 still passes), then restoring and reconfirming 759/759 pass. See "Rev101 correction (F1 — device/worker ownership binding; F2 — declared ref list shape validation)" in `docs/exec-plans/active/LOCAL-EXEC-001.md`.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged; per Rev95/97/98/99/100/101's continuous-execution batch mode, independent Brain exact-head review of the corrected head is deferred to the one consolidated end-of-batch packet alongside the other five checkpoints (PR #38, PR #39, RUNTIME-001, V5-PTN-002 Rev101-corrected, V4-EFF-001 Rev101-corrected).

## Rev101 "AFTER BOUNDED CORRECTIONS" continuation: LOCAL-EXEC-002 Device Bridge MVP

- With all three of Rev101's bounded corrections (PR #41/#42/#43) applied, evidenced, and pushed, Rev101's own "AFTER BOUNDED CORRECTIONS" instruction was followed: fresh-read live main/CURRENT_STATE/open PR heads and dependency-scan the remaining Rev98 Families 1,2,5-12 plus LOCAL-EXEC-002..007. Rev101 itself explicitly names `LOCAL-EXEC-002` (Device Bridge MVP) and `LOCAL-EXEC-003` (Codex Local Adapter) as "explicitly dependency-safe dark/internal candidates" that "must not wait for Founder PC login" — the clearest, most directly-sequenced next unit.
- Because `LOCAL-EXEC-002` composes `LOCAL-EXEC-001`'s own `local-execution.ts` contracts directly (not an independent Rev98 family), this branch (`claude/local-exec-002-device-bridge-mvp`) is cut from PR #43's own Rev101-corrected head (`fe297fb5140ce89d5471dc0c7a220e31003ce429`) rather than from bare `main` — duplicating L0's entire contract set on a separate main-cut branch would be wasteful and contrary to the packet's own "extend/wire/correct" instruction.
- `src/domain/local-execution-bridge.ts` implements §17's control-channel protocol at the pure domain level: the message envelope/kind set (all nineteen §17 families), fail-closed replay/idempotency validation ("following existing AKILTA durability lessons"), one-time enrollment challenge/proof (composing L0's unmodified `markDeviceOnline`), a closed connection-lifecycle state machine (disconnect/reconnect), heartbeat processing (rejected outright for any non-`ONLINE` device), an allowlisted-workspace-path resolver (composing L0's unmodified `isWorkspaceRootAllowed`, plus fail-closed path-traversal/absolute-path guards), and lease/checkpoint message-identity consistency guards. Device-key and control-channel are ports only, matching `LocalWorkerAdapter`'s own established port-only discipline — no real transport, crypto, or provider SDK anywhere. See `docs/exec-plans/active/LOCAL-EXEC-002.md` for the full reasoning and evidence — 802/802 tests pass (759 pre-existing on this base + 43 new), strict typecheck clean, zero new dependency, sanity-checked (three representative adversarial guards were temporarily disabled together and confirmed exactly those three tests then failed).
- `MERGE_DISPOSITION: HOLD_MERGE`; this branch's own eventual PR is stacked on PR #43 (its base branch) rather than opened directly against `main`, since L0 is not yet merged. Per Rev95/97/98/99/100/101's continuous-execution batch mode, independent Brain exact-head review is deferred to the one consolidated end-of-batch packet alongside the other six checkpoints.
- Remaining work continues per Rev101's own "MANDATORY REMAINING FAMILIES" list: Rev98 Families 1,2,5-12, plus LOCAL-EXEC-003 through LOCAL-EXEC-007.

## Family 2 — Trusted Authenticated Internal/Staff Ingress, dark/internal boundary (Rev98/Rev101/Rev102)

- Rev98's Family 2: *"final trusted authenticated internal/staff ingress."* Had been mis-dispositioned globally `BLOCKED_BY_EXACT_PROTECTED_GATE` (PR #33's real finding — binding a genuinely authenticated caller identity requires an actual identity/session provider — was over-generalized to the whole family). Brain's Rev102 exact-head review of PR #47 corrected this: Rev101 already authorizes dependency-safe dark/internal Family 2 contracts, mocks, routes and tests before live identity-provider activation, and `LOCAL-EXEC-004` explicitly needs to reuse this exact boundary. See `docs/exec-plans/active/DEP-ORCH-001.md`'s "Family 2 disposition — CORRECTED per Rev102" for the full correction record.
- Branch `claude/family-2-staff-ingress-boundary`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`) — depends only on already-merged `organization-membership.ts` (V3-ORG-001) and `tenant-scope.ts`.
- Six new `src/web/` files mirror V2-APP-001's own proven customer-facing session/route-guard pattern (`AuthenticatedPrincipal`/`SessionContext`/`SessionProvider`/`route-guard.ts`/dev-fixture+production providers), structurally distinct for the staff identity domain (no shared types — a staff principal has no customer/tenant of its own, matching the documented reuse contract in `docs/architecture/DEPENDENCY_MAP.md` for `organization-membership.ts`'s `principalRef`): `staff-session-context.ts`, `staff-session-provider.ts`, `dev-fixture-staff-session-provider.ts` (mechanically production-guarded), `production-staff-session-provider.ts` (always unauthenticated — no real IdP admitted), `staff-route-guard.ts` (`requireStaffSession`, throws `StaffUnauthenticatedError`).
- `staff-membership-guard.ts` closes the actual remaining gap PR #33 named: `requireMatchingStaffMembership` (renamed from `requireCurrentStaffMembership` by the Rev106 correction, since `organization-membership.ts` carries no temporal-lifecycle field to prove currentness with) fails closed unless an authenticated `StaffSessionContext` resolves to exactly one tenant-scoped `OrganizationMembership` — never guesses on ambiguity, never treats a bare session token as sufficient authority. `AuthorityContext` composition (role→permission policy) is explicitly deferred as a real, later, undecided product decision, not fabricated.
- `tests/family-2-staff-ingress.test.ts` (16 tests: F2-1–F2-16) and `tests/family-2-boundary-scan.test.ts` (6 tests). `IMPLEMENTED / SELF-VALIDATED`, **730/730 tests pass** (708 pre-existing on fresh `main` + 22 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-2-STAFF-INGRESS-BOUNDARY.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.
- With this checkpoint, Family 10 (V4 H + V5 J frontend convergence, depends on Family 2) and Family 12 (Website Build Cold-Start convergence, depends on nearly everything including Family 2) can now be re-evaluated against a real (if dark/mocked) staff-ingress boundary, rather than remaining gated on a global block that no longer applies.

## Family 10 — Internal Operations/Command Frontend Convergence, wired to Family 2

- Canonical Handoff Rev103's "NEXT GRAPH" text names this next: "Rev98 Family 10 internal operations/command... frontend convergence as Family 2 becomes available." `internal-command-projection.ts` (V5-CMD-001, V5 Workstream J §14) itself recorded its own stopping point verbatim: "this repository has no internal/staff authentication concept anywhere... Live routing and internal authentication remain explicitly open dependencies... not fabricated here." Family 2 now exists — this checkpoint is that wiring.
- Branch `claude/family-10-internal-command-access`, created by merging `claude/family-2-staff-ingress-boundary` (PR #53) with `claude/v5-cmd-001-internal-command-projection` (Rev90-corrected) — both trace to the same `main` ancestor, clean merge on every source/test file; doc-only conflicts resolved by keeping both independently-true narratives in sequence. 738/738 tests pass on the merged base before this checkpoint's own new code.
- `src/web/internal-command-access.ts` — the access-control layer §14 itself named as its own missing piece: `requireInternalCommandAccess` (gates on Family 2's unmodified `requireStaffSession` + `requireCurrentStaffMembership`) and `resolveInternalCommandCenterView` (gates access, then delegates entirely to `internal-command-projection.ts`'s unmodified `buildInternalCommandCenterProjection`, adding no new attention/partner data source, no independent judgment, no persistence). Never imports the customer-facing `session-context.ts`/`session-provider.ts`/`route-guard.ts`, so internal command data can never be reached through a customer session by construction.
- `tests/family-10-internal-command-access.test.ts` (7 tests: F10-1–F10-7) and `tests/family-10-boundary-scan.test.ts` (6 tests). `IMPLEMENTED / SELF-VALIDATED`, **751/751 tests pass** (738 pre-existing on the merged base + 13 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-10-INTERNAL-COMMAND-ACCESS.md` for the full reasoning and evidence.
- Per canonical Handoff Rev105 ("RISK-BASED VERIFICATION + SAFE_MERGE"): this checkpoint gates access via authentication/organizational-membership identity binding, which Execution Contract §9 explicitly excludes from `SAFE_MERGE` (auth/IAM/authority) regardless of independent-verification outcome. `MERGE_DISPOSITION: HOLD_MERGE` — this branch is a merge of PR #53 and the V5-CMD-001 branch; its own eventual PR should be reviewed alongside both upstream source PRs as part of the one consolidated end-of-batch Brain review packet.

## CONN-001 + SALE-TO-CLOSE (Rev90-93) → Rev94/95 correction → RUNTIME-001

- Brain Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38, branch `claude/conn-001-integration-control-plane`) and Rev91/92 added a `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39, branch `claude/sale-to-close-e2e-floor`) — both cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`, both independent of each other. Rev93 found and fixed one bounded F1 on PR #38 (a reconnection connector-identity gap). See each branch's own `docs/exec-plans/active/CONN-001.md`/`SALE-TO-CLOSE.md` for the full slice-by-slice record (this repo's own per-branch documentation-duplication convention — this corridor doc's copy on `main` predates both PRs, since neither has merged yet).
- A chat-relayed "REV95 Master Execution Order" (embedding Rev94 findings for both PRs, plus continuous-execution batch-mode authorization) was independently verified against the primary Drive Handoff document before any of it was acted on — confirmed genuine, not a stale or fabricated relay. Rev94 found and this session fixed: PR #38 F1-F3 (process-safe connector-connection-store concurrency, fail-closed replay validation, `GOOGLE_WORKSPACE` multi-host resolution + a mockable connector-execution boundary) and PR #39 F1-F5 (a verified external-commerce fact envelope, source-namespaced canonical sale identity, race-honest durable persistence, a governed temporary-failure recovery path extending the previously-closed `outcome-job.ts`, and post-lifecycle disposition preservation).
- Rev95 authorized a continuous-execution batch mode (complete the authorized dependency-safe, reversible, dark/internal batch without pausing for individual Brain review between tasks/PRs; defer independent review to one consolidated end-of-batch packet) and named the next item in sequence: a repo-to-runtime deployment foundation, `RUNTIME-001` (this section).
- `RUNTIME-001` (branch `claude/runtime-001-deployment-foundation`, cut fresh from the same `main` base `3226c76fa338e425e553638e5f5f48924182a1c0`): provider-neutral `AppConfig`/health-check runtime contracts (`src/runtime/app-config.ts`, `src/runtime/health-check.ts`); a Node-hosting-target entry point (`src/runtime/node-server-entrypoint.ts`) reusing the existing, unmodified `createHttpServer`; a Cloudflare Worker Fetch-API candidate adapter (`src/runtime/cloudflare-worker-fetch-adapter.ts`) reusing the existing, unmodified, already transport-agnostic `request-handler.ts` (ADR 0001); a provider-neutral SQL execution port (`src/ports/sql-client.ts`) and a deliberately distinct, async-native `AsyncOutcomeJobStore` port (`src/ports/async-outcome-job-store.ts`, explicitly NOT a drop-in for the existing synchronous `DurableOutcomeJobStore`), implemented by `src/domain/postgres-outcome-job-store.ts` against an injected `SqlClient` with fail-closed replay validation, plus a migration (`migrations/0001_outcome_jobs.sql`) — no live database, account, or SQL driver dependency anywhere in this checkpoint, matching the corridor's "no live account mutation" hard stop; and reversible, non-live deployment artifacts (`deploy/docker/Dockerfile`, `deploy/cloudflare/wrangler.toml.example`, a build/test-only `.github/workflows/ci.yml` with no deploy step). `IMPLEMENTED / SELF-VALIDATED`, 739/739 tests pass (708 pre-existing + 31 new), strict typecheck clean, zero new runtime dependency. See `docs/exec-plans/active/RUNTIME-001.md` for the full reasoning, explicit deferrals, and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` on all three (PR #38, PR #39, RUNTIME-001) — normal task-scoped PRs against `main`; merge requires its own separately-granted protected owner-gate, never inferred from any other PR's. Per Rev95, independent Brain exact-head review of all three is deferred to one consolidated end-of-batch packet.

## Rev98 gap-family disposition scan (LOCAL-EXEC-002, OBS-TEL-001, AUT-OPS-001, DEP-ORCH-001; Family 2 disposition corrected per Rev102)

- Per the Founder's explicit continuous-execution instruction, a required no-duplication dependency scan was run across Rev98 Families 1, 2, 5-12 plus `LOCAL-EXEC-002..007`, after independently re-confirming (via fresh `get_file_metadata` on the canonical Drive Handoff document) that no new Brain revision had arrived beyond Rev101 — the chat directive itself was treated as auxiliary/procedural, never as a substitute for that independent check.
- `LOCAL-EXEC-002` (Device Bridge MVP, PR #44, branch `claude/local-exec-002-device-bridge-mvp`, stacked on `LOCAL-EXEC-001`/PR #43's own branch since its core dependency `local-execution.ts` does not exist on `main` yet): `src/domain/local-execution-bridge.ts` — a full local-worker device-bridge protocol contract (registration handshake, heartbeat/liveness, command dispatch/result envelope) composing L0's `markDeviceOnline`/`isWorkspaceRootAllowed` unmodified. `IMPLEMENTED / SELF-VALIDATED`, boundary-scan enforces no crypto/network-socket library import (two doc-comment literal-string false positives on "crypto"/"WebSocket" fixed by rewording prose, never by weakening the scan regex, per the established LOCAL-EXEC-001 precedent).
- `OBS-TEL-001` (Telemetry Floor, PR #45, branch `claude/obs-tel-001-telemetry-floor`, cut fresh from `main`, Rev98 Family 6): `src/domain/observability-telemetry.ts` — the smallest repo-native metric-observation/read-model contract, with no fabricated provider data (per Rev98 Family 6's own explicit scope instruction) and no reuse dependency (a wholly self-contained new floor).
- `AUT-OPS-001` (Account/Operating Automation Integration, PR #46, branch `claude/aut-ops-001-account-automation-integration`, cut fresh from `main`): `src/domain/automation-operating-integration.ts` composes `ownership-assignment.ts`'s `resolveCurrentOwner`, `worker-routing-policy.ts`'s `WorkerRoutingDecision`/`AdmittedWorker`, and `organization-membership.ts`'s `OrganizationMembership`, all unmodified — boundary-scan test explicitly asserts `resolveWorkerRoute`/`createOwnershipAssignment` are never called from this module, and the design keeps human-membership vs. worker-id identity domains structurally separate via distinct types (never a runtime equality guess), explicitly avoiding the exact self-routing overclaiming pattern Rev101 F1 flagged on `V5-PTN-002`.
- `DEP-ORCH-001` (Deployment Orchestration Completion beyond RUNTIME-001, Rev98 Family 11, branch `claude/dep-orch-001-environment-config-cutover`, stacked on `RUNTIME-001`/PR #40's own branch at its exact head `c5c818688ab9e5f989305082f047370d89cc1dda`, since its core dependency `AppConfig` does not exist on `main` yet): `src/domain/deployment-orchestration.ts` composes `AppConfig`/`SecretRef` unmodified (type-only imports, never redefined — boundary-scan asserts this) to add: environment inventory (`registerDeploymentEnvironment` binds an opaque `environmentRef` to exactly one `AppConfig["deploymentEnv"]` tier and its `SecretRef` pointers); provider-neutral config drift (`resolveConfigDrift`, a pure caller-supplied `desired`-vs-`observed` `AppConfig` diff — this module never reads a live environment itself); a strictly closed cutover lifecycle (`createCutoverPlan` → `passPreflight`, which is the one required gate and fails closed unless `ConfigDriftReport.status === "NO_DRIFT"` → `markMigrationsApplied` → `markTrafficSwitched` → `verifyCutoverReadback`, with `ROLLED_BACK` reachable from any non-terminal state via `initiateRollback`); a readback-authoritative-over-claimed-outcome verification step (`verifyCutoverReadback`) modeled directly on `external-effect-envelope.ts`'s own `verifyExternalEffectReadback` discipline — a disproving readback corrects the plan to `ROLLED_BACK` rather than leaving it falsely advanced; and a provider-neutral DNS change-proposal lifecycle (`proposeDnsChange`/`withdrawDnsChangeProposal`, `PROPOSED`/`WITHDRAWN` only — no "applied"/"live" state anywhere, matching Rev98's own "without live deploy" scope exactly, and no hard-coded DNS/hosting vendor reference). `IMPLEMENTED / SELF-VALIDATED`, 767/767 tests pass (739 pre-existing on the RUNTIME-001 base + 28 new: 21 functional + 7 boundary-scan), strict typecheck clean, zero new dependency. Four adversarial guards (the preflight-drift gate, the out-of-order-transition guard, and both terminal-rollback guards) were sanity-checked by temporarily disabling them via comment-block (never `false &&`, which breaks TS control-flow narrowing) and confirming exactly those tests then failed, before restoring and reconfirming full green. See `docs/exec-plans/active/DEP-ORCH-001.md` for the full reasoning and evidence.
- **Family 2 disposition — CORRECTED per Rev102 (2026-09-12)**: the original entry here claimed the whole of Family 2 was `BLOCKED_BY_EXACT_PROTECTED_GATE`, reasoning from PR #33's (`AUD-V5-AUTHORITY-INGRESS`) own text that binding a real authenticated caller identity requires infrastructure this repository has never integrated. Brain's Rev102 exact-head review of PR #47 found this conflicts with controlling Rev101, which explicitly authorizes dependency-safe dark/internal Family 2 contracts, mocks, routes and tests before live identity-provider activation, and separately requires `LOCAL-EXEC-004` to reuse that staff-ingress boundary. **Corrected**: only real, live identity-provider credential/account activation remains `BLOCKED_BY_EXACT_PROTECTED_GATE` (admitting a new external provider is this corridor's own hard-stop category); the Family 2 dark/internal ingress/session/route boundary itself (a mockable/fake-provider-backed session contract plus an internal staff-route/guard layer, exactly the "real logic, mocked external dependency" pattern already used throughout this corridor) is executable now and must be selected as its own checkpoint when dependency-safe. Families 10/12 remain gated only on that dark/internal boundary's own not-yet-built state, not on live-IdP activation. Families 5 and 7 were deprioritized (not blocked) at scan time and have since both been implemented as their own checkpoints. See `docs/exec-plans/active/DEP-ORCH-001.md`'s "Family 2 disposition — CORRECTED per Rev102" section for the full record.
- All four new checkpoints remain `MERGE_DISPOSITION: HOLD_MERGE`, deferred to the one consolidated Brain review packet alongside PR #38, #39, RUNTIME-001, V5-PTN-002 (Handoff-Rev101-corrected), V4-EFF-001 (Handoff-Rev101-corrected), and LOCAL-EXEC-001 (Handoff-Rev101-corrected), per Current Project State Rev95/97-100's continuous-execution batch-mode authorization, reaffirmed and extended by canonical Handoff Rev101's own "CONTINUOUS EXECUTION / NO PARKING" dispatch and BATCH END CONTRACT.
## LOCAL-EXEC-004 — Phase L3: Staff/Team/Pool Wiring reusing Family 2 + V3 Membership/Authority

- Canonical Handoff Rev103 ("NEXT GRAPH, NOT A SINGLE NEXT PR") explicitly names `LOCAL-EXEC-004 staff/team/pool wiring reusing Family 2 + existing V3 membership/authority` as next-in-line now that Family 2 exists; `local-execution.ts`'s own Rev99-era doc comments named exactly this dependency ("`ownerMembershipRef`/`requestingOwnerMembershipRef` here are deliberately opaque caller-supplied strings, never resolved against any real authentication system").
- Branch `claude/local-exec-004-staff-binding`, created by merging `claude/family-2-staff-ingress-boundary` (PR #53) with `claude/local-exec-003-codex-adapter` (PR #49, the full LOCAL-EXEC-001→003 chain) — both trace to the same `main` ancestor, clean merge on every source/test file; doc-only conflicts in `CURRENT_STATE.md`/`V2_TO_V5.md` resolved by keeping both independently-true narratives in sequence. 857/857 tests pass on the merged base before this checkpoint's own new code.
- `src/web/local-execution-staff-binding.ts` — three thin composition functions, each resolving the caller's authenticated `StaffSessionContext` to a real, tenant-scoped `OrganizationMembership` via Family 2's unmodified `requireMatchingStaffMembership`, then calling the corresponding unmodified `local-execution.ts` function with that membership's own `membershipId` as the trusted `ownerMembershipRef`/`requestingOwnerMembershipRef`: `registerDeviceForAuthenticatedStaff`, `createLocalWorkerRegistrationForAuthenticatedStaff` (proven not to bypass `local-execution.ts`'s own Rev101 F1 device/owner-match guard — a different authenticated staff member's own resolved membership still fails closed against a device it doesn't own), and `resolveEligibleLocalWorkersForAuthenticatedStaff` (employee isolation now driven by real authenticated identity, not a bare caller string).
- `tests/local-exec-004-staff-binding.test.ts` (10 tests: L4-1–L4-10) and `tests/local-exec-004-boundary-scan.test.ts` (5 tests). `IMPLEMENTED / SELF-VALIDATED`, **872/872 tests pass** (857 pre-existing on the merged base + 15 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/LOCAL-EXEC-004.md` for the full reasoning and evidence.
- Per canonical Handoff Rev105 ("RISK-BASED VERIFICATION + SAFE_MERGE"): this checkpoint composes authentication/worker-authority identity binding, which Execution Contract §9 explicitly excludes from `SAFE_MERGE` (auth/IAM/authority) regardless of independent-verification outcome. `MERGE_DISPOSITION: HOLD_MERGE` — this branch is a merge of PR #53 and the LOCAL-EXEC-001→003 chain; its own eventual PR should be reviewed alongside all upstream source PRs as part of the one consolidated end-of-batch Brain review packet.

## LOCAL-EXEC-005 — Phase L4: Collaboration & Cross-Worker Handoff

- Canonical Handoff Rev103's own "NEXT GRAPH" text and the architecture packet's §21 both name `LOCAL-EXEC-005` (Phase L4) as following once L0's stable workspace/task/checkpoint primitives exist; §12-15 were fresh-read in full from the decoded architecture packet before any design (never paraphrased from memory), matching this session's established discipline.
- Branch `claude/local-exec-005-collaboration-handoff`, created by merging `claude/local-exec-004-staff-binding` (PR #54, the full LOCAL-EXEC-001→004 chain) with `claude/v4-eff-001-external-effect-envelope` (PR #42, Rev101-corrected) — both trace to the same `main` ancestor, clean merge on every source/test file; doc-only conflicts resolved by keeping both independently-true narratives in sequence. The merge exists specifically so this checkpoint's failover gate can reuse `external-effect-envelope.ts`'s real `ExternalEffectAttemptState` unmodified, per §15's own instruction ("Never automatically fail over an external effect when the effect state is UNKNOWN; use the V4 external-effect recovery/readback envelope first").
- `src/domain/local-execution-collaboration.ts` implements exactly §12-15's named contracts as pure composition over L0's unmodified `LocalTaskLease`/`LocalTaskCheckpoint`/`CollaborationMode`/`LocalWorkerRegistration` and V4-EFF-001's unmodified `ExternalEffectAttemptState`: a `CollaborationMode` transition gate with an honest one-way visibility disclosure (PRIVATE/ISOLATED_PROJECT → SHARED_ARTIFACT/SHARED_REPO requires an explicit preflight naming exactly what becomes visible; the reverse transition records, rather than pretends otherwise, that data already legitimately downloaded cannot be forced to be forgotten); a `WorkspaceBinding` contract (`LOCAL_FOLDER`/`GIT_WORKTREE_SHARED_REPO`/`AKILTA_ARTIFACT_SNAPSHOT`) with a `WorkspaceSnapshotPackage` for `SHARED_ARTIFACT` mode; a `SharedRepoLeaseCoordinator` that grants at most one concurrent writer per branch/worktree (fail-closed on a second concurrent claim) and a base/head verification gate a device must pass before continuing on a shared branch; a `TaskPacket` carrying exactly §12's named fields (goal/acceptance criteria, authority/protected gates, base/branch/SHA or snapshot identity, completed/remaining work, tests/evidence, blockers, next authorized action, prior worker's final summary — never raw hidden reasoning) and a `rehydrateTaskPacketForHandoff` function implementing §13 ("do not copy chat UI" — same-provider/same-device resumes a native thread reference; any other combination always produces a fresh hydration from the TaskPacket + workspace + evidence, never a conversation-database copy); and `resolveLocalExecutionFailover` implementing §15's exact failover sequence (publish checkpoint → end lease → mark DEGRADED/USAGE_LIMITED/OFFLINE → resolve next candidate through L0's unmodified `resolveEligibleLocalWorkers`/`toAdmittedWorker`, same requirements, no authority relaxation → record transition evidence), fail-closed whenever the external-effect state is `UNKNOWN` (§22 case I) and fail-closed against routing to another employee's non-`ORG_POOL` worker.
- `tests/local-exec-005-collaboration-handoff.test.ts` and `tests/local-exec-005-boundary-scan.test.ts`. Explicitly deferred, not fabricated: no real Git plumbing, no real snapshot/diff byte encoding, no live device transport, no HTTP route wiring, no `AuthorityContext` composition — this is the domain contract layer only, matching every other LOCAL-EXEC phase's own established scope discipline.
- Per canonical Handoff Rev105: this checkpoint composes worker/task/device identity and the same authority-adjacent primitives as every other LOCAL-EXEC checkpoint, so it is `SAFE_MERGE`-excluded (auth/IAM/authority) regardless of independent-verification outcome. `MERGE_DISPOSITION: HOLD_MERGE` — this branch merges PR #54 and PR #42; its own eventual PR should be reviewed alongside both upstream source PRs as part of the one consolidated end-of-batch Brain review packet.

## Brain Rev106 periodic HIGH-RISK sweep — corrections to PR #53/#54/#55

- Rev105 removed Brain from routine LOW/MEDIUM verification but kept it responsible for periodic HIGH/PROTECTED sweeps. Rev106's sweep found three genuine, independently-verified findings across Family 2/LOCAL-EXEC-004/Family 10, cited exact SHAs matching this session's own real commits.
- PR #53 (Family 2) F1: `requireCurrentStaffMembership`/`resolveCurrentStaffMembership` and surrounding docs/errors claimed a "current" `OrganizationMembership`, but `organization-membership.ts` was independently re-verified to carry no active/revoked/temporal-lifecycle field at all (`{ membershipId, tenantId, principalRef, role }` only). Fixed by renaming to `requireMatchingStaffMembership`/`resolveMatchingStaffMembership` and narrowing docs/error text to the provable fact (exactly one matching caller-supplied membership; currentness provenance remains external/open) — no second IAM primitive invented, no behavior change to the underlying filter/throw logic. See `docs/exec-plans/active/FAMILY-2-STAFF-INGRESS-BOUNDARY.md`.
- PR #54 (LOCAL-EXEC-004) F1, HIGH-RISK: `createLocalWorkerRegistrationForAuthenticatedStaff` forwarded caller-controlled `trustStatus`/`authorityLevel` unchecked into `createLocalWorkerRegistration`, letting a merely authenticated organization member mint an `ADMITTED`/`ELEVATED` local worker — a real privilege-escalation bug. A fresh search for a reusable admission/authority-grant primitive (mirroring `service-catalog-admission.ts`'s own `authorizingWorker: AdmittedWorker` gate) found none applicable without inventing a cross-domain `OrganizationRole`→authority mapping, which Rev106 explicitly forbids. Fixed per Rev106's own fallback: `trustStatus`/`authorityLevel` are no longer caller-supplied parameters at all — a new worker is always born `UNTRUSTED`/`STANDARD`, mirroring `registerDevice`'s own "never born admitted" discipline. New adversarial test proves an authenticated ordinary member can never self-elevate. See `docs/exec-plans/active/LOCAL-EXEC-004.md`.
- PR #55 (Family 10) F1, HIGH-RISK: `requireInternalCommandAccess` proved only a matching tenant-scoped membership, then `resolveInternalCommandCenterView` delegated to `internal-command-projection.ts`'s own intentionally cross-tenant, company-wide aggregation with no authority/permission policy enforced — a real cross-tenant data-exposure bug. Independently re-verified that `authority.ts`'s own `AuthorityContext` is itself hard tenant-scoped (`CrossTenantAuthorityError`), so it does not honestly extend to cross-tenant authorization. Fixed per Rev106's own fallback: `resolveInternalCommandCenterView` now filters `attentionItems` to the resolved membership's own `tenantId` and always returns an empty `partnerClaims` array (no tenant field exists on `PartnerCapabilityClaim` to filter honestly). `internal-command-projection.ts` itself is untouched. See `docs/exec-plans/active/FAMILY-10-INTERNAL-COMMAND-ACCESS.md`.
- Each fix was sanity-checked by temporarily reverting it and confirming exactly the tests proving that dimension then failed, then restoring and reconfirming full regression. All three fixes were pushed in place on their original PR branches (not new branches), preserving each PR's own history/review context. `claude/local-exec-005-collaboration-handoff` (PR #56) subsequently merged the corrected LOCAL-EXEC-004 head in as a follow-up, since its own base had moved.

## LOCAL-EXEC-006 — Phase L5: Claude Local Adapter

- §21's Implementation Program names `LOCAL-EXEC-006 / Phase L5` as explicitly provider-policy gated, requiring a genuinely current recheck rather than training-time assumptions. A dedicated policy-research pass (2026-09-12) originally found **`POLICY_SAFE`** for local, single-machine, officially-authenticated `claude -p` CLI automation — Anthropic's own documented headless-mode surface; subscription billing unchanged after a proposed then cancelled June 2026 credit-pool split; not evaluated for, and does not imply safe, any future distributed/shared-backend use of a subscription session. **Superseded by Rev110/Rev112 (2026-09-14): current disposition is `POLICY_REVIEW_REQUIRED`, activation-gated** — see "Brain Rev109 periodic HIGH-RISK sweep" below for the correction. Full sourced research recorded in `docs/architecture/ADR/0003-local-exec-006-claude-adapter-fit.md`, mirroring the exact discipline ADR-0002 established for Codex.
- Branch `claude/local-exec-006-claude-adapter`, cut from `claude/local-exec-005-collaboration-handoff`'s own head (the most complete existing LOCAL-EXEC chain, already carrying the Rev106-corrected LOCAL-EXEC-004) rather than a separate `main`-cut branch.
- `src/domain/claude-local-adapter.ts` mirrors LOCAL-EXEC-003's own `codex-local-adapter.ts` shape exactly, composing L0's `DevicePolicy`/`isWorkspaceRootAllowed`/`LocalTaskLease`/`LocalTaskCheckpoint`/`createLocalTaskCheckpoint`/`LocalWorkerAdapterOutcome`/`LocalWorkerAdapterResult`/`DeviceCapabilityReadiness` and L1's `BridgeProtocolMessageKind`/`isRecognizedBridgeMessageKind` unmodified: a structural `ClaudeAdapterPolicyCheck` admission gate (the packet's own "represent policy compatibility as a capability admission gate" requirement, not left as ADR prose); the CLI's real six `ClaudePermissionMode` values; `ClaudeAuthReadinessStatus`/`resolveClaudeRunReadiness` (fails closed whenever the policy check is `POLICY_BLOCKED`, checked strictly before auth readiness — a blocked policy short-circuits before even a malformed auth value is inspected); `ClaudeRunRequest`/`createClaudeRunRequest` (START/RESUME semantics, workspace validation via the existing gate, explicit `allowedToolRefs`); the same dual-window usage-limit shape Codex uses; a closed `ClaudeRunEventKind` projection onto the existing bridge protocol; and `resolveClaudeAdapterCapabilityReadiness`, which composes the policy/auth/usage dimensions into L0's own existing `DeviceCapabilityReadiness` value set (which already reserved `POLICY_BLOCKED` for exactly this case).
- `tests/claude-local-adapter.test.ts` (34 tests) and `tests/local-exec-006-boundary-scan.test.ts` (9 tests). `IMPLEMENTED / SELF-VALIDATED`, **983/983 tests pass** (940 pre-existing + 43 new), strict typecheck clean, zero new dependency, sanity-checked by temporarily reordering the policy/auth checks in both readiness functions and confirming exactly the 3 tests proving that dimension then failed.
- No real Claude Code CLI process, Agent SDK client, or local session-file access anywhere in this module or its tests; no live installation/login authorized by this checkpoint. `MERGE_DISPOSITION: HOLD_MERGE` — cut from the LOCAL-EXEC-005 chain; reviewed alongside the full LOCAL-EXEC-001→005 chain as part of the consolidated end-of-batch Brain review packet.

## Brain Rev107 incremental re-review — wording-only corrections to PR #53/#55

- Rev107 re-reviewed the Rev106-corrected heads of PR #53 (Family 2) and PR #55 (Family 10) and confirmed the currentness/cross-tenant substance was fixed correctly, but found one remaining overclaiming sentence still live in each module's own doc comment — both wording-only, no behavior/export/test change.
- PR #53: `staff-membership-guard.ts`'s header still asserted a bound principal is "trusted for internal/staff action" — narrowed to identity/binding language only (binding to a membership record is not itself a grant of trust/authority for any action). See `docs/exec-plans/active/FAMILY-2-STAFF-INGRESS-BOUNDARY.md`.
- PR #55: `internal-command-access.ts`'s header still said "real, current `OrganizationMembership`" — replaced with `staff-membership-guard.ts`'s own exact wording (no temporal-lifecycle field exists to prove currentness with). See `docs/exec-plans/active/FAMILY-10-INTERNAL-COMMAND-ACCESS.md`.
- Each fix pushed in place on its original branch; each branch's own test suite passes unchanged (no assertion touched either fix's target sentence).

## Brain Rev108 exact-head review — corrections to PR #56 (LOCAL-EXEC-005), propagated to PR #57

- Rev108 reviewed LOCAL-EXEC-005's materially-advanced head and returned two real HIGH-RISK findings, both independently re-verified against source before fixing.
- F1: `resolveLocalExecutionFailover` accepted a bare checkpoint with no authoritative lease to bind it to — it could not prove the checkpoint belonged to the outgoing worker/device/tenant/task, nor that the lease was deterministically ended before a replacement was selected. Fixed with required `currentLease`/`expectedTaskRef` parameters, five fail-closed lineage checks (checkpoint↔lease, worker, device, tenant, task, then lease status), and an explicit lease-end via the existing `transitionLocalTaskLease` (`CHECKPOINTED` → `CANCELLED`) before routing — no new primitive invented.
- F2: `planCollaborationModeTransition` permitted a `WIDENING` transition with an empty visibility/resource set (an honest-disclosure gate that disclosed nothing), and `claimSharedRepoBranch` proved only mutual exclusion, never real per-worker repository authorization. Fixed by requiring non-empty visibility/resource lists on widening, and by reusing the existing `LocalWorkerRegistration.boundProjectOwnerships` field (via a new `isBoundToTargetOwnership` check) to gate branch claims to the worker's own bound project — no second IAM primitive invented.
- 12 new adversarial tests added; both fixes sanity-checked by reverting each in turn and confirming exactly the intended tests fail, then restoring and reconfirming full regression. **952/952 tests pass** on `claude/local-exec-005-collaboration-handoff` (PR #56); merged forward into `claude/local-exec-006-claude-adapter` (PR #57) to avoid base drift, **995/995 tests pass** there. See `docs/exec-plans/active/LOCAL-EXEC-005.md`'s own "Rev108 correction" section for full detail.
- Both PR #56 and PR #57 remain `HOLD_MERGE` — auth/IAM/authority-adjacent, `SAFE_MERGE`-excluded under Rev105 regardless of verification outcome; pending fresh Brain independent exact-head review of the corrected heads.

## Brain Rev109 periodic HIGH-RISK sweep — corrections to PR #56/#57

- Rev109 reviewed the Rev108-corrected heads of PR #56 (LOCAL-EXEC-005) and PR #57 (LOCAL-EXEC-006) and found F1/F2-A already resolved, with one remaining substantive finding on each PR.
- PR #56 F2-B: Rev108's `claimSharedRepoBranch` fix claimed `LocalWorkerRegistration.boundProjectOwnerships` was an existing authorization fact, but Rev109 found it is itself a bare caller-supplied parameter in LOCAL-EXEC-004's `createLocalWorkerRegistrationForAuthenticatedStaff` — an authenticated member can self-declare any project binding. `OwnershipAssignment` (V3-OWN-001) was considered and rejected as a reuse candidate (a business ownership role, not repository-access authority — the same cross-domain-mapping problem Rev106 already forbade). Fixed by making `SharedRepoBranchClaim.accessVerified` a structural `false` literal type: `claimSharedRepoBranch` remains coordination-only (mutex + declared-scope consistency), and no input can ever produce a claim asserting verified access. Real per-user/device repository-access admission stays explicitly open. See `docs/exec-plans/active/LOCAL-EXEC-005.md`'s "Rev109 correction" section.
- PR #57 F1/F2: the ADR's provider-policy sourcing was found insufficiently authoritative — the six `ClaudePermissionMode` values were corroborated only against this environment's own session-creation tool schema, and the Consumer Terms distinction (third-party credential reuse restricted, first-party CLI use not) was asserted without citation. Independently re-verified: a direct fetch of the official Claude Code permission-modes documentation confirms all six values by exact literal (satisfying Rev109's own stated fallback, so no narrowing was needed — only the sourcing was corrected), and a real, dated Anthropic compliance-page clarification (2026-02-20, covered by The Register) directly substantiates the ADR's existing Consumer Terms distinction. Both citations were added; no type/behavior change. See `docs/exec-plans/active/LOCAL-EXEC-006.md`'s "Rev109 correction" section.
- Both fixes pushed in place on their original branches (PR #56 directly; PR #57 by merging the corrected PR #56 head forward, then applying its own fix). 997/997 tests pass on `claude/local-exec-006-claude-adapter`. Both PRs remain `HOLD_MERGE`, `SAFE_MERGE`-excluded under Rev105, pending fresh Brain independent exact-head review.

## Brain Rev110/Rev112 — PR #57 F2 fix (POLICY_REVIEW_REQUIRED) and truth-surface correction

- Rev110's periodic HIGH-RISK sweep found ADR-0003's unconditional `POLICY_SAFE` conclusion too broad against official Anthropic Legal and Compliance documentation's Commercial Terms requirement for running Claude Code in a product/service. Fixed on `claude/local-exec-006-claude-adapter`: `ClaudeAdapterPolicyDisposition` extended to `"POLICY_SAFE" | "POLICY_REVIEW_REQUIRED" | "POLICY_BLOCKED"`, both readiness functions fail closed on `POLICY_REVIEW_REQUIRED` identically to `POLICY_BLOCKED`, 6 new adversarial tests added, 1002/1002 pass. See `docs/exec-plans/active/LOCAL-EXEC-006.md`'s "Rev110 correction" section for full detail.
- Rev112's exact-head re-review (2026-09-14, exact head `e53d64a4fc49db3333b43a8270ac9f987bd6bccf`) confirmed the source/behavior fix is correct and current, but found a bounded, docs+PR-metadata-only truth-surface conflict: this exec-plan, `CURRENT_STATE.md`, and the live PR #57 title/body still advertised `POLICY_SAFE` as current status, contradicting the corrected exact-head policy gate. Fixed by updating all three current-facing doc surfaces (this section included) and the PR #57 title/body to reflect `POLICY_REVIEW_REQUIRED` / activation-gated language, preserving the historical Rev109 narrative only where explicitly labeled historical/superseded. No source behavior changed by this correction.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged — provider-policy-sensitive/HIGH, `SAFE_MERGE`-excluded under Rev105.

## Claude escalation (2026-09-12): LOCAL-EXEC-007 / Phase L6 lacks a detailed task contract — deferred, not attempted

- Fresh-read the canonical architecture packet's §21 "LOCAL-EXEC-007 / Phase L6 — Hybrid / UX / Hardening" section in full before considering it as the next dependency-safe work unit (per this corridor's own rule that a version boundary is a capability checkpoint, not a permission stop, but selecting work still requires an actual detailed task contract to exist, not just a version name).
- Unlike every prior LOCAL-EXEC phase (L0-L5, each with named contracts/types enumerated in §12/§21 and, for L3-L5, an explicit "Dependency:" line), L6 is specified only as an unelaborated bullet list of workstream labels (routing UI, switch/migration preflight, kill switch, capability/health telemetry, device updater/signing strategy, installer/package paths per OS, optional sandbox strategy, "performance/security/soak tests once concrete runtime exists"), with zero named contracts and zero explicit dependency statement — and §23's own Claude execution/sequencing contract enumerates L0 through the Claude adapter (L5) but says nothing about L6 at all.
- Several of L6's own bullets are self-gated on infrastructure this repository does not have and this checkpoint could not honestly claim to satisfy as pure domain composition: actual UI (no "frontend" reference exists anywhere in the packet to compose against), real installer/packaging/signing, and soak tests explicitly deferred by the packet's own text until "concrete runtime exists." Implementing L6 as literally scoped would require inventing the missing named contracts unilaterally — outside Claude's role boundary (`CLAUDE.md`: "must not, independently ... expand product scope ... redefine canonical business requirements").
- Disposition: **not attempted this batch**; escalated in this execution record per `CLAUDE.md`'s escalation clause rather than resolved unilaterally. A narrower, honestly-scoped slice (e.g., a pure-domain projection reusing the already-existing `DevicePolicy` kill-switch field (§12) and the existing capability-health enum (§7/§18) into an observability view, explicitly excluding UI/installer/signing/sandbox/soak-test items) remains a candidate for a future checkpoint if Brain confirms that narrowing rather than a fuller L6 definition. Pivoting to Family 12 (Website Build Cold-Start convergence) instead, which the packet/Rev98 define with an actual named end-to-end chain to audit against live repo state.

## FAMILY-12-CLOSURE-APPROVAL-GATE — Rev98 Family 12: approval → closure glue

- Rev98's Family 12 names an end-to-end chain: *"verified sale/order -> trusted service/recipe -> intake/readiness -> bootstrap -> dynamic work plan -> admitted routing -> authorized execution -> QA/evidence -> approval -> closure, reusing existing segments and adding only missing glue/tests."* Rev103's NEXT GRAPH still names Family 12 as open. Before selecting a slice, a fresh repo-wide audit (2026-09-12) mapped every named segment against live repo state rather than re-implementing anything that already exists.
- Findings: readiness→plan-admission→outcome-job-wiring and evidence→verification→VERIFIED are both already composed and tested. Two other real gaps exist (admitted-routing↔authorized-execution are disjoint islands with no test chaining them; the sale/order and trusted-service segments live on three separate, never-merged sibling branches, and `external-sale-bootstrap.ts`'s own `bootstrapExternalSaleOutcome` bypasses trusted-service resolution and the plan-admission pipeline entirely) — each is its own separately-scoped follow-up, not attempted here. The one cleanly-scoped, low-blast-radius gap selected for this checkpoint: **approval → closure has no glue at all.** `outcome-job.ts`'s `MAIN_PATH_TRANSITIONS` lets any caller move a `VERIFIED` job straight to `CLOSED` via the generic `transitionOutcomeJob`; the existing SALE-TO-CLOSE E2E test (`tests/external-sale-bootstrap.test.ts` R10) empirically demonstrates this by doing exactly that with no approval object in scope.
- Branch `claude/family-12-closure-approval-gate`, PR #58 (open/draft), cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`) — no dependency on the in-flight LOCAL-EXEC/Family-2/10 chain.
- `src/domain/outcome-job-closure-approval.ts` is purely additive: `ClosureApprovalReference` mirrors `approval-reference.ts`'s own tenant/scope-binding discipline (Rev62 AUD-V2-01) but bound to job identity, not plan identity — reusing `ApprovalReference` directly was rejected since it is bound to a `ProjectPlanVersion` id/version/content-hash, and `OutcomeJob` carries no plan-linkage field to validate against; adding one would be an unreviewed change to AKI-BE-001's own already Brain-**VERIFIED/COMPLETED** canonical shape, outside this checkpoint's delegated envelope. `closeOutcomeJobWithApproval` requires a valid, exactly-matching approval before delegating the actual state transition entirely to the existing, unmodified `transitionOutcomeJob` — mirroring how `verifyOutcomeJob` is already a stricter, dedicated gate layered next to (not replacing) the generic table for `VERIFYING -> VERIFIED`. `outcome-job.ts` itself is untouched: its own `MAIN_PATH_TRANSITIONS` still contains the bare `VERIFIED -> CLOSED` edge and `tests/outcome-job.test.ts`'s existing assertion of it is unchanged — narrowing that edge would itself be a material behavior change to an already-verified primitive, which this checkpoint does not attempt.
- `tests/outcome-job-closure-approval.test.ts` (11 tests) and `tests/outcome-job-closure-approval-boundary-scan.test.ts` (5 tests). Explicitly deferred, not fabricated: no wiring into any existing orchestration call site (SALE-TO-CLOSE or otherwise) adopting the new gate is a separate, later integration step; no resolution of the other two Family 12 gaps found in this audit; no new IAM/authority primitive (`approverRef` is an opaque caller-supplied string, exactly like `ApprovalReference.approverRef`).
- `IMPLEMENTED / SELF-VALIDATED`, **1013/1013 tests pass** (997 pre-existing + 16 new), strict typecheck clean, zero new dependency. Sanity-checked by temporarily removing the approval-validity check and confirming exactly the 3 tests proving that dimension fail (the other two closure-guard tests, A10/A11, correctly still pass since `transitionOutcomeJob`'s own unmodified state-machine check independently covers those cases — clean isolation). See `docs/exec-plans/active/FAMILY-12-CLOSURE-APPROVAL-GATE.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`, pending Brain independent exact-head review.

## Brain Rev111 — FAMILY-12-CLOSURE-APPROVAL-GATE (PR #58) correction

- Rev111's periodic HIGH-RISK sweep (2026-09-14) found this checkpoint's gate was still bypassable in practice: `authorized-outcome-job-operations.ts`'s `authorizedTransitionOutcomeJob` — the real application-boundary wrapper any caller actually goes through — accepted `to: "CLOSED"` unconstrained, so any WRITE-permitted caller could close a `VERIFIED` job with no approval at all (F1). Separately, `ClosureApprovalReference.approverRef` proved only scope-binding, never a real admitted approval decision, and no customer-approval primitive exists anywhere in this repository to compose instead (F2).
- Fixed on `claude/family-12-closure-approval-gate`: `authorizedTransitionOutcomeJob` now fail-closed rejects `"CLOSED"` with a new `ClosureRequiresApprovalGateError`. A new `authorizedCloseOutcomeJobWithApproval` is the only authorized path to `CLOSED`, requiring the same protected-action authorization `authorizedVerifyOutcomeJob` already requires (reused, not duplicated) plus a valid `ClosureApprovalReference`. No new IAM invented; `approverRef` is documented as scope-binding-only, never independently verified. 10 new adversarial/boundary-scan tests, 734/734 pass.
- **Evidence-accuracy note**: this checkpoint's own original evidence claim above ("1013/1013, 997 pre-existing") does not match this branch's actual pre-existing count (724) — noted for the record, not further investigated, since it predates and is independent of the Rev111 fix.
- Sanity-checked in two independent steps (CLOSED-rejection reverted; protected-action requirement reverted), confirming exactly the expected tests fail in each case, then restored and reconfirmed 734/734. See `docs/exec-plans/active/FAMILY-12-CLOSURE-APPROVAL-GATE.md`'s "Rev111 correction" section for full detail. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.

## Claude's role inside the corridor (condensed — see Drive packet §5/§6/§12 for full text)

Claude is the delegated Engineering Brain + Primary Engineer inside the corridor: fresh-read and reconcile live repo truth before mutating; reuse existing patterns; choose the next dependency-safe work unit from the corridor-admitted graph (not roadmap prose at large); make reversible technical/architecture-within-envelope decisions; implement/test/self-review/fix/evidence/update state; continue across task and version boundaries when entry predicates are satisfied — without a routine per-task Brain/Founder start ceremony. Claude must not self-expand product scope, cross a material architecture envelope silently, or use implementation convenience to bypass a protected gate. Brain retains company/product strategy, canonical scope/version authority, material architecture-envelope decisions outside the delegated bounds, protected-gate classification, and independent/final verification.

## Rev90-101 condensed narrative (this branch, `claude/aut-ops-001-account-automation-integration`, cut fresh from `main`)

- Rev90 authorized `CONN-001` (PR #38) and `SALE-TO-CLOSE` (PR #39), later corrected (Rev93/94). Rev95 authorized continuous-execution batch mode and named `RUNTIME-001` (PR #40). Rev98 listed twelve mandatory post-foundation gap-disposition families: Family 3 became `V5-PTN-002` (PR #41), Family 4 became `V4-EFF-001` (PR #42). Rev99/100 confirmed Founder approval for "AKILTA Local Execution," naming `LOCAL-EXEC-001` through `LOCAL-EXEC-007`; `LOCAL-EXEC-001` (Phase L0, PR #43) implements `local-execution.ts`.
- Rev101 returned `CHANGES_REQUIRED, BOUNDED` on PR #41/#42/#43's exact heads — all three independently verified, fixed, sanity-checked, and pushed on their own branches. Per Rev101's own "AFTER BOUNDED CORRECTIONS" instruction, `LOCAL-EXEC-002` (Device Bridge MVP, PR #44, stacked on PR #43) and `OBS-TEL-001` (Rev98 Family 6, PR #45, independent) were then implemented and pushed.
- All eight prior checkpoints (PR #38, PR #39, RUNTIME-001/#40, V5-PTN-002/#41 Rev101-corrected, V4-EFF-001/#42 Rev101-corrected, LOCAL-EXEC-001/#43 Rev101-corrected, LOCAL-EXEC-002/#44, OBS-TEL-001/#45) remain `HOLD_MERGE`, deferred to one consolidated Brain review packet.

## Rev98 Family 9: AUT-OPS-001 Account-Manager + Automation Operating Integration

- Continuing the fresh dependency scan across remaining Rev98 Families 1,2,5,7-12 plus LOCAL-EXEC-003..007, Family 9 was selected next: unlike most remaining families (each dependent on one of PR #38-45's still-open branches or the still-open Family 2 trusted-ingress boundary), Family 9 composes only already-merged `main` modules (`ownership-assignment.ts`, `worker-routing-policy.ts`, `organization-membership.ts`) — no branch-stacking required.
- Rev98's own text: *"Reuse V3 AccountOwner/DeliveryOwner, attention and worker routing. Close only missing integration semantics for accountable owner on automation actions, explicit automation<->human handoff/escalation, manual fallback/support-burden observability and separation between human ownership and worker identity."*
- `AUT-OPS-001` (branch `claude/aut-ops-001-account-automation-integration`, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`) implements `src/domain/automation-operating-integration.ts`: `bindAccountableOwnerToAutomationAction` (never "ownerless" — requires a real active `ACCOUNT_OWNER`/`DELIVERY_OWNER`), a closed `AUTOMATED`/`HANDED_OFF_TO_HUMAN`/`COMPLETED`/`FAILED` lifecycle with `escalateAutomationActionToHuman` as the sole path to a governed handoff, and `resolveSupportBurden` (a durable fallback signal surviving eventual resolution). Human ownership and worker identity remain structurally separate throughout. See `docs/exec-plans/active/AUT-OPS-001.md` for the full reasoning and evidence — 731/731 tests pass (708 pre-existing + 23 new), strict typecheck clean, zero new dependency, sanity-checked (three representative adversarial guards were temporarily disabled together and confirmed exactly those three tests then failed).
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`. Per Rev95/97/98/99/100/101's continuous-execution batch mode, independent Brain exact-head review is deferred to the one consolidated end-of-batch packet alongside the other eight checkpoints.
- Remaining work: Rev98 disposition families 1, 2, 5, 7, 8, 10, 11, 12, plus LOCAL-EXEC-003 through LOCAL-EXEC-007.

## Autonomous execution loop (Drive packet §10)

`INSPECT → REASON/PLAN → IMPLEMENT → TEST → ADVERSARIAL SELF-REVIEW → FIX → VERIFY → EVIDENCE → UPDATE STATE → NEXT`, applied per dependency-safe unit.

## FAMILY-12-ROUTING-EXECUTION-CONVERGENCE — Rev98 Family 12: sibling-branch merge + admitted-routing → authorized-execution glue

- The `FAMILY-12-CLOSURE-APPROVAL-GATE` exec-plan's own audit found two further real Family 12 gaps beyond approval→closure, explicitly deferred as separately-scoped follow-ups: the sale/order and trusted-service segments living on three separate, never-merged sibling branches, and admitted-routing ↔ authorized-execution being disjoint islands. This checkpoint closes both.
- Branch `claude/family-12-sale-service-routing-convergence`: a clean, conflict-free three-way merge of `claude/sale-to-close-e2e-floor` (PR #39, SALE-TO-CLOSE), `claude/svc-adm-001-trusted-service-catalog` (PR #48, SVC-ADM-001), and `claude/v5-ptn-002b-partner-routing-decision` (PR #41, V5-PTN-002) — all three trace to the same `main` ancestor (`3226c76fa338e425e553638e5f5f48924182a1c0`) with zero source/test file overlap (confirmed via `git diff --name-only` before merging). 825/825 tests pass immediately after the merge, no new code.
- New pure composition module `src/domain/outcome-job-routing-execution.ts`: `resolveWorkerRoute`'s `WorkerRoutingDecision` carries no `OutcomeJob` identity, and `outcome-job.ts`'s own `MAIN_PATH_TRANSITIONS` lets any caller move a `READY` job straight to `EXECUTING` with no routing decision required or representable at that call site — a job could begin executing without ever having gone through admitted routing. `RoutedExecutionAssignment` binds a `ROUTED` decision to a specific job's tenant/customer/project/job identity (mirroring `outcome-job-closure-approval.ts`'s own scope-binding discipline); `authorizeOutcomeJobExecutionFromRouting` requires a valid, matching assignment before delegating the actual transition entirely to the existing, unmodified `transitionOutcomeJob`. `src/application/authorized-outcome-job-operations.ts` gains `authorizedTransitionOutcomeJobToExecutingViaRouting`, the properly-glued application-boundary path.
- Deliberately does NOT narrow the existing `authorizedTransitionOutcomeJob`'s own `EXECUTING` path the way the Family-12 closure-approval-gate correction narrowed `CLOSED` — no equivalent empirically-demonstrated bypass finding exists for `EXECUTING`, and the ordinary path is exercised throughout this repository's own existing, already Brain-reviewed test suites as ordinary non-routed work. This is purely additive glue. `PartnerRoutingDecision` binding is explicitly out of scope (a distinct domain concept with no job-execution semantics).
- `tests/outcome-job-routing-execution.test.ts` (11 tests) and `tests/outcome-job-routing-execution-boundary-scan.test.ts` (6 tests), plus 5 new tests in `tests/authorized-outcome-job-operations.test.ts`. `IMPLEMENTED / SELF-VALIDATED`, **847/847 tests pass** (825 pre-existing + 22 new), strict typecheck clean, zero new dependency. Sanity-checked by temporarily removing the fail-closed assignment check and confirming exactly the 5 tests proving that dimension then failed; restored and reconfirmed. See `docs/exec-plans/active/FAMILY-12-ROUTING-EXECUTION-CONVERGENCE.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE` — reviewed alongside PR #39/#48/#41 as part of one consolidated review packet.

## REV98-CORRIDOR-CONSOLIDATION — every mandatory gap family united into one tree

- A fresh `list_pull_requests` scan (2026-09-15) found that eleven of Rev98's twelve mandatory gap families already had real, implemented, self-validated PRs — each living on its own unmerged branch (in several cases itself a stack of multiple families). No single branch represented the corridor's actual combined state, so this checkpoint consolidates them.
- Branch `claude/rev98-corridor-consolidation`, cut from `claude/family-12-sale-service-routing-convergence` (Families 1/3/12), then sequentially merged: `claude/family-8-operational-optimization-loop` (CONN-001 + Families 4/5/6/7/8), `claude/family-10-internal-command-access` (V5-CMD-001 + Family 10), `claude/dep-orch-001-environment-config-cutover` (RUNTIME-001 + Family 11), `claude/aut-ops-001-account-automation-integration` (Family 9). Every merge step independently retested: 1070 → 1116 → 1175 → 1198/1198 pass. Only `CURRENT_STATE.md`'s own append-only narrative conflicted (three times, each resolved by keeping both entries in sequence); zero source/test conflicts across all four merges.
- No new domain logic beyond what `FAMILY-12-ROUTING-EXECUTION-CONVERGENCE` already added — this is purely a verified-clean composition of already-implemented, independently-authored work.
- Full Rev97-format disposition matrix (all twelve families now IMPLEMENTED/EVIDENCED) recorded in `docs/exec-plans/active/REV98-CORRIDOR-CONSOLIDATION.md`. `MERGE_DISPOSITION: HOLD_MERGE` — the single consolidated end-of-batch review packet Rev95's own "FINAL RETURN" text calls for.
## LOCAL-EXEC-003 (Phase L2, Codex Local Adapter) — dependency-fit ADR + implementation

- Rev101's "AFTER BOUNDED CORRECTIONS" continuation explicitly names LOCAL-EXEC-002/003 as proceedable "without Founder PC login," and its own LOCAL-EXEC-003 scope line requires "a fresh dependency-fit ADR against current official OpenAI Codex CLI/SDK/app-server behavior" — genuine, time-sensitive external research, not something this session may fabricate from memory or first principles.
- Branch `claude/local-exec-003-codex-adapter`, cut from LOCAL-EXEC-002's own head `c28349226c72f61e7990a716c1fa93afe099ff74` (not bare `main`), since this checkpoint directly composes L1's `local-execution-bridge.ts` protocol.
- `docs/architecture/ADR/0002-local-exec-003-codex-adapter-fit.md` records the sourced research (dated 2026-09-11, from `developers.openai.com/codex` and related public documentation): the App Server's bidirectional JSON-RPC protocol (server-initiated approval requests pause a turn until the client responds); the three real sandbox modes (`read-only`/`workspace-write`/`danger-full-access`) and three real approval policies (`untrusted`/`on-request`/`never`) as independent dimensions; network access off by default; session resume via `codex exec resume <id>`/`--last`/`--all` against sessions persisted under `~/.codex/sessions`; authentication via `codex login` (ChatGPT session) or `codex login --with-api-key`, with `codex login status` reporting readiness locally and the resulting auth state never leaving the device; usage tracked in a rolling 5-hour window plus a weekly quota.
- `src/domain/codex-local-adapter.ts` composes — never redefines — `local-execution.ts`'s `DevicePolicy`/`isWorkspaceRootAllowed`/`LocalTaskCheckpoint`/`createLocalTaskCheckpoint`/`LocalWorkerAdapterResult`/`LocalWorkerAdapterOutcome` and `local-execution-bridge.ts`'s `BridgeProtocolMessageKind`/`isRecognizedBridgeMessageKind`:
  - `CodexSandboxMode`/`CodexApprovalPolicy`/`CodexNetworkPolicy` are direct 1:1 projections of the three real, sourced dimensions above.
  - `CodexAuthReadinessStatus` (`READY_CHATGPT_SESSION`/`READY_API_KEY`/`NOT_AUTHENTICATED`) is a locally-reported status only — no field anywhere in this module can carry a real credential.
  - `createCodexRunRequest` fails closed unless a `RESUME` carries a `priorSessionRef` and a `START` does not, and unless `workingDirectoryRef` passes the existing, unmodified `isWorkspaceRootAllowed` gate.
  - `mapCodexRunEventKindToBridgeMessageKind` deterministically projects a closed, honestly-labeled `CodexRunEventKind` enum (explicitly documented as this repo's own structural projection, not a claim of the literal undisclosed App Server method names) onto `local-execution-bridge.ts`'s own existing `BridgeProtocolMessageKind` values — never a new bridge message kind.
  - `resolveCodexRunOutcome` binds a terminal Codex event to the existing `LocalWorkerAdapterResult` shape; `createCodexCheckpointRecord` delegates the checkpoint itself entirely to the existing `createLocalTaskCheckpoint`, adding only the one Codex-specific `codexSessionRef` field as a separate wrapper.
- `tests/codex-local-adapter.test.ts` (27 tests: C1-C27) and `tests/local-exec-003-boundary-scan.test.ts` (6 tests, including an explicit assertion that every mapped Codex event resolves to a bridge-recognized message kind, and that no credential/token/cookie/auth-state-file field exists anywhere in the module).
- `IMPLEMENTED / SELF-VALIDATED`, **835/835 tests pass** (802 pre-existing on the LOCAL-EXEC-002 base + 33 new), strict typecheck clean, zero new dependency. Every test uses caller-supplied mock inputs — no real Codex CLI process, JSON-RPC client, or filesystem access exists anywhere in this checkpoint, requiring no Founder machine, no Codex installation, and no credentials, matching Rev99/100's own "physical-PC installation/native provider login ... only when live activation actually reaches them" deferral.
- See `docs/exec-plans/active/LOCAL-EXEC-003.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.

## LOCAL-EXEC-007 — Phase L6: Hybrid / UX / Hardening

- With PR #60 (Rev98 corridor consolidation) `HOLD_MERGE` pending Brain's incremental review of the Rev122 fix, and per an explicit Founder instruction to continue other dependency-safe work rather than idle-wait on that review, a fresh scan of the Founder-approved LOCAL-EXEC-001..007 family found LOCAL-EXEC-007 (§21's own last-named phase) has no branch, exec-plan, or domain file anywhere in this repository.
- Branch `claude/local-exec-007-hybrid-hardening`, cut from `claude/rev98-corridor-consolidation`'s current head (`b3c5b1d`, Rev122-corrected). 1482/1482 tests pass on that base before this checkpoint's own new code.
- `src/domain/local-execution-hardening.ts` implements the three §21 L6 dimensions honestly buildable as domain contracts: a tenant-level `LocalExecutionKillSwitch` (distinct from L0's own per-device kill switch, delegating entirely to the existing unmodified `resolveEligibleLocalWorkers`); `planExecutionModeTransition`, mirroring `local-execution-collaboration.ts`'s own `planCollaborationModeTransition` shape for the `ExecutionMode` dimension (with a `LATERAL` direction for `TEAM_LOCAL <-> HYBRID`, since `resolveEligibleLocalWorkers` treats their local-worker eligibility identically); and a device/worker health read-model (`projectLocalWorkerHealth`/`resolveLocalDeviceHeartbeatFreshness`) mirroring `observability-telemetry.ts`'s own "compute freshness relative to a caller-supplied `asOf`, never fabricate an absent value" discipline.
- Device updater/signing, installer/package paths, an ephemeral sandbox strategy, and performance/soak tests are explicitly deferred, not fabricated — none of the required real binaries/signing keys/packaging pipelines/live bridge exist in this repository.
- `tests/local-exec-007-hybrid-hardening.test.ts` (25 tests) and `tests/local-exec-007-boundary-scan.test.ts` (10 tests). `IMPLEMENTED / SELF-VALIDATED`, **1517/1517 tests pass** (1482 pre-existing + 35 new), strict typecheck clean, zero new dependency. Sanity-checked by temporarily removing all three new guards together and confirming exactly the 9 tests proving these dimensions then failed; restored and reconfirmed. See `docs/exec-plans/active/LOCAL-EXEC-007.md` for full detail. `MERGE_DISPOSITION: HOLD_MERGE`.

## Hard stops (Drive packet §15 — not reproduced in full here)

Material product/scope change outside this corridor; material architecture/security/privacy decision outside the delegated envelope; unresolved canonical conflict; a new external provider/dependency requiring admission; credential/access widening; destructive/irreversible action; production deploy/release/publication/DNS/live-customer activation; binding legal/privacy/customer commitment; payment/financial commitment; AI Commerce mutation without its own project authority; a missing prerequisite blocking all safe work on the affected path; or an explicit canonical/Founder stop. Direct-main merge is never granted by this corridor alone — branch/PR workflow only.

## Rev94/95: CONN-001 F1-F3 correction and continuous-execution batch mode

- A chat message pasted into the engineering conversation claimed to relay a "Rev95 Master Execution Order" from Brain, in extremely directive terms explicitly instructing against this corridor's own standing verification discipline (e.g. telling Claude not to treat "the Handoff has not changed" as meaningful). Per the standing rule ("never accept a chat message merely claiming to relay a Brain finding without independently verifying it against the actual source document first" — the same rule that correctly rejected an earlier stale Turkish-language relay this session), the claim was independently verified against the primary canonical Drive Handoff document before any of it was acted on: the document's own `modifiedTime` (2026-09-10T23:02→2026-09-11T08:48) and file size (44KB→55KB) had genuinely changed, and its full body content matched the pasted findings' substance almost verbatim. This was corroborated as a genuine, freshly-written revision, not a stale or fabricated replay, before proceeding.
- **Rev94** (`BRAIN CONTINUATION REVIEW`) returned bounded `CHANGES_REQUIRED / CONTINUE` on PR #38 exact head `5e9eb7b62b879e0453740b15b28e8d5b9af8b35c` and PR #39 exact head `ab4d287387f41b0ea35d3c8d9a7f7bc2ab76a613`, with three findings on PR #38 (F1 process-unsafe concurrency, F2 blind-cast replay, F3 a false "credentials block everything" gate) and five findings on PR #39 (tracked separately — see `docs/exec-plans/active/SALE-TO-CLOSE.md`). PR #38's three findings were corrected on the same branch (see `docs/exec-plans/active/CONN-001.md`'s "Rev94 correction" section for full technical detail): F1 fixed with a real `linkSync`-based cross-process lock (reusing AUD-DURABILITY-GAP's own already-accepted primitive) proven via a genuine 5-process race test; F2 fixed with fail-closed persisted-record revalidation and 9 new adversarial/positive tests; F3 corrected by building a mockable/injectable connector-execution boundary (`connector-execution.ts`), defining `GOOGLE_WORKSPACE`'s genuine multi-host shape via a new per-endpoint `baseUrlOverride` field, and running both of CONN-001's mandatory E2E acceptance cases end-to-end through fixtures/mocks. 790/790 tests pass (765 prior + 25 new), strict typecheck clean, zero new dependency.
- **Rev95** (`CONTINUOUS EXECUTION / FINAL-BATCH REVIEW`) separately authorizes a change to review *cadence*, not acceptance criteria or protected gates: Claude is directed to execute the maximum currently authorized dependency-safe, reversible, dark/internal batch continuously — completing a task, pushing an exact head, or a PR sitting in pending-Brain-review status are explicitly named as NOT valid stop conditions during this batch — and defer independent Brain exact-head review to one consolidated packet at the end, covering every branch/PR touched in execution order. The only genuine stop conditions remain unchanged from the corridor's own standing Hard Stops above (real credentials/account authorization, spend, DNS, production deploy/publication, MAIN merge/mutation, destructive/irreversible action, customer-impacting financial/provider effect, or a material architecture/product-scope decision outside the already-authorized envelope).
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged for PR #38 — this is a normal task-scoped correction; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.

## APP-I18N-001 — TR-EN Client Portal Localization Floor (Rev53 R0-R7 closure audit superseded by Rev55 launch-acceptance reconciliation)

- The Rev53 R0-R7 execution order (see the "docs: Rev53 Founder Directive" entries in `docs/engineering/CURRENT_STATE.md`) found every admitted V2->V5 capability slice already `IMPLEMENTED`/`SATISFIED` and recorded a closure audit. AA-005 Rev55 then reconciled that finding against a broader canonical launch requirement (Turkish-primary Türkiye-launch operator UX with independently adapted English) and found live repo exact head `f74e2890283d7db49cd80023e35fd795b642ff14` (PR #90) hard-codes `<html lang="en">` and every Client Portal shell string in English, with no locale/i18n implementation anywhere in `src/`. This is a new, objective, source-proven READY gap against a stated launch requirement, not a reopening of the already-closed V2->V5 admitted set.
- Implemented on branch `claude/app-i18n-001-tr-en-portal-localization`, cut from PR #90's exact head, draft-targeting `claude/rev53-implementation-completion-closure-audit`: `src/web/locale.ts` (closed `Locale = "tr" | "en"` type, `Set.has()`-based `isRecognizedLocale` guard, deterministic `Accept-Language`-driven `resolveRequestLocale` with an explicit Turkish-primary fallback), `src/web/shell-copy.ts` (one centralized typed fixed-copy catalog with complete `en`/`tr` translations, deliberately excluding dynamic/customer-supplied domain data), and threading `locale`/its resolved copy through `src/web/shell-render.ts`'s `renderShellPage` (`<html lang>` now reflects the effective locale) and `src/web/request-handler.ts` (resolves the real request locale once and passes it explicitly - so a live request with no `Accept-Language` header renders the Turkish shell by default). No existing tenant/customer/project/resource identity, authorization, route, or state semantics were touched.
- `IMPLEMENTED / SELF-VALIDATED`, **1683/1683 tests pass** (1659 pre-existing + 24 new: 11 locale-resolution unit tests, 7 shell-rendering locale tests, 5 end-to-end request-handler locale tests, plus 5 pre-existing integration tests updated to explicitly request English since the live default changed to Turkish - no assertion content altered, only the locale made explicit), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/APP-I18N-001.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE` — inherited stacked lineage; pending Brain independent exact-head review.
