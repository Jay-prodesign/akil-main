# V2 → V5 Autonomous Engineering Corridor (repo-native projection)

This is the repository-native projection of the canonical corridor contract. It is a condensed pointer, not a replacement — the full contract lives in Drive and is the authority of record if this projection and the Drive original ever disagree.

- **Canonical source**: Drive `113M8-J5mYAUPM_BbMlOp0-EgDD4OpCVhNad2yk4crVA` — "AKILTA — V2→V5 Autonomous Engineering Corridor — Repository Projection & Deep Execution Packet v1.0".
- **Decision authority**: DEC-160 (corridor), reinforced by DEC-150/153/157/158/159; DEC-161 (public-site parallel lane), DEC-162 (version-ahead readiness + parallel audit), DEC-163 (full-program-ahead readiness) apply where non-conflicting.
- **Status of this projection**: created after Brain's `PASS / VERIFIED / CLOSED` disposition on the `V2-APP-001_TO_V2-CDO-006` transition checkpoint (`33f95a3dc1ae1fdae02992b781002b92267f3110`), per the canonical packet's own §17 instruction to project this model into repo-native files once that transition checkpoint clears.

## What the corridor is

One bounded, multi-version engineering corridor from AKILTA v2.0 (Client & Delivery OS) through v5.0 (AI-Native Technology Company OS), removing routine per-task Brain↔Claude permission ping-pong without weakening canonical scope, independent verification, tenant/security boundaries, project isolation, or protected external/live gates. The corridor is the single active engineering selector; a `CURRENT_WORK_SUBCURSOR` (see `docs/engineering/CURRENT_STATE.md`) is mutable execution state inside it, not a second selector. Roadmap is globally `NON_SELECTOR`; inside this corridor its admitted V2→V5 task/dependency graph is the candidate pool Claude may choose the next dependency-safe work unit from.

## Version capability map (outcomes only — see the Drive packet §3 for full detail)

| Version | Outcomes (target) | Status | Canonical blueprint |
|---|---|---|---|
| V2.0 — Client & Delivery OS | Logged-in client workspace; provider-neutral identity/session/tenant boundary; customer/project status & attention projection; Delivery Recipe capability; bounded AI Project Advisor; communication/evidence projection; accountable outcome delivery; admitted capability/connection boundaries; truthful loading/empty/unavailable/blocked/error states. | **Active** — see "Current V2 state" below. | Master program blueprint, Drive `1OjPfxJaBJCWsZrillMJk9hXdjrPkRqz6pSsDR-ICrps` (R22, `PREPARED / NON_SELECTOR / FULL-PROGRAM-AHEAD`) |
| V3.0 — Team, Sales & Agency OS | Roles/ownership models; sales ownership & commission semantics; SLA/escalation; agency/partner org model, dashboards, context switching; multi-org controls; tenant-safe customer/project authority. | **Active** (Workstream A only) — see "Current V3 state" below. | Drive `1joeMcCRtnDSotEmWa63fFG6uxGvbeU1LZApm7_5ap50` |
| V4.0 — 360 Digital Operations | Unified operational routing/approval/reporting across admitted Website/CMS, Commerce, Social, Paid Media, AI/Content, CRM/Email, Listings/Maps, Hosting/DNS/Development surfaces; unsupported providers stay explicit. | **Active** (floor slices of Workstreams A and B only) — see "Current V4 state" below. | Drive `1M5vANxdfuH7Vro_CAFenioAmWitGPS3TRlzd06d5DNs` |
| V5.0 — AI-Native Technology Company OS | Company intelligence; cross-client reusable capability learning under evidence/policy controls; Product Lab; AI-native internal delivery; orchestration across AKILTA modules and the isolated versioned AI Commerce boundary. | `PREPARED / NON_SELECTOR` | Drive `1HfQ8Mzbos63LZPE_vZryd4TiLyfa8GFP_AdtmLR-el8` |

Version names are capability/release checkpoints, not engineering permission stops. `Commercial Ready` / `GA` / `RELEASE_ELIGIBLE` / `LIVE` and public/customer activation remain separately governed and are never implied by reaching a version boundary.

## Current V2 state

- `V2-APP-001` → `V2-CDO-006` (DEC-158 continuous train) reached checkpoint `33f95a3dc1ae1fdae02992b781002b92267f3110`, `IMPLEMENTED / SELF-VALIDATED`, and — after two rounds of evidence-integrity correction on the CR-E3 rendered captures — Brain returned `PASS / VERIFIED / CLOSED` at that same checkpoint. See `docs/exec-plans/completed/V2-APP-001.md` for the full evidence trail.
- **Merged (2026-08-29):** after Brain's exact-head §12A evidence-review PASS on PR #4's full accumulated history (through `V3-SLA-001`, checkpoint `fdcb4a29f26a616664ec07cffdfb6a1ec92e41a3`), the Founder explicitly granted the protected merge owner-gate scoped only to this merge. PR #4 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `4ba5231309ac2fc72dd089cc947030a958eab77e` (parents `8a95c721024da4bfee99e082a01e34b86321637f` + `fdcb4a29f26a616664ec07cffdfb6a1ec92e41a3`), independently re-verified by fresh `git fetch`. That owner-gate is now fully consumed; no standing merge/deploy/release/production authority carries forward from it. Further work uses independent new task-scoped branches cut from current `main`, never the retired `claude/akilta-continuity-readback-5hm3ed` branch.
- **Merged (2026-09-03):** after Brain's exact-head §12A evidence-review PASS on PR #5 (`V3-COM-001`, checkpoint `4ff2bca5ab2a62f0e0941a9dd978917665ef81f2`), PR #6 (`V4-SVC-001`, checkpoint `42530f9e31ddd08898aa6fd0c33e4a12a82fe07f`), and PR #7 (`V4-OPS-001`, checkpoint `cf3c329c94417b95829207d19c57723c63cde59c`), the Founder explicitly granted the protected merge owner-gate for each PR (part of the same round that also covers PR #8, each merged independently on its own exact-head PASS — no single grant is treated as blanket standing authority for PRs it wasn't scoped to). PR #5 was merged into `main` at `5c91f18034a7ec435a4d83fccac856d354f8394f`; PR #6 at `c93a9cc1e8caf5da3c13d102bdb9fa7c778a5982`; PR #7 at `6f403db130434b0228783584487dfdf43f3544f7`. PR #8 (`V3-F-001`) is being merged next in this same round.
- Completed V2 slices carried forward unchanged: `V2-CDO-001` through `V2-CDO-005` (see `docs/exec-plans/completed/`), `AKI-BE-001`, `ENG-ORCH-001`, and the `DEL-003` bounded slices.
- **Correction to the prior version of this note**: both candidates' actual canonical task packets exist and were fetched and read in full (they were previously only known by name, via a secondary reference inside the roadmap tracker) — Drive `1WWRKNk7IgL51Mkb-9qK-eFTZpG-exMvC3IN7zVzitWQ` (V2-CDO-007) and Drive `18bMxG_76s3n2axD3jsSZT3sY_DUQiWqDB4geMZHtKNY` (V2-CDO-008), both meeting §9's bar (goal, dependencies, entry conditions, in-scope/hard-non-scope, acceptance criteria, evidence contract). Live repo preflight against those packets' own stated preconditions found:
  - `V2-CDO-007` ("First Delivery Recipe Contract Slice — WEBSITE_BUILD_v1") is **superseded**: its own Activation Precondition #3 ("no equivalent DeliveryRecipe contract has already landed") fails — `V2-CDO-002` (Brain VERIFIED/PASS/CLOSED at `f99cd2f46b178d2190ac94ff6052c955d3e5cd43`) already implemented exactly this contract (`src/domain/delivery-recipe.ts` + `src/fixtures/website-build-v1-recipe.ts`). Selecting V2-CDO-007 as specified would duplicate already-verified work.
  - `V2-CDO-008` (Delivery Project Advisor, L0 Observe / L1 Recommend) had both of its stated prerequisites satisfied by existing closed work (`V2-APP-001_TO_V2-CDO-006`'s verified portal/projection; `V2-CDO-002`'s verified recipe/capability substrate) and no equivalent Advisor code existed anywhere in `src/`. It is now `IMPLEMENTED / SELF-VALIDATED` (domain contract + presentation-layer wiring) — see `docs/exec-plans/active/V2-CDO-008.md` for the full reasoning and evidence.

- **Superseded finding, corrected by Brain `HANDOFF_HEAD_REV: 10`**: after V2-CDO-008, this section stated that a repo-wide search for `Prepared candidate: Drive <id>` packets found only the V2-CDO-007/008 entries above, and treated that absence of a further named packet as a corridor-exhaustion finding. **Brain explicitly rejected this as a valid DEC-160/DEC-163 exhaustion predicate** (independently verified by fresh-reading the actual Rev-10 handoff document in full, not accepted on a chat paraphrase): absence of a separately-published prepared packet does not mean no dependency-safe work exists — Claude is authorized and required to progressively elaborate the Master Program Blueprint's and version-specific Full Blueprints' own PROVISIONAL dependency spine into concrete repo-native tasks itself. A "no prepared packet found" search is therefore not, by itself, a valid stop condition under this corridor; see "Current V3 state" below for what that progressive-elaboration scan produced.

## Current V3 state

- Following Brain's Rev-10 correction, a real dependency scan was run against the V3 Full Blueprint (Drive `1joeMcCRtnDSotEmWa63fFG6uxGvbeU1LZApm7_5ap50`, R21) — not limited to `Prepared candidate`-named documents. Its §2 Definition-of-Ready predicates relevant to §3 Workstream A (organization/role/assignment authority foundation) were checked against live repo truth and found satisfied: the one-time `33f95a3` transition has Brain PASS, and the V2 identity/session/tenant substrate (`src/web/session-context.ts`) it extends is stable. Workstream A has no dependency on Delivery Recipe/capability/evidence semantics per the blueprint's own text, so it does not wait on any further V2 substrate.
- `V3-ORG-001` (Organization Membership / Role / Assignment Authority Foundation, spine item B) was progressively elaborated and implemented: `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-ORG-001.md`.
- `V3-OWN-001` (Multi-Owner Sales/Account/Delivery Semantics — LeadOwner/DealOwner/AccountOwner/DeliveryOwner, spine item C / §5 Workstream B) continued the same loop immediately: its only prerequisite (`OrganizationMembership` from `V3-ORG-001`) had just landed, so it was elaborated and implemented in the same checkpoint session without waiting for Brain review of the prior slice, per Rev10 item 8's "continue the DEC-160 loop... CONTINUE immediately" instruction. `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-OWN-001.md`.
- `V3-PTR-001` (Agency / Partner Multi-Organization Boundary, spine item D / §6 Workstream C) continued the same loop immediately per Brain `HANDOFF_HEAD_REV: 12`'s explicit next-action directive (independently fetched and read in full before acting). `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-PTR-001.md`.
- **Long-run execution contract (Brain `HANDOFF_HEAD_REV: 11`)**: this session operates under a run-until-genuine-stop directive — completing a task, writing a checkpoint, or persisting an evidence-bridge pointer is explicitly NOT a valid stop condition. The exclusive valid stop conditions are: a genuine Founder/protected-owner gate; a material product/scope/architecture/security/privacy/legal/payment/provider/credential/access decision outside delegated authority; a destructive/irreversible or production/deploy/release/publication/DNS/live-customer action; an unresolved canonical conflict; a required-unavailable capability blocking ALL admitted work; durable enumerated proof that ALL V2→V5 paths are exhausted/revoked/blocked (absence of a named packet is explicitly insufficient); or an execution-surface (context/time/tool/session) limit, in which case a `PAUSED_BY_EXECUTION_LIMIT` checkpoint is persisted instead of a plain stop.
- `V3-SLA-001` (SLA / Escalation / Attention Model, spine item E / §7 Workstream D) continued the same long-run loop immediately per Brain `HANDOFF_HEAD_REV: 11`'s run-until-genuine-stop contract. Reuses the existing `OutcomeJobState` exception-state signal and `AuditEvent` rather than inventing an AT_RISK heuristic (no due-date/SLA-clock source exists in this repository). `IMPLEMENTED / SELF-VALIDATED`. See `docs/exec-plans/active/V3-SLA-001.md`.
- **Post-PR#4-merge continuation (Brain handoff Rev26, independently fresh-read before acting; explicitly rejects a/b/c-style waiting for Founder choice)**: with `main` now carrying V3 Workstreams A-D, a fresh dependency/authority reconciliation was run directly against merged `main`. `V3-COM-001` (Commission/Pricing/Discount Authority Read Models, spine item F / §8 Workstream E) was elaborated to its policy-neutral/read-only/fail-closed floor only — `CommissionLedgerStatus` single literal `UNKNOWN`, `DiscountAuthorityLevel` producing only `APPROVAL_REQUIRED`, zero numeric/financial fields. No real commission %, payout, discount threshold, reseller economics, or binding SLA value was invented or activated (DEC-146/DEC-153 remain controlling for any of those). `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #5 (`5c91f18034a7ec435a4d83fccac856d354f8394f`). See `docs/exec-plans/active/V3-COM-001.md`.
- **Second post-merge scan**: with `V3-COM-001`/`V4-SVC-001`/`V4-OPS-001` (PRs #5/#6/#7) open but unmerged at the time, a further scan of V4 Workstreams C-H and V5 Workstreams A-D found no further honest unit without either merging one of those three PRs or a new architectural decision (see "Current V4 state" below for the V4/V5 side of that scan). The Founder explicitly authorized one such decision — a new provider-neutral shell read-port surfacing already-merged V3 A-D substrate into the web shell, not consuming any unmerged sibling PR — as a reversible, architecture-within-envelope technical call inside Claude's delegated authority. `V3-F-001` (Team & Attention Shell Projection, spine item G / §9 Workstream F floor slice) was elaborated on that authority: organization/client context, viewer role, and each of LeadOwner/DealOwner/AccountOwner/DeliveryOwner rendered separately in the existing shell, plus internal attention/exception state, using only merged-main substrate; commercial state renders as a fixed "Unavailable" literal rather than consuming the `V3-COM-001` checkpoint. `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #8 in this same round. See `docs/exec-plans/active/V3-F-001.md`.
- **Correction (2026-09-03) — "V3 exit audit" is not a Claude engineering task**: this document and `docs/architecture/VERSION_EVOLUTION_MAP.md` previously called the final V3 spine item "Workstream G (cross-slice integration + V3 exit audit)" as if it were a numbered implementation workstream awaiting elaboration, parallel to A-F. A fresh read of the V3 Full Blueprint (Drive `1joeMcCRtnDSotEmWa63fFG6uxGvbeU1LZApm7_5ap50`) shows this is inaccurate: §3's dependency spine lists eight lettered items (A: verified V2 substrate; B-G map onto blueprint §4-§9's six numbered "WORKSTREAM A-F" sections, i.e. `V3-ORG-001`/`V3-OWN-001`/`V3-PTR-001`/`V3-SLA-001`/`V3-COM-001`/`V3-F-001`; H is the final item), and spine item H has no corresponding numbered "WORKSTREAM" section with its own acceptance-direction/required-semantics text at all. Its actual definition lives in blueprint §14 ("V3 VERSION-END FULL AUDIT / EXIT CRITERIA"), which opens: *"At the V3 capability checkpoint **Brain performs** a comprehensive integration audit using accumulated delta evidence plus fresh cross-slice verification."* This is explicitly a Brain review activity consuming evidence Claude has already produced across the six landed slices — not a Claude-authored source module, domain type, or test suite to elaborate. With V3 Workstreams A-F (`V3-ORG-001` through `V3-F-001`, i.e. PR #4's four slices + PR #5 + PR #8) all now merged into `main`, there is no further V3 engineering unit for Claude to select here: the dependency-safe next action on this edge is Brain's own §14 exit audit against the evidence already on `main`, not a new "V3-EXIT-001"-style task. Inventing one would fabricate scope the blueprint does not define.

## Current V4 state

**Note (2026-09-03): this section was briefly absent from this document during PR #5/#6's merge reconciliation** — a doc-only conflict-resolution slip folded V4-SVC-001's own addition of this section into the "Current V3 state" text above without preserving its header. No source file was affected; this is a repo-doc-only correction restoring the section, reconstructed from PR #6's, PR #7's, and PR #8's own branch content (each independently added this section when cut from the same post-PR#4 `main`).

- The same continuation directive (Brain handoff Rev26) also authorized selecting the next dependency-safe unit from V4 once its own §2 Definition-of-Ready predicates were live-checked, without waiting on V3 Workstreams F/G. `V4-SVC-001` (Service-to-Capability Routing, §5 Workstream A) was elaborated to its honest floor only: the blueprint's full 10-rung `ExecutionMaturity` ladder assumes real provider adapters that do not exist anywhere in this repository, so the type declares all 10 rungs (additive-safe for later) but `resolveServiceCapabilityRoute` only ever produces `UNAVAILABLE`/`READ_ONLY`, fail-closed on ownership mismatch. `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #6 (`c93a9cc1e8caf5da3c13d102bdb9fa7c778a5982`). See `docs/exec-plans/active/V4-SVC-001.md`. (This narrowing from "V4 Workstream A" to "its floor only" was disclosed in that PR's own description and exec-plan, not silently absorbed.)
- `V4-OPS-001` (Unified Operations Attention/Exception Model, §6 Workstream B) continued the same loop immediately, per the V4 blueprint's own R32 hot-buffer overlay naming Workstream B as the preferred readiness path right after Workstream A and instructing it to reuse the existing V3 attention semantics. A repo-wide check found exactly one of the blueprint's eleven candidate attention families (blocked dependency, customer approval required, provider wait, credential/access problem, SLA/operating risk, failed external effect, telemetry anomaly, policy restriction, data-quality uncertainty, recovery required, optimization opportunity) has a real observable source in this repository today — internal delivery/job exception state (`AttentionState`, V3-SLA-001) — so `AttentionSourceDomain` declares only that one literal (`DELIVERY_OUTCOME_JOB`) rather than fabricating the other ten. `IMPLEMENTED / SELF-VALIDATED`, Brain PASS, merged into `main` via PR #7 (`6f403db130434b0228783584487dfdf43f3544f7`). See `docs/exec-plans/active/V4-OPS-001.md`.
- **Second scan finding (before the V3-F-001 authorization above)**: V4 Workstreams C-H were each checked directly against their §7-§12 text. C (Service-Specific Control Adapters) requires real provider SDKs; D (Approval/Effect/Recovery) depends on C; E (Evidence/Outcome Reporting) requires real effect/telemetry data; F (Bounded Optimization Loop) depends on D+E; G (Account-Manager Operating Model) requires an "automation worker identity" concept with no real source; H (frontend) consumes C-G. None has an honest floor reachable without either a real external provider/effect or fabricating scope — none was selected. V5 Workstreams A-D were also checked: they are meta-engineering-process concepts (WorkUnit/Task/PR/Evidence identity, worker routing policy, repo bootstrap templates) that the V5 blueprint's own R32 hot buffer warns against re-creating as "a second workflow engine, second canonical state system" — `ENG-ORCH-001` (V1, merged) already provides the durable engineering-state contract V5 Workstream A describes, so building a parallel one would be non-additive fabricated scope, not genuine new capability. This finding was re-confirmed live as still current as of the Rev31/Rev32 dependency-scan rounds (2026-09-03), prior to the Founder's merge-round authorization below.
- `V3-COM-001` (PR #5), `V4-SVC-001` (PR #6), `V4-OPS-001` (PR #7), and `V3-F-001` (PR #8) were each cut fresh from merged `main` on independent branches — none depended on, or was blocked by, any sibling's review status. All four are now Brain-PASSed and merged into `main` under one explicit Founder-granted merge round (see "Current V2 state" above for exact merge SHAs). V4 Workstreams C-H and V5 remain not-yet-elaborated for the reasons above; the V3 spine's final item (H, blueprint §14 exit audit) is Brain's own review activity, not a further Claude engineering unit — see the correction above in "Current V3 state". The next dependency scan should re-check live repo truth against this newly-merged state rather than assume a fixed order.

## V3 §14 version-end integration audit — CLOSED (Brain Rev34)

- PR #9 (the "Workstream G" mislabel correction above) was merged into `main` at `6758de759bf17ecda49da7f4f8e1d07faa526655` (parents `837214dc863970bdb05f29bf8611a3438735b3f4` + `63da4ab839f231ca6f13ce2256ca7542bf87504f`). Its merge followed a conversational Founder authorization exchange in the engineering session, consistent with the PR #5–#8 merge round; per the same `OWNER_GATE_PROOF_MISSING` provenance standard already applied to that round (see the Rev33 evidence package referenced below), that conversational record is accepted as historical merge-execution context but does not itself constitute a durable, independently-verifiable proof artifact of Founder approval. No such artifact exists for PR #9 specifically — this section previously stated PR #9 "was Founder-approved and merged" as flat fact without carrying that caveat forward; corrected here per Brain Rev35's `GOVERNANCE_PROVENANCE_CONFLICT` finding. The historical merge itself is not disputed, only the unqualified approval-provenance phrasing. A compact, transferable, SHA-bound post-merge evidence package for PR #5 through PR #9 (repo identity, full merge-chain ancestry, proof that every reconciliation/correction commit touched documentation only, fresh strict-typecheck + 580/580 regression at that exact SHA, dependency/lockfile/protected-boundary readback, and an honest `OWNER_GATE_PROOF_MISSING` statement rather than a fabricated durable approval artifact) was composed, uploaded to Drive, and read-back-diff-verified clean — main document Drive `1ezXwkj8zYLqwk7A-qxFVRwy8PVVLGx5C1H9hk339rEQ`.
- Brain independently reviewed that package (Handoff `HANDOFF_HEAD_REV: 34`, fresh-read before acting, not accepted on a chat paraphrase) and returned `BRAIN_REVIEW_DISPOSITION: PASS — POST_MERGE_TECHNICAL_INTEGRATION_EVIDENCE / OWNER_GATE_PROVENANCE_EXCEPTION_RECORDED`. Rev34 explicitly closes the V3 Full Blueprint §14 version-end integration audit edge (`V3_§14_AUDIT_STATE: PASS / VERSION_END_INTEGRATION_EDGE_CLOSED`) at exact final main `6758de759bf17ecda49da7f4f8e1d07faa526655`, using the already-accepted exact-head semantics of each of the six landed slices plus this merged-main proof. It records the owner-gate provenance exception honestly (a historical governance/provenance gap, not a source defect) rather than retroactively fabricating a durable approval artifact.
- Rev34's continuation instruction (`STOP_STATUS: CONTINUE`, `NEXT_EXPECTED_ACTOR: CLAUDE`, `PENDING_BRAIN_RESPONSE: NO`, `OWNER_GATE: NONE` for ordinary fresh-read/dependency-scan/bounded-already-authorized engineering) directed a fresh DEC-160 dependency/authority scan across V4 Workstreams C-H and V5 Workstreams A-D for the highest-value dependency-safe V5-first slice. That fresh scan (this checkpoint) reconfirmed V4 Workstreams C-H remain blocked: no new provider/effect adapter or connector SDK has been added anywhere in `src/` since the last scan (`providerRef`/`connection-authority.ts`/`worker-invoker.ts` remain structurally opaque string references by design, per their own doc comments — no field anywhere in the repo can hold a provider SDK object, OAuth token, or invocation config), so those workstreams still depend on a real external provider/effect that does not exist. It also concluded, for V5 Workstreams A/D, that "no field anywhere holds a provider object" is not itself their blocking edge — that reasoning wrongly treated the *existence* of the already-merged `ENG-ORCH-001` durable engineering-state contract as grounds to call any further V5-A/D work a forbidden duplicate, when the V5 Full Blueprint's own R32 hot-buffer overlay (§5, "V5 Reuse Foundation Now Visible") states the opposite: the V5 AI Engineering Delivery Engine **must extend** `ENG-ORCH-001` and the DEC-160 repo-native execution package, not avoid touching it. That prior 10/10 BLOCKED conclusion is withdrawn for V5 A/D per Brain Rev35's `CORRIDOR_EXHAUSTION_DEPENDENCY_ANALYSIS_INVALID` finding (independently fresh-read and verified against both the canonical Handoff document and the V5 Full Blueprint itself before acting — see "V5 A/D corrected re-scan" below).

## V5 A/D corrected re-scan + V5 C bootstrap re-evaluation (Brain Rev35)

- **V5 Workstream A (AI Engineering Delivery Engine) / D (Spec/Task/Test/PR/Review Orchestration) — ELIGIBLE (extend, not duplicate)**: the blueprint's §5 acceptance criteria require, among other things, that "stale SHA approval cannot merge" and that a "post-review push invalidates old merge approval." Fresh reading of the already-merged ENG-ORCH-001 primitives (`src/domain/engineering-run-state.ts`, `src/domain/engineering-event-envelope.ts`) shows this specific acceptance criterion is not yet enforced as an explicit, tested transition: `applyEvent`'s `CHECKPOINT` handling only accepts a new checkpoint from `NONE`/`CHANGES_REQUIRED`/`RESUME_AUTHORIZED` status (`CHECKPOINT_ACCEPTING_STATUSES`, `src/domain/engineering-run-state.ts:93-97`); a fresh push (new `CHECKPOINT` event) arriving while status is `PASS` is a silent no-op that leaves the stale `BrainResolution.checkpointSha` in place with no explicit authorized path to reopen the run for the new SHA. This is not an oversight bug — it is a deliberately-scoped, already-tested boundary of the V1 ENG-ORCH-001 slice (see `tests/engineering-run-state.test.ts:459`, test name: *"a fresh higher-fenced CHECKPOINT cannot reopen/regress an already-PASS run **without an explicit authorized path**"* — the italicized qualifier is the exact gap V5-A/D's acceptance criterion asks to close). Closing it is a narrow, additive extension of the existing reducer/event vocabulary (most likely: a new explicit event/transition that lets a fresh `CHECKPOINT` on a `PASS`-resolved run reopen into `CHECKPOINT_RECEIVED` with the prior `BrainResolution` explicitly retained as superseded history rather than silently dropped) — not a second orchestration engine, not a parallel canonical-state system. **This is the resolved highest-value dependency-safe V5-first candidate.** Exact task/file scope remains LIVE-RESOLVED at implementation time, per the corridor's own convention; it is named and located here, not yet implemented in this documentation-only correction (see below).
- **V5 Workstream C (Project/Repository Bootstrap) — re-evaluated, not BLOCKED by target-count**: the prior scan's "only one project/repository exists, therefore blocked" reasoning is withdrawn. The blueprint's §7 candidate reusable assets — compact AGENTS/CLAUDE navigation, `CURRENT_STATE`, `SYSTEM_MAP`/`VERSION_EVOLUTION_MAP`, a `DEPENDENCY_MAP`, an execution contract, task templates, evidence manifests, and a review/finding schema — already exist as this repository's own scaffolding (`AGENTS.md`, `CLAUDE.md`, `docs/engineering/CURRENT_STATE.md`, `docs/architecture/VERSION_EVOLUTION_MAP.md`, `docs/exec-plans/`). A second live customer repository is not a stated entry predicate anywhere in §7; the real predicate is the ability to extract/parameterize this scaffolding into a reusable, namespace-isolated template with cross-project-contamination tests proving it (§7 acceptance: "cross-project state contamination tests; stale template authority cannot override project truth; secret/customer artifact cloning prohibited"). This is technically ELIGIBLE in principle, but is architecturally larger (it defines a new "template" artifact/contract, not an extension of an existing typed domain module) than the V5-A/D gap above, so it is not the candidate selected for the next implementation round — it is recorded here as ELIGIBLE_NOT_SELECTED rather than BLOCKED.
- **V5 Workstream B (Worker/Model Routing)**: not part of Rev35's requested re-scan; its prior "not yet elaborated" status is unchanged and not re-asserted as BLOCKED here.
- Per Brain Rev35's explicit step 5 bound for this documentation-only PR #10 correction ("do not mutate product source"), the V5-A/D slice named above is resolved/located, not yet implemented, in this correction.

## V5-ENG-001 implemented (Brain Rev38 confirmed candidate)

- PR #10 (carrying the V3 §14 closure and the Rev35 correction above) merged into `main` at `90e989935a443462f372fbbb9ff1caa6987c82ce` on the Founder's own direct, explicit approval given in the engineering conversation. Brain Rev38 (independently fresh-read from the canonical Handoff document before acting) accepted the merge as technically PASS (merged tree byte-identical to the reviewed exact head, fresh typecheck exit 0, fresh 580/580 regression), corrected Rev37's own incorrect "Dvm = merge approval" claim to `INVALID / RETRACTED` (Claude's independently-asked-and-answered "Yes" is recorded as `OWNER_GATE_PROOF_UNVERIFIED_AT_BRAIN_SURFACE` — accepted as historically real, not fabricated into independently-proven authority), and confirmed the V5-A/D candidate from the section above as the authorized next action, with `OWNER_GATE: NONE` for the bounded internal source slice.
- `V5-ENG-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-eng-001-checkpoint-invalidation`) cut from merged `main`: `src/domain/engineering-run-state.ts`'s `EngineeringRunState` gained a `supersededResolutions` field, and `CHECKPOINT_ACCEPTING_STATUSES` now includes `PASS`, so a fresh `CHECKPOINT` (post-review push) carrying a NEW `checkpointSha` explicitly reopens a `PASS`-resolved run into `CHECKPOINT_RECEIVED`, clearing the stale `resolution` and appending it to `supersededResolutions` — a duplicate `CHECKPOINT` for the SAME `checkpointSha` remains a safe no-op. `IMPLEMENTED / SELF-VALIDATED`, 584/584 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-ENG-001.md` for the full reasoning and evidence.
- **Rev39 correction**: Brain's exact-head review of PR #12 found the submitted evidence didn't prove that EQUAL fencing (not just stale/lower) also cannot invalidate a `PASS` approval, per the Rev36/Rev38 acceptance contract. On independent verification (fresh empirical probe against the compiled reducer, not just trusting either the original self-assessment or Brain's own "not proven either way" framing), this was found to be a genuine source gap, not only a missing test: the `PASS`-guard's `isGenuineNewPush` check verified only that `checkpointSha` differed, not that `fencingToken` was strictly greater than current — so an equal-fenced `CHECKPOINT` with a new `checkpointSha` was incorrectly accepted as a valid invalidation. Fixed with a one-condition narrowing (`event.fencingToken > state.currentFencingToken` added to the existing check) on the same PR #12 branch, plus the required focused test. 584/584 tests pass.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #10's grant or any other prior PR's.
- Brain Rev40 (independently fresh-read before acting) reviewed the Rev39-corrected exact head, reclassified the prior "no material source defect" finding as `SUPERSEDED BY EMPIRICAL EVIDENCE`, and returned `BRAIN_REVIEW_DISPOSITION: PASS` with `OWNER_GATE: REQUIRED / NOT_YET_APPROVED` for the PR #12 merge.

## PR #12 merged (Founder-approved exact-head merge)

- Brain Rev41 subsequently claimed the Founder had told Brain directly, in a separate channel, to merge PR #12 (exact head `acfc674fc2f1606204ff8953994156af317f98df`). Per standing practice, Claude did not act on this claim alone — the underlying event happened in a channel outside this conversation and could not be independently confirmed from it. Claude instead confirmed PR #12's live GitHub state matched exactly, then asked the Founder the specific exact-head merge question directly in the engineering conversation and received an explicit "Merge" confirmation before merging.
- PR #12 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `19df961498896427223842f7de82df2d9f6a8304` (parents `90e989935a443462f372fbbb9ff1caa6987c82ce` + `acfc674fc2f1606204ff8953994156af317f98df`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #12's exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (584/584 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.

## V5-BOOT-001 implemented (Brain Rev42 confirmed candidate)

- Brain Rev42 (independently fresh-read from the canonical Handoff document before acting) accepted PR #12's post-merge state as PASS, confirmed the Rev41 owner-gate was consumed for that merge only, and reauthorized a fresh DEC-160/V5 dependency scan against the newly-merged main with `OWNER_GATE: NONE` for the scan and for bounded internal engineering work it selects — explicitly rejecting "waiting on your call" as a valid stop condition once that scan was already standing-authorized.
- That fresh scan reconfirmed V4 Workstreams C-H remain blocked (no real provider/effect adapter exists anywhere in `src/`) and V5 Workstream B (Worker/Model Routing) remains blocked (only `WORKER`/`BRAIN`/`OWNER` roles exist; DEC-142 still forbids Codex admission pre-V1; no multi-provider capability registry exists to route against). V5 Workstream C, previously `ELIGIBLE_NOT_SELECTED` per Rev35's "architecturally larger" framing, was reconsidered: its §7 acceptance criteria (cross-project contamination tests, stale-template-cannot-override-truth, secret-cloning prohibition, unique identifiers) can be modeled and adversarially tested as a pure planning function over synthetic project namespaces — a real second live repository was never actually a stated entry predicate.
- `V5-BOOT-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-boot-001-bootstrap-template`) cut from merged `main`: `src/domain/project-bootstrap-template.ts`'s `resolveProjectBootstrapPlan` resolves which structural assets (from a closed, blueprint-verbatim `BootstrapAssetKind` enum with no credential/provider-secret kind) a new project may reuse from an existing template, stamping every copied asset `supersedesLocalAuthority: false`, failing closed on a colliding project namespace, and forwarding `contentRef` as an opaque, never-interpreted pointer. `IMPLEMENTED / SELF-VALIDATED`, 597/597 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-BOOT-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.

## Rev43: PR #14 CHANGES_REQUIRED — provenance/local-applicability gaps fixed same-branch

- Brain Rev43 (independently fresh-read from the canonical Handoff document) reviewed PR #14's exact head (`4df83393002c62323fb5c277c5b1766eb5d14b62`) and returned `BRAIN_REVIEW_DISPOSITION: CHANGES_REQUIRED — MATERIAL ACCEPTANCE COMPLETENESS`, with `NO_MERGE_AUTHORITY: YES`. Both findings were independently verified against the live source before acting, not accepted from the framing alone: F1 (the per-asset loop never validated `sourceProjectRef`/`sourceVersion` for emptiness — an asset with a recognized `kind` but blank provenance could be copied) and F2 (no field represented the §7-required local-applicability revalidation decision — a recognized `kind` alone was sufficient for acceptance) were both confirmed as real gaps by reading the actual `src/domain/project-bootstrap-template.ts`.
- Fixed same-branch (`claude/v5-boot-001-bootstrap-template`), no new PR, no redesign: added fail-closed provenance validation (empty/whitespace `sourceProjectRef`/`sourceVersion` now rejected) and a required `localApplicability: "ACCEPTED" | "NOT_APPLICABLE"` field — only `"ACCEPTED"` assets can enter `copiedAssets`. 4 new focused tests (B9-B12); all prior invariants (duplicate-namespace fail-closed, secret/provider boundary, opaque `contentRef`, cross-project isolation, zero imports, zero new dependency) preserved. Strict typecheck exit 0; full regression 601/601 pass.
- `OWNER_GATE: NONE` for this bounded same-branch correction per Rev43; no Founder relay required. PR #14 remains `OPEN / DRAFT / UNMERGED / HOLD_MERGE`, returned for Brain exact-head re-review at the corrected head.

## PR #14 merged (Founder-approved exact-head merge)

- Brain Rev44 (independently fresh-read from the canonical Handoff document) reviewed the Rev43-corrected exact head `88f84e5d5c0e98ee1a2a9e83d39d3ae18655df64` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming F1/F2 closed, 601/601 tests pass, zero new dependency.
- Brain Rev45 subsequently claimed the Founder had told Brain directly, in a separate channel ("Ok edilsin"), to merge PR #14 exact head `88f84e5d5c0e98ee1a2a9e83d39d3ae18655df64`. Per standing practice, Claude did not act on this claim alone — the underlying event happened in a channel outside this conversation and could not be independently confirmed from it. Claude instead confirmed PR #14's live GitHub state matched exactly, then asked the Founder the specific exact-head merge question directly in the engineering conversation and received an explicit "Merge et devam et" confirmation before merging.
- PR #14 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb` (parents `19df961498896427223842f7de82df2d9f6a8304` + `88f84e5d5c0e98ee1a2a9e83d39d3ae18655df64`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #14's exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (601/601 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.

## V5-ENG-002 implemented (post-PR#14 dependency scan, Founder "devam et, sen seç")

- After PR #14 merged, the Founder authorized ("Devam et, sen seç" — continue, you choose) the standing post-merge continuation: per Rev42/Rev45's instruction, ordinary bounded internal engineering work following a merge does not require another Founder gate. A fresh DEC-160/V5 dependency scan was run against merged `main` (`1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb`).
- The scan reconfirmed V4 Workstreams C-H and V5 Workstream B remain blocked (no real provider/effect adapter or multi-provider capability registry exists anywhere in `src/`). Re-reading the V5 blueprint §5 (Workstream A) acceptance list in full against the already-merged `src/domain/engineering-run-state.ts` found one criterion not yet enforced: *"agent cannot self-mark final VERIFIED."* `applyEvent`'s `RESOLVE` case never inspected `event.fromRole` — only run status and `checkpointSha` binding gated it. This was independently, empirically confirmed as a real, exploitable defect (not merely inferred from reading the code): compiling the reducer and probing it directly showed a `fromRole: "WORKER"` `RESOLVE(PASS)` event was accepted and produced a durable `BrainResolution` with `decision: "PASS"`.
- `V5-ENG-002` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-eng-002-resolve-authority-separation`) cut from merged `main`: `applyEvent`'s `RESOLVE` case now rejects (as a safe no-op, same treatment as its other RESOLVE-path rejections) any event whose `fromRole` is not `"BRAIN"` or `"OWNER"`. All 14 pre-existing `RESOLVE`-emitting tests were updated to pass `fromRole: "BRAIN"` explicitly (they were relying on the shared test helper's `fromRole: "WORKER"` default, which the fix now closes) with no change to any assertion; 4 new tests prove the self-certification rejection for both `PASS` and `CHANGES_REQUIRED`, confirm an `OWNER`-authored `RESOLVE` still works, and confirm the rejected attempt still advances the fencing floor per the existing E7 convention. `IMPLEMENTED / SELF-VALIDATED`, 605/605 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-ENG-002.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.

## PR #16 merged (Founder-approved exact-head merge)

- Brain Rev46 (independently fresh-read from the canonical Handoff document) reviewed PR #16's exact head `3c4f20aadb1d43e6f93d58b5c6010301d884aa38` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming the empirical pre-fix defect finding, the bounded correction, and 605/605 tests passing. `OWNER_GATE: REQUIRED / NOT_YET_APPROVED`.
- The Founder said, unprompted, "Merge edebilirsin bu arada" (you can merge it, by the way) before Brain's review had landed. Per standing practice, Claude did not merge on that alone — it flagged that Brain had not yet independently reviewed PR #16 (a distinct gate from Founder approval, and notably the very principle PR #16 itself encodes: independent verification, not self-certification), and asked whether to merge anyway or wait for Brain first. The Founder said to wait. Once Brain's Rev46 PASS arrived, Claude re-verified PR #16's live GitHub state matched the audited head exactly, then asked the Founder the specific exact-head merge question directly in the engineering conversation and received an explicit "I confirm" before merging.
- PR #16 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `460871cba43273f89bbf8004e547664c40b90794` (parents `1c254ada2a5178099d4b7a1a3b8a2fc3289ee4fb` + `3c4f20aadb1d43e6f93d58b5c6010301d884aa38`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #16's exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (605/605 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.
- Subsequently, Brain Revs 47 and 48 (fresh-read at a later check-in) were found to describe a Founder approval/merge-execution sequence for PR #16 that had already completed live in this conversation — Brain's connected GitHub surface returned a 404 on this private repo and so could not observe the already-merged state. Rev47's quoted Founder utterance ("Edilsin bence") does not match the actual live exchange that authorized the merge (the real sequence is recorded above); this was noted as a Rev37-style misattribution but required no corrective action since the underlying merge had already happened correctly.

## V5-INT-001 implemented (post-PR#16 dependency scan, Founder continuation nudge)

- After PR #16 merged, the Founder's "Dvm edemiyor musun" (can't you continue?) applied the standing Rev42/Rev45/Rev46/Rev48 continuation authorization: ordinary bounded internal engineering work following a merge needs no further Founder gate. A fresh DEC-160/V5 dependency scan was run against merged `main` (`460871cba43273f89bbf8004e547664c40b90794`).
- The scan reconfirmed V4 Workstreams C-H and V5 Workstream B remain blocked. Re-reading V5 blueprint §5 (Workstream A) in full against the now-twice-extended `engineering-run-state.ts` found all five named acceptance criteria satisfied: stale-SHA invalidation (`V5-ENG-001`), wrong-context fail-closed (pre-existing `ENG-ORCH-001` guard), no self-certification (`V5-ENG-002`), auditable finding lifecycle (`supersededResolutions`), and durable-truth resumability (`FileDurableEngineeringStore`/`reconstructState`) — a finding recorded here, not a status Claude claims authority to close. With V5 A/C now built and B/D exhausted at the currently-elaboratable level, the scan located V5 Workstream F (§10, Cross-Domain Company Intelligence) as buildable via a pure reconciliation function over opaque insight pointers, reusing the existing `TenantScope` isolation convention — the same "real second system was never the actual entry predicate" reasoning already applied to `V5-BOOT-001`.
- `V5-INT-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-int-001-cross-domain-intelligence`) cut from merged `main`: `src/domain/cross-domain-intelligence.ts`'s `buildCrossDomainIntelligenceSnapshot` combines insights from a closed six-surface domain enum (SALES/DELIVERY/OPERATIONS/ENGINEERING/COMMERCIAL/PRODUCT, matching the blueprint's own §10 text) about the same subject, failing closed on tenant/project scope mismatch or an unrecognized domain, and reconciling by freshness (against a caller-supplied `asOf`, never the system clock) into `CURRENT`/`STALE`/`CONFLICTING` per subject — disagreement is never silently arbitrated, and stale evidence never masquerades as current. `IMPLEMENTED / SELF-VALIDATED`, 625/625 tests pass, strict typecheck clean, zero new dependency. PR #18 opened, `HOLD_MERGE`. See `docs/exec-plans/active/V5-INT-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.

## Rev49 correction: PR #18 (V5-INT-001) exact-head review, CHANGES_REQUIRED

- Brain Rev49 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 49`, confirmed genuine before acting) reviewed PR #18's exact submitted head `0a7715d03c7d4e89c648433de79058f203cd0b38` and returned `BRAIN_REVIEW_DISPOSITION: CHANGES_REQUIRED` / `MERGE_DISPOSITION: HOLD_MERGE / DO_NOT_MERGE`, with two findings:
  - **F1 — provenance fail-closed gap**: `IntelligenceInsight` carries `sourceRef` as its provenance pointer, but the audited implementation never validated it before accepting an insight — an empty/whitespace `sourceRef` could masquerade as governed evidence.
  - **F2 — future-timestamp freshness gap**: the freshness comparison (`asOfMs - capturedAtMs <= freshnessThresholdMs`) never checked `capturedAtMs <= asOfMs`; a `capturedAt` later than the caller-supplied `asOf` produces a negative age, which satisfies any non-negative threshold and could wrongly resolve `CURRENT`.
- Consistent with this corridor's standing empirical-verification discipline, both findings were independently confirmed against the compiled `dist/src/domain/cross-domain-intelligence.js` output (not accepted from Brain's framing alone) before any source change: a probe insight with `sourceRef: ""` was accepted with zero rejections; a probe insight dated `2099` against a 2026 `asOf` and a 1000ms threshold still resolved `CURRENT`. Both were genuine, exploitable gaps.
- Fixed on the **same branch/PR** (`claude/v5-int-001-cross-domain-intelligence`, PR #18) per Rev49's own explicit bounded-correction authorization — no new branch, no redesign: two new rejection checks added to the per-insight validation loop in `buildCrossDomainIntelligenceSnapshot` (reject empty/whitespace `sourceRef`; reject `capturedAt` in the future relative to `asOf`, checked before the freshness filter so a negative age can never be reached), plus 4 new adversarial tests (F16-F19 in `tests/cross-domain-intelligence.test.ts`) covering both fixes and the exact-equality boundary (`capturedAt === asOf` is accepted, not "in the future").
- `V5-INT-001` is now `IMPLEMENTED / SELF-VALIDATED (Rev49-corrected)`: strict typecheck exit 0, full regression 629/629 pass (625 prior + 4 new), zero new dependency. `MERGE_DISPOSITION: HOLD_MERGE` unchanged — Brain's fresh exact-head review of the corrected head is the next step, not merge.
- Rev49 also reconfirmed the V5 Workstream A five-criterion finding remains `FINDING_PENDING_BRAIN_ADJUDICATION` — Claude's recorded finding is accepted as directionally noted but not yet independently adjudicated by Brain.

## PR #18 merged (Rev50 PASS, Founder-approved exact-head merge)

- Brain Rev50 (independently fresh-read from the canonical Handoff document) reviewed PR #18's Rev49-corrected exact head `76a352b9fc0ed782b5112bff581e578e4cabaca9` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming both Rev49 findings (F1 sourceRef, F2 future-capturedAt) were closed and 629/629 tests passing. `OWNER_GATE: REQUIRED / NOT_YET_APPROVED`.
- Rev51 (fresh-read at the next check-in) recorded a Founder utterance ("Ok merge") authorizing the merge for that exact head. Per standing practice, this document-relayed utterance was not treated as sufficient on its own — Claude independently re-verified PR #18's live GitHub state matched the reviewed head/base exactly (open, draft, unmerged, `mergeable_state: clean`), then asked the Founder the specific exact-head merge question directly in the live engineering conversation and received an explicit "I confirm" before merging.
- PR #18 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `839aca0f6dd830c46dcf4576e46035c3fdbd9207` (parents `460871cba43273f89bbf8004e547664c40b90794` + `76a352b9fc0ed782b5112bff581e578e4cabaca9`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #18's reviewed exact head showed zero difference, so the already-completed exact-head evidence transferred directly; fresh strict typecheck (exit 0) and full regression (629/629 pass, exit 0) were re-confirmed on the exact merge commit as additional proof.
- Rev51's `WORKSTREAM_A_FINDING` line reconfirms the V5 Workstream A five-criterion finding is not implicitly closed by this merge — it remains `FINDING_PENDING_BRAIN_ADJUDICATION`.

## V5-WRK-001 implemented (post-PR#18 dependency scan, Founder continuation instruction)

- After PR #18 merged, the Founder's explicit instruction to fresh-read the Handoff and continue applied the standing post-merge dependency-scan authorization. A fresh DEC-160/V5 dependency scan was run against merged `main` (`839aca0f6dd830c46dcf4576e46035c3fdbd9207`).
- The scan reconfirmed V5 Workstreams A (finding recorded, pending Brain adjudication), C, and F all have a landed floor slice. Re-reading V5 blueprint §6 (Workstream B, Worker/Model Routing Policy) in full found its acceptance criteria — "unavailable/untrusted worker cannot be selected as authorized executor; cost optimization cannot bypass policy; reviewer independence preserved where required; provider outage degrades gracefully" — describe a pure routing-decision function over a caller-declared registry of admitted workers, buildable without any real provider/model API, mirroring the same reasoning already applied to `V5-INT-001` and `V5-BOOT-001`.
- `V5-WRK-001` was elaborated and implemented on a fresh task-scoped branch (`claude/v5-wrk-001-worker-routing-policy`) cut from merged `main`: `src/domain/worker-routing-policy.ts`'s `resolveWorkerRoute` selects the first caller-supplied candidate that is `ADMITTED`, `AVAILABLE`, capable, evidenced, and risk-authorized as executor, and (when independent review is required) the first such candidate distinct from the executor as reviewer — never letting cost/position/availability override policy, never letting an executor review its own work, always returning an explicit `REJECTED` decision with a reason rather than throwing when no eligible worker exists. `IMPLEMENTED / SELF-VALIDATED`, 656/656 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-WRK-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #18's grant or any other prior PR's.

## Rev52 correction: PR #18 post-merge acceptance + PR #20 (V5-WRK-001) CHANGES_REQUIRED

- Brain Rev52 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 52`, confirmed genuine before acting) formally accepted PR #18's post-merge state as `TECHNICAL_DISPOSITION: PASS / POST-MERGE ACCEPTED`, confirming the merge commit/parentage/zero-diff/typecheck/regression evidence, and closed Rev51's owner-gate for the PR #18 exact-head merge only. Rev52 also noted that Rev51's directly-observed Founder "Ok merge" utterance is itself sufficient durable merge authority on its own terms, and that Brain does not need to independently prove or elevate the additional live "I confirm" this session separately obtained — that live confirmation was this corridor's own standing practice (never merge from a document claim alone) and remains a valid additional safeguard, not a claim that Brain itself verified it.
- Rev52 also reviewed PR #20 (`V5-WRK-001`) and returned `CHANGES_REQUIRED`, with two findings, both independently confirmed against the module's own source before fixing:
  - **F1 — required worker policy dimensions not modeled**: §6's required semantics ("each worker has declared capabilities/tools/policy constraints/evaluation evidence") and "provider availability != admission != authority" require tool access, policy/privacy-security constraints, and an explicit authority dimension to be modeled as separate routing inputs. The submitted `AdmittedWorker` had no field for any of the three — confirmed true by direct inspection of the source.
  - **F2 — exact-head transferability gap**: the original evidence recorded only the abbreviated head `c106161`, not a full 40-character SHA, so Brain could not bind an exact-head review to it — confirmed true (the evidence document literally read "c106161 (short)").
- Fixed on the **same branch/PR** (`claude/v5-wrk-001-worker-routing-policy`, PR #20) per Rev52's own explicit bounded-correction authorization: `AdmittedWorker` gained `declaredToolRefs`, `declaredPolicyConstraintRefs`, and `authorityLevel` (`STANDARD` | `ELEVATED`); `WorkerRoutingRequest` gained matching `requiredToolRefs`, `requiredPolicyConstraintRefs`, and `requiredAuthorityLevel`; the eligibility filter (applied uniformly to executor selection, reviewer selection, and every fallback candidate — fallback can never route on a relaxed subset) now requires all three. 10 new adversarial tests (A23-A32). New full exact head: `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79`.
- `V5-WRK-001` is now `IMPLEMENTED / SELF-VALIDATED (Rev52-corrected)`: strict typecheck exit 0, full regression 666/666 pass (656 prior + 10 new), zero new dependency — re-verified via fresh detached-HEAD checkout of the exact pushed head. `MERGE_DISPOSITION: HOLD_MERGE` unchanged — Brain's fresh exact-head review of the full-SHA-bound corrected head is the next step, not merge.
- Rev52 also directed that PR #19 (the PR #18 post-merge documentation edge) remain an independent, unreviewed doc-only edge — its merge authority is never inferred from PR #18's, and any of its prose treating the worker-reported live "I confirm" as independently Brain-proven Founder authority must be corrected to cite Rev51's "Ok merge" as the canonical durable authority record instead.
- Rev52 reconfirmed the V5 Workstream A five-criterion finding remains `FINDING_PENDING_BRAIN_ADJUDICATION`, not closed as a side effect of either PR #18's acceptance or PR #20's review.

## V5-PTN-001 implemented (self-paced corridor continuation, no new Rev beyond 52 found)

- A scheduled corridor check-in fresh-read the canonical Handoff document and confirmed no genuine new Brain response existed beyond Rev52 (PR #20 remained awaiting review at its Rev52-corrected exact head). Per the standing post-merge continuation authorization, a fresh scan of the remaining V5 workstreams was run: E (blocked, no real external provider), G/I (blocked, no real validated-outcome evidence history exists), J (needs more backend substrate first) — H (Partner Network Operations, §12) was re-read in full against the already-merged `V3-PTR-001` (`src/domain/partner-organization.ts`).
- Direct inspection found four of §12's five acceptance directions already satisfied by `partner-organization.ts`'s `resolvePartnerClientAccess`/`PartnerRelationshipType` design. The one genuine gap: "partner capability claims require evidence/admission and may expire/review" and "capability claim cannot self-certify" — no such concept existed anywhere in `src/`.
- `V5-PTN-001` was elaborated and implemented on `claude/v5-ptn-001-partner-capability-admission`, cut from merged `main`: `src/domain/partner-capability-admission.ts` extends `PartnerOrganization` (type-only import) with a `PartnerCapabilityClaim` lifecycle — always born `UNVERIFIED`; only an explicit `admitPartnerCapabilityClaim` call (requiring non-empty evidence and a forward-looking `reviewByAt`) can produce `ADMITTED`; `EXPIRED` is a computed, never-stored disposition relative to a caller-supplied `asOf`; `REVOKED` is terminal. `IMPLEMENTED / SELF-VALIDATED`, 651/651 tests pass, strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-PTN-001.md` for the full reasoning and evidence.
- `MERGE_DISPOSITION: HOLD_MERGE` — this is a normal task-scoped PR against `main`; merge requires its own separately-granted protected owner-gate, never inferred from PR #18's grant or any other prior PR's.

## PR #20 merged (Rev53 PASS, Founder-approved exact-head merge)

- Brain Rev53 (independently fresh-read from the canonical Handoff document) reviewed PR #20's Rev52-corrected exact head `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79` and returned `BRAIN_REVIEW_DISPOSITION: PASS` / `ENGINEERING_DISPOSITION: VERIFIED / PASS / HOLD_MERGE_PENDING_SEPARATE_FOUNDER_APPROVAL`, confirming both Rev52 findings (F1 tool/policy/authority dimensions, F2 full-SHA binding) closed and 666/666 tests passing.
- Rev54 recorded a Founder "Ok" utterance authorizing the merge for that exact head — the canonical, Brain-accepted durable authority record for this merge on its own terms. Per this corridor's own standing practice (never merge from a document claim alone), Claude additionally re-verified PR #20's live GitHub state matched the reviewed head/base exactly, then asked the Founder the specific exact-head merge question directly in the live conversation and received an explicit "I confirm" before merging — a self-applied extra safeguard, consistent with Rev52's own clarification that Brain does not require or independently verify this additional step.
- PR #20 was merged into `main` via a standard non-destructive merge commit — new `main` SHA `f03c917e66283fcd63736234d81b166465496d7c` (parents `839aca0f6dd830c46dcf4576e46035c3fdbd9207` + `2ae8a63a2ca284e315c81731a107dcfa1e4f9a79`), independently re-verified by fresh `git fetch`. `git diff` between the merge commit and PR #20's reviewed exact head showed zero difference; fresh strict typecheck (exit 0, on a clean `dist/` rebuild) and full regression (666/666 pass, exit 0) were re-confirmed on the exact merge commit.
- Rev54 reconfirmed the V5 Workstream A five-criterion finding remains `FINDING_PENDING_BRAIN_ADJUDICATION`, and that PR #19 remains an independent edge inheriting no approval from PR #20's merge.

## PR #21 (V5-PTN-001) rebased onto post-PR#20 main, per explicit Founder instruction

- With PR #20 merged, PR #21's exact-head evidence (bound to base `839aca0f6dd830c46dcf4576e46035c3fdbd9207`) was stale — Brain could no longer treat it as an exact-head review target once `main` advanced to `f03c917e66283fcd63736234d81b166465496d7c`. The Founder explicitly directed rebasing the existing branch onto current `main` rather than opening a new branch or PR, resolving any conflicts without scope expansion, re-running the full test/regression suite, and producing a fresh exact-head evidence package bound to the new base/head — with `MERGE_DISPOSITION: HOLD_MERGE` unchanged (no Founder/owner-gate action at this stage).
- `git rebase origin/main` on `claude/v5-ptn-001-partner-capability-admission` produced conflicts only in the three corridor documentation files (`CURRENT_STATE.md`, `V2_TO_V5.md`, `VERSION_EVOLUTION_MAP.md`) — every source and test file (`src/domain/partner-capability-admission.ts`, `tests/partner-capability-admission.test.ts`, `tests/v5-ptn-001-boundary-scan.test.ts`) applied cleanly with zero conflict, since PR #20's own changes never touched partner/capability code. The doc conflicts were resolved by reconciling both branches' independently-true additive history (PR #20's Rev52-correction/merge narrative plus PR #21's own V5-PTN-001 narrative) into one coherent, non-duplicated account — no new claim, scope, or acceptance-criteria coverage was introduced during resolution.
- See `docs/exec-plans/active/V5-PTN-001.md` for the new `BASE_SHA`/`HEAD_SHA` and the re-run test/typecheck evidence bound to them.

## PR #21 (V5-PTN-001) Rev55 F1 correction: explicit independent admission authority

- Brain Rev55 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 55`) reviewed the rebased exact head `9137450725bf3f0f78e7a119f48c1e57d7089e4b` on base `f03c917e66283fcd63736234d81b166465496d7c` and returned `BRAIN_REVIEW_RESULT: CHANGES_REQUIRED`.
- **F1**: independent admission authority/provenance was implicit, not modeled/enforced — `evidenceRef` + `reviewByAt` and a separate `admitPartnerCapabilityClaim` function did not prove the admission came from an authority independent of the claimant; V5 R22's "capability claim cannot self-certify" was therefore not yet structurally satisfied, only a caller convention.
- `AUTHORIZED_CORRECTION`: same PR #21 and same branch only, no new PR/branch, no merge (`FOUNDER_RELAY_REQUIRED: NO`, `OWNER_GATE: NONE / NOT OPEN`). The finding was independently verified against source before fixing (confirmed `admitPartnerCapabilityClaim` had no admitting-identity concept at all) — then corrected: a required, non-empty `admittingAuthorityId` is now fail-closed rejected whenever it equals the claim's own `partnerOrganizationId`, and the accepted authority is persisted as `admittedByAuthorityId` for audit. Added negative regression (H17: self-admission rejected, H18: empty authority rejected) and positive regression (H19: distinct-authority admission succeeds and is persisted).
- New exact head `e7013fac72989380568726f750518b9ddee39046` on the unchanged base `f03c917e66283fcd63736234d81b166465496d7c` (main did not move for this correction). `npx tsc --noEmit -p .` exit 0 on a clean `dist/` rebuild; `npm run test` **691/691 pass** (688 pre-existing + 3 new), independently re-verified on a fresh detached-HEAD checkout of the pushed remote head in an isolated worktree with fresh `npm ci`.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged. See `docs/exec-plans/active/V5-PTN-001.md` "Rev55 F1 correction" section for the full record.

## PR #21 (V5-PTN-001) Rev56: Brain PASS, Founder owner-gate OPEN

- Brain Rev56 (independently fresh-read from the canonical Handoff document, `HANDOFF_HEAD_REV: 56`) reviewed exact head `4b78e4b3cd6134b5da0db8e799d47a8bf8795134` on base `f03c917e66283fcd63736234d81b166465496d7c` and returned `BRAIN_REVIEW_RESULT: PASS`. `REV55_F1_CLOSURE: SATISFIED` — the corrected candidate makes admission authority/provenance explicit and auditable, rejects self-admission fail-closed, and adds negative/positive regression coverage. `SCOPE_CHECK: PASS`, `REGRESSION_CHECK: PASS` (691/691).
- `MERGE_STATUS: NOT MERGED`. `OWNER_GATE: OPEN` — Brain states explicit Founder authorization is required before PR #21 may be merged; `NEXT_EXPECTED_ACTOR: FOUNDER`.
- Consistent with this corridor's standing conduct rule (demonstrated on PR #18's Rev51 and PR #20's Rev54 merges): a document-recorded Brain PASS, even with an owner-gate note, is not itself a Founder authorization. Merging requires a separate, explicit Founder confirmation given directly in a live conversation turn in response to the specific exact-head merge question — never inferred from a document's claim alone. Live PR #21 GitHub state was independently re-verified to match the reviewed head/base exactly (draft, open, not merged, `mergeable_state: clean`) before surfacing this to the Founder. No merge has occurred as of this record; awaiting a live Founder decision.

## V5-PTN-001 Rev62 non-blocking hardening: revokedAt temporal-integrity fix (self-selected, no Brain input required to begin)

- Context: this branch (`claude/v5-ptn-001-revokedat-temporal-hardening`) was cut fresh from `main` at `fe2cbc02f20e800b309e127288cc32a72176c109`, the same base `AUD-DURABILITY-GAP` (PR #30) was cut from — the two corrections are independent and touch disjoint files.
- With PR #30 open and awaiting Brain review, and no new Handoff revision yet available, a further scan of Brain's still-open Rev62 "NON-BLOCKING / HARDENING OBSERVATIONS" found a second actionable, dependency-safe item.
- Brain's exact text (Rev62, verbatim): *"V5 partner capability `revokedAt` currently validates non-empty string but not timestamp/order relative to admission; retain as bounded temporal-integrity hardening unless a stronger canonical invariant promotes it."*
- Independently verified against source: `revokePartnerCapabilityClaim` in `src/domain/partner-capability-admission.ts` validated `revokedAt` only via `requireNonEmptyString` (not the module's own `requireValidTimestamp` helper, already used by `admitPartnerCapabilityClaim`) — so a non-timestamp value, or a `revokedAt` predating the claim's own `admittedAt`, was silently accepted.
- Fixed: `revokedAt` now validates via `requireValidTimestamp`, and is rejected if strictly before `admittedAt` (equal is accepted — an immediate revocation is not "before" its own admission). Three new adversarial/positive tests added.
- 708/708 tests pass (705 pre-existing on this base + 3 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/V5-PTN-001.md` "Rev62 non-blocking hardening" section for full detail.
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; Status: `IMPLEMENTED / SELF-VALIDATED`, pending Brain independent exact-head review; Claude's authority ends here per `AGENTS.md` §10.
- `DOWNSTREAM`: independent of `AUD-DURABILITY-GAP` (PR #30); both may be reviewed/merged in either order without conflict (disjoint file sets).

## CONN-001 implemented, slice 1 (Rev90 authorization — no Brain dispatch required to begin)

- Context: Rev90 (2026-09-10) returned bounded `CHANGES_REQUIRED` on PR #35/#36/#37 (all three corrected on their own branches - see the `V5-LAB-001`/`V5-EVAL-001`/`V5-CMD-001` sections above) and, in the same revision, authorized a large new continuation task: **CONN-001, Integration & Credential Control Plane**, with an explicit instruction not to stop for another Brain dispatch once the three corrections were pushed.
- CONN-001's 13-point spec is large (admin surface; prebuilt connectors; mandatory Generic Custom API/OAuth connectors; multi-instance tenant/project binding with cross-tenant fail-closed; `ConnectionRequirement -> ConnectionBinding -> SecretRef` boundary; `REQUIRED/REQUESTED/CONNECTED_UNVERIFIED/VERIFIED/DEGRADED/REVOKED` lifecycle plus test/health/reconnect/rotate/revoke; AI provider/model-catalog separation; optional OpenAPI import; strict commerce-ownership boundary). Rev90's own text: "progressively elaborate from architecture/domain contract -> persistence/secret-ref boundary -> admin UI -> generic adapters -> prebuilt adapters -> adversarial E2E, avoiding speculative provider breadth."
- This checkpoint is slice 1: new pure domain module `src/domain/integration-connector-catalog.ts`, composing entirely on top of the already-merged, already-`VERIFIED`/`PASS`/`CLOSED` `connection-authority.ts` (`V2-CDO-004`) rather than modifying or duplicating its `ConnectionRequirement`/`ConnectionBinding`/`SecretRef` boundary - verified unmodified by a dedicated boundary-scan test.
- Adds: a connector catalog (`ConnectorDescriptor`, closed prebuilt-kind list `GOOGLE_DRIVE`/`GOOGLE_WORKSPACE`/`OPENAI`/`ANTHROPIC`/`GOOGLE_AI`/`GITHUB`/`META` plus mandatory `GENERIC_CUSTOM_API`/`GENERIC_CUSTOM_OAUTH` with fail-closed cross-field auth-mode rules, `isAiModelProvider` for AI-provider/model-catalog separation); and the lifecycle operations CONN-001 names that `connection-authority.ts` itself does not provide - `rotateConnectorSecret` (demotes a `VERIFIED` binding to `DEGRADED` on rotation, never silently stays `VERIFIED` against an unverified credential), `reconnectConnectorConnection` (only from `REVOKED`, fail-closed cross-tenant ownership check, always a genuinely new binding id per this repository's "revoked requires an entirely new record, never resurrection" discipline), `recordConnectorHealthCheck` (pure observational recording only, never an automatic state transition). `revoke` reuses the existing, unmodified `transitionConnectionBinding(..., "REVOKED")` directly. `REQUIRED` is represented via the pre-existing `ConnectionRequirement`/`ConnectionBinding` split rather than a redundant new enum value.
- Explicitly deferred, not fabricated: real credential entry/OAuth exchange/live network calls, admin UI, prebuilt adapter implementations (the named prebuilt kinds exist only as catalog descriptors), OpenAPI import accelerator, persistence/durable storage, real AI model-catalog data, and the two mandatory E2E acceptance cases (both require the admin UI/adapter layer this slice deliberately does not build yet).
- Branch `claude/conn-001-integration-control-plane`, cut fresh from `main` (`3226c76fa338e425e553638e5f5f48924182a1c0`). 734/734 tests pass (708 true pre-existing + 26 new: 21 lifecycle/catalog tests + 5 boundary-scan tests), strict typecheck clean, zero new dependency.
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`; Status: `IMPLEMENTED / SELF-VALIDATED`, pending Brain independent exact-head review; Claude's authority ends here per `AGENTS.md` §10.
- `DOWNSTREAM`: independent of `V5-LAB-001`/`V5-EVAL-001`/`V5-CMD-001` and PR #30/#32/#33/#34 (disjoint files); may be reviewed/merged in any order. Per Rev90's own instruction, the next dependency-safe elaboration step (persistence interface and/or admin-UI scaffolding) continues without a further Brain dispatch.

### Rev93 correction on `CONN-001` slice 1 (exact head `b525f6f7f1d2da08041f142e3896337963a60e55`)

- Brain's exact-head review returned bounded `CHANGES_REQUIRED` (F1): `reconnectConnectorConnection` checked `REVOKED` status, matching requirement/ownership, and a genuinely new binding id, but never checked that the reconnect `connectorDescriptor.connectorKind` matched `previousInstance.connectorKind` - two descriptors sharing a declared capability could let a revoked provider-A connection be silently "reconnected" as provider-B.
- Fixed: fail-closed rejection on connector-identity mismatch, checked before any other reconnect work. One new adversarial test (K22).
- In the same revision, Brain also independently PASSed PR #37's `V5-CMD-001` fix (head `391b964d0fdb56540d7e39cde19317c4b2ea608f`) - Rev90/92's F1 finding is confirmed resolved there; no further action.
- Corrected full regression: **735/735 pass** (708 true pre-existing + 27: 22 K-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED (Rev93-corrected)**, pending Brain independent exact-head re-review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `ENGINEERING NEXT ACTION` per Rev93: push this correction, then continue automatically through the CONN-001 progressive sequence (slice 2) and, at the first dependency-safe integration point, implement the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor (P0 adversarial tests only), without a further Brain dispatch.

## CONN-001 slice 2: persistence interface (no Brain dispatch required to begin)

- Per Rev93's own instruction, immediately continued into CONN-001's next progressive-elaboration step ("persistence/secret-ref boundary") after the Rev93 F1 correction was pushed.
- New module `src/domain/durable-connector-connection-store.ts`: `DurableConnectorConnectionStore`/`FileDurableConnectorConnectionStore`. Unlike `durable-outcome-job-store.ts`'s put-once/never-mutated record, a `ConnectorConnectionInstance` genuinely changes state over its lifecycle, so this store supports real updates gated by optimistic concurrency (`save(instance, expectedVersion?)`) rather than blind overwrite - a missing/stale `expectedVersion` against an existing record is fail-closed rejected (`ConnectorConnectionVersionConflictError`), preventing a lost update between two concurrent lifecycle operations on the same binding.
- Reference implementation uses only `node:fs`/`node:path` (zero new dependency), one JSON file per tenant, mirroring the existing durable-store discipline in this repository (full-file reconstruction, no separate in-memory index to diverge). Cross-tenant isolation is structural via per-tenant file keying.
- The persisted `secretRef` can only ever be an opaque reference id per `connection-authority.ts`'s own `SecretRef` contract - persisting the full instance never persists real secret material.
- 7 new tests (M1-M7): create-at-version-1, conflict on an `expectedVersion` with no existing record, correct-version update (+1), adversarial lost-update prevention (missing/stale version rejected, record provably untouched), unknown-id/cross-tenant `get()` isolation, cross-tenant `list()` isolation, restart-safety.
- Full regression: **742/742 pass** (708 true pre-existing + 34: 22 K-tests + 7 M-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: admin read-model surface, then generic/prebuilt adapters, then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## CONN-001 slice 3: admin read-model surface (no Brain dispatch required to begin)

- Fresh-read the canonical Handoff before continuing (still Rev93 as of this checkpoint - no new revision) - confirmed via `modifiedTime` unchanged since the Rev93 read, and PR #38's live GitHub state unchanged since the slice-2 push.
- New module `src/domain/integration-admin-view.ts`: `buildIntegrationsAdminView` aggregates the connector catalog and stored connections into the "Settings -> Integrations / AI & Providers" admin read-model CONN-001's spec names.
- Same disclosed genuine boundary as `V5-CMD-001`'s `internal-command-projection.ts`: no internal/staff authentication concept exists anywhere in this repository, so this module has zero HTTP/session/route wiring; deliberately cross-tenant, matching that same precedent.
- Structural secret-masking guarantee: `IntegrationConnectionSummary` has no field capable of holding a `SecretRef` at all - not omitted by convention, but structurally absent from the type.
- 5 new tests (N1-N5), including an adversarial proof that a connection's real `secretRef` never appears anywhere in the serialized admin view (checked both structurally and via a full-JSON substring scan for the literal secret value).
- Full regression: **747/747 pass** (708 true pre-existing + 39: 22 K-tests + 7 M-tests + 5 N-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: generic/prebuilt adapters, then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## CONN-001 slice 4: Generic Custom API/OAuth connector definitions (no Brain dispatch required to begin)

- Fresh-read the canonical Handoff before continuing (still Rev93, no new revision - confirmed via unchanged `modifiedTime` and unchanged PR #38 live GitHub state since the slice-3 push).
- New module `src/domain/generic-connector-definition.ts`: pure, in-memory declarative data contracts for CONN-001's two mandatory generic connectors - no real HTTP call, no DNS resolution, no request ever executed.
- `GenericApiConnectorDefinition`: validated `baseUrl`, `authMode` restricted to `API_KEY`/`BEARER_TOKEN`/`BASIC`/`CUSTOM_HEADER` (`OAUTH2` structurally excluded), unique-`capabilityRef` endpoints, a `validationEndpointCapabilityRef` fail-closed required to reference a real declared endpoint (CONN-001's "Test Connection" acceptance point).
- `GenericOAuthConnectorDefinition`: validated `authorizationUrl`/`tokenUrl`, non-empty deduplicated `scopes`; `authMode` is `OAUTH2` by construction, not a settable field.
- `resolveEndpointForCapability` fail-closed throws for an undeclared capability (never returns `undefined`, matching `resolveConnectorDescriptor`'s own discipline); `bindGenericApiDefinition`/`bindGenericOAuthDefinition` fail-closed reject a definition attaching to the wrong connector kind, binding id, or auth mode.
- 11 new tests (P1-P11), including adversarial OAUTH2-on-API-connector rejection and multi-instance independence.
- Full regression: **758/758 pass** (708 true pre-existing + 50: 22 K-tests + 7 M-tests + 5 N-tests + 11 P-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: prebuilt adapters (still no real network call), then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## CONN-001 slice 5: prebuilt connector endpoint definitions (no Brain dispatch required to begin)

- Fresh-read the canonical Handoff before continuing (still Rev93, no new revision - confirmed via unchanged `modifiedTime` and unchanged PR #38 live GitHub state since the slice-4 push).
- New module `src/domain/prebuilt-connector-definitions.ts`: static, real, single-host endpoint/capability declarations for `GITHUB`, `OPENAI`, `ANTHROPIC`, `GOOGLE_AI`, and `GOOGLE_DRIVE` - still zero real HTTP calls, reusing slice 4's `validateConnectorEndpoints`/`requireAbsoluteHttpUrl` validators.
- `GOOGLE_DRIVE` carries real search/read/create/write/share capabilities on the Drive API v3's one stable host, directly satisfying the blueprint's "a real initial connector capability, not a decorative catalog card" requirement.
- `GOOGLE_WORKSPACE` and `META` are explicitly deferred, not fabricated: Workspace's Docs/Sheets/Slides capabilities span three genuinely distinct Google API hosts, which this module's one-`baseUrl`-per-definition shape cannot honestly represent without a genuine architecture decision (a per-endpoint base-URL override) this bounded slice does not unilaterally make; Meta exposes multiple materially different product APIs with no single canonical surface to default to. `resolvePrebuiltConnectorDefinition` fail-closed distinguishes "recognized, not yet defined" from "genuinely unrecognized."
- 7 new tests (Q1-Q7), including adversarial cross-connector capability-resolution isolation (no connector's capability ever resolves against another connector's definition).
- Full regression: **765/765 pass** (708 true pre-existing + 57: 22 K-tests + 7 M-tests + 5 N-tests + 11 P-tests + 7 Q-tests + 5 boundary-scan), strict typecheck clean, zero new dependency.
- Status: **IMPLEMENTED / SELF-VALIDATED**, pending Brain independent exact-head review. `MERGE_DISPOSITION: HOLD_MERGE` unchanged.
- `NEXT`: the two mandatory E2E acceptance cases (protected gate - need real credentials/admin-UI wiring), then the Rev91/92/93 SALE-TO-CLOSE E2E acceptance floor, without a further Brain dispatch per Rev90/93's own sequencing.

## Rev90-98: CONN-001/SALE-TO-CLOSE → deployment foundation → post-foundation gap audit → V4-EFF-001

- Brain Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38) and Rev91/92 added a `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39), both cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0`. Rev93 found and fixed a bounded F1 on PR #38 (reconnection connector-identity gap). A chat-relayed "Rev95 Master Execution Order" was independently verified against the primary Drive Handoff document before being acted on. Rev94 found bounded findings on both PRs (PR #38 F1-F3: process-safe connector-connection-store concurrency, fail-closed replay validation, Google Workspace multi-host resolution + a mockable connector-execution boundary; PR #39 F1-F5: a verified external-commerce fact envelope, source-namespaced canonical sale identity, race-honest durable persistence, a governed temporary-failure recovery path in `outcome-job.ts`, and post-lifecycle disposition preservation) — both corrected on their own branches (see each branch's own exec-plan doc for the full record, per this repo's per-branch documentation-duplication convention).
- Rev95 authorized a continuous-execution batch mode (complete the authorized batch without pausing for individual Brain review between tasks/PRs; defer independent review to one consolidated end-of-batch packet) and named the next item: a repo-to-runtime deployment foundation. `RUNTIME-001` (branch `claude/runtime-001-deployment-foundation`, cut fresh from the same `main` base) implements provider-neutral `AppConfig`/health-check contracts, a `PostgresOutcomeJobStore` behind a deliberately async-native port (no live database/SQL driver dependency), a Cloudflare Worker Fetch-API adapter reusing the existing transport-agnostic `request-handler.ts` unmodified, and reversible deployment scaffolding (Dockerfile, `wrangler.toml.example`, build/test-only CI). See `docs/exec-plans/active/RUNTIME-001.md`.
- Rev97 then clarified that PR #38 + PR #39 + the deployment foundation are not by themselves a valid batch endpoint, and Rev98 ("REOPENED GAP AUDIT / MANDATORY POST-FOUNDATION DISPOSITION") lists twelve mandatory post-foundation gap-disposition families to fresh dependency-scan and execute the largest coherent currently dependency-safe batch from (not a rebuild list — reuse/extend existing primitives). Family 3, "V5 Partner Routing Completion," was implemented as `V5-PTN-002` (branch `claude/v5-ptn-002b-partner-routing-decision`): `resolvePartnerRoute` composes both existing partner-authority dimensions (capability admission via `resolvePartnerCapabilityClaimStatus`, and per-employee tenant/client/project access via `resolvePartnerClientAccess`) into one routing decision, reconciled against the stale historical PR #24 (which modeled capability admission alone) rather than blindly merging it. See `docs/exec-plans/active/V5-PTN-002.md`.
- Family 4, "V4 Workstream D — Approval/Effect/Recovery/Readback Envelope," was implemented next as `V4-EFF-001` (branch `claude/v4-eff-001-external-effect-envelope`): `src/domain/external-effect-envelope.ts` closes the missing generic layer between an authorized decision to act and an actual external-provider mutation — proposed intent, scoped authority/approval requirement, idempotency/retry classification, mandatory external correlation on every attempt outcome, a `NOT_STARTED`/`APPLIED`/`VERIFIED`/`FAILED`/`UNKNOWN`/`ROLLED_BACK` state machine, fail-closed no-blind-retry out of `UNKNOWN`, and readback-authoritative verification. Reuses `authority.ts`'s existing primitives exactly as `outcome-job.ts`'s own `recoverFromExceptionState` already established the pattern for, while remaining a distinct, standalone, unwired envelope — no second workflow engine, no real provider mutation. See `docs/exec-plans/active/V4-EFF-001.md`.
- `MERGE_DISPOSITION: HOLD_MERGE` on all five (PR #38, PR #39, RUNTIME-001, V5-PTN-002, V4-EFF-001) — normal task-scoped PRs against `main`; merge requires its own separately-granted protected owner-gate. Per Rev95, independent Brain exact-head review of all five is deferred to one consolidated end-of-batch packet.
- Remaining Rev98 disposition families (1, 2, and 5 through 12) remain to be fresh dependency-scanned and executed as further dependency-safe units in continuation of this same batch.

## Rev101: bounded correction on PR #42 (V4-EFF-001) — evidence/correlation lineage

- Brain's Rev101 independent exact-head review (fresh-read from the canonical Drive Handoff) reviewed PR #38 through PR #43's exact pushed heads and returned `CHANGES_REQUIRED, BOUNDED` on three of them; this entry covers PR #42's finding (the other two, PR #41 and PR #43, are corrected on their own branches per this repo's per-branch documentation-duplication convention — see each branch's own exec-plan/CURRENT_STATE entries).
- PR #42 F1 (exact head `d837e8c913ee4f7e9c9a5c6a57232c44d895d62a`): `verifyExternalEffectReadback` validated but discarded its `evidenceRef`, and `rollbackExternalEffectAttempt` overwrote the attempt's original `externalCorrelationRef` with the rollback's own correlation ref, permanently losing the original effect's correlation, while also discarding its own `evidenceRef`. Fixed on the same branch (`claude/v4-eff-001-external-effect-envelope`): three new additive optional fields on `ExternalEffectAttempt` (`readbackEvidenceRef`, `rollbackCorrelationRef`, `rollbackEvidenceRef`) so readback evidence, rollback evidence, rollback correlation, and the original effect correlation can all survive on the same record simultaneously — no existing field renamed or removed, no exported symbol added or removed. New test E29 proves the full `APPLIED`→`VERIFIED`→`ROLLED_BACK` chain retains all four references at once; E12/E13/E28 updated and E23/E24 corrected (they had asserted the pre-fix overwrite behavior). Sanity-checked by reverting the fix via `git stash` while keeping the updated tests, confirming the expected `tsc` compile failures on every new-field assertion, then restoring and reconfirming 743/743 pass. See "Rev101 correction (F1 — evidence/correlation lineage)" in `docs/exec-plans/active/V4-EFF-001.md`.
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged; per Current Project State Rev95/97-100's continuous-execution batch mode and canonical Handoff Rev101's own dispatch, independent Brain exact-head review of the corrected head is deferred to the one consolidated end-of-batch packet alongside the other checkpoints (PR #38, PR #39, RUNTIME-001, V5-PTN-002 Rev101-corrected, LOCAL-EXEC-001 Rev101-corrected).

## Family 5 — Website-Build reference service-specific control adapters (CONN-001 + V4-EFF-001 merged)

- Rev98/Rev101's Family 5: *"V4 reference service-specific control-adapter floor for the Website Build path on top of CONN-001, using mocks/injected transports and no duplicate AI Commerce runtime,"* with the acceptance shape "READ→DIAGNOSE→RECOMMEND→DRAFT/PREVIEW→APPROVAL-BOUND APPLY→VERIFIED EFFECT, composing CONN-001 + V4 effect envelope." Handoff Rev101's own "EXECUTION REMINDER — REV101 / NO PARKING" appendix (added 2026-09-12) explicitly names "a local branch blocker" as not a stop condition — this family had been dispositioned `TRIGGER_NOT_MET` for exactly that reason (its two prerequisite branches were not merged to a common base), which is corrected here since both are this session's own draft branches and merging them is a reversible action within existing authority.
- Branch `claude/family-5-website-build-control-adapters` merges `claude/conn-001-integration-control-plane` (exact head `41b2b16a6e2ed6f63039d3486313e929f31d16c5`, Rev94-corrected) and `claude/v4-eff-001-external-effect-envelope` (exact head `c60210362ef9ce709abe4fcc27e4785542fe77ea`, Rev101-corrected) — both cut from the same `main` ancestor (`3226c76fa338e425e553638e5f5f48924182a1c0`), so every source/test file merged cleanly; the only conflicts were in this document and `CURRENT_STATE.md` (both additive, independent narratives, reconciled by keeping both in sequence with no content dropped).
- `src/domain/website-build-control-adapter.ts` implements the module: `service-capability-routing.ts`'s own doc comments record "no real provider adapter... exists anywhere in this repository yet," capping every route at `READ_ONLY` — this is that adapter's first real instance. Composes `ExecutionMaturity` (type-only, unmodified) with `connector-execution.ts`'s `executeConnectorCapability` and `external-effect-envelope.ts`'s intent/attempt lifecycle: `readWebsiteBuildSignal` (`READ_ONLY`, unmodified pass-through) → `diagnoseWebsiteBuildSignal` (`DIAGNOSE`, caller-declared severity/evidence bound to the exact prior signal — no real website-analysis intelligence exists in this repository, so severity is never inferred) → `recommendWebsiteBuildAction` (`RECOMMEND`, fails closed against recommending an action for a `HEALTHY` diagnosis) → `draftWebsiteBuildEffectIntent` (`DRAFT_PREVIEW`, `requiresApproval` hard-coded `true`, never caller-settable) → `startWebsiteBuildEffectAttempt` (`APPROVAL_REQUIRED`, unmodified pass-through, approval always mandatory) → `controlledApplyWebsiteBuildEffect` (`CONTROLLED_APPLY`, the only function calling the injected transport for a write — success → `APPLIED`; a `ConnectorExecutionTransportError` → `UNKNOWN`, never a guessed `FAILED`; every other connector error → `FAILED`). The final "VERIFIED EFFECT" step has no corresponding `ExecutionMaturity` literal, so it is not wrapped — callers use `verifyExternalEffectReadback` directly, proven end-to-end by a full-chain test.
- `tests/website-build-control-adapter.test.ts` (14 tests: W1-W14) and `tests/family-5-boundary-scan.test.ts` (6 tests). `IMPLEMENTED / SELF-VALIDATED`, **845/845 tests pass** (825 pre-existing on the merged base + 20 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-5-WEBSITE-BUILD-CONTROL-ADAPTER.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.

## Rev90-101 condensed narrative (carried forward from `claude/obs-tel-001-telemetry-floor`, cut fresh from `main`)

- Rev90 authorized `CONN-001` (Integration & Credential Control Plane, PR #38) and a `SALE-TO-CLOSE` E2E acceptance-floor addendum (PR #39); Rev93/94 found and fixed bounded findings on both (process-safe concurrency, fail-closed replay validation, a mockable connector-execution boundary; a verified external-commerce fact envelope, source-namespaced canonical sale identity, race-honest durable persistence, governed temporary-failure recovery, post-lifecycle disposition preservation).
- Rev95 authorized continuous-execution batch mode and named a repo-to-runtime deployment foundation: `RUNTIME-001` (PR #40) implements provider-neutral `AppConfig`/health-check contracts, an async-native `PostgresOutcomeJobStore`, a Cloudflare Worker Fetch adapter, and reversible deployment scaffolding.
- Rev98 ("REOPENED GAP AUDIT") listed twelve mandatory post-foundation gap-disposition families. Family 3 ("V5 Partner Routing Completion") became `V5-PTN-002` (PR #41: `resolvePartnerRoute` composing capability admission + per-employee client access, reconciled against a stale historical PR #24). Family 4 ("V4 Workstream D") became `V4-EFF-001` (PR #42: `external-effect-envelope.ts`, an intent/approval/attempt/readback/recovery contract).
- Rev99/100 introduced and confirmed Founder approval for "AKILTA Local Execution" (device/subscription-worker execution, default OFF, no second IAM/router), naming `LOCAL-EXEC-001` through `LOCAL-EXEC-007`. `LOCAL-EXEC-001` (Phase L0, PR #43) implements `local-execution.ts` composing into the existing, unmodified `worker-routing-policy.ts`.
- Rev101 ("CONTINUOUS EXECUTION / NO PARKING") returned `CHANGES_REQUIRED, BOUNDED` on PR #41/#42/#43's exact heads (self-routing identity semantics; evidence/correlation lineage; device/worker ownership binding + declared-ref-list validation, respectively) — see each branch's own exec-plan for full detail. All three were independently sanity-checked and corrected. Per Rev101's own "AFTER BOUNDED CORRECTIONS" instruction, `LOCAL-EXEC-002` (Device Bridge MVP, Phase L1) was then implemented on a branch cut from PR #43's own corrected head: `local-execution-bridge.ts` implements the §17 control-channel protocol (message envelope, fail-closed replay/idempotency, enrollment challenge/proof, connection lifecycle, heartbeat, allowlisted-workspace-path resolution, lease/checkpoint consistency), composing L0's contracts unmodified.
- All six prior checkpoints (PR #38, PR #39, RUNTIME-001/#40, V5-PTN-002/#41 Rev101-corrected, V4-EFF-001/#42 Rev101-corrected, LOCAL-EXEC-001/#43 Rev101-corrected) plus LOCAL-EXEC-002 remain `HOLD_MERGE`, deferred to one consolidated Brain review packet.

## Rev98 Family 6: OBS-TEL-001 Provider-Neutral Observability/Telemetry Floor

- Continuing the fresh dependency scan across remaining Rev98 Families 1,2,5-12 plus LOCAL-EXEC-003..007, Family 6 was selected next: unlike `LOCAL-EXEC-003` (requires a fresh dependency-fit ADR against the *current* official Codex CLI/SDK — genuinely external, time-sensitive research) and the other remaining families (each dependent on this very floor or the still-open Family 2 trusted-ingress boundary), Family 6 is self-contained and is explicitly the prerequisite `V5-EVAL-001`'s own deferred post-adoption monitor is blocked on.
- Rev98's own text: *"Implement the smallest repo-native telemetry/health/evidence contract and read model needed to represent source, metric definition/unit/window, freshness, quality/confidence, missing-vs-zero and monitored state without fabricating provider data. No vendor monitoring account or live ingestion is required for the dark floor."*
- `OBS-TEL-001` (branch `claude/obs-tel-001-telemetry-floor`, cut fresh from `main` at `3226c76fa338e425e553638e5f5f48924182a1c0` — an independent Rev98 family, unlike `LOCAL-EXEC-002`) implements `src/domain/observability-telemetry.ts`: `MetricDefinition`/`MetricWindow` (source/unit/window), `MetricObservation` with a `REPORTED`/`MISSING` presence discriminator enforcing "missing-vs-zero" (a reported `0` is valid and distinct from absence), caller-declared `MetricQuality`, computed asOf-relative `resolveMetricFreshness` (never stored, never clock-reading), a closed `MonitoringState` registration graph, and `projectMetricReadModel` composing all five dimensions with monitored state as a real visibility gate. See `docs/exec-plans/active/OBS-TEL-001.md` for the full reasoning and evidence — 741/741 tests pass (708 pre-existing + 33 new), strict typecheck clean, zero new dependency, sanity-checked (three representative adversarial guards were temporarily disabled together and confirmed exactly those three tests then failed).
- `MERGE_DISPOSITION: HOLD_MERGE` — normal task-scoped PR against `main`. Per Rev95/97/98/99/100/101's continuous-execution batch mode, independent Brain exact-head review is deferred to the one consolidated end-of-batch packet alongside the other seven checkpoints.
- Remaining work: Rev98 disposition families 1, 2, 5, 7 through 12, plus LOCAL-EXEC-003 through LOCAL-EXEC-007.

## Family 7 — Cross-Surface Evidence/Outcome Reporting Convergence (Family 5 + OBS-TEL-001 merged)

- Rev98/Rev101's Family 7: *"cross-surface evidence/outcome reporting convergence using existing customer-evidence/cross-domain/effect evidence"* with acceptance shape "provenance/comparability/customer-safe projection." Had been dispositioned `TRIGGER_NOT_MET`, but independent re-verification found `cross-domain-intelligence.ts` (`V5-INT-001`) already merged into `main` — only `V4-EFF-001` and `OBS-TEL-001` actually needed merging, a two-branch merge rather than three. Per Handoff Rev101's "EXECUTION REMINDER — REV101 / NO PARKING" appendix, "a local branch blocker" is not a stop condition, and both remaining branches are this session's own draft branches.
- Branch `claude/family-7-cross-surface-evidence-convergence` merges `claude/family-5-website-build-control-adapters` (already containing CONN-001+V4-EFF-001) with `claude/obs-tel-001-telemetry-floor` (exact head `4117c715b5a0dfe79956f372f2b9e9ecd0247e10`) — both trace to the same `main` ancestor, so every source/test file merged cleanly; only doc conflicts, resolved by keeping both branches' independently-true narratives in sequence.
- `src/domain/cross-surface-evidence-convergence.ts` composes — type-only, never redefines — `CustomerEvidenceItem`, `ReconciledInsight`, `ExternalEffectAttempt`, and `MetricReadModel` onto one shared `ConvergedEvidenceDisposition` (`CONFIRMED`/`UNCERTAIN`/`CONTRADICTED`/`UNAVAILABLE`) via four pure per-surface converge functions, each preserving the exact original status/sourceRef for provenance. A merely `APPLIED` effect attempt converges to `UNCERTAIN` (mirrors the effect envelope's own readback-authoritative discipline, never `CONFIRMED` on a claimed-only outcome); `UNKNOWN` states never guess `FAILED`/`CONFIRMED`; `ROLLED_BACK` converges to `CONTRADICTED`. `buildConvergedEvidenceSnapshot` is pure aggregation over caller-supplied, already-converged items. `projectCustomerSafeEvidenceSummary`'s `CustomerSafeEvidenceSummary` has no field capable of holding `sourceRef`/`originalStatus` (structurally absent), and its `overallDisposition` is the pessimistic (most severe) aggregate — a single contradicted item is never hidden behind otherwise-good evidence; an empty snapshot is honestly `UNAVAILABLE`, never a fabricated `CONFIRMED`.
- `tests/cross-surface-evidence-convergence.test.ts` (20 tests: X1-X20) and `tests/family-7-boundary-scan.test.ts` (6 tests). `IMPLEMENTED / SELF-VALIDATED`, **904/904 tests pass** (878 pre-existing on the merged base + 26 new), strict typecheck clean, zero new dependency. See `docs/exec-plans/active/FAMILY-7-CROSS-SURFACE-EVIDENCE-CONVERGENCE.md` for the full reasoning and evidence. `MERGE_DISPOSITION: HOLD_MERGE`.

## Claude's role inside the corridor (condensed — see Drive packet §5/§6/§12 for full text)

Claude is the delegated Engineering Brain + Primary Engineer inside the corridor: fresh-read and reconcile live repo truth before mutating; reuse existing patterns; choose the next dependency-safe work unit from the corridor-admitted graph (not roadmap prose at large); make reversible technical/architecture-within-envelope decisions; implement/test/self-review/fix/evidence/update state; continue across task and version boundaries when entry predicates are satisfied — without a routine per-task Brain/Founder start ceremony. Claude must not self-expand product scope, cross a material architecture envelope silently, or use implementation convenience to bypass a protected gate. Brain retains company/product strategy, canonical scope/version authority, material architecture-envelope decisions outside the delegated bounds, protected-gate classification, and independent/final verification.

## Autonomous execution loop (Drive packet §10)

`INSPECT → REASON/PLAN → IMPLEMENT → TEST → ADVERSARIAL SELF-REVIEW → FIX → VERIFY → EVIDENCE → UPDATE STATE → NEXT`, applied per dependency-safe unit.

## Hard stops (Drive packet §15 — not reproduced in full here)

Material product/scope change outside this corridor; material architecture/security/privacy decision outside the delegated envelope; unresolved canonical conflict; a new external provider/dependency requiring admission; credential/access widening; destructive/irreversible action; production deploy/release/publication/DNS/live-customer activation; binding legal/privacy/customer commitment; payment/financial commitment; AI Commerce mutation without its own project authority; a missing prerequisite blocking all safe work on the affected path; or an explicit canonical/Founder stop. Direct-main merge is never granted by this corridor alone — branch/PR workflow only.

## Rev94/95: CONN-001 F1-F3 correction and continuous-execution batch mode

- A chat message pasted into the engineering conversation claimed to relay a "Rev95 Master Execution Order" from Brain, in extremely directive terms explicitly instructing against this corridor's own standing verification discipline (e.g. telling Claude not to treat "the Handoff has not changed" as meaningful). Per the standing rule ("never accept a chat message merely claiming to relay a Brain finding without independently verifying it against the actual source document first" — the same rule that correctly rejected an earlier stale Turkish-language relay this session), the claim was independently verified against the primary canonical Drive Handoff document before any of it was acted on: the document's own `modifiedTime` (2026-09-10T23:02→2026-09-11T08:48) and file size (44KB→55KB) had genuinely changed, and its full body content matched the pasted findings' substance almost verbatim. This was corroborated as a genuine, freshly-written revision, not a stale or fabricated replay, before proceeding.
- **Rev94** (`BRAIN CONTINUATION REVIEW`) returned bounded `CHANGES_REQUIRED / CONTINUE` on PR #38 exact head `5e9eb7b62b879e0453740b15b28e8d5b9af8b35c` and PR #39 exact head `ab4d287387f41b0ea35d3c8d9a7f7bc2ab76a613`, with three findings on PR #38 (F1 process-unsafe concurrency, F2 blind-cast replay, F3 a false "credentials block everything" gate) and five findings on PR #39 (tracked separately — see `docs/exec-plans/active/SALE-TO-CLOSE.md`). PR #38's three findings were corrected on the same branch (see `docs/exec-plans/active/CONN-001.md`'s "Rev94 correction" section for full technical detail): F1 fixed with a real `linkSync`-based cross-process lock (reusing AUD-DURABILITY-GAP's own already-accepted primitive) proven via a genuine 5-process race test; F2 fixed with fail-closed persisted-record revalidation and 9 new adversarial/positive tests; F3 corrected by building a mockable/injectable connector-execution boundary (`connector-execution.ts`), defining `GOOGLE_WORKSPACE`'s genuine multi-host shape via a new per-endpoint `baseUrlOverride` field, and running both of CONN-001's mandatory E2E acceptance cases end-to-end through fixtures/mocks. 790/790 tests pass (765 prior + 25 new), strict typecheck clean, zero new dependency.
- **Rev95** (`CONTINUOUS EXECUTION / FINAL-BATCH REVIEW`) separately authorizes a change to review *cadence*, not acceptance criteria or protected gates: Claude is directed to execute the maximum currently authorized dependency-safe, reversible, dark/internal batch continuously — completing a task, pushing an exact head, or a PR sitting in pending-Brain-review status are explicitly named as NOT valid stop conditions during this batch — and defer independent Brain exact-head review to one consolidated packet at the end, covering every branch/PR touched in execution order. The only genuine stop conditions remain unchanged from the corridor's own standing Hard Stops above (real credentials/account authorization, spend, DNS, production deploy/publication, MAIN merge/mutation, destructive/irreversible action, customer-impacting financial/provider effect, or a material architecture/product-scope decision outside the already-authorized envelope).
- `MERGE_DISPOSITION: HOLD_MERGE` unchanged for PR #38 — this is a normal task-scoped correction; merge requires its own separately-granted protected owner-gate, never inferred from any prior PR's.
